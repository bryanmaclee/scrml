# scrml — Session 261 (bryan) — WRAP · ⭐ freeze-campaign reintegration sweep (4 PRs) · 2 soundness holes · E-MARKUP retire · tier-1 execution re-verify

**Date:** 2026-07-16. **Profile:** A (`/boot`). Successor to the wrapped S260. A large reintegration + de-risking session: landed 4 PRs (conformance **667 → 721**, +54 cases), fixed two ruled tier-1 soundness holes + implemented three more §17/§35 diagnostics (each compiler-source PR through the full S239 pass, each caught real bugs → fix round → re-review), and ran two diagnostic investigations that corrected the freeze denominator. A mid-session **GitHub Actions outage** blocked all landings ~40 min; offline diagnostic work continued and everything landed on recovery. **Mechanical detail: delta-log `[546]`-`[556]` + board `../scrml-support/handOffs/active-sessions/S261-bryan.md` + the two audit reports.**

## ⚠️ READ FIRST — state as of close
- **main = `8134fb55`**, gate GREEN, conformance **721/721**, coherence 0/0. 4 PRs landed: #75 ss75 data · #76 E-CTRL-004/E-LOOP-007 · #77 ss77 data · #78 E-CTRL-010/E-LIN-004/E-TILDE/E-LIN-005.
- **Two foundational arcs still HELD, UNCHANGED this session** (branches pushed, worktrees RETAINED — do NOT clean): `feat/colorless-async-seam-a` @ `211ab331` (worktree `../scrml-phase1-async`) · `feat/css-wave1-emission` @ `5f7cf235` (worktree `../scrml-css-wave1`). Still OPEN on the thread-board.
- **Freeze count is now more accurate: ~196 tier-1** (was ~200; the 247 V1-hole set sheds ~11-12). The remaining campaign distance is SHORTER than the grep count said. See the tier-1 re-verify report.

## 📋 OPEN THREADS / OWED (next session) — all ruled or documented; nothing needs re-deliberation
1. **E-MARKUP §34 RETIRE (bryan-RULED, ready to execute).** Retire E-MARKUP-002/003/VALUE-UNCLOSED + E-STATE-004 from §34 (amend toward impl/§24.2 E-HTML-*); align §4.4.1 closer-mismatch → impl's **E-CTX-001**; resolve the E-CTX-002 collision (SPEC=E-CTX-002 / §34=E-MARKUP-003 / impl's E-CTX-002 = an unrelated block-splitter rule). SPEC-text only → a wrap-style PR. Report: `../scrml-support/docs/audits/emarkup-ctx-reconciliation-2026-07-16.md` (root cause: guard `type-system.ts:9154` reads `n.name`; markup name is `n.tag`).
2. **9 tier-1 RECLASSIFICATIONS + dead-fingerprint gating (bryan-RULED).** Exclude the 13-code retire/reclassify list from the tier-1 freeze denominator (S256 audit + `spa-lists/CAMPAIGN-tier1-freeze.md`); adopt "gate fireable by execution over the dead-fingerprint cohort, skip the POS-proven." **+ Add-back: E-LIN-005** is a real hole (already pinned by #78). Report: `../scrml-support/docs/audits/tier1-execution-reverify-2026-07-16.md`.
3. **2 audit-ref corrections:** `s34-catalog-vs-impl-2026-07-16.md` lines ~58/62 read the E-MARKUP dead guards as live; S256 `fireable-codes.txt` over-counts (lines 24, 267-269, 353, 487, 495, 500-502) + mislabels E-LIN-005 RESERVED.
4. **`animationFrame` / `when` E-LIN-004 — DESIGN RULING NEEDED (bryan).** §35.6 treats a closure capture of a `lin` as a SYNCHRONOUS consume (capture = consume); E-LIN-004 as a recurring-context says a lin consumed in a callback that fires N times is a violation. Those two models CONFLICT for callbacks — which governs? (batch-2 landed `<timer>`/`<timeout>` cleanly; only the callback contexts are deferred.)
5. **§34 E-LIN-004 `<poll>` mislabel** (`SPEC:18424`) — the row lists `<poll>` among E-LIN-004 contexts, but §35.5 + the E-LIN-006 row + impl all put `<poll>` under E-LIN-006. Small SPEC reconcile (fold into the E-MARKUP §34-retire PR).
6. **flogence #6b ledger fold** (inbox, still owed — carried S259→S261). A LEDGER UPDATE not a build ask (two consumers now measured the axis+tier classifier / opaque=behavioral primitive); v-next/no-clock; weigh vs the §61 roadmap. In `handOffs/incoming/`.
7. **E-DG-002 separate dispatch** (queued, ruling-3) — the §17.1.1 `if=`/`else` sugar false-fires E-DG-002 "no readers" (the if-chain collapse hides the `@var` read from reader-accounting; sibling of the SB1 class `dependency-graph.ts:364`). Compiler-source.
8. **E-TILDE-001/002 WIRING** (scoped-separate per ruling-2) — the static analysis exists (`TildeTracker` type-system.ts:17293) but the parser never emits `tilde-init`/`tilde-ref` (:17645/:17651) → orphaned `~` compiles to `null` (`emit-expr.ts:713`), unsound. A parser arc (how `~` is represented). The message-text half landed in #78.
9. **§34-cleanup follow-on (carried S260):** the Info-vs-Warning trio direction-call, 163 DEAD rows, ss59 E-STATE cases 18/19, E-MARKUP-003 dup — much of this now FOLDS INTO items 1-3 (the E-MARKUP retire + tier-1 reclassifications). Reconcile rather than duplicate.
10. **Held-arc builds** (async seam-a bucket-(a) combinators + coverage; css wave-1 round-3 + theme-switch) · **auth-content build** · **SSR-prerender land** (`fix/ssr-auth-scoped-prerender-leak`, held) — carried from S258-S260.
11. **Worktree sweep (~50 stale)** — `git worktree list` shows ~9 spa-ss* + ~40 `.claude/worktrees/agent-*` + `s251`, all from prior sessions. Dry-run FIRST (respect sPA liveness); the 3 this-session spent ones were cleaned at wrap (see 6b). The held-arc worktrees (async/css) are RETAINED.

## 🔬 IRREDUCIBLE NARRATIVES / ANOMALIES (what to watch)
- **The S239 adversarial gate caught real bugs on BOTH compiler-source PRs** that a green 20k-test suite missed: #76 had a node-level E-CTRL-004 under-fire (standalone state-opener-with-else silently accepted — only the chain path was fixed); #78 had two E-CTRL-010 child-key-traversal bugs (a **false-fire** telling adopters to add a `lift` that exists inside a match arm + a **missed-fire** in if-chains). Both root-caused to hand-rolled AST traversal → fixed with generic child-walks. **The pattern: a new post-parse walk that hand-rolls a fixed child-key list WILL miss node kinds (match arms as raw text, if-chain `branches`/`elseBranch`) — use a generic array/object child-walk.** This is the third time this class bit (E-CTRL-004 markup-gate, E-CTRL-010 x2).
- **The grep-"fireable" classifier is unsound for the emit-present-but-unreachable class** (~100% wrong inside the dead-fingerprint cohort; ~4% of tier-1 overall). A code can have a source fire-site that is structurally dead (a wrong-key guard like the E-MARKUP `n.name` bug, a native-parser-only emit the adapter never reaches, a defensive/reserved code). Going forward the campaign gates "fireable" by EXECUTION over the dead-fingerprint cohort — not grep. The tier-1 re-verify is a floor (≥4 dead), not a ceiling: residual risk is another hidden wrong-key dead function among the ~178 not-individually-probed healthy-fire-site codes.
- **GitHub Actions OUTAGE mid-session (~22:20-23:00, 503s).** Blocked all landings + pushes for ~40 min. Two gotchas learned: (a) a PR can be `MERGEABLE` but `BLOCKED` when one of TWO gate check-runs on the same SHA infra-fails (the failed run holds the rollup) — re-run the failed run so a passing gate becomes the latest for the context; (b) re-running DURING the outage keeps failing → wait for recovery. Test GH health with `gh run list` (503 = still down), NOT `gh auth status`.
- **Pre-existing §17.4a gap (E-CTRL-010 adjacent, NOT fixed):** the Tier-0 `for (...) {...} else {...}` empty-state codegen is NOT wired in the live pipeline (the `else` parses as a bare-expr sibling; only §41.16 table-for materializes an `elseBody`), so a VALID for/lift+else fires `E-CODEGEN-INVALID-LOGIC`. E-CTRL-010 only gives the NO-LIFT case a precise diagnostic; the empty-state codegen itself is a separate gap. The `ctrl-010-for-lift-in-match-arm-neg` case documents the E-CODEGEN co-fire (asserts notCodes E-CTRL-010 specifically).
- **Auto-`isolation:"worktree"` STILL broken** (S258→S261) — used the PRE-MADE manual worktree `../scrml-ctrl-loop-fix` (cleaned at wrap) for the codegen dispatches. Keep doing that.
- **Commit-hook timeout-but-lands** (S261 saw it 3×) — the foreground commit Bash "times out" (exit 143) at 5min but the commit FINALIZES (pre-commit ~150-180s + a post-commit hook holds the pipe). Verify git STATE (`git log -1` / `git show --stat`), not the exit code.
- **Duplicate sPA boot** happened this session (a 2nd `/spa ss77` landed on the live one; stood down cleanly, nothing lost) — `/spa` has NO liveness check. An sPA-claim marker (or a `spa-lists/ss<N>.progress.md` stat at boot) would close it. The stand-down instance's freeze-gate findings were the seed for the E-MARKUP investigation + tier-1 re-verify.
- **flogence digest script STILL gone** (`../flogence/scripts/digest.ts`) — boot step 0 ref stale; absorbed state from boards/hand-off directly (the named fallback).

## 🚦 STATE @ CLOSE
- git: main `8134fb55`, gate GREEN, coherence 0/0. Held branches pushed + worktrees retained: async `211ab331` (`../scrml-phase1-async`), css `5f7cf235` (`../scrml-css-wave1`).
- Conformance **721** on main (was 667 at boot). Tier-1 freeze campaign: ss75+ss77 drained; §17/§35 diagnostics materially hardened (E-CTRL-004/E-LOOP-007 fixed, E-CTRL-010/E-LIN-004/E-LIN-005 implemented); freeze denominator corrected to ~196.
- Worktrees cleaned at wrap 6b: `../scrml-ctrl-loop-fix`, `../scrml-spa-ss75`, `../scrml-spa-ss77` (all spent). ~50 older stale worktrees = the owed sweep (dry-run first).
- Maps: compiler code landed (ast-builder.js, type-system.ts, SPEC.md) — refresh owed (cloud-maps daily regenerates, or dispatch project-mapper).

## pa.md directives in force
R1-R5 · PR-flow (branch→PR→gate→merge; 0-review) · **S239 mandatory adversarial review on EVERY compiler-source dispatch** (earned its keep 2/2 this session) · R26 empirical · manual-worktree provisioning · orchestrate-don't-grind · verify-state-not-narrative (the tier-1 re-verify + E-MARKUP investigation are this applied to the freeze classifier).

## Tags
#session-261 #conformance-721 #4-prs #ss75-ss77-reintegrated #e-ctrl-004-e-loop-007-fixed #e-ctrl-010-e-lin-004-e-lin-005 #e-markup-RETIRE-ruled #tier1-execution-reverify-196 #s239-caught-bugs-both-prs #github-outage-survived #two-arcs-still-held
