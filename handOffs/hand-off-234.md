# scrml — Session 231 (CLOSE)

**Date:** 2026-06-29 → 2026-06-30 (spanned the date change; ONE session = S231, follows S230). **Profile:** A — FULL (booted via `/boot`). A **build-execution session** that turned S230's V1-strategy into landed code: finished the V1-scoping (N4/N5/N6 + robustness-floor triage), then **scoped → ratified → BUILT the D3 conformance suite** (the load-bearing split build — 51 cases, gated, OQ1 resolved, and it FOUND A BUG), wired cross-pa-notify live, and **ratified the V1 capability-declaration vocab**. Mechanical stream → `handOffs/delta-log.md` [223]–[230].

## 🚨 NEXT-START
Boot Profile A. Board @close: **HIGH 0 · MED 8 · LOW 10 · Nom 7 · v0.7.1**. **PUSH-PENDING** (user said "wrap no push"): scrml 2-ahead (`5e93dd43` W3, `e86a76d0` P0) + the wrap commit; scrml-support `372bcc4` (DD OQ1) + the capability deep-dive edit (in the wrap commit). Say **push** to ship both.

**THE AUTHORITY for everything V1:** `scrml-support/docs/deep-dives/language-compiler-split-2026-06-29.md` (the V1=scrml-language-1.0 reframe; D1–D5; §9 the full remaining-work map). **The build's status doc:** `docs/changes/conformance-suite-d3-2026-06-29/SCOPE.md` (§6 carries the W2/W3/P0 results + follow-on decisions D-1/D-2). Re-read both if cold.

**Open execution threads (priority order):**
1. **The capability-vocab SPEC landing** (V1-security; design RATIFIED this session [230], READY to author). The `<program> [capabilities: [...]]` form (co-located, inline `_={}` inherits) + `§34 W-FOREIGN-UNDECLARED-CAPABILITY` (advisory; **opacity-honest = presence-nudge NOT accuracy** per §23.2.3) + the `[capabilities: []]` default + the deferred-aggregate/Pole-D-enforcement notes. Vocab: `{network[],fs-read[],fs-write[],spawn[],env[],db:bool}`. Nominal/discipline-forward, the **§14.8.9 shape**. Full disposition: the capability deep-dive's `RATIFIED S231` block (`scrml-support/docs/deep-dives/foreign-code-capability-gating-design-space-2026-06-27.md`) + delta-log [230]. **PA-authorable SPEC-text** (not a dispatch).
2. **The conformance full-lift** (the (b)-runtime IS the real contract — the (a)-codes half is ~done). P1: errorBoundary/components/lifecycle. **NET-NEW center: server-fn §52 + channels** (need a stubbed server boundary; the `wait:settle` verb + the `__scrml_conformance` hook are ALREADY in place) + multi-file RUNTIME (sibling-bundle eval) + index-form-`each` (SPEC check first). Friction map: delta-log [229] + SCOPE §6. sPA-able lift.
3. **The 3 found bugs:** `g-inline-value-match-in-text-interp-empty` (MED — the conformance suite's FIRST catch; `${ match @x {...} }` in a text interp renders empty, no diagnostic) · `g-nominal-foreign-forms-not-failclosed` (MED — N5/N6 fail-closed-honest-code build) · `g-fn-shortform-arrow-callback-invalid-js` (LOW).
4. **Other V1-security:** the §14.8.9 protect-floor BUILD (ratified S230, build-pending — `docs/changes/g-sql-row-protect-leak-2026-06-28/RULING.md`) + the V1 security-positioning statement (owed — note the "no-ambient-authority" half is ALREADY real: import-only stdlib).
5. **Remaining V1 content/artifact:** SSR (N8 → `g-tier1-ssr-prerender`) · D1 version scheme · D2 spec partition · D4 deprecation policy · W4/W5 of the conformance build (version-labeling + Road-B gate). All in the split deep-dive §3+§9.

## 🎯 Design narrative (IRREDUCIBLE — this is the session)

**1. V1-scoping FINISHED.** N4/N5/N6 (SPEC §23 foreign-code Nominals) RULED [223]: empirical probe flipped 2 of 3 — N4 fail-closed (defer clean); **N5 mis-diagnoses, N6 SILENTLY MISCOMPILES** (`use foreign:` → local no-op + HTML leak) → mechanism→v1.next but **a fail-closed honest diagnostic → V1**. Adopted the **v1.0 fail-closed-Nominal INVARIANT** (3rd V1 criterion): a Nominal form fail-closes honestly or isn't in the surface — never silent-miscompile/misleading-code. First concrete bite of the **D3 conformance contract** (which-codes-fire). Robustness-floor TRIAGED [224]: **no net-new silent-footgun blocker** (the 2 worst were already resolved); the split's lens sorts native/self-host gaps OUT of impl#1's V1.

**2. The D3 conformance suite — SCOPED → RATIFIED → BUILT (the spine of language-1.0).** Build-scoped [225] (asymmetry: the (a)-codes half is ~done-by-lift; the (b)-runtime half is the real work). **W1 ratified** (case format + adapter; runtime-effect = final-DOM+state). **W3 DD ratified** [226] (the (b)-runtime design — 6 OQs; `run()` = execute-in-DOM + 7 selector-verbs + a `__scrml_conformance` snapshot/settled hook; authoring = lift browser-twins → golden-capture+mandatory-spec-review; within-node parity OUT of the contract; execute-never-string-compare = invariant). **W2 pilot** [227] proved the (a)-format (15 cases) AND corrected the SCOPE: "109 to lift" is ~3-4× optimistic — only ~29 are codes-liftable, ~80 are impl-internal `splitBlocks` = impl freedom. **W3 pilot** [228] proved the (b)-runtime layer (7 cases); OQ3 REFINEMENT = the hook is **adapter-provided** (reaches `_scrml_state`), runtime UNTOUCHED, zero production surface. **P0 consolidation** [229] → **corpus 22→51, GATED** (`compiler/tests/conformance/corpus-bridge.test.js`), schema complete (`notCodePrefixes`/severity/`files`), **OQ1 RESOLVED → `domAnchored` default** (whole-tree leaked impl#1-private structure in 4 classes; inverted the DD's lean on evidence). **THE SUITE FOUND A BUG day-one** via the OQ4 golden-capture honesty mechanism (`g-inline-value-match-in-text-interp-empty`) — the D3 thesis validated. impl#2-native implementing `compile()`/`run()`/`__scrml_conformance` IS the Road-B gate.

**3. V1 capability-declaration vocab — RATIFIED** [230]. Picked up dpa-008 (vocab + 3 no-regret moves ratified S225) under the S230 reframe (the "no `_{}` to lint yet" rationale went stale — inline `_{}` ships ambient → window OPEN → decouple to standalone V1). **Locus (user "Both"): author co-located on `<program lang=>`** (inline inherits) + a compiler-AGGREGATED manifest view that **rides enforcement (deferred)**. V1 = the authoring half. Enforcement = Pole-D (Nix-model hybrid) still gated until multi-tenant `_{}`. Opacity → presence-nudge lint, accuracy at the C4 flip. Ready to SPEC.

**4. cross-pa-notify — BUILT (flogence) + co-adopted + WIRED LIVE.** The inter-PA push-notification (S230 spec'd/routed) is built as the flobase `cross-pa-notify` module (a `UserPromptSubmit` hook, checkpoint-not-interrupt). Co-adopt reply → flogence; the hook is wired into scrml's `.claude/settings.local.json` (machine-local; gitignored). Bidirectional push live. The nervous system for the post-PA-mitosis world.

## 🛟 Recovered anomalies / lessons
- **W3 pilot crash-recovery (clean, zero loss).** Agent a9f6365e crashed on a connection-closed mid-FINAL-REPORT (~41min/73-tooluses) but committed incrementally → work fully banked + worktree clean → PA verified independently (22/22) + landed. ONLY the report was lost (the OQ1 rec — superseded anyway by P0 resolving OQ1). Reaffirms: incremental commits + git-state-not-narrative = clean recovery.
- **Commit-timeout on the now-gated corpus.** The P0 commit "timed out" at 5min but LANDED — once `corpus-bridge.test.js` runs in-gate, pre-commit + the informational post-commit full-suite together exceed 300s. **Use `timeout: 600000` for commits going forward** (the commit lands at pre-commit-pass; the timeout only kills the foreground wait on the post-commit).
- **cross-pa-notify auto-mode guard = working-as-designed.** Wiring the `UserPromptSubmit` hook into `settings.local.json` was BLOCKED by the auto-mode classifier (startup-config self-modification) until the user explicitly said "sign off" — then clean. "sign off" is the authorization that clears the self-mod guard.

## Board @close
**HIGH 0 · MED 8 · LOW 10 · Nom 7 · v0.7.1.** Commits this session (scrml): `1c1742d7` (N4/N5/N6 + floor triage) · `b6821f88` (build-scope) · `e7718c04` (W2 pilot) [these 3 PUSHED] · `5e93dd43` (W3 pilot) · `e86a76d0` (P0 consolidation) [unpushed] + the wrap. scrml-support: `c3c74a8` [pushed] · `372bcc4` (DD OQ1) + capability-deepdive (wrap). delta-log [223]–[230]. **0 worktrees** (all dispatch worktrees cleaned). New: `conformance/` (the suite, 51 cases + adapter/run/normalize/driver) + `compiler/tests/conformance/corpus-bridge.test.js` (the gate).

## §push / cross-repo
**PUSH-PENDING** (user "wrap no push"). scrml 2-ahead + wrap; scrml-support 1-ahead + wrap. **Sent → flogence:** the cross-pa-notify co-adopt reply. **cross-pa-notify LIVE both ways.** No other outbound owed.

## pa.md directives in force
R1–R5 · `---` delimiter · Profile A · **S231 NEW: the v1.0 fail-closed-Nominal invariant** (3rd V1 criterion — deep-dive §4) · the D3 conformance contract (which-codes-fire + runtime-effect; text/JS/AST = impl freedom) · OQ1=`domAnchored`-default · the capability-vocab V1 disposition (delta-log [230]). Carried: S230 language/compiler-split + V1=language-1.0 · S228 flobase-routing · S219 PRIMARY-GOAL + flogence digest-boot · S227 dock · S226 landing-concurrency · S215 adversarial-verify · S138 R26 · S147 coherence · S136 BRIEF archival · S88/S99/S126 path-discipline · wrap 8-step. **Op note: use `timeout:600000` for commits** (the in-gate corpus + post-commit full-suite).

## Tags
#session-231 #close #v1-scoping-done #d3-conformance-suite-BUILT #conformance-gated #oq1-domanchored #conformance-found-a-bug #fail-closed-nominal-invariant #capability-vocab-ratified #cross-pa-notify-live #w3-crash-recovered #push-pending
