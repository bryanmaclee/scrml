${
    type LoadError:enum = { Boom }
    function risky()! -> LoadError { fail LoadError.Boom }
    <status> = "init"
}
<program>
    on mount {
        let r = risky() !{
            | _ e -> { @status = "caught-wild" }
        }
    }
    <p id="s">${@status}</p>
</program>
