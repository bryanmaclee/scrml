${
    function mintToken() { return "t" }
    function submitOne(t) { return t }
    let items = [1, 2]
    lin token = mintToken()
    for (const item of items) {
        submitOne(token)
    }
}
<p>x</>
