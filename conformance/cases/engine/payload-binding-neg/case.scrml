${
    type Drag:enum = { Idle, Dragging(id: number) }
}
<engine for=Drag initial=.Idle>
    <Idle rule=.Dragging></>
    <Dragging id rule=.Idle : "Dragging ${id}"></>
</>
<program>
<p id="s">${@drag}</>
</program>
