${
    type Drag:enum = { Idle, Dragging(id: number) }
}
<engine for=Drag initial=.Idle>
    <Idle rule=.Dragging></>
    <Dragging id extra rule=.Idle></>
</>
<program>
<p id="s">${@drag}</>
</program>
