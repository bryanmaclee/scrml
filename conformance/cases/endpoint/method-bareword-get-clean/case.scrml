<program>

type Query:enum = {
  Ping
  Lookup(id: int)
}

<endpoint path="/q" method=GET accepts=Query>
  <Ping : { jsonrpc: "2.0", result: { pong: true } }>
  <Lookup(id) : { jsonrpc: "2.0", result: { id: id } }>
</endpoint>

</program>
