${
    <rows> = [{ id: 1, name: "Alice", on: true }, { id: 2, name: "Bob", on: false }]
}
<ul id="list">
    <each in=@rows key=@.id as it>
        <li class="row" data-id=it.id if=it.on><span class="nm">${it.name}</span></li>
    </each>
</ul>
