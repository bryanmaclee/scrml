${
  type Role:enum = { Admin, Editor, Viewer }
  type Post:struct = { role: Role req }

  fn label(p: Post) -> string {
    match p.role {
      .Admin :> "admin"
    }
  }
}
<program>
  <p>hi</p>
</program>
