<program>

type UserQuery:struct = { id: int }
type UserResult:enum = {
  Found(name: string)
  NotFound
}

<api base="https://api.example.com">
  getUser(UserQuery) -> HEAD "/users/${id}" : UserResult
</api>

</program>
