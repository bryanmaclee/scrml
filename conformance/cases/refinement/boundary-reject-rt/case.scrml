${
    <amount>: number = 5000
    function process(amt: number(>0 && <10000)) { @amount = amt }
}
<program>
<button id="ok" onclick=${process(@amount + 100)}>Ok</>
<button id="bad" onclick=${process(@amount * 3)}>Bad</>
<p id="amt">Amount: ${@amount}</>
</program>
