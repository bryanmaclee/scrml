<program db="postgres://localhost/app">

  function loadOwner(id) {
    ?{`SELECT id FROM owners WHERE id = ${id}`}.all()
    loadPet(id)
  }

  function loadPet(id) {
    ?{`SELECT id FROM pets WHERE owner_id = ${id}`}.all()
  }

  <button onclick=loadOwner(1)>Load</button>

</program>
