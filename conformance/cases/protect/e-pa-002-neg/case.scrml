<program>
  <schema>
    ?{`CREATE TABLE users (id INTEGER PRIMARY KEY, name TEXT, passwordHash TEXT)`}
  </schema>
  <db src="nonexistent.db" protect="passwordHash" tables="users">
    ${
      function ping() {
        return 1
      }
    }
  </db>
  <div><p>hi</p></div>
</program>
