# scrml — Session 221 (IN PROGRESS)

**Date:** 2026-06-25. **Profile:** A — FULL. Booted clean (`read pa.md and start session`). Prior close (S220) archived → `handOffs/hand-off-225.md`. This file fills as S221 progresses; finalize at wrap.

> Board @ S221 boot: **HIGH 0 · MED 24 · LOW 17 · Nom 7** · v0.7.0. Suite 25223/0/214. Git clean, 0/0 with origin. Inbox empty. No deputy (eliminated S219). flogence digest-boot VERIFIED working this boot.

## ⏸️ OPEN — carried from S220 (priority order, until S221 reshapes it)
1. **server-emit BUILD cluster (sequence — share route-inference/emit-server):** `nominal-8` (per-role server-render-time gating §40.9.5 — hard half [static `isVisibleForRole`] DONE; missing = per-request render path omitting non-admitted subtrees; SCOPE delta-log [80]) → `W5a/W5b` (library-mode `?{}` — `docs/changes/library-mode-db-w5ab-2026-06-25/SCOPE.md`; flogence first consumer) → `dpa-013` (flogence transport JSON-RPC⇄`<endpoint>` — **option A RATIFIED S220**: `<endpoint accepts=E by="method">` configurable discriminator; build-candidate).
2. **MED/LOW backlog (24 MED, 17 LOW)** — S220 deferred cluster: render-residuals (`g-tablefor-column-slot-literal-interp` · `g-if-chain-branch-display-null-interp` · `g-errors-anchor-not-reactively-clearing`) · server-emit residuals (`g-peer-call-in-raw-template-unawaited` · `g-ecg001-protect-invariant-overfire` · `g-arrow-expr-body-sql-parser-truncate`) · `g-unary-left-of-exponent-no-paren` · `g-inline-struct-return-type-misparse`. Plus ss12 selfhost, ss6 lifecycle.
3. **#12 (GH, open)** — `?{}`-in-arrow: fires `E-SQL-009`+hint; full lowering deferred behind structured-lambda-block-body (`g-arrow-expr-body-sql-parser-truncate`).
4. **Editorial:** `examples/33-endpoint.scrml` can now dog-food the canonical `<Variant : fn()>` arm form (endpoint-arm fix made §61.2 work) — within-node-allowlist baseline implications.
5. **flogence #3 ("squashed bubbles")** = self-host class collector (`section-assembly.js`), B4-deferred (`g-selfhost-class-collector-each-match-no-walk`, LOW).

## Maps
OWED (PA-at-wrap now, deputy gone). Watermark behind HEAD — refresh via `project-mapper` at wrap (or earlier if a dispatch needs current maps).

## Boot residue (uncommitted)
- `handOffs/hand-off-225.md` (archived S220 close) + this fresh `hand-off.md`. Fold into first authorized commit / wrap.

## pa.md directives in force
R1–R5 · `---` · Profile A · **S219 PRIMARY-GOAL** (finish-the-project / orchestrate-don't-grind / default-GO / blocking-Q-only-pause) · **S219 flogence digest-boot** (verified working) · S88/S99/S126 path-discipline · S136 BRIEF · S138 R26 · S147 coherence (S205 merge-before-push RETIRED — deputy gone) · S215 adversarial-verify + 10× random-sample · S217 per-user (bryan) · wrap 8-step (full PA-maintenance).

## Tags
#session-221 #boot #profile-a #high-0 #digest-boot-verified
