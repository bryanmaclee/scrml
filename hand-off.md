# scrml — Session 265 (bryan) — WRAP · ⭐ CSS Wave-1 emission (§65) LANDED + the §25 component-reactive-CSS bug it exposed, FIXED

**Date:** 2026-07-18. **Profile:** A (`/boot`). Successor to the wrapped S264. A long execution session: resumed + completed the held CSS Wave-1 emission arc through a 3-agent S239 gate (caught **3 HIGH** past a green suite, incl. the headline feature silently DOA), landed it (#95), then scoped + fixed the pre-existing §25 bug that review exposed (#98). **Mechanical state lives in the executable carriers — `bun scripts/threads.ts` + `handOffs/delta-log.md` [590]-[598] + `docs/changelog.md` S265.** This hand-off carries the narrative + the anomalies + owed.

## ⚠️ READ FIRST — state as of close
- **scrml main = `bf316828`** (this wrap PR merges on top), cloud gate GREEN, conformance **740/740**, gate suite (unit+integration+conformance) **20691 pass / 0 fail** [190s], coherence 0/0. scrml-support 0/0.
- **Two arcs LANDED this session:** #95 CSS Wave-1 emission (§65) · #98 §25 component reactive-CSS-var bridge fix.
- **Concurrent: PETER is LIVE cross-machine on his adopter-issue lane** — he landed **#96** (issue #82: content-hash bundles + cache headers) and **#97** (#29-D: `disabled=@var` reactive binding) DURING this session. Fully disjoint from my footprint (CSS/§25 codegen); zero collisions. He did NOT register a fresh `active-sessions/S26x-peter.md` (PR-flow: board is superseded-for-authority). If you boot as a successor: his footprint = adopter issues (`gh issue list`), mine = CSS/tier-1. Partition holds.
- **No held branches retained.** Both worktrees (`../scrml-css-wave1`, the two agent worktrees) cleaned at 6b. (Other pre-existing held: `feat/colorless-async-seam-a` @ `211ab331` — untouched, still the next foundational arc after CSS.)

## 🎬 WHAT LANDED — changelog S265 + delta-log [590]-[598]. The irreducible:
1. **#95 CSS Wave-1 emission (§65)** — theme→`:root` token lowering · `.Variant`/`@media` selectors · the §65.3.4 reset · `:where()`-flat · the `@`-sigil model · component-beats-program-global via `@layer` · **a runtime theme-switch that actually flips the page** (§65.6). Conformance 731→740. Resume was: rebase the held branch onto main (SPEC.md-only 3-way, clean) → round-4 build (4 tasks) → S239 → fix-round → land.
2. **The S239 gate earned its keep, hard — 3 HIGH past a fully-green suite.** (a) an **apostrophe** in component UI text defeated the descendant-combinator fix (the CSS-block masker treated `'` as a delimiter; scrml attrs are `"`-only). (b) the flat-inline lowering **rejected valid derived/engine reactive cells** (membership collector was state-decl-only → use `collectReactiveVarNames`). (c) ⭐ **the theme-switch was silently DOA** — a `<theme>` token was wrongly collected as a §25 reactive-CSS-var bridge, hitting an unbuilt `_scrml_el` target that threw `ReferenceError` at bundle load → the reflection never ran. **Both my R26 and the build-agent's grep'd the emitted TEXT ("reflection present ✅"); a reviewer who EXECUTED the bundle caught it.** All fixed + re-verified by EXECUTION.
3. **#98 — the §25 bug that (c) exposed, FIXED.** Component-scoped reactive CSS (`#{ prop: @cell }`) was broken on main generally: the scoped-bridge target `_scrml_el` (`emit-reactive-wiring.ts:874`) was an unbuilt stub → client bundle threw at load. Scoped it (a read-only agent): components are compile-time INLINED — no per-instance runtime element, every cell reaching this path is a GLOBAL cell → the custom prop belongs on `document.documentElement` (inherits into the component's inline `var(--scrml-name)`, §65.3.1). One-line fix (collapse the ternary) + dead-code cleanup + a browser test that EXECUTES the bundle. Zero blast radius.

## 🔬 ANOMALIES / WHAT TO WATCH
- **"Emitted" ≠ "runs" (the session's headline lesson).** For a client-runtime surface, the empirical verify MUST execute the bundle (happy-dom / browser test), NEVER grep the emitted text. Grep'ing "reflection present" felt like R26 but missed a load-time ReferenceError that made a whole feature DOA. → the S239 verify block for any client-runtime dispatch now requires execution. Saved as a memory.
- **Peter landing concurrently moved main twice mid-dispatch** (`3b62839a`→`e444f9b7`→`17fd2beb`). The §25 fix agent based on `3b62839a`; I landed it via **file-delta onto current main** (disjoint file-sets verified — `comm -12` empty vs Peter's changes). Watch for more of this — Peter is actively draining his adopter lane; every land must re-check `origin/main` + intersect file-sets before file-delta.
- **The 12 whole-suite browser fails + 19 self-host + 6 migration = 36, are PRE-EXISTING** (identical base-vs-tip via the overlay's whole-suite comparison; GitHub's own `tracking` job labels them "Browser tests (real fails + env) — tracking" + "parser-parity backlog — tracking"). They are NON-GATING (the cloud `gate` = unit+conformance+gauntlet excludes them). Do NOT be alarmed by a red `tracking`/`ai-review` on a PR — only `gate` (+ `windows`) gate the merge.
- **The css-wave1 DONE-PROBE was a false-open** (fixed this wrap): `BRIEF.md` probed `git grep 'data-scrml-theme-mode' emit-client.ts` — that literal is only in compiled OUTPUT; source uses `themeVariantAttr(cell)`. Corrected to grep `emitThemeSwitchReflection`.

## 🚦 OPEN THREADS / NEXT — `bun scripts/threads.ts --open`. The prose-irreducible:
- **Newly filed (this session) — 3 pre-existing CSS/§25 gaps in `known-gaps.md`:** `g-css-selector-nested-reactive-ref-no-bridge` (a `#{ .box { color:@cell } }` selector-form in a component collects NO bridge → `var()` with nothing setting it → E-DG-002; sibling of the #98 flat-inline fix — the natural warm-adjacent NEXT pickup) · `g-css-inline-string-at-ident-miscompile` (`@ident` in a CSS string/url literal mis-read as a §25 ref) · `g-css-value-string-brace-mangle` (`content:"}"` mis-parsed as terminator).
- **Foundational arc still HELD (unchanged):** `feat/colorless-async-seam-a` @ `211ab331` (GITI-037 Phase-1; combinator-transform + coverage remain). The next big one after CSS.
- **The other 9 open threads** (`threads.ts --open`): e-attr-012-spec-remove · e-error-010-spec-catalog · e-fn-009-spec-defer-mark · e-markup-002-native-emit · e-mw-002-005-006-reconcile · esql004-corpus-nodb-bugs · ss75-conformance-linear-35 · theme-token-at-sigil-spec is now DONE.
- **Owed (bookkeeping carry):** the #6b + #7 oracle-ledger fold (S263/S264 carry) · the 2 S264-migrated examples' human re-verify (`examples/VERIFIED.md`) · the pa-base v2.1 vendored-copy sync for giti/6nz (S263 carry).

## Concurrent-session note — ⚠️ PETER DELEGATED #29-E + #81 TO BRYAN'S LANE
Peter (pjoliver11) ran a full concurrent S265 cross-machine and **WRAPPED** (his `active-sessions/S265-peter.md`). He drained a big adopter sweep — landed **#96** (issue #82: content-hash bundles + cache headers) · **#97** (#29-D: `disabled=@var` reactive binding) · **#100** (#27: §20.8.3 `<a href>` soft-nav link-boost) · **#101** (his wrap continuity). All disjoint from the CSS/§25 footprint — zero collisions; I file-delta'd #98 onto his moving main cleanly.
- **⚠️ HAND-OFF FROM PETER (his board, verbatim): "#29-E + #81 on bryan; next #87".** So the next adopter items **#29-E** (auth-example session-establishment — issue #29, he pre-cut `origin/feat/i29e-session-establishment` — verify its state at boot) and **#81** (dynamic VALUE attributes `class=`/`style=`/`title=`/`data-*` silently dropped outside `<each>`) are **delegated to me**. Peter's own next is **#87** (nested server-call auto-await). Boot 0.6: `gh issue list --repo bryanmaclee/scrml --state open` → currently #87 · #81 · #29 · #27 open. Peter's harness noted "AFK auto-merge blocked" — his merges needed manual completion.
- No formal inbox message — this delegation is ONLY on Peter's board. I registered + WRAPPED `S265-bryan.md`.

## pa.md directives in force
PR-flow (branch→PR→cloud `gate`→merge; 0-review; only `gate`+`windows` gate) · **S239 mandatory adversarial pass on EVERY compiler-source land** (3 HIGH caught this session — NON-NEGOTIABLE) · **EXECUTE-don't-grep for client-runtime verify** (this session's lesson) · **file-delta re-checks origin/main + intersects file-sets** (Peter concurrency) · R26 empirical · orchestrate-don't-grind.

## Tags
#session-265 #css-wave1-emission-landed #runtime-theme-switch-works #s239-caught-3-high #theme-switch-was-DOA-execute-dont-grep #s25-component-reactive-css-bridge-fixed #peter-concurrent-adopter-lane #conformance-740
