# scrml — Session 223 (OPEN)

**Date:** 2026-06-26. **Profile:** A — FULL. Boot via `/boot` (legacy `pa.md`→`pa-scrml.md` path; flobase `.pa-base/profile` still absent — non-blocking). Prior session's IRREDUCIBLE detail → `handOffs/hand-off-227.md` (S222 CLOSE). Mechanical stream → `handOffs/delta-log.md` [109]–[128].

## 🚨 NEXT-START (state @ boot)
Board: **HIGH 0 · MED 16 · LOW 13 · Nom 8 · v0.7.0 · suite 25474/0/214.** Tree clean (`main` + `spa/ss36`-SURVEY historical, no worktree). Coherence 0/0. Inbox empty. NO deputy (S219 — eliminated; push = plain S147 coherence). Maps 3 commits behind HEAD (watermark `12600217`; bookkeeping-only — refresh at dispatch if a dispatch touches mapped files).

## ⏸️ OPEN — S223 (priority order, carried from S222 close → hand-off-227.md for full detail)

1. **W4 chunk-model `dpa-014` — ✅ RATIFIED S223** (user "ratify W4"). Ship **B** (load-plan/manifest over the existing `_scrml_modules` registry), CONDITIONAL on (1) compiler-emitted `modulepreload`/103-Early-Hints over the N=0 set + (2) PROACTIVE prefetch (hover nav / programmable state-hook render-loop). Pure A DOMINATED (B 50/C 36.5/A 33). **MODEL-now / W4-ACTIVATION-gated-on-Component-3.** OQ-2 (programmable `prefetchTier(id)`) DEFERRED to Component-3/flux. C RECORDED (measured-cold-paint-regression-keyed held refinement). OQ-1 modulepreload mandate = part of the W4 wave spec (lands with the build). **Landed:** design-insight `~/.claude/design-insights.md` [S223/dpa-014] · dpa-queue dpa-014 → ratified · artifact marked RATIFIED · SCOPE §3 ruling recorded · user-voice + delta-log [129]. **No further W4 action until Component-3** (the upstream RS wave that makes the split pay off).

2. **🎯 THE COMPILER-REIMAGINING PROGRAM — narrow-Road-B COMMITTED** (`docs/changes/compiler-reimagining-derisk-2026-06-26/RULING.md`). De-risk DONE. Prereqs → lexer-in-scrml build.
   - **FBIP** (the flagship-maker, de-risked FAVORABLY): graduated path — (1) **ss38 HAMT maps** = bounded zero-risk first increment, **FIREABLE NOW** (kills the O(n)-clone tax; ss37↔ss38 coupling DISSOLVED — Set desugar is rep-agnostic) → (2) `lin`-annotated in-place → (3) full inferred reuse. Model = compile-time uniqueness-directed in-place (NOT Perceus runtime RC — disqualified on a JS host).
   - **The scanning stdlib** (mechanics-beauty #4) — scrml-native cursor/char-class/span vocab; sequences WITH the lexer build (dogfood).
   - **The lexer-in-scrml build** = first real Road-B wave (after prereqs). Approach B (pure-`fn` match-fold over typed enums) — `compiler-reimagining-lexer-slice-2026-06-26.md`.

3. **Fireable sPA lanes** (user-fired): **ss38 (HAMT — FBIP increment-1) ✅ LANDED S223 `5fb41cb9`** (PA file-delta; crash-recovery; §59 map rep COW→HAMT; S94 super-linear blow-up FIXED ~988× @N=8000; differential gate 4729 assertions observably-identical; full suite 25478/0. **ss37↔ss38 coupling DISSOLVED** — set is a thin map-desugar, so ss38 auto-covers it; **ss37 needs NO separate landing for the rep swap**). **🔵 IN-FLIGHT (user-fired, own instances):** ss32 (auto-await) + ss33 (cleanup) — disjoint, parallel-safe; PA lands/recovers on return (⚠️ ss32 may touch codegen — intersect file-sets vs ss38's `runtime-template.js` at landing per S211). Remaining fireable: ss34 (endpoint/SSR survey-first) · ss30-continuation (W3 RS-solver). **ss35 FROZEN** (native-parser superseded; lone fire-anyway = `g-mount-hang-rails-dev`). **ss36 KILLED** (no-tuple). Lineup: `spa-lists/INDEX.md`.
   - **Prunable worktrees (defer to wrap 6b — ss32/ss33 ACTIVE, don't disturb):** dead agent `agent-a923ae997f5796380` (LOCKED — crashed prior-ss38 session; work cherry-picked + landed) + branch `worktree-agent-a923ae997f5796380`; the ss38 sPA worktree `/home/bryan-maclee/scrmlMaster/scrml-spa-ss38` + branch `spa/ss38`. Do NOT prune `spa/ss32`/`spa/ss33` (in-flight).

4. **bug-1 @apply** (user lean `@apply`, banked — needs its own scoping pass: parse site + utility-resolution reuse + §26.7 var()-family composition); the rotting backlog (16 MED · 13 LOW + 8 Nominal). Per S219 primary-goal: drive the WHOLE board, not just the inbox.

## 🎯 Design narrative (carried — see hand-off-227.md for full)
- Compiler-reimagining fork de-risked to a COMMIT (3 slices: lexer CLEAN / DG-builder HOLDS-but-WEAKER / `<machine>` NO-primitive). FBIP is the flagship-maker. Limit-primitives held TWICE (Set B2 map-alias + no-tuple). The user-derived `±reactivity` algebra (engine = machine + reactivity).

## Open questions to surface immediately
- ~~W4 dpa-014~~ ✅ RATIFIED S223. ~~ss38~~ ✅ LANDED `5fb41cb9`. ~~ss32 item-1~~ ✅ LANDED `a0559651` (g-auto-await-reactive-server-no-error-arm CLOSED). **ss33** 🔵 in-flight (isolated worktree; emit-client → S211 intersect vs ss32 at landing). **ss34** 🔵 in-flight **NON-ISOLATED** (see below).
- **🎯 DESIGN RULING OWED — ss32 item-2 + discovered fork(g).** Both share ONE structural root: *a sync successor statement reads an async-deferred producer before it resolves.* fork(g) = reactive-server `@cell = fn() !{...}` dispatch checks the IIFE **Promise** → always-falsy → `!{}` handler DEAD (pre-existing, NOT a regression; ss32's `.catch` safety-nets the rejection but doesn't restore the handler). item-2 = top-level `@derived` off an async-server cell is a one-shot snapshot / pre-resolve TypeError. **4 options** (SURVEY `docs/changes/g-auto-await-read-before-resolve-race/SURVEY.md`): (1) data-dependency barrier [narrow] · (2) reactive-derivation [broad — changes top-level `@derived` semantics language-wide] · (3) hybrid [async-tainted only] · (4) lint/diagnostic stance. sPA recommends ONE deliberation covering the pair, then (if codegen) one dispatch. **NOT yet read the SURVEY in full — ground there before ruling (Rule 4).**
- ⚠️ **SHARED-CHECKOUT / NON-ISOLATION (ss34).** An ss34 sPA ran NON-ISOLATED in the PA checkout — switched HEAD to spa/ss34 (recovered: PA `git checkout main`), committed 1 to spa/ss34 (`main...spa/ss34 = 1 1`), left a stray untracked `docs/changes/g-endpoint-multi-statement-arm-diagnostic-2026-06-26/BRIEF.md` in main's working tree. Contained (ss34 has its own branch; ss38+ss32 landings safe). **Do NOT prune/disturb spa/ss34 or the stray dir until ss34 status known.** Need: is ss34 done? Fire sPAs ISOLATED (S222).
- **Push pending:** 2 unpushed commits (`5fb41cb9` ss38, `a0559651` ss32) + a large pile of uncommitted W1/scoping/prep bookkeeping. No push authorization yet — surface at wrap or on request.
- **Fireable now** (alongside in-flight ss32/ss33): **ss34** (endpoint/SSR, prepped S223) · **ss39** (legacy ast-builder/markup parse fixes, built S223, 3 MED). Both disjoint from the in-flight pair; ss39 watches `ast-builder.js` shared-file at landing (S211).
- **Backlog routing (S223 survey):** 5 MED + 5 LOW routed to lanes ss32/ss33/ss34/ss39. Carve-outs: `g-component-body-markup-parser-absent` (MED) → DESIGN-TRACK (needs a from-scratch component-body markup parser — DD, not a fix); `g-native-inline-struct-return-twin` (MED) → FROZEN native parser (defer per ruling). Remaining: small codegen (`g-unary-of-exponent-arg-no-paren`, `g-arrow-expr-body-sql-parser-truncate` — quick), Bucket-B (`bug-1` @apply — **v1 RATIFIED + SPEC §26.8 authored S223 (W1, Nominal); W2 build = a fireable survey-first sPA lane** [recognize directive in `#{}` rule parser + expand in `emit-css.ts:renderCssBlock` + `E-APPLY-*` diagnostics] · `g-library-mode-sql-no-db-context` · `g-sql-row-protect-leak`), deferred-parked (a5/bug-14/r28-c2/bug-20/21/22/r28-2b/g-const-collides-state/g-selfhost-class-collector). Per S219 drive these too, not just the lanes.

## Board @ open
**HIGH 0 · MED 16 · LOW 13 · Nom 8 · v0.7.0.** Suite 25474/0/214. Tree clean. Coherence 0/0. Maps watermark `12600217` (3 behind).

## pa.md directives in force
R1–R5 · `---` delimiter · Profile A · **S219 PRIMARY-GOAL** (finish the project; orchestrate-don't-grind; default-GO; only a blocking-Q pauses) · S219 flogence digest-boot · **S219 deputy-eliminated** (push = plain S147 coherence) · S88/S99/S126 path-discipline · S136 BRIEF archival · S138 R26 · S147 coherence · S215 adversarial-verify + 10× random-sample audit · S217 per-user (bryan) · wrap 8-step.

## Tags
#session-223 #open #profile-a #w4-dpa-014-pending #compiler-reimagining-road-b #fbip-ss38-fireable
