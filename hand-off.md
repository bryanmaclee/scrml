# scrml — Session 233 (CLOSE)

**Date:** 2026-06-30 → 2026-07-01. **Profile:** A — FULL (booted via `/boot`; full expert reads — the S231 lesson honored). A **marathon strategic session**: the user reframed V1, scoped+started+secured SSR, and — the center of gravity — **decided the compiler architecture skeleton** while the data was still unified (before mitosis). Mechanical stream → `handOffs/delta-log.md` [242]–[255] (+ the wrap close below).

## 🚨 NEXT-START
Boot Profile A. Board @close: **2 landed (SSR B-substrate `e72f058a` + server-load authority `f90bca9d`) · pushed pending (wrap step 7) · HIGH 0 · v0.7.1**. The SSR **security floor is complete**; the SSR **A-terminus** (true render) is the next big build.

**THE THREE DURABLE DECISIONS OF S233 (read these first):**
1. **V1 = the LANGUAGE, not the compiler.** V1 critical path = language frozen+conformance-tested + **impl#1 (TS) conformant**. **D5/native conformance is OFF the V1 critical path** — "compilers get there when they get there" (C++17/test262 model). impl#1-green = V1-shippable. Banked → `scrml-support/docs/deep-dives/language-compiler-split-2026-06-29.md` §4 S233-AMENDMENT.
2. **Compiler architecture SKELETON RATIFIED** → `docs/changes/compiler-architecture-skeleton-2026-06-30/RULING.md`. 4-phase re-cut (lex[fold]/parse[recursive-descent]/analyze[nanopass]/lower+emit[fold], FORCED by §45.6) · ~5 Elm-shaped IRs + id-indexed side-tables + threaded NodeId · **resolver HYBRID** (Pole-B env-threading catamorphism + local binding-group-SCC for the LOCAL; Pole-A id-indexed value-graph side-table for the GLOBAL §31.5/§40.9/§47 + FBIP-alias = 4 shared consumers) · FBIP rung-3 (3-fold inferred-reuse + differential harness) · conformance-driven loop-until-green build (near-autonomous LITERALLY-TRUE, 2 human bookends = decide-arch[done] + author-contract[V1]). **Core-IR fork DEFERRED to a codegen-wave slice.** Grounded on the S222 derisk RULING (the paradigm) + 5 S233 dives (`scrml-support/docs/deep-dives/compiler-arch-*` + `resolver-as-folds-pole{A,B}-2026-06-30.md`).
3. **SSR arc — the design + the security floor.** 3 Step-0 rulings + Forks 3+4 all RATIFIED (delta-log [245][247]). B-substrate + authority BOTH LANDED. `@currentUser` = compiler-provided ambient identity cell (.id/.role/.isAuth; SPEC §52.15). Authority: `scrml-support/docs/deep-dives/server-load-request-context-authority-2026-06-30.md`; SSR: `ssr-prerender-step0-rulings-2026-06-30.md`.

**Remaining SSR/V1 work (priority):**
1. **SSR A-terminus (V1-REQUIRED, the last SSR piece)** — server-side markup renderer (`emit-html.ts`/`emit-each.ts` — the genuinely-new capability) + DOM-adoption hydration (`_scrml_reconcile_list`), consuming the B-substrate's inline seed; **retires W-AUTH-002** + closes `g-tier1-ssr-prerender`. Multi-dispatch, weeks. **Gated PER-VAR** on the authority work for per-user data (public cells proceed parallel; per-user cells were the `W-SSR-PRERENDER-UNSCOPED` gate — now satisfiable via `@currentUser` row-scope). SCOPE substrate: the SSR dive §4.3 (Dispatch 2..N).
2. **Compiler architecture build** — the skeleton is decided; the build is the Road-B program (multi-year). Prereqs (Set ss37, FBIP-HAMT ss38) LANDED. Next = the **lexer wave** (Approach B, de-risked). The Core-IR slice fires at the codegen wave.
3. **D1/D2/D4 version surface** (language-semver · spec-partition · deprecation-lifecycle) — deep-dived S233 (`language-version-semver-scheme` · `deprecation-lifecycle`); ready to ratify+land as V1-language scope (contract-half of the split — CONCURRENT with V1, cheap). D1+D4 share ONE ruling: *does scrml-language ever cut a removing-major, or freeze permanent-soft?* (D4-Fork1 lean: permanent-soft).
4. **PA-mitosis** — designed (`pa-mitosis-concrete-shape-2026-06-30.md`); gated on the artifact split. This PA continues as the language-PA STEM.

## 🎯 Design narrative (IRREDUCIBLE)

**The arc was: reframe V1 → secure SSR → decide the compiler.** The user pushed three times toward the same insight — that the *language* is the artifact and the *compilers* are implementations on their own clock — and each push sharpened it: (a) "that whole list belongs in V1, the compilers get there when they get there" DECOUPLED D5/native from V1; (b) firing 5 architecture dives + a resolver debate "here, where all the data is" DECIDED the skeleton before mitosis can partition the context; (c) the near-autonomous-build payoff became structural fact (conformance-suite-as-oracle + the two human bookends).

**The resolver debate is the load-bearing architecture call.** Two advocate-dives (Pole A mirror-the-TS-DG-as-folds · Pole B fold-native-no-graph). PA judged **HYBRID** and it wasn't split-the-difference: Pole B WINS the contested local analysis (env-threading catamorphism + local binding-group-SCC; cycles = Tarjan back-edges never stored → the §59.5 "cyclic-value" keystone evaporates; collapses B7+B10+B16+§48.6.4). Pole A's id-indexed value-graph is the RIGHT form for the whole-program side-tables (§31.5/§40.9/§47) BOTH poles conceded are global — and the FBIP dive added a **4th consumer** (its alias fold projects the §40.9 def-use edges), making the graph a genuinely-shared artifact, not scaffolding. The pipeline+IR dive reached the same "analyze micropasses → id-indexed depGraph side-table" independently. User ratified.

**SSR turned out FAR more tractable than the SCOPE feared** — the deployment-target "gate" was ILLUSORY (SSR inherits §52's boundary; the SCOPE conflated §58-Merkle + §40.9.5-content-elision). The dives' verify-before-claim caught two real things: (a) the SSR SCOPE's Ruling-3 "no redaction" was STALE (the S232 protect-floor already covers `/__serverLoad` — PA confirmed in code); (b) the authority dive found the "no new exposure" claim was WRONG for per-user data (unscoped SSR-prerender = cross-user leak → the `W-SSR-PRERENDER-UNSCOPED` per-var gate). The user OVERRODE two dive defers (Forks 3+4 pulled forward — both vindicated: the authority build confirmed a live anon-POST hole) and OVERRODE the `session`-vs-`currentUser` handle (chose the first-class ambient cell).

## 🛟 Recovered anomalies / lessons
- **Commit-gate timeout: 300s is now STALE.** The pre-commit subset is ~25.9k tests / >5min. The B-substrate commit SIGTERM'd at 300000 but LANDED anyway (the gate finished + committed; the timeout killed the trailing echoes + the informational post-commit re-run). Verified git-state-not-narrative (HEAD moved, no index.lock, staged-set consumed). **Foreground commits need `timeout:600000`** (the authority commit used 600s cleanly). → **owed: bump memory `feedback_commit_hook_timeout_and_F_flag` 300000→600000.**
- **Both landings clean** — explicit-pathspec staging kept session-bookkeeping (hand-off/delta-log/arch-RULING) OUT of the feature commits (S119). Coherence checked each (0/1, 0/2).
- **Pre-existing (not introduced): the isServer-block warning double-fire** — `annotateNodes` visits the state-decl twice; W-AUTH-001 + the 2 new codes double-fire. Family-wide; **owed: a separate dedup pass.**

## Board @close
**2 PA landings:** `e72f058a` SSR B-substrate (§52.8 inline-state seed + conditional fetch; +23 tests; does NOT close g-tier1-ssr-prerender / NOT retire W-AUTH-002) · `f90bca9d` server-load authority (Forks 3+4: `/__serverLoad` 401 gate + `@currentUser` ambient cell + per-user row-scope + SPEC §52.15/§34×2/§52.12-SPEC-ISSUE-027-resolved; closes the anon-POST hole; full suite green). **NEW gaps:** none HIGH. **SPEC:** §52.15 (server-load authority), §34 +2 (W-SERVERLOAD-UNGATED, W-SSR-PRERENDER-UNSCOPED), §52.12 SPEC-ISSUE-027 RESOLVED. **0 worktrees** (cleaned). **Tests:** full suite green at both landings (25905/0 at B-substrate; authority zero-new-failures). **Deep-dives (9 this session, all scrml-support/docs/deep-dives):** ssr-prerender-step0-rulings · server-load-request-context-authority · language-version-semver-scheme · deprecation-lifecycle · pa-mitosis-concrete-shape · compiler-arch-pipeline-and-ir · resolver-as-folds-pole{A,B} · compiler-arch-fbip-rung3-and-harness · compiler-arch-conformance-driven-build. **RULING:** compiler-architecture-skeleton-2026-06-30.

## Owed / deferred (wrap-items NOT done this close — context)
- **Maps refresh** — NOT run this wrap (heavy source changes → maps ARE stale; owed next session: `project-mapper` incremental on the S233 landings, or a full refresh).
- **Resolver-debate design-insight** — the HYBRID ruling; the RULING captures it; a `design-insights.md` entry is owed.
- **derisk→skeleton forward-pointer** — add to `docs/changes/compiler-reimagining-derisk-2026-06-26/RULING.md` a pointer to the S233 skeleton RULING (currency).
- **commit-timeout memory bump** (300→600), **the double-fire dedup pass**.
- **Authority build deferred (by design):** client-side `@currentUser` (UI gating — the per-role/GITI-027B arc); login-side session-store population (`session.set` — read-side is complete, tests seed directly); mixed-interpolation Pattern-C (W-AUTH-004 param follow-on); SSR-compose 401 (page-level gate handles it; row-scope fails-closed).
- **currentUser fork residual:** server-context only this build (ratified); client-UI-gating is the deferred arc.

## §push / cross-repo
**Pushing this wrap (step 7):** scrml 2 feature commits (`e72f058a`+`f90bca9d`) + the wrap bookkeeping commit. scrml-support has uncommitted deep-dives (9 new) + the V1-doc S233 amendment → commit+push those too (storage repo). **Security note:** both SSR landings are V1-security code (the protect-floor reuse + the auth gate + the row-scope) — independently PA-verified (diff-review + R26 + full-suite), but the shipped auth-gate + redaction-stacking merit a human security review before production reliance.

## pa.md directives in force
R1–R5 · `---` delimiter · Profile A · **S233 NEW: V1=language / D5-native-off-the-gate; compiler-architecture-skeleton RULING; commit foreground timeout:600000 (300000 stale)**. Carried: S232 Nominal-SPEC-codes-in-section/§34-with-impl · S231 fail-closed-Nominal + D3 conformance contract · S230 language/compiler-split · S226 landing-concurrency · S219 PRIMARY-GOAL + flogence-digest-boot · S215 adversarial-verify (applied hard on both security landings) · S138 R26 · S147 coherence · S136 BRIEF archival · S112 worktree-base-staleness · S88/S99/S126 path-discipline. **Op: `timeout:600000` foreground commits; `git commit -F -` for §/${}-bearing messages; land-before-cleanup.**

## Tags
#session-233 #close #marathon #v1-is-the-language #compiler-architecture-skeleton-ratified #resolver-hybrid #ssr-b-substrate-landed #server-load-authority-landed #currentUser-ambient #9-dives #wrap-and-push
