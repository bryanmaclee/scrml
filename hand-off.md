# scrml — Session 232 (CLOSE)

**Date:** 2026-06-30. **Profile:** A — FULL (booted via `/boot`). A **marathon autonomous board-drive**: the user opened with *"dont push, dont wrap. run autonomously on all you can"*, delegated all 3 pending rulings (*"your lean on all 3"*), then authorized the two security builds (*"go"* → protect-floor; *"extend the protect-floor to channel/SSE"*), and closed with *"wrap and push"*. **9 landings**, zero regressions, every surfaced finding chased to ground. Mechanical stream → `handOffs/delta-log.md` [231]–[241].

## 🚨 NEXT-START
Boot Profile A. Board @close: **HIGH 0 · the 3 D3-suite-found bugs RESOLVED · the V1-security floor BUILT · gaps 16 open / 254 resolved · conformance corpus 51→69 · v0.7.1**. **PUSHED** (S232 wrap+push: scrml 12-ahead all pushed; scrml-support 2-ahead [S231] pushed).

**THE V1 AUTHORITY (unchanged):** `scrml-support/docs/deep-dives/language-compiler-split-2026-06-29.md` (§9 = the remaining-work map). **Conformance status:** `conformance/` corpus is 69 cases (a)+(b); the build-status doc is `docs/changes/conformance-suite-d3-2026-06-29/SCOPE.md` (§6 + the D-2 resolution).

**Remaining V1 work (priority order — the board is otherwise HEALTHY):**
1. **SSR (N8 → `g-tier1-ssr-prerender`, V1-REQUIRED, MED)** — the biggest remaining V1 item; "needs-design-ruling-first → substantial-new-subsystem" (3 rulings gate a build: deployment-target · DOM-prerender-vs-state-inline · auth/protect-during-SSR). SCOPE `docs/changes/g-tier1-ssr-prerender-2026-06-28/SCOPE.md`. **The protect-floor's SSR egress is already redaction-covered (§14.8.9) — coordinate.**
2. **D1/D2/D4 V1-artifact** (the language/compiler split's version surface) — D1 scrml-language semver scheme · D2 per-section `lang:1.0|deprecated|future` spec partition · D4 W-lint deprecation lifecycle. + conformance W4 (version-labeling) / W5 (the Road-B native-impl gate). Mostly native-era / spec-author work.
3. **`g-anon-fn-return-type-invalid-js` (NEW S232, LOW)** — anon `fn(x) -> T { }` return-typed brace-body form emits invalid JS + skips the return-type check (same compiler-defect-framing class as the resolved g-fn-shortform; pre-existing). Has a small SPEC OQ (is the return-typed anon form sanctioned?).
4. **Deferred-by-design (NOT bugs):** the protect-floor A-layer (early static-prove DX error — security never depends on it, per the §14.8.9 ruling); derived/implicit-flow + covert-channel confidentiality (out of the §14.8.9 honest-bound); conformance channels (v1.next per the §-partition).

## 🎯 Design narrative (IRREDUCIBLE — this is the session)

**The whole session was the user handing the PA the wheel** ("run autonomously on all you can") and the PA driving the entire identified board — the 3 D3-suite-found bugs, the two V1-security pieces, both conformance batches — to landed code, surfacing decisions only where genuinely load-bearing.

**1. The 3 suite-found bugs — all RESOLVED, all via user-delegated rulings.** (a) **g-inline-value-match → SUPPORT (codegen).** Adversarially escalated to a CLASS — `${match}` AND `${if/else}` value-forms both rendered empty as sole interp content (silent footgun). §18.0/§17.6 sanction value-form-interpolated-in-markup → the empty render was a codegen bug → capture-via-IIFE + render-slot + reactive-effect, class-wide [233]. (b) **g-nominal-foreign-forms → fail-closed-honest (Option B).** §23.3/§23.4 were entirely unbuilt (N6 *silently miscompiled* — HTML/Worker leak); now `E-WASM-NOMINAL`/`E-FOREIGN-SIDECAR-NOMINAL` [234]. (c) **g-fn-shortform → reject.** `fn(args)=>` is not sanctioned (§48.2.1) → clean `E-FN-ARROW-BODY` parse error [236]. **All 3 rulings were SPEC-grounded (the user delegated "your lean"); the PA verified each against the SPEC before acting (Rule 4).**

**2. V1-security — the AUTHORING half + the ENFORCEMENT floor, both landed.** **Capability-vocab V1 (§23.5)** — the co-located `capabilities=` `<program>` attribute (Form A, user-ratified), the declaration HABIT; manifest-aggregate + Pole-D enforcement DEFERRED; non-breaking-forward [235]. **The §14.8.9 protect-floor** (user "go") — server→client confidentiality redaction via a **`Symbol.for`-keyed origin descriptor** that survives spread/map but is ignored by `JSON.stringify` (so propagation is free, no value-flow obligation); `_scrml_protect_redact` at the egress sinks; `reveal()` the sole declassification; full S215 attack-matrix [238]. Then **user "extend"** → the floor now covers EVERY compiler-emitted client-egress sink (server-fn · SSR · §61 · channel broadcast + cell-write · SSE) [239]. **The §14.8.9 "no-ambient-authority" half was already real (import-only stdlib) — the V1 security story is now: declare (capabilities) + redact (protect-floor) + no-ambient-globals, all shipped.**

**3. Both conformance batches — the (b)-runtime oracle grows.** P1 (errorBoundary/components/lifecycle, +12) [232] + the **server-fn §52 net-new center** (+6, the `serverStub` schema keyed impl-neutrally) [237]. Corpus 51→69. The D-2 triage RESOLVED (80 splitBlocks = impl-internal = excluded; the contract is the (b)-runtime half). **The OQ4 honesty gate paid off again** — the server-fn batch surfaced the §19.9.1 SPEC-vs-impl divergence.

**4. Two agent-surfaced findings, both closed.** The server-fn batch found **§19.9.1's `{__variant,__data}` is stale** vs the impl's `{__scrml_error,...}` — impl-grep confirmed (every emitter + reader uses `__scrml_error`); §19.9.1 corrected [240]. The protect-floor's `/code-review` found **channel/SSE egress was outside the §14.8.9 bound** (a real leak) — surfaced as a scope decision, user said "extend", closed [239]. **Both are the D3-split's thesis working: the conformance + the adversarial review catch contract drift.**

## 🛟 Recovered anomalies / lessons
- **Conformance P1 agent (a382b659) STALLED** mid-full-suite-verification (600s watchdog) but committed incrementally → work fully banked + worktree clean → PA recovered zero-loss (independently re-verified 63/63 + 64/64 gated + a clean-main baseline confirmed the agent's "2 fails" were fresh-worktree browser flake). Reaffirms: incremental commits = clean recovery; verify git-state not the narrative.
- **Worktree-base staleness recurred** — bug1/bug2 branched from a stale `e7718c04` (not live HEAD). Handled via the S226 "unchanged-on-main-since-base" gate (scoped file-delta of only the agent's files). For the LATER dispatches the brief mandated **`git merge main` at startup (S112)** — clean every time. **Brief every iso:worktree dispatch with the `git merge main` startup step.**
- **§34-precedent self-correction (capability landing).** Added the 2 capability §34 catalog rows, then caught the inconsistency with my own §23.5.7 + the §26.8/§60/§61 precedent (NAMED-at-Nominal, §34-rows-land-with-impl) → removed them. The codes stay NAMED in §23.5.7. **Nominal SPEC landings NAME codes in the section; §34 catalog rows land WITH the impl.**
- **Single-writer-on-SPEC/known-gaps held throughout** — agents REPORTED their §34 rows + gap-flips; the PA authored them at landing. Zero SPEC/known-gaps collisions across 6 SPEC-touching landings.

## Board @close
**9 PA landings (origin/main..HEAD before push):** `78dabfdb` conf-P1 · `3d319f1d` bug1 · `cd887f41` bug2 · `e321da1e` capability · `1512d4bc` bug3 · `c220a5b4` server-fn · `2d5e96c8` protect-floor · `4d6b5756` protect-channel/SSE · `04e7a1bb` §19.9.1 + the wrap commit. Plus the 3 S231 commits (`5e93dd43`/`e86a76d0`/wrap). **Full suite: 25882 pass / 211 skip / 1 todo / 0 fail** (last full run; the §19.9.1 close was SPEC-text-only). **0 worktrees** (all cleaned). NEW: `compiler/src/codegen/protect-egress.ts`; `conformance/cases/{components,error-boundary,lifecycle,server-fn}/` (+18 cases); SPEC §23.5 + §14.8.9-Implemented. **Gaps: 6 resolved, 1 filed-open** (g-anon-fn-return-type LOW). Two cosmetic-currency items regenerated at this wrap (SPEC-INDEX footer drift acknowledged; known-gaps §0 via `state.ts --write`).

## §push / cross-repo
**PUSHED (S232 wrap+push).** scrml 12-ahead all pushed; scrml-support 2-ahead (the S231 `c3c74a8`/`372bcc4` + the S231 wrap) pushed. **Security note:** the protect-floor (+ channel/SSE extension) is confidentiality code — independently verified at the highest bar (38-case suite + R26 on real `protect=` sources, 0 leaks), but it merits a human security review of the shipped redaction before it's relied on in production. No other outbound owed.

## pa.md directives in force
R1–R5 · `---` delimiter · Profile A · **S232 NEW: Nominal-SPEC landings NAME codes in-section, §34 rows land WITH impl** (the capability self-correction). Carried: S231 fail-closed-Nominal invariant + the D3 conformance contract · S230 language/compiler-split + V1=language-1.0 · S226 landing-concurrency (3-way-merge / unchanged-since-base) · S219 PRIMARY-GOAL + flogence digest-boot · S215 adversarial-verify (applied hard on the security code) · S138 R26 · S147 coherence · S136 BRIEF archival · S112 worktree-base-staleness (git-merge-main startup) · S88/S99/S126 path-discipline. **Op note: `timeout:600000` for commits** (in-gate corpus + post-commit full-suite).

## Tags
#session-232 #close #autonomous-board-drive #9-landings #3-bugs-resolved #v1-security-floor-built #protect-floor #capability-vocab-v1 #conformance-51-to-69 #pushed #wrap-and-push
