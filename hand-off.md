# scrml — Session 240 (CLOSE)

**Date:** 2026-07-05. **Profile:** A — FULL (booted `/boot`, then a mid-session RE-GROUND after the boot was found thin). A boot-continuity + adopter-verification session: fixed a thinned boot (cross-machine sync-miss), landed the S239-held W5b arc, caught + fixed a real `scrml build` production-route bug, ran a BaaS-parity DD, and corrected a mis-framed Peter/#24 strategic thread. Mechanical stream → `handOffs/delta-log.md` [383]–[389].

## ⚠️ READ FIRST — the boot lesson (why S240 opened thin)
The S240 `/boot` loaded **~20% of Profile A**. Root cause: **this machine's `scrml-support` clone was 141 commits behind origin** (frozen S172) → `pa-scrml.md` + PRIMER-refs + user-voice read as ABSENT, and the flobase `/boot` (no `.pa-base/profile`) collapsed to a hand-off-only read. The S239 hand-off's cross-machine note SAID to `pull --rebase scrml-support`; the boot skipped it (S43 miss). **FIXED this session:** scrml-support synced (141 FF), full contract re-read, and `.pa-base/profile` (`a2678357`) hand-authored so the next `/boot` loads the full chain (pa-scrml.md IN FULL → PRIMER → SPEC-INDEX → master-list §0 → hand-off → user-voice tail). **Next boot: the profile now exists — but STILL run the S43 cross-machine `git fetch && pull --rebase` for scrml AND scrml-support before reading state.**

## 🚦 STATE @ S240 close
- **git:** scrml HEAD `364def58`, **origin PUSHED** (S147 coherence 0/0 at wrap — the 3 S240 commits: build-fix `ef902815` + profile `a2678357` + W5b `364def58`). scrml-support synced to origin `374ec92` (read-only this session; nothing written there — the BaaS DD wrote to it, see below).
- **⚠️ scrml-support has an UNCOMMITTED write:** the BaaS DD doc (`docs/deep-dives/baas-parity-worth-it-2026-07-05.md`) was written by the deep-dive agent but NOT committed/pushed to scrml-support. **Owed: commit + push scrml-support** (the DD doc + any user-voice append).
- **Board:** HIGH **0** · MED **16** · LOW **14** · Nominal **7** (known-gaps §0 @generated; g-tool-import-db-fn-null-stub flipped resolved this session). Integrated full suite **26520/26879** (143 fail = the machine's browser/subprocess env-noise floor; ZERO codegen-regression delta). Version 0.7.1.
- **⚠️ MACHINE ENV:** `bun test` needs `bun install` first (this machine had never run it → `acorn` missing → the full-suite segfaulted until installed). The 143 "failures" are ALL env-dependent (browser tests need a browser+dist; endpoint tests shell out to `node --check`). Real gate = the DELTA vs this floor (which was zero for W5b).
- **⚠️ MAPS:** watermark `66a3afb1` — ~19 commits stale. Bounded refresh OWED (full-set project-mapper OVERFLOWS — same as S239).
- **Worktrees:** CLEAN (main only; w5b-consolidation worktree+branch removed at 6b). No deputy (retired S219).

## ✅ LANDED + PUSHED this session
- **W5b in-process db-library — CONSOLIDATED + LANDED (`364def58`).** The S239-held 14-finding arc closed via ONE consolidation (canonical `bodyContains` predicate + structural async fixpoint + tool-dep-set reroute [import+re-export edges] + server-ss1 parity + export fail-closed). Adversarial `/code-review` (3 reviewers, S239 mandate) found+fixed 3 MORE real bugs (escape-hatch async under-coloring · re-export tool-dep miss · type-annotated-export-const regression). SPEC un-stages §44.7.1/§12.6/§21.5.1/§64.5.1. S67 file-delta (fully disjoint). `g-tool-import-db-fn-null-stub` RESOLVED. Forensic branch removed (was `0e0cfe91`).
- **`scrml build` production-route bug — FIXED (`ef902815`).** `build.js` route-collector regex only matched `_scrml_*` → DROPPED every `__ri_route_*` server-fn RPC route from the production `_server.js` → data-tier 404 in a `scrml build` deploy (worked under `scrml dev`). Hits EVERY adopter who deploys a server fn. +2 regression tests. Verified end-to-end (headless Chrome → db-backed render).
- **PA boot manifest (`a2678357`, `.pa-base/profile`)** — hand-authored (NOT `/flobase`-assembled: this is the reference PA flobase emulates). Encodes the Profile-A read-set + the S43 cross-machine sync.

## 📋 NEXT-START — the queue (prioritized)
1. **⛔ GITI-033 (HIGH, unaddressed) — `<each>` item accessor `@.` not lowered inside a ternary-markup `${cond ? <markup> : ""}`** → E-CODEGEN-INVALID-LOGIC. **Blocks giti's status + land pages from compiling at all.** Clean minimal repro + control provided (inbox `handOffs/incoming/read/2026-07-05-1306-giti-...`; giti `ui/repros/repro-32`). The item-accessor lowering pass doesn't descend into the ternary-markup branch (same family as the partially-fixed GITI-032). Likely a contained fix. **This is the top adopter-blocker on the board and it's been sitting since boot.**
2. **⭐ BaaS-parity — the WORTH-BUILDING set** (`scrml-support/docs/deep-dives/baas-parity-worth-it-2026-07-05.md`), ranked by developer-work-around-effort (bryan's ratified metric; adopter-demand EXCLUDED):
   - **#1 realtime over EXTERNAL db writes** — the biggest glue-gap + completes scrml's realtime story. LISTEN/NOTIFY vs logical-replication is the design fork (a debate candidate). Build: `emit-channel.ts` publish into `ws.publish(topic)` + `scrml:redis` pub/sub backbone. Ties into Peter's live-alert + flogence's compiler-as-oracle/concurrency threads.
   - **#2 auth flows + JWKS** (best ROI — primitives exist, flows don't; `stdlib/auth/flows.scrml`) · **#3 remote PG schema introspection** (`schema-differ.js readActualSchema` PG sibling; `scrml introspect`) · **#4 blob storage upload/presign** (`stdlib/storage`).
   - SKIPS (small work-around / not-scrml's-job): vector/FTS · email/SMS/push · platform-cron · managed-DB-ops · generated-typed-client (already have) · offline-sync (defer).
3. **Peter / GH #24 reply** — DRAFTED (`scratchpad/issue-24-reply-DRAFT.md`), HELD. Now that the whole-stack thesis is re-loaded, REVISE (drop the "overturns a ratified architecture" framing — that was a thin-boot mis-read; scrml's whole-stack identity is the 215-session thesis, and the Peter-side Supabase arch rests on a false "scrml can't be hosted" premise disproven by the build.js fix + the deploy verification). bryan to POST (his voice; not posted yet). Peter is SOLO + non-technical — his S15/S16 "5-expert DDs" were his AI PA. Not blocked (his crew-bridge works).
4. **Owed cross-repo:** commit+push scrml-support (the BaaS DD doc + user-voice); flogence PINGED (W5b landed → they collapse fsp-route into fsp-core + import `?{}` db fns directly — awaiting their R26).
5. **Carried from S239 (still valid):** clean-print `print`/`println` primitive (ruled (a); SPEC+impl+flogence re-port) · flogence residuals D (`g-foreign-multistmt-value-block-mislowers`) + F (`g-tool-over-imports-all-lib-exports`, crash-half fixed by W5b, over-import remains) · `g-e-sql-009-no-db-src-not-fired` (typer gate) · fail-variant-arity E-TYPE mint · hostmethod-poison · bounded maps refresh · flogence "compiler as oracle" 1351 thread (still UNREAD in `incoming/` for a gut-read — the 4-ask ledger ties into BaaS #1's concurrency/region-leasing).

## 🔭 Strategic frame (re-grounded this session — pa-scrml.md + PRIMER + user-voice S233-238)
V1 = **scrml-LANGUAGE 1.0** (language split from compiler; conformance suite ~220→200-270 cases; impl#1(TS) conformant). Freeze bar RATIFIED = **high-coverage-first ("do it right")**. Remaining V1 = conformance coverage-GROWTH + D1/D2/D4 labeling, NOT big feature builds. The language/compiler SPLIT (mitosis → many smaller PAs + a near-autonomous conformance-driven compiler build) is the near horizon; Road-B impl#2 lexer token-complete. The flogence 100%-scrml + standalone-tool + W5b thread is the adopter-driven current work. **scrml is WHOLE-STACK** (Rule 2 — "the future of how programming is done for the browser"); it IS the backend, not a view over a BaaS — verified this session (`scrml build --target` → deployed → db-backed browser render).

## pa.md directives in force (re-read IN FULL this session)
R1–R5 · Profile A · **S43 cross-machine sync — pull scrml-support at boot (the S240 miss)** · S239 addendum (adversarial /code-review MANDATORY on our own codegen) · commit PATHSPEC + background-commit (config-B hook; foreground push needs timeout≥300000) · S147 coherence · S226 landing-concurrency (3-way for shared files; W5b was fully disjoint) · S67 file-delta · S88/S90/S99/S126 worktree/path discipline · S138 R26 empirical · S219 orchestrate+default-GO · S227 dock · commit-to-main only after explicit authorization.

## Tags
#session-240 #close #boot-thinning-fixed #w5b-landed #scrml-build-route-bug-fixed #baas-parity-dd #workaround-effort-metric #peter-24-whole-stack #cross-machine-sync
