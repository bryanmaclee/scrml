# ss40 — `@apply` utility-composition build (bug-1 W2) · progress

**Branch:** `spa/ss40` (worktree `../scrml-spa-ss40`, base `7d8b527a`) · **Build tip:** `913bf8be` + sPA bookkeeping commit · **Status:** ALL items done/landed. List COMPLETE.

- **Item 0 — SURVEY** → `done`. Rule-4 correction: `@apply` is SILENTLY DROPPED (not at-rule-passthrough); live path is LEGACY `ast-builder.js:parseCSSTokens` (native opt-in, not updated). All de-risks held; no STOP/park.
- **Item 1 — recognize `@apply` in parser** → `landed-on-branch` (part of `875874e5`). `parseApplyAtRule` + tagged `{apply:[tokens], span}` nodes (both top-level + rule-body loci).
- **Item 2 — expand in `emit-css.ts`** → `landed-on-branch` (part of `875874e5`). `renderApplyGroupedDeclarations` inlines per-utility decls in source order + property-level last-wins dedup (composing-family shorthands collapse to one; distinct `--tw-*` setters survive).
- **Item 3 — 3 diagnostics + §34 + banner** → `landed-on-branch` (`875874e5` diagnostics + `e0f41583` docs). All 3 `E-APPLY-*` Error codes with `loc`; §34 +3 rows; §26.8 banner→Implemented; bug-1 known-gaps RESOLVED; §0 MED 14→13.
- **Item 4 — tests + dogfood** → `landed-on-branch` (`913bf8be`). 17-test integration suite (dogfood `.btn`/`.card` + §26.8 sample compiled + CSS-inspected). R26 + full suite.
- **Resolver** → `ad087b62` (`resolveApplyToken` + `applyTokenHasVariant`).

**Verify:** agent full `bun run test` = 25536 pass / 0 fail / 214 skip (1110 files); each commit hook-gated incl. browser/TodoMVC. sPA FF-merged + re-ran the new test (17/0) + confirmed §34/§26.8/§0 flips + adversarial composing assertion. No `--no-verify`.

**sPA currency fix:** SPEC ~line 16285 cross-ref `§26.8 (S223, Nominal)` → `(S223 spec; Implemented ss40)` (agent flipped the section banner but left this stale).

**Design-judgment calls (PA to ratify):** last-wins dedup scoped to apply-bearing rules · `space-x-4` combinator → non-inlinable · dogfood-in-test-not-example. **Deferred:** pre-existing `//`-in-arbitrary-value `#{}` limitation; `master-list @generated:recent-sessions` stale (PA-owned).

**End-state:** all items done/landed. No parked/dropped.
