# ss29 — Tailwind utility coverage — progress log

Branch `spa/ss29` (base `b11fa160`, worktree `../scrml-spa-ss29`). 3 items, all on `tailwind-classes.js` (one surface → sequential within-list; per-item PA inbox ping). Boot S221 (this session continues from ss18).

## Timeline
- **Item 1 — g-tailwind-bare-transition-family** (MED) — ✅ **LANDED `00fd6e40`.** Agent commit 3582ad38 (FF-merge, no merge commit) cherry-pick-`-n`-squashed. `registerTransition()` + the test (34). R26 independent: 13/13 utilities emit correct CSS, zero ghost-lint; 34/34 from repo root. **ADVERSARIAL-VERIFIED the agent's 23-fail report:** confirmed pre-existing CWD artifact — the doc-marker tests read `compiler/PIPELINE.md`/`SPEC.md` relatively, ENOENT from `compiler/`, pass 64/0 from the repo root (the hook's CWD). NOT item-1-caused (item 1 touches only the 2 tailwind files); hook genuinely passed. Memory-gated commit (landed at 8932MB-free window).
- **Item 2 — bug-1 safelist/@apply** (MED) — **PARKED → escalate to PA.** Verify-scope-first OUTCOME: it's a DESIGN RULING, not a buildable scope. SPEC §26.5 ("Open Items") confirms "a safelist/`@apply` mechanism … remains deferred"; known-gaps bug-1 (L1281) is explicit: "no ruled direction → PARKED to PA (design ruling: safelist config knob vs `@apply` support vs `#{}`-class-scan suppression)." The `lint.tailwind-unrecognized-class = off` escape hatch (§28) already covers heavy-custom-CSS adopters. **Nothing to build until the PA/user rules a direction.** All sub-arc 1+3 (string-shaped + ring-offset) + the 4 composing families already LANDED (S210/S191). → re-integration message.
- **Item 3 — g-tailwind-group-parent-state-variant** (MED) — ✅ **LANDED `4d219f49`.** Agent commits 143ddce3 (code+test+3 migrated suites) / d69e7a91 (SPEC §26.3/§26.5) cherry-pick-`-n`-squashed. NEW parent-state variant KIND (descendant combinator). R26 independent: `.group:hover .group-hover\:p-4{padding:1rem}` + `.group:focus .group-focus\:bg-blue-500{}`; group marker no rule; peer-hover: still warns (both lints via direct API) + emits no CSS (peer-* NOT over-reached). Migrated suites 291/0 from root; 0 new fails. SPEC amendment FLAGGED for PA ratification (un-defers group-* half of SPEC-ISSUE-012). Memory-gated commit (11141MB window).

## RUN COMPLETE — ss29 fully dispositioned
spa/ss29 tip `4d219f49`: item 1 `00fd6e40` (landed) · item 2 (PARKED→PA, design ruling) · item 3 `4d219f49` (landed). Re-integration message → `handOffs/incoming/2026-06-26-from-spa-ss29-…`. PA owns: merge→main, push, ratify §26.3/§26.5 group-* wording, route bug-1 item 2 to a design-ruling track, flogence S14 reply (items 1+3 closed). Clean tree. sPA stands down.

- **Item 3 (original pre-scope note, archived)** — **SPEC TENSION (flagged + handled per above):** §26.5 currently DEFERS `group-*`/`peer-*` (SPEC-ISSUE-012) + normatively mandates `SHALL emit W-TAILWIND-001` on `group-hover:p-4`. Building item 3 requires AMENDING §26.5 (un-defer `group-*`, remove the warn-mandate for the now-supported form, add the parent-state-variant normative text). Fix DIRECTION is ruled (descendant-combinator `.group:hover .group-hover\:X{}`) + PA listed it as a build → BUILD with a minimal §26.5 currency update; flag the normative-text RATIFICATION to PA (ss18-§61.5 model — sPA doesn't unilaterally author SPEC normative text for a new variant kind). Locus: `STATE_PSEUDO_CLASSES:1544` (no group), exclusions `:2537`/`:3060` — new variant KIND (descendant-combinator emission path, distinct from pseudo/media).
