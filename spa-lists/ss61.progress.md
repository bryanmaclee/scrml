# sPA ss61 — progress log (conformance authoring: L22 type-as-argument family)

Branch: `spa/ss61` (worktree `../scrml-spa-ss61`) · base `origin/main` @ `cfba6295`.
Baseline: `bun conformance/run.ts` → **72/72 cases pass** before any item.
node_modules symlinked into the fresh worktree (root + compiler).

Land per-item on `spa/ss61` (file-delta from the dev-agent worktree → verify `run.ts` green → single sPA commit).
Ping `handOffs/incoming/` per item. Never advance main / never push. ESCALATE impl#1-vs-SPEC divergences.

NB — ss61 items author DISJOINT case dirs (parse-variant/ · schema-for/ · table-for/), so each dev-agent bases
independently off spa/ss61 (no inheritance chain). Run by the SAME sPA session as ss55 (self-host lexer),
kept strictly separate; all commits memory-gated against the parallel-sPA OOM storm (S219).

| # | item | status | SHA | run.ts | divergence |
|---|------|--------|-----|--------|------------|
| 1 | parseVariant §41.13 (RT + E-PARSEVARIANT-* codes) | **landed** | `2a37e296` | 79/79 (72+7) | **D1/D2/D3 parked → escalated** |
| 2 | schemaFor §41.15 (codes + happy) | **landed** | `ab8eef6a` | 84/84 (79+5)* | DDL not RT-observable (server-side) → codes+compiles-clean |
| 3 | tableFor §41.16 (RT render + E-TABLEFOR-* codes) | **landed** | `ab8eef6a` | 88/88 (79+9) | **tbody-hoist harness note → escalated** |

*schema-for + table-for landed in ONE commit `ab8eef6a`; 88/88 total (79 baseline + 4 schemaFor + 5 tableFor).

## Log
- **item 1 dispatched** (agent ad6c4fd6) — parseVariant §41.13. Cases under `conformance/cases/parse-variant/`:
  happy-path (valid JSON → tagged variant in state), the 4 runtime ParseError modes (MissingDiscriminator /
  UnknownVariant / InvalidPayload / Malformed via `!{}`), + the E-PARSEVARIANT-* compile-time codes. Method:
  author-from-impl#1 → cross-check §41.13 → escalate divergences. Brief archived to
  `docs/changes/ss61-parse-variant/BRIEF.md`.
- **item 1 LANDED** `2a37e296` (dev-agent `ss61-parse-variant` @ 7b314e44, file-delta'd in). 7 cases: happy-payload /
  happy-unit / error-{missing-discriminator,unknown-variant,invalid-payload,malformed-json} / misuse-non-enum-type.
  **79/79 green (72 baseline + 7), independently re-verified.** Coverage: §41.13 has exactly ONE compile code
  (`E-PARSEVARIANT-TYPE-NOT-ENUM`); the other 3 are RUNTIME codes via ParseError variants. **3 impl#1-vs-SPEC
  divergences PARKED + escalated** → `handOffs/incoming/ss61-item1-ESCALATION-divergences.md` (D1 null-routing;
  D2 single-field `!{}` binding; D3 recovery-value-on-assign) — cases authored to NOT depend on them.

## Items 2-3 (continuation — completed by a later sPA session, S235 2026-07-03)
- **items 2-3 LANDED** `ab8eef6a` — schemaFor §41.15 (4 cases: happy + TYPE-NOT-STRUCT/PICK-INVALID/
  PICK-OMIT-CONFLICT) + tableFor §41.16 (5 cases: render-rt + TYPE-NOT-STRUCT/NOT-IMPORTED/ROWS-MISSING/
  COLUMN-FIELD-UNKNOWN). **88/88 green** (79 baseline + 9), independently re-verified. Empirical-first
  (probe impl#1 → cross-check §41.15/§41.16 read in full). NO impl#1-vs-SPEC divergence in the codes.
  - **schemaFor is codes-only + compiles-clean** in the harness: its DDL output is a compile-time/server
    migration artifact (not a client-runtime effect) + the exact SQL is D3 impl-freedom, so no RT/DDL-shape
    assertion is sound. Codes are the contract (like ss60's server-side surfaces).
  - **ESCALATION (tableFor tbody-hoist):** tableFor's row cells render into an each-mount `<div>`; the
    conformance adapter mounts via `document.body.innerHTML = ...` (string parse), and an HTML parser HOISTS
    a `<div>` out of `<tbody>` (invalid table content) → the live `<tbody>` is EMPTY and the 6 cells sit in a
    sibling `<div>` before the table. Header `<thead>` is intact. The render-rt case asserts the impl-neutral
    effect (3 `th` + 6 `td`) LOCATION-AGNOSTICALLY (total counts, never tbody nesting). Likely a string-mount
    artifact — a real browser hydrates via DOM APIs (the D2 DOM-adoption path), not innerHTML string-parse —
    so this is probably a harness limitation, NOT an impl bug; but emitting a `<div>` mount inside `<tbody>`
    is at minimum table-context-fragile. → PA: confirm harness-artifact vs a tableFor table-mount codegen fix.
