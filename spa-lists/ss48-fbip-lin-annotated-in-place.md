# ss48 — FBIP increment-2: `lin`-annotated in-place (compiler-reimagining mechanics-beauty) — SURVEY-FIRST

**Fill-note:** the SECOND graduated step of the FBIP (functional-but-in-place) program — increment-1 (HAMT structural-sharing maps) LANDED as ss38 (`5fb41cb9`, S223). **Q-FIP RATIFIED S224:** the annotated-tier marker is the existing **`lin`** (REUSE — NOT a distinct Koka-style `fip` keyword), with **SILENT fallback to clone** when in-place is unprovable (NO loud "prove-in-place-or-error" guarantee). At a COW site whose receiver is a `lin`-bound value and whose update is the single (last) consumption, codegen emits the **in-place** form (mutate the existing storage, skip the clone); everywhere else, today's clone is unchanged. **Soundness is carried ENTIRELY by the existing §35 linear pass** (exactly-once consumption + dead-after) — this increment has **NO new inference pass** (the inferred tier is a later Road-B program wave, item 2 below). Source stays 100% pure — in-place is an unobservable emit-time optimization (limit-primitives: the source language is NOT widened). **Design-complete** (Q-FIP + the fbip-feasibility DD §"Minimal first increment" (2)+(3)).

**⚠️ SEQUENCING.** (a) Increment-1 (HAMT, ss38) MUST be landed first — it is (`5fb41cb9`, S223); inc-2 rides the HAMT/array rep (a HAMT can be mutated in place when proven unique — the Clojure-transient case). (b) Touches **`type-system.ts`** (the §35 `lin` pass — surface the exactly-once/dead-after fact to codegen) → **SHARES `type-system.ts` with ss43** (multi-scrutinee match, `checkMatchDiagnostics`/exhaustiveness region) — different regions (lin pass vs match exhaustiveness), likely disjoint, but **reconcile per-base at landing** if both are in flight. **The reactive-cell carve-out is HARD** — reactive `@` cells are EXCLUDED (§6.5.3 keyed reconciliation needs the new reference); target = non-reactive `let`/`const`/`fn`-param/`lin` accumulators ONLY.

**Shared ingestion:** the §35 `lin` linear-type pass (`type-system.ts`, ~74 E-LIN sites — exactly-once consumption + dead-after-consumption) · the COW codegen seams (`emit-bindings`/`reactive-deps` + `_scrml_deep_set` + the `.insert`/`.concat`/array clone-mutate-replace lowering) · the §59 map/array runtime (HAMT from ss38) · the differential-testing-harness pattern (ss38's `value-native-map-hamt-differential` is the in-miniature precedent — FBIP-on vs FBIP-off, any diff is a bug).

**coreFiles:** `compiler/src/type-system.ts` (lin pass → expose a per-COW-site "receiver is `lin` + this update is the single consumption + dead-after" flag) · `compiler/src/codegen/{emit-bindings,reactive-deps}.ts` + `_scrml_deep_set` (add the in-place branch gated on the flag) · the runtime map/array in-place path on the HAMT/array when flagged · a NEW differential-testing-harness test (FBIP-on vs FBIP-off byte-identical emitted-JS / observably-identical runtime).

**Brief reminders:** **SURVEY-FIRST** — Phase 0 confirms (a) the §35 lin pass already computes exactly-once/dead-after at each consumption and can surface it to codegen WITHOUT a new analysis pass (the annotated tier inherits `lin`'s proof; there is NO inferred tier in this increment); (b) the precise COW seam(s) to branch; (c) the reactive-cell carve-out is structurally enforceable (only non-reactive bindings reach the in-place branch). **The differential-testing harness is the soundness gate** — a correct in-place optimization is observationally INVISIBLE, so any output diff is a bug. **Soundness is adversarial-verification-grade** (`feedback_adversarial_verify_not_confirmatory`): a wrong in-place flag is a SILENT purity violation (the worst possible failure — no error, wrong answer). Clone-always is the FREE sound floor → flag in-place ONLY on the lin proof, default to clone on ANY doubt. **Silent fallback (Q-FIP)** — NO loud error when in-place is unprovable. SPEC = SMALL (an optional §35/§34 normative note that in-place is an unobservable optimization + `lin` is reused; per Rule 4 any normative text lands WITH the impl). R26 + **FULL `bun run test`** (the differential harness + within-node parity live in the full suite, not the pre-commit subset).

## Items

1. **`lin`-annotated in-place codegen (FBIP increment-2, annotated tier)** `[status=PARKED · agent=a7cc926f329994478 · 2026-06-27 · needs USER/PA design ruling]` **SURVEY-FIRST · adversarial-verify mandatory**
   - **PARK (Phase-0 soundness fork, sPA-verified):** the ratified premise *"soundness carried ENTIRELY by the §35 lin pass"* is INCOMPLETE. §35 certifies **binding-level** exactly-once + dead-after; in-place additionally needs **storage-level** uniqueness, which §35 does NOT establish. `lin a = base` (live `const base`) compiles with ZERO E-LIN → bare alias `const a = base` (lin is analysis-only, no defensive copy) → an in-place rewrite of the single consumption `a.concat([4])` would corrupt the still-live `base` (clone→3, in-place→4). sPA-confirmed: zero-E-LIN on the alias (2 clean compiles). Secondary (agent-claimed, PA-to-confirm): map-op seam is `@`-reactive-gated only (carved out) + non-reactive local-map `.insert` emit-broken; §35.2 lin-immutable kills the `acc = acc.concat` reassignment shape; §34.4.4 bars lin loop consumption → headline DG-fixpoint/lexer accumulators are inferred-tier (out of scope). No sound build exists within the ratified constraints (lin-only proof · no inference · no runtime check). Agent rec: **fold annotated tier into the inferred-tier Road-B wave**; surface non-reactive-map-emit as a separate bug. See `handOffs/incoming/` re-integration msg + agent transcript.
   - **Phase 0 (survey):** locate where the §35 lin pass records exactly-once/dead-after per consumption (`type-system.ts`); locate the COW seams (`.insert`/`.concat`/`_scrml_deep_set`/array clone-mutate-replace) in codegen; confirm a `lin`-bound collection consumed by an update can be flagged dead-after AT that seam; confirm the reactive-cell carve-out is structurally enforced (no `@`-cell write reaches the in-place branch). Park any genuine design fork for PA→user.
   - **Build:** wire the lin exactly-once proof → a codegen-consumable "in-place-safe" flag at each COW site whose receiver is `lin` + single-consumption + dead-after; emit the in-place mutate (skip the clone) when flagged; emit today's clone unchanged otherwise (the silent fallback, Q-FIP).
   - **Soundness gate (NEW differential-testing harness):** compile a corpus — INCLUDING the DG-fixpoint and lexer-token-accumulator shapes from the de-risk slices (the threaded-accumulator sweet spot) — with FBIP-on and FBIP-off, assert byte-identical emitted JS / observably-identical runtime. Mirror ss38's `value-native-map-hamt-differential` harness shape. This harness is PERMANENT (it gates any future inferred tier too).
   - **Verify:** R26 (compile a `lin`-accumulator reproducer + `node --check` + runtime correctness) + **S215 adversarial** — the soundness-frontier "stays a clone" cases MUST stay clones: a `lin` value that is read again on another path / captured in a closure (§35.6) / returned-aliased in a struct field / crosses the server boundary (§35.7) — each must NOT be flagged in-place. + FULL `bun run test`.
   - **Footprint:** `type-system.ts` (lin→flag) + codegen COW-seam branch + runtime in-place path + the new differential harness test. SPEC: optional §35/§34 normative note (in-place = unobservable; `lin` reused) lands WITH impl per Rule 4.

2. **(FOLLOW-ON — NOT this lane) full INFERRED reuse** — auto-detect uniqueness on UN-annotated `let`/`const`/`fn`-param accumulators via a backward last-use/escape pass (no value identity §45.6 → liveness, not alias analysis). A later Road-B **program wave** (multi-session; needs its own checkable soundness invariant, not just "passes tests"), GATED on this increment + the harness + a measurement that the annotated+HAMT increment isn't already enough. **NOT an sPA** — recorded here for the program map only.

---

**Authority:** `docs/changes/compiler-reimagining-derisk-2026-06-26/RULING.md` §"mechanics-beauty program" (2) · `scrml-support/docs/deep-dives/fbip-feasibility-2026-06-26.md` §"Minimal first increment" (2)+(3) + §"Soundness frontier" · Q-FIP ratification S224 (user-voice S224: *"I think Im ok with lin for this"* + *"I think silent fall back is fine"*) · ss38 (increment-1 HAMT — the landed precedent + the differential-harness pattern). Memory: `feedback_limit_primitives_not_godify` · `feedback_adversarial_verify_not_confirmatory` · `feedback_self_host_is_from_scratch`.
