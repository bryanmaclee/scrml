# ss52 — non-reactive local map/set method lowering (the (c) bug) — progress log

- **2026-06-27 boot:** list `ss52-nonreactive-local-map-set-method.md` read; footprint scoped
  (emit-expr.ts 3+1 `@`-gates · collectors in `reactive-deps.ts:373/463` [list named call-sites] ·
  scheduling.ts threading). Branch `spa/ss52` cut from **local main `2310b53a`** (NOT origin/main
  — local main carries the landed ss43 emit-expr.ts change; stale-origin base would conflict).
  Branch created WITHOUT switching the shared checkout (ss43 anomaly lesson).
- **2026-06-27 dispatch:** item 1 → `scrml-js-codegen-engineer`, isolation:worktree, opus.
  VERIFY-FIRST + two-part fix (scope-aware collector + relax `@`-gates) + R26 (compile + RUN) +
  adversarial. Brief archived `docs/changes/nonreactive-local-map-set-2026-06-27/BRIEF.md`.
- **2026-06-27 agent result:** fix complete, 7 commits, tip `2c088292`, 10 files +836/-76.
  `bun run test` 25610 pass / 0 fail. Worktree base was stale (c134e500) → agent FF'd to
  2310b53a before work. Scope larger than the 3-gate sketch (justified: per-fn scope-awareness
  threads through ~10 emit-ctx sites incl. control-flow; +2 reachability gaps surfaced at
  R26-RUN — stdlib-data chunk gate for local-set algebra [fixed in scope, real crash] + a
  pre-existing REACTIVE control-flow twin gap [out of scope, filed]).
- **2026-06-27 verify+land:** adversarial review — **reactive `@`-path byte-identity confirmed
  structurally** (`mapCellBareName`/`setCellBareName` keep the `@`-branch logic identical; only
  add a non-`@` local branch). known-gaps + §59/§59.12 banner edits reviewed (accurate).
  Squash-merged the 7 agent commits + BRIEF into ONE sPA commit via an isolated land worktree
  under `.claude/worktrees/` (node_modules resolves upward — avoided the ss43 symlink dance).
  Shared checkout left on main, untouched. **tip `5ebdbce3`** — full pre-commit suite + gauntlet
  TodoMVC + browser post-checks GREEN. No allowlist re-baseline.
- **Item 1:** landed @ `5ebdbce3`.
- **Re-integration:** main advanced +4 (ss49 work) during the run; `emit-logic.ts` OVERLAPS
  ss49's `b731fda2` but `git merge-tree --write-tree main spa/ss52` = exit 0 (**clean
  auto-merge**, non-overlapping hunks). PA: re-run the suite post-merge (textual-clean ≠
  semantic-clean on a shared file). Re-integration message filed.
- **RUN COMPLETE — 1/1 landed-on-branch.** Agent worktree `worktree-agent-abe77606676ae05e0`
  (tip `2c088292`) left intact as PA fallback.
