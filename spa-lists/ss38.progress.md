# ss38 — HAMT structural-sharing maps · progress

- **Item 1 — HAMT map runtime swap** → `landed-on-branch` SHA `9787078a` on `spa/ss38`.
  - Crash-recovery: prior ss38 session dispatched scrml-js-codegen-engineer (worktree
    `agent-a923ae997f5796380`), agent committed the full work as `f30d61bf` (clean tree,
    pre-commit gate green at base 12600217), then the sPA session died before verify+land.
  - This session: confirmed 0 file-overlap between the agent's 4 files and the 3 intervening
    doc commits (12600217..69cee28b) → rebased onto current `origin/main` (69cee28b) via
    cherry-pick → `9787078a`. Pre-commit blocking gate (unit+integration+conformance) PASSED.
  - Adversarial verify: ran the soundness gate directly — `value-native-map-hamt-differential-ss38`
    + `value-native-map-e2e-d4` + `value-native-set-e2e` = 32 pass / 0 fail / 4793 assertions.
  - Strategy A (lazily-materialized memoized `.entries` compat view) → zero edits to the 3
    direct-`.entries` readers + 2 replicas. Perf ~988x faster at N=8000 (S94 blow-up fixed).
  - **Set came for free** (set[K] desugars onto the §59 map — reactive-deps.ts:331); the
    list's "swap map+set together" contingency is satisfied automatically by the single swap.
  - Only failures: browser-todomvc dist env-gap (S209 worktree gitignored-dist; excluded from
    the blocking gate; post-commit gauntlet recompiled + reported TodoMVC PASS).

**End-state:** item 1 = landed-on-branch. No parked / dropped items. List COMPLETE.
