# ss64 progress — conformance: §26.8 `@apply` utility composition

Branch: `spa/ss64` · Worktree: `../scrml-spa-ss64` · Base: `40b580c5`

- **[COMPLETE]** All 7 items landed on `spa/ss64` @ `40ddc015` (single commit — thin, cohesive `apply/` category). conformance (impl#1) **393/393** pass (was 386, +7). Pre-commit full suite green (20031 pass / 0 fail).
  - Authored the NEW `conformance/cases/apply/` category (7 dirs × {case.scrml, expected.json}).
  - All compile-time, codes-only (emitted CSS is D3 impl-freedom — never byte-asserted).
  - Every expected code EMPIRICALLY verified against the impl#1 adapter before locking (README OQ4).
  - Lifted from `compiler/tests/integration/apply-utility-composition.test.js` (Option-D twin; sources match: `flexx`, `hover:bg-blue-500`, `prose`, dynamic `.card`).
  - neg cases pin severity=Error + disjointness from the sibling E-APPLY codes (classifier pinned).
  - `prose` empirically fires E-APPLY-NON-INLINABLE-UTILITY (not UNKNOWN) — confirms §26.8.2's "prose is recognized-but-multi-rule" claim.

## Observation flagged to PA (non-blocking, out of sPA scope)
- **E-DG-002 false-fire on class-attr interpolation reads.** A state cell read ONLY inside
  `class="${@cell ? 'x' : ''}"` false-fires E-DG-002 ("declared but never consumed") — the DG
  does not count a class-attr interpolation read as a consumption. Parallels the already-resolved
  `<span : @label>` E-DG-002 false-fire (SPEC.md:1009). Worked around in the case-7 fixture by also
  reading the cell in text; the case is a clean positive. Possible real DG gap — PA to triage.

## Parked / dropped
- None. Full list landed.
