# ss37 — value-native Set (§59.12 un-defer) — progress

Branch: `spa/ss37` (from `origin/main` @ `1ada2b3e`).

- **Item 1 — value-native Set §59.12 (un-defer) — PARKED (SURVEY-FIRST shape-fork → PA→user).**
  - Phase-0 survey verdict: **un-defer RATIFIED S222** (`RULING.md` §20; user-voice 10769) — supersedes the S170 deferral; build warranted. **But SHAPE NOT build-ready** — §59.12 specifies NO set type ("B2-over-map on the shelf"); no normative grammar/literal/method roster. S170 pre-leaned B2 + eliminated A *conditioned on "zero demand"*; the S222 DG-builder exerciser re-opens A-vs-B2. The ss37 list's described surface (`.add` + method-form algebra + "literal syntax to confirm") straddles A and B2.
  - sPA cannot rule the shape or author §59.12 normative text → parked. Fork developed for the PA in `handOffs/incoming/2026-06-26-from-spa-ss37-set-shape-fork-FOR-PA-RULING.md`.
  - No build dispatched (dispatching now would force the dev-agent to invent the shape = a design ruling by proxy — exactly what SURVEY-FIRST guards against).
  - Once shape ruled + §59.12 text lands: build is mechanical (mirror S169 Map landing D1-D4; ss38 HAMT already in main → shared-§59-runtime sequencing satisfied, Set rides HAMT rep).

**UPDATE — ruling received (in-place via list-file edit, NOT inbox).** PA ruled **B2 (map alias)** via AskUserQuestion; §59.12 normative text authored (SPEC §59.12 set bullet, `49e04332`). Item 1: parked → **BUILD-READY → BUILDING**.
- PA correction noted: my "ss38 HAMT already in main" was wrong — only the ss38 LIST FILE is in main; no HAMT in the runtime; ss37↔ss38 sequencing STILL LIVE.
- **Sequencing resolved (sPA):** B2 Set is a pure DESUGAR (`set[K]`→map `[K:bool]`; `.add`→`.insert(_,true)`; algebra delegates to `scrml:data`) → **adds NO new runtime → rep-agnostic**. Building Set on the current COW map now is safe + explicitly sanctioned by the ss38 list ("if ss37 landed on COW, HAMT-swap map+set together"). No ss38/HAMT branch or parallel session active (verified) → no parallel-fire / shared-baseline collision.
- **Build dispatched** scrml-js-codegen-engineer (worktree, opus); brief `docs/changes/set-type-b2-2026-06-26/BRIEF.md`. Mirror S169 Map landing D1-D4 as a desugar; flip §59.12 Nominal banner; R26 + full `bun run test`.

- **LANDED (2026-06-26):** agent (scrml-js-codegen-engineer, worktree `af02d2a2`) completed all 3 D-step commits. sPA verified, squash-merged onto `spa/ss37` @ **`70b11383`** (19 files, +1047/-21; single sPA commit). SPEC.md auto-merged clean (tuple-reverse @8181 + set banner @32905/33080 both preserved).
- **R26 verify — adversarial catch:** dev's full-suite run (unit+integration+conformance, == pre-commit hook scope) MISSED the top-level M6.5.b.0 within-node parser-conformance gate. TRUE full `bun test compiler/tests/` (25,689 tests) flagged `examples/34` over-budget (residual 191, no allowlist entry). Diagnosed ORDINARY native-vs-within-node span/field divergence (normal class-distribution; KIND-NAME 3 → not set-specific; parser was a verified no-op). Re-baselined the allowlist per brief mandate → **0 fail / 25474 pass**. Independent `node --check` of emitted client + worked-example compile (exit 0) confirm R26.
- **Sequencing dissolved:** Set = pure desugar → rep-agnostic; rides COW now / HAMT after ss38; ss38 has nothing set-specific to do. Not a blocker.

**End-state: item 1 = `landed-on-branch` @ `70b11383`. List COMPLETE (1/1 landed, 0 parked, 0 dropped).** Re-integration message → `handOffs/incoming/`.
