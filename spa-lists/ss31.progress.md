# ss31 progress — codegen expression / JS-validity (3 bugs)

**Branch:** `spa/ss31` (worktree `../scrml-spa-ss31`, rebased onto current main `1ada2b3e`) · **Status:** ALL 3 LANDED + 1 out-of-scope bug surfaced/parked.

Dispatched ONE `scrml-js-codegen-engineer` (worktree, opus) — fixed B1→B2→B3 sequentially (shared serializer surface; no parallel). sPA verified independently (S215 adversarial), then squashed the agent's 3 per-bug commits into one sPA landing commit on `spa/ss31`.

## Items — all LANDED
1. **B1 g-unary-left-of-exponent-no-paren** (MED) ✅ — bare `-@a ** 2` now fires a LOUD, targeted `E-CODEGEN-INVALID-JS` ("a unary operator immediately left of `**` is invalid JavaScript … write `(-@a) ** 2`") instead of silently emitting invalid JS. Root: the AST-path guard (`binaryOperandNeedsParens:1207`) already handled `(-@a)**2` (Bug-A, S221); the bare form errored in acorn → regex/escape-hatch fallback that bypassed the guard. Fix surfaces the diagnostic at parse time (`expression-parser.ts` `detectUnaryLeftOfExponent`/`exponentDiagnostic` → `ast-builder.js` TABError), mirroring the existing `sqlDiagnostic`/E-SQL-008 pattern. REJECT-LOUD per the brief (NOT auto-paren — no design fork taken). `compiler/src/expression-parser.ts` + `ast-builder.js`.
2. **B2 g-enum-toenum-client-structured-decl** (MED) ✅ — client structured `<status> = Status.toEnum(@raw)` now lowers to `(Status_toEnum[…] ?? null)` (both C1 dispatch + C5 reset init-thunk); table present, runtime resolves. Mirror of the ss22 server fix; client-gated (server path untouched). Agent used the AST-native lowered template rather than the Pass-9 regex (the regex `[^)]+` arg-capture mishandles nested-paren args like `Enum.toEnum(f(a,b))` — strictly more robust, Rule 3). giti-relevant (§14.4.3). `compiler/src/codegen/emit-expr.ts` (`emitCall`).
3. **B3 g-double-unary-minus-emit-decrement** (LOW) ✅ — `Unary(-,Unary(-,a))` → `- -a` (was `--a`); `+(+a)` → `+ +a`; in BOTH serializers (`emitUnary` AST path + `emitStringFromTree` twin). Minimal space-split (S215 — avoids new parens). `!!`/`~~`/update-ops untouched. `compiler/src/codegen/emit-expr.ts` + `expression-parser.ts`.

## Within-node re-baseline (S215, in-landing)
ONE fixture legitimately shifted: `g-unary-left-of-exponent-paren.test.js` §1 `nested -(-a) ** 2` from the pre-fix `(--a) ** 2` → `(- -a) ** 2` (now the runtime value `=== 4` is assertable, double-negation not a pre-decrement). Sibling `reactive-compound-assign-and-postfix.test.js` `not.toContain("- -")` UNAFFECTED. Re-baselined in the landing.

## sPA adversarial verification (S215)
- 3 new regression suites re-run independently: 56 pass / 0 fail.
- Direct real-shaped repros: B1 bare → LOUD E-CODEGEN-INVALID-JS (good message); B1 paren `(-@a)**2` → clean; B4 `-(@a**2)` → caught loud-but-generic (confirms the parked finding below).
- Full `bun run test`: agent reported 25395→25429 pass / 0 fail / 214 skip (+34 new tests, 0 new failures); re-gated by the landing commit's pre-commit hook.

## PARKED — discovered out-of-scope bug (PA triage)
**g-unary-of-exponent-arg-no-paren** — `emitUnary` drops needed parens around a `**` ARGUMENT: `-(@a ** 2)` emits `-_scrml_reactive_get("a") ** 2` (same unary-left-of-`**` SyntaxError). DISTINCT from B1 (AST path — acorn parses `-(@a**2)` fine — not the escape-hatch) and B3 (double-unary). Currently caught loud-but-GENERIC ("compiler emitted JS it cannot itself parse") — wants its own targeted fix (wrap a `**`-binary unary-argument in `emitUnary`) + diagnostic. The agent deliberately recommended ONLY `(-@a) ** 2` in the B1 message (not `-(@a ** 2)`) to avoid steering users into this trap; once fixed, the B1 message can add the square-then-negate alternative. **Recommend a follow-up item.**

## Landed on `spa/ss31`
One sPA commit: the 3-bug fix (3 src + 3 test files, +633/-12) + this progress + BRIEF + list-status. Rebased onto current main `1ada2b3e` (the 2 sibling S222 commits are docs/spa-lists-only — disjoint). No main advance, no push.
