# sPA ss23 — progress log (library-mode + foreign-inline codegen seam)

**Branch:** `spa/ss23` (off origin/main `cf9f1109`) · **Worktree:** `../scrml-spa-ss23`

## Boot (2026-06-26 ~early)
- Branched `spa/ss23` from `cf9f1109` (== origin/main, aligns with harness). Verified ALL ss23 deps are in cf9f1109 (df6f747b, the W5a/W5b SCOPE doc, S145 library-mode shaping) → no dependency on the local-only ss21/ss22/ss27 re-integrations. Local main is far ahead at `a1e10727` (PA re-integrated ss22 as `9f27626c` + others, unpushed).
- dist + node_modules symlinked. Briefs carry the S112 `git merge origin/main` + `df6f747b`-ancestor startup step.
- Weekly limit reset overnight (was 7am MDT); all 6 ss22 agents completed cleanly → no probe needed, dispatched all 3.
- Collision note: items 1 (safecall `!{}`) + 2 (W5a auto-detect) may both touch codegen/index.ts library-mode region — briefed BOTH for region-isolation (item 1 = `!{}` wiring; item 2 = library-classification ~L130/L852); reconcile at landing. Item 3 (`_{}` emit-logic) independent.

## Dispatch — all 3 parallel (scrml-js-codegen-engineer · worktree · opus)
| Item | change-id | agentId | status |
|---|---|---|---|
| 1 g-safecall-bang-handler-not-lowered-in-library-mode (MED) | ss23-safecall-bang-library-mode | ab15e44b3d510e933 | dispatched |
| 2 g-library-mode-sql-no-db-context W5a/W5b (MED, SURVEY-FIRST) | ss23-library-mode-sql-w5ab | a0aea3a6357719dba | dispatched (survey→land W5a→park-if-W5b-subsystem) |
| 3 g-foreign-inline-crossing-shadow (LOW) | ss23-foreign-inline-crossing-shadow | a7a74c4c63060cb33 | dispatched |

Landing = cherry-pick onto spa/ss23. **#2 is the STOP-if-bigger candidate** — survey-first, expect W5a standalone + W5b scope-finding (likely parked). **flogence reply owed on land** (#1 + #2 unblock it; #1 reporter = giti).

## CLOSED 2026-06-26 0746 MDT — items 1+3 FIXED, item 2 W5a-landed/W5b-parked. Branch tip `2f8b7bbb`.
- spa/ss23 = base `cf9f1109` + 3 fix commits (item1 `714faaec`, item3 `4f1da876`, W5a `2f8b7bbb`), clean linear, `0 3` vs origin/main → clean PA merge.
- Integrated library/foreign sweep: **62/0 across 4 files**. All 3 items file-disjoint (emit-library / emit-logic+emit-server+SPEC / api.js+compile.js).
- Re-integration message: `handOffs/incoming/2026-06-26-0746-from-spa-ss23-reintegration.md`.
- **flogence reply OWED** (partially unblocked: logic libraries yes via #1+W5a, SQL no until W5b subsystem). **W5b = PA own-list.** Item 3 adds a SPEC row (PA ratify). All 3 agents clean. sPA STOOD DOWN.

## Results (all 3 agents completed cleanly; all file-disjoint)
- **Item 1 LANDED `714faaec`** — locus emit-library.ts (the whole-block extraction path did textual slicing only; never routed through emitLogicNode). collectGuardedExprs + lowerGuardedExprsInBlock span-splice the existing guarded-expr lowering. node --check clean, gate 17854/0.
- **Item 3 LANDED `4f1da876`** — pre-emit crossing-shadow scan → ERROR E-FOREIGN-006 (family is uniformly ERROR + always-invalid-JS). Added SPEC row §23.2.6/§34 + normative §23.2.4a + SPEC-INDEX regen; foreignCrossingErrors sink in emit-server.ts. Gate 17853/0.
- **Item 2 W5a LANDED `018a8507`** (committing bovs0wjuh) — auto-detect library mode (api.js mode-flip + compile.js CLI). ~2h actual vs ~3-6h headline (reused isPureModuleFile + S145 shaping). Gate 25227/0.
- **Item 2 W5b PARKED → PA** (own-list subsystem ~13-22h). **SCOPE-DOC CORRECTION:** the doc's "extend <program db> resolution" framing is EMPIRICALLY WRONG — db-resolution already works (collectDbScopes handles §44.7.1 Form-2 <db src>); the REAL blocker is emit-library.ts being a REGEX source-slicer that never invokes structured SQL-lowering (?{} leaks verbatim → E-CODEGEN). W5b sub-steps: (1) route db-bearing library server-fns through structured emit-server SQL path ~6-10h, (2) cross-file db-context travel §44.7.1 "module owns its connection" ~4-8h + likely SPEC micro-ruling, (3) narrow E-SQL-009 ~1-2h, (4) tests ~2h.

## flogence reply owed (PA, on re-integration)
**PARTIALLY unblocked.** Items 1 (safecall !{} library-mode, reporter giti) + W5a (auto-detect) let flogence author pure-fn LIBRARIES (logic layer) in scrml today — no --mode flag. Its SQL layer (?{} against own <db src>) stays on the TS bridge until W5b's own-list subsystem lands.
