# sPA ss15 — progress

*(append per item: `[order] item-name → scoped / dispatched <agent> / verified / landed <SHA> | parked <reason> | NOT-REPRODUCED`. The PA reads this at re-integration.)*

## S214 sPA run (2026-06-22)
- boot: read spa.md ss15 → spa-scrml.md → list + coreFiles (tailwind-classes.js lint tail, collect.ts, emit-html.ts render-slot handler, emit-lift.js); worktree `../scrml-spa-ss15` @ origin/main 1ce8de34 created (was absent), node_modules symlinked.
- [1] g-tailwind-lint-false-fires-on-scoped-class → R26 CONFIRMED (`/tmp/css-dogfood.scrml`: @scope CSS emits .card/.card-title yet both draw W-TAILWIND-UNRECOGNIZED-CLASS). Root: `findUnrecognizedClasses` (tailwind-classes.js:2984, fired api.js:925) is a text pre-pass blind to author `#{}`/`<style>` selectors. BRIEF archived. DISPATCHED scrml-js-codegen-engineer a5714abcea473b258.
- [2] g-on-mount-bare-call-render-slot → R26 CONFIRMED + footprint matrix CORRECTED (empirical): bug is NOT on-mount-specific — bare `${ val() }` at program-body AND `on mount {@x=val()}` assignment body BOTH emit a spurious render slot. Root: `stmtContainsRenderableLogic` (emit-html.ts:113) returns true for ANY bare-expr; handler at emit-html.ts:~2200/2275 allocates data-scrml-logic span + addLogicBinding(→_scrml_render_value). Fix must be POSITIONAL (markup-child interpolation renders per §17.3/§40.8; program/component-body + lifecycle-hook bodies are EFFECTS per §6.7.1a) — NOT expression-type-based (would break <div>${call()}</div>). PENDING dispatch (sequential, after item 1).
- [3] g-request-lift-nested-interp-mangle → R26 CONFIRMED (`/tmp/lift-item3.scrml`, canonical syntax): `lift <h1>${<#feed>.data}</h1>` in for/lift emits (a) `_scrml_input_state_registry.get("feed").data` instead of `_scrml_request_feed.data` + (b) content-split corruption — leaked literal text node `"feed_.data}"`. Root: emit-lift.js doesn't thread requestIds (built collectRequestIds @ emit-reactive-wiring.ts:293). BRIEF archived (bundled with 4+5). PENDING dispatch.
- [4] g-request-lift-bare-if-reads-input-registry → R26 CONFIRMED (`/tmp/lift-item4.scrml`): `${ if(<#feed>.loading){lift} }` emits `if (_scrml_input_state_registry.get("feed").loading)` instead of `_scrml_request_feed.loading`. Same root as [3]; fixes together.
- [5] spec-677-worked-example-1-doc-migrate → DIRECTION CONFIRMED, MECHANISM CORRECTED. §6.7.7 Worked Example 1 (SPEC.md:4306-4328) AND Example 2 (4330-4348) both use bare if(){lift}/else in <div> (no ${}). INVALID per §17.4/§7 → migrate to ${}-wrapped. BUT list's claim "fires E-CONTROL-FLOW-IN-MARKUP" is EMPIRICALLY WRONG: bare if(){lift} in <div> compiles CLEAN and SILENTLY SHIPS RAW TEXT (`lift <p>Loading...</>`) into .html. Bundled into items-3/4 dispatch; migrated examples compile-verified after the lift fix.
- NEW FINDING (not on list, FILE → PA): E-CONTROL-FLOW-IN-MARKUP diagnostic HOLE — bare control-flow whose body contains `lift` evades the gate (ast-builder.js:1518, block.type==="text" + BARE_CONTROL_FLOW_IN_MARKUP_RE) → silent raw-text ship instead of the error. Fires correctly for bare `for(){<li>}` (no lift). Separate codegen item; PA triages clustering.
- footprint nits: list says emit-lift.ts → actually emit-lift.js (S162 native-parser feature-stale class).
