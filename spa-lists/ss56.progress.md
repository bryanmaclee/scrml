# sPA ss56 — progress log (conformance engine §51 completion)

Branch: `spa/ss56` (base `origin/main` 1c7526f6). Land per-item; never advance main.
Baseline: 69/69 conformance cases → **97/97** after ss56 (+28 engine cases).

Note: heavy cross-session commit contention (ss55/ss58 parallel sPAs) → items 1-3 landed
as per-item commits; items 4-9 batched into 2 grouped commits (P=4-7, Q=8-9) to cut
full-suite hook runs on the shared environment. Per-item detail preserved here + in messages.

| # | item (spec) | case-ids (engine/) | status | commit | note |
|---|------|----------|--------|-----|------|
| 1 | rule= violation (§51.0.F) | rule-violation-{pos,neg} | landed | 185da937 | FLAG: static check fires on bare `.Variant` only; qualified `Type.Variant` skips static detection (runtime still enforces) |
| 2 | payload-binding (§51.0.B.1) | payload-{unit-variant-pos,arity-mismatch-pos,reserved-collision-pos,binding-neg} | landed | 1d528c6f | 3 reject codes + neg; SPEC-exact, no divergence |
| 3 | msg-dispatch (§51.0.S) | {msg-without-accepts-pos,msg-arm-not-exhaustive-pos,msg-unknown-pos,accepts-not-enum-pos} | landed | 820a52a9 | 4 reject codes; SPEC-exact |
| 4 | self-write (§51.0.F.1) | {self-write-pos,self-target-ontransition-pos,self-write-neg} | landed | 6105fb32 | trichotomy: no-op arm (info lint, not error) vs error arm (onTransition self-target) vs cross-state |
| 5 | internal:rule= (§51.0.O) | {internal-rule-not-composite-pos,internal-transition-preserves-rt,external-transition-resets-rt} | landed | 6105fb32 | codes + RT; FLAG: internal-preserve observable coincides w/ §51.0.F.1 self-write no-op (necessary-not-sufficient) |
| 6 | history (§51.0.N) | {history-restore-rt,history-fresh-reset-rt,history-no-inner-engine-pos} | landed | 6105fb32 | RT restore vs fresh reset verified live + codes |
| 7 | hierarchy (§51.0.Q) | {nested-engine-inner-transition-rt,component-engine-scope-pos} | landed | 6105fb32 | RT inner mount+transition verified live + codes |
| 8 | onTimeout (§51.0.M) | {ontimeout-invalid-target-pos,ontimeout-misplaced-pos,ontimeout-legal-neg} | landed | 74a15a4f | codes; RT harness-gated (no virtual clock). FLAG: onTimeout in plain `<program>` markup fires E-ATTR-001/E-SCOPE-001 not E-STRUCTURAL-ELEMENT-MISPLACED (SPEC says latter for any 'outside' locus); ${}-logic-body locus fires SPEC code |
| 9 | onIdle (§51.0.R) | {onidle-misplaced-pos,onidle-invalid-variant-pos,onidle-duplicate-pos,onidle-legal-neg} | landed | 74a15a4f | codes; RT harness-gated (virtual-clock follow-on) |

## Escalations for PA (do NOT decide — flagged, not enshrined)
1. **§51.0.F static-check granularity** (item 1): the compile-time rule= violation check fires on the bare `.Variant` write form but NOT the qualified `Type.Variant` form; runtime enforcement covers the latter. Minor static-analysis-completeness gap.
2. **internal:rule= runtime observability** (item 5): a self-target internal transition's inner-preservation is not separable from the §51.0.F.1 self-write no-op via state snapshot. A discriminating internal-vs-external observable needs an `<onTransition>`-fires-on-external-only side effect (or a mechanism probe). Harness/observability follow-on.
3. **§51.0.M misplacement code divergence** (item 8): `<onTimeout>` in plain `<program>` markup fires `E-ATTR-001`+`E-SCOPE-001` instead of `E-STRUCTURAL-ELEMENT-MISPLACED`. §51.0.M states "outside an engine state-child → E-STRUCTURAL-ELEMENT-MISPLACED" for any outside locus; impl only fires the specific code for the `${}`-logic-body locus. An error still fires (fail-closed), but the specific code diverges — diagnostic-precision spec-vs-impl gap.
4. **Virtual-clock driver** (items 8-9): `<onTimeout>`/`<onIdle>` RUNTIME halves (timer→variant fire) are blocked on a virtual-clock driver (driver.ts has no clock). Codes halves authored; runtime halves flagged `runtime-half-pending`. Infra follow-on.
