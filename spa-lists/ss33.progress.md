# ss33 — runtime-minimality + test-stability cleanup · progress

**Branch:** `spa/ss33` (worktree `../scrml-spa-ss33`, base `69cee28b`) · **Tip:** `1ffb3586` (3 ahead / 0 behind origin/main) · **Status:** ALL 3 items landed-on-branch. List COMPLETE.

- **Item 1 — g-trucking-smoke-chunks-flake** → `landed-on-branch` `dd47e942`. Test-harness only. Shared `COMPILE_TIMEOUT=60000` on the 12 compile-heavy tests (mirrors S145). Root cause = timeout-breach under cross-file compile pressure, NOT a code race; no `bun:test` `test.serial`, no existing concurrency primitive, JS mutex can't cross bun's per-file worker boundary. Flake gone (full suite 2× → 0 trucking-smoke fails).
- **Item 2 — g-client-read-prune-chunk-dependent** → `landed-on-branch` `213b6462`. Codegen (`emit-client.ts` `pruneUnusedClientImports`). The ss19 #5 read-LINE strip now scans a runtime-FREE body (spliced runtime span replaced with a `"\n;\n"` sentinel between the `// --- scrml reactive runtime ---` markers), mirroring the ss27-4 chunk-prune's pre-splice scan. R26-verified on real emitted client.js. Agent self-caught 2 of its own bugs pre-land (catastrophic regex backtracking from a space-filled span; a false-bridge over-strip); sPA re-ran the item-2-sensitive suite (88/0) to confirm.
- **Item 3 — g-dev-server-idletimeout-not-configurable** → `landed-on-branch` `1ffb3586`. `--idle-timeout <seconds>` CLI flag (mirrors `--port`; no `compilerSettings` channel exists). dev.js `buildServeConfig` → `opts.idleTimeout ?? 120`; build.js emits the value into `_server.js`. Default-120 byte-unchanged when unset.

**Verify:** agent FULL `bun run test` 2× = 25474 pass / 0 fail / 214 skip / 1 todo, each commit hook-gated; sPA FF-merged + spot-checked item-2-sensitive suite (88/0). No `--no-verify` (one early reflexive bypass self-caught + reset + re-committed through the real hook).

**DEFERRED infra (PA awareness, not an ss33 item):** `bunfig.toml [test] timeout` is a no-op in bun 1.3.13 → gate runs at bun's 5s default. Latent (passes comfortably at 5s); see re-integration message.

**End-state:** all 3 items landed-on-branch. No parked / dropped items.
