# sPA ss55 — progress log (self-host-v2 LEXER completion)

Branch: `spa/ss55` (worktree `../scrml-spa-ss55`) · base `origin/main` @ `1c7526f6`.
Baseline oracle GREEN before any item: slices 1+2+3 = 34+31+54 = **119/119**.
node_modules symlinked into the fresh worktree (root + compiler) — S209 gitignored-dist/nm gap.

Land per-item on `spa/ss55` (file-delta from the dev-agent worktree → verify oracle green → single sPA commit).
Ping `handOffs/incoming/` per item. Never advance main / never push.

| # | item | status | SHA | oracle | dogfood |
|---|------|--------|-----|--------|---------|
| 1 | slice-4a precise cooked-decode | **landed** | `41aef81d` | 49/49 (+119 unchanged = 168/168) | ZERO NEW; host-support probe clean |
| 2 | slice-4b typed BracketStack threading | **landed** | `fe53bf78` | 53/53 (+168 unchanged = 221/221) | ZERO NEW; F4 qualified-BracketKind.X clean |
| 3 | slice-4c ErrorRecovery threading | pending | — | — | — |
| 4 | slice-5a BareVariant `.foo` token | **landed** | `e700b0d1` | 51/51 (+221 unchanged = 272/272) | ZERO NEW; @.field → Unknown-false-list |
| 5 | slice-5b value-keyword-then-regex | **landed** | `4c452e29` | 65/65 (+272 unchanged = 337/337) | **NEW: F10 (is-payload-bind in if-cond fails)** |

**LIST COMPLETE** (items 1,2,4,5 landed; item 3 parked+escalated). impl#2 lexer = TOKEN-CLASS + REFINEMENT COMPLETE.
Branch `spa/ss55` tip **`4c452e29`**. 337/337 lexer token-diff green vs impl#1. Re-integration message filed.

## Log
- **item 1 LANDED** `41aef81d` (dev-agent `self-host-v2-slice4a` @ c6395a8b, file-delta'd in). slice-4a
  precise cooked-decode: `scanEscape`/`scanHexEscape`/`scanBraceUnicodeEscape` (EscapeScan struct) shared by
  scanString + scanTemplateChunk; TmplScan.cooked added; templateBodyStep carries true cooked. raw/span
  invariant. Oracle extended (`self-host-v2-lexer-slice4a.test.js`) to diff the cooked field (impl#1
  token.cooked / impl#2 token.kind.data.cooked) — **49/49 green, slices 1-3 119/119 unchanged (168/168),
  independently re-verified in the spa/ss55 worktree.** Brief archived to
  `docs/changes/self-host-v2-lexer-slice4a/BRIEF.md`.
  - **Dogfood: ZERO NEW.** Host-support probe RESOLVES the slice-2/3 "unverified" flag — `parseInt(hex,16)`,
    `String.fromCodePoint(cp)`, `Number.isNaN` all host-supported + correctly lowered, NO workaround. F5
    annotations (`const cp: int`) + reused F1 (`<program>` wrapper). F7 match-arm-reachability noise stays benign.
  - **DEPLOY NOTE for PA:** `isolation:"worktree"` did NOT provision a worktree — the dev-agent landed in the
    MAIN checkout on branch `main` and self-provisioned `.claude/worktrees/self-host-v2-slice4a` (no main leak;
    verified). Future dispatches: brief an explicit self-provision + pwd/branch verify startup step.
- **item 2 LANDED** `fe53bf78` (dev-agent `self-host-v2-slice4b` @ fd53127a, self-provisioned from spa/ss55 @ 41aef81d,
  file-delta'd in). slice-4b typed BracketStack threading: `LexState.bracketDepth:int` → `bracketStack:BracketKind[]`
  (openers push kind, closers pop; DEPTH = length); `bracketDelta` → `pushBracket`/`popBracket`; `isInterpCloseHere`
  reads `bracketStack.length` (same value). Pure internal-state refactor, token output byte-identical. New
  `self-host-v2-lexer-slice4b.test.js` (bracket-heavy + deep-nested-`${}` corpus). **53/53 green, slices 1-4a 168/168
  unchanged (221/221), independently re-verified in the spa/ss55 worktree.** Dogfood: ZERO NEW (F4 probe: qualified
  `BracketKind.X` resolves clean in `.concat` element position — bare `.X` also worked). Brief archived.
  - **The explicit self-provision-from-spa/ss55 startup step WORKED** — agent based correctly off 41aef81d (item 1 inherited).
- **item 3 (slice-4c ErrorRecovery) PARKED** — PA design ruling needed (impl#1 silently skips strays / truncates at EOF
  with NO error token, and impl#2's `deferAdvance` already matches that; "honest ErrorToken emission" would DIVERGE
  from impl#1 → oracle red unless its contract changes — a parity-vs-fail-closed + oracle-contract ruling). Escalated →
  `handOffs/incoming/ss55-item3-ESCALATION-error-recovery.md`. Skipped; proceeded to items 4-5.
- **item 4 LANDED** `e700b0d1` (dev-agent `self-host-v2-slice5a` @ 63210ab7, self-provisioned from spa/ss55 @ fe53bf78,
  file-delta'd in). slice-5a BareVariant `.foo`: new `BareVariant(name)` TokenKind + `kBareVariant` helper (F4) +
  `SawBareVariant` event + classify value-position arm + `scanBareVariant` step; `regexAllowedAfter` false-list gains
  BareVariant + Unknown. **51/51 green (40 differential vs impl#1 + anchors + negatives), slices 1-4b 221/221 unchanged
  (272/272), independently re-verified.** Dogfood: ZERO NEW.
  - **`@.field` judgment:** impl#2 has no ScrmlAt yet (`@`→Unknown); set `regexAllowedAfter(Unknown)=false` so `.field`
    after `@` is NOT a spurious BareVariant. Full `@.field`→one-ScrmlAt parity deferred to a future ScrmlAt sigil slice.
  - **`.5` NumberLit / `...` Ellipsis:** out-of-scope token classes impl#2 lacks — asserted as impl#2-internal negatives.
- **item 5 LANDED** `4c452e29` (dev-agent `self-host-v2-slice5b` @ 26d2e2fa, self-provisioned from spa/ss55 @ e700b0d1,
  file-delta'd in). slice-5b value-keyword-then-regex: extended `isKeyword` to impl#1's EXACT `JS_KEYWORDS` (excl.
  contextual `type` + unreserved case/switch) so `typeof /re/` lexes KW→regex; `regexAllowedAfter` made text-aware
  (value-keyword→false, else→true via `isValueKeyword`+`keywordName`). **65/65 green (differential vs impl#1),
  slices 1-5a 272/272 unchanged (337/337), independently re-verified.**
  - **NEW dogfood — F10:** `if (last is .Keyword(kw)) { …kw… }` payload-binding in an `if`/boolean CONDITION does NOT
    compile (`E-SCOPE-001 Undeclared identifier kw` + `E-VARIANT-AMBIGUOUS`); the `is`-with-payload binder works as a
    `match`-ARM pattern but is silently DROPPED in an `if`-condition. Workaround: single-arm `match` extraction helper
    (`keywordName(k)`) + bare `is .Keyword` tag test. Cleanly workaroundable; filed for a typer/parser follow-up.
