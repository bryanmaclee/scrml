# sPA ss60 — progress log (conformance: SSR §52.8 + protect-floor §14.8.9)

Branch: `spa/ss60` (worktree `../scrml-spa-ss60`) · base `origin/main` @ `cfba6295`.
node_modules symlinked (root + compiler) — S209. Baseline oracle GREEN: 72/72.
Operating model: sPA-DIRECT (disjoint additive `conformance/cases/` data); empirical-first
(probe impl#1 → SPEC-sanity-check §52.8 / §14.8.9 read in full). Land on `spa/ss60`; no main advance, no push.

## ⚠️ HARNESS-VERIFY OUTCOME (item-1 gate — the list's central finding)
The list's item-1 gate asked: can the conformance adapter run the SSR compose handler? **NO — and
the gap is bigger than item 1.** Verified empirically + from the impl:
- The adapter (`conformance/adapters/impl1-ts.ts`) **mocks `globalThis.fetch`** — a server-fn call
  returns the case's `serverStub` **verbatim**; the compiler-emitted **server handler never runs**
  (confirmed: `basic-load-hydrate` shows stub→client-state verbatim).
- ALL of **§14.8.9 redaction** (`codegen/protect-egress.ts` `_scrml_protect_redact`) and **§52.8 SSR
  first-paint compose** (`codegen/emit-ssr-render.ts` + `_scrml_ssr_compose_handler`) are **SERVER-SIDE**.
  The impl proves them via `serverJs.toContain(...)` + the D2 browser test — NEVER via a client fetch-mock.
- Therefore the **RUNTIME halves of items 1, 2, 3, 4 are NOT soundly observable** in the current adapter:
  a `serverStub` IS the client value, so "protected column absent from client state" would only assert my
  own stub shape, not the compiler's redaction (unsound). SSR first-paint markup has no compose path at all.
- Extra gap: a protect-`<db>` client mount references `_scrml_fetch_with_csrf_retry`, **absent from the
  harness runtime template** (`users` stayed `[]` on load) — the protect-db client path doesn't cleanly mount either.

**→ ADAPTER EXTENSION (track B, PA/infra):** run the actual compiled server route handler (not a mock) so
the redaction sink executes; for SSR, invoke `_scrml_ssr_compose_handler` + seed `window.__scrml_ssr_state`
(borrow the D2 browser harness `ssr-a-terminus-hydration.browser.test.js`); add the CSRF-retry helper to the
harness runtime. This unblocks the RUNTIME halves of items 1-4 + item 6. **The PA's footprint ("items 2/4
harness-clean via fetch-mock") is corrected: the fetch-mock BYPASSES the redaction sink.**

## Landed (compile-time codes — the sound, harness-clean surface) @ `d8368589`
| # | item | authored | gated (escalated) | cases |
|---|------|----------|-------------------|-------|
| 1 | SSR §52.8 first-paint render | compile-clean (runtime-half-pending) | RUNTIME (SSR compose) | ssr/ssr-first-paint-render |
| 2 | §14.8.9 server-fn return redaction | CODES (I-PROTECT-STRIP-001) | RUNTIME (client-visible absence) | protect/strip-info-select-star |
| 3 | §14.8.9 SSR redaction | — (E-AUTH-005 on the protect+SSR combo; runtime is the essence) | RUNTIME (SSR compose) + no clean compile shape | (folded into #1 escalation + #2/#5 strip codes) |
| 4 | reveal declassification | CODES (reveal suppresses E-PROTECT-004) | RUNTIME (client-visible presence) | protect/reveal-suppresses-e004 |
| 5 | protect codes | LANDED (full) | — | protect/{raw-egress-e004,safe-projection-no-strip}(+strip/reveal above) |
| 6 | channel/SSE protect egress | CODES (I-PROTECT-STRIP-001 at broadcast + SSE yield) | RUNTIME (no WS/EventSource driver) | protect/{channel-broadcast-strip,sse-yield-strip} |

**7 cases, oracle 72→79 green.** severity assertions pin §34 partition (E-PROTECT-004=error, I-PROTECT-STRIP-001=info).
**Parked (runtime, adapter-gated):** items 1,2,3,4 client/SSR runtime + item 6 WS/SSE runtime. **Dropped:** none.

## Escalations (PA — do NOT decided by sPA)
- **E-ADAPTER (biggest)** — the SSR-compose + server-handler-execution adapter extension above. Blocks all
  runtime halves. Item-1 gate resolved NO; the gap spans items 1-4 + 6. Track B.
- **E-CSRF-HELPER** — the harness runtime template lacks `_scrml_fetch_with_csrf_retry`; a protect-`<db>`
  client mount throws on load. Sub-item of the adapter extension.
- No impl#1-vs-SPEC DIVERGENCE found — the protect codes + SSR compile behavior all match §14.8.9 / §52.8.

## Log
- **harness-verify (item-1 gate)** — resolved NO (adapter mocks server; redaction+SSR are server-side).
  Escalated as E-ADAPTER; authored the compile-time codes meanwhile per the list's own "author codes half" directive.
- **7 cases landed** @ `d8368589` — 6 protect codes (protect/) + 1 SSR compile-clean pending (ssr/). 72→79 green.
