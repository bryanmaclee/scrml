# sPA ss74 — progress log (conformance: inline foreign §23.2 + meta-eval §22)

**Branch:** `spa/ss74` (base == origin/main `211dc076`; 593/593 conformance green at boot)
**List:** `spa-lists/ss74-conformance-foreign-23.md` — 8 codes, codes-half, reject `-neg` + clean `-pos`.

## Dispatch
- 2026-07-15 — dispatched scrml-js-codegen-engineer (worktree, sonnet) to author all 12 case
  dirs (9 foreign + 3 meta) covering the 8 codes. Brief: `docs/changes/conformance-foreign-meta-eval-ss74-2026-07-15/BRIEF.md`.

## Landed — all 8 codes, single commit `18a7ca87` on `spa/ss74` (base 211dc076; 1 ahead)
Conformance 593 -> 605 green (verified standalone in fresh ss74 worktree AND by the pre-commit suite).
12 case dirs (9 foreign + 3 meta), 24 case files + BRIEF.md + list-mark.

1. E-FOREIGN-003 — landed (foreign-inline-no-lang-neg fires / -lang-declared-pos clean)
2. E-FOREIGN-004 — landed (foreign-bare-block-neg fires)
3. E-FOREIGN-005 — landed (foreign-lang-unsupported-neg fires, `<foreign lang="python" />`)
4. E-FOREIGN-006 — landed (foreign-crossing-shadow-neg fires / -crossing-clean-pos clean) [DIVERGENCE below]
5. E-FOREIGN-LANG-DUPLICATE — landed (foreign-lang-duplicate-neg fires / -single-pos clean)
6. E-FOREIGN-LANG-IN-PROGRAM — landed (foreign-lang-in-program-neg fires)
7. E-META-EVAL-001 — landed (meta-eval-runtime-error-neg fires; recipe `^{ emit(JSON.parse("{")) }`)
8. E-META-EVAL-002 — landed (meta-eval-reparse-error-neg fires / meta-eval-clean-pos clean)

## Landing env notes (for future ss74-style landings)
- Concurrent PA active (main checkout on a reintegrate branch) → landed in a DEDICATED worktree
  `../scrml-spa-ss74`, never touched the shared checkout.
- Fresh worktree ENV-GAP: needed `node_modules` symlink -> main (`acorn` unresolved otherwise);
  pre-commit excludes browser tests so no dist needed. Bash TOOL timeout must be 300000 (suite ~120s+).

## Divergences flagged for PA (author-to-impl; do NOT decide)
- **E-FOREIGN-006**: list description "crosses a client/server boundary" ≠ impl (crossing-name shadow collision, emit-logic.ts:2980, §23.2.4a).
- **E-FOREIGN-003**: list description "a foreign block with no content" ≠ impl ("no `lang=` declaration in any ancestor", type-system.ts:21748).
