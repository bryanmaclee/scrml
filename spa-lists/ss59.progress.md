# sPA ss59 progress — conformance: V5-strict reactivity §6 edges

Branch: `spa/ss59` (based on `origin/main` @ cfba6295 per boot contract). Worktree: `../scrml-spa-ss59`.
Method: author cases directly in the worktree (pure-additive `conformance/cases/reactive/` +
`error`-style data; tightest empirical golden-capture loop — full §6 SPEC pre-ingested). Each
item: author → `bun conformance/run.ts` green → SPEC sanity-check → land per-item on `spa/ss59`.
node_modules symlinked from main; baseline 72/72 green.

**CURRENCY CORRECTION (list premise stale):** the virtual clock has LANDED
(`conformance-virtual-clock-2026-07-03`): `driver.ts` documents an 8th ratified verb
`{ "advance-time": N }` (fake-clock.ts), and a `reactive/throttle-leading` runtime case already
ships. So Item 6's "harness-gated on virtual-clock" is no longer the blocker. HOWEVER the
debounce/throttle TRAILING runtime is blocked by an OPEN bug `G-DEBOUNCE-THROTTLE-TRAILING-NO-COMMIT`
(known-gaps.md; the reactivity-bypass re-route never commits the trailing write). So Item 6 =
codes now + trailing-runtime blocked-on-bug (NOT harness-gated). See ESCALATIONS.

| Item | Cases | Status | SHA |
|------|-------|--------|-----|
| 1 — compound Variant-C §6.3 (RT) | reactive/compound-variant-c | landed | f50bdb73 |
| 2 — Shape-4 no-RHS typed defaults §6.2 (RT+codes) | reactive/shape4-{canonical-empty, struct-not-lifecycle, refinement-no-default, refinement-satisfied-neg} | landed | d790b05a |
| 3 — array reassignment reactivity §6.5 (RT) | reactive/array-reassign-reactive | landed | ef54cd16 |
| 4 — default= / reset(@cell) §6.8 (RT) | reactive/reset-to-default, reset-compound-field, reset-compound-all | landed | 84eda797 |
| 5 — pinned §6.10 / hoisting §6.9 (codes) | reactive/pinned-forward-ref, hoist-forward-ref-neg, fn-mutual-recursion-hoist | landed | 618f1567 |
| 6 — debounce/throttle §6.13 (codes; trailing-RT bug-blocked) | reactive/debounce-on-{derived,server}, reactivity-attr-conflict, debounce-valid-neg | landed | 013a8f16 |

**Suite: 72 baseline → 88 cases, all green** (+16: 1+4+1+3+3+4).

## Log
- Item 1: compound-variant-c — `${ <formRes> <name>="" <email>="" </> }` wrapped form; fill()
  dot-nav writes both fields; snapshot keys `formRes.name` / `formRes.email` (golden-capture
  correct first-run). No SPEC divergence.
- Item 2: four cases. RT canonical-empty defaults (int→0, string→"", bool→false, T[]→[]);
  bare-`:struct` no-RHS read-before-assign → E-TYPE-001 (implicit (not to T), §14.12);
  refinement-violating `number(>0)` → E-REFINEMENT-NO-DEFAULT (+ companion E-CONTRACT-001,
  superset-ignored); refinement-satisfying `number(>=0)` → clean neg. Note: enum no-RHS + read
  fires E-TYPE-025 (not E-TYPE-001) — used the SPEC-canonical STRUCT case for E-TYPE-001 to
  avoid the match-muddied enum path. No SPEC divergence.
- Item 3: array-reassign-reactive — `@items = [...@items, x]` spread reassignment (§6.5.5,
  DQ-2) re-renders an `<each in=@items key=@.id as item>` (2→3 rows) + re-reads @items.length.
  Golden-capture correct first-run. No SPEC divergence.
- Item 4: three cases. reset-to-default (`default=` distinct from init → reset lands default,
  not init re-eval); reset-compound-field (`reset(@form.name)` reverts one field, sibling
  keeps typed); reset-compound-all (`reset(@form)` reverts all fields to own default=). Handlers
  named non-`reset` deliberately. NOTE (out-of-scope, flagged): the pre-existing
  `reactive/reset-handler` case declares `function reset()` and passes, but §6.8.2 says `reset`
  is a RESERVED IDENTIFIER → E-RESERVED-IDENTIFIER should fire — a possible divergence, not on
  this list (ESCALATIONS #note). No divergence in Item-4 cases.
- Item 5: three codes cases, clean matched pair. pinned-forward-ref (`<count pinned> = 0`
  referenced above its decl → E-STATE-PINNED-FORWARD-REF); hoist-forward-ref-neg (identical
  minus `pinned` → emits ONLY W-PROGRAM-001, verified — hoists clean); fn-mutual-recursion-hoist
  (`fn` file-scope hoist, isEven↔isOdd mutual recursion, clean). No SPEC divergence.
- Item 6: four cases. Three §6.13 reject codes — E-DEBOUNCED-WITH-DERIVED (on const-derived),
  E-REACTIVITY-ATTR-CONFLICT (both attrs), E-DEBOUNCED-WITH-SERVER (on server cell) — all
  SPEC-matched (incidental E-DG-002 / E-AUTH-005 superset-ignored). Plus a valid-decl codes-neg
  (used cell → clean). Trailing-write RUNTIME deliberately NOT authored (bug-blocked, ESCALATIONS
  #2). No SPEC divergence in the codes.

## ESCALATIONS / NOTES (PA-facing — NOT decided by sPA)

### #1 — `reset` reserved-identifier NOT enforced (§6.8.2 vs impl) — possible divergence, out of list scope
§6.8.2: "`reset` is a RESERVED IDENTIFIER — declaring a local `function reset()` … is
E-RESERVED-IDENTIFIER." But the PRE-EXISTING `conformance/cases/reactive/reset-handler` case
declares `function reset() { @count = 0 }` and PASSES (72/72 baseline) — no E-RESERVED-IDENTIFIER.
Surfaced incidentally while authoring Item 4 (I named my handlers non-`reset` to avoid it). Not on
this list; flagged for a PA ruling (impl gap vs the existing case is spec-divergent). My Item-4
cases use the `reset(@cell)` KEYWORD correctly and are clean.

### #2 — Item 6 runtime: currency correction + open bug (NOT a new divergence)
The ss59 list said §6.13 timing runtime is "harness-gated on virtual-clock (deferred)". STALE:
the virtual clock LANDED (`conformance-virtual-clock-2026-07-03`; `driver.ts` `advance-time`
verb; `reactive/throttle-leading` runtime case already ships). The real blocker for the
debounce/throttle TRAILING runtime is the OPEN bug **G-DEBOUNCE-THROTTLE-TRAILING-NO-COMMIT**
(known-gaps.md, MED) — the reactivity-bypass re-route never commits the trailing write
(confirmed with real timers). I did NOT author a trailing-fire runtime case (would enshrine the
bug). Codes covered; leading-edge runtime already covered. **Follow-on (already noted in
known-gaps):** once the bug is fixed, add debounce-trailing + throttle-trailing runtime siblings
via `advance-time`. Case dir `debounce-valid-neg/NOTES.md` documents this at the site.
Probes: `scratchpad/probe-{shape4,pinned,pinned2,debounce}.ts`.
