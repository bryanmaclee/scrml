# ss75 progress — conformance authoring: control-flow §17 + linear §35

## S275 — re-fire verification pass (NO-OP; list closed)

`/spa ss75` was fired against a list whose `[status=pending]` markers were never updated at
re-integration. The work was already done. This pass verified that claim from landed artifacts +
the oracle rather than from the markers, then closed the list.

**Verdict: 12 of 14 items LANDED, 2 PARKED under a standing ruling. No new corpus authored —
authoring items 13-14 would have produced FALSE-GREEN conformance data.**

### Evidence chain (independent, this session, on `58c8161d`)

| Check | Result |
|---|---|
| `bun conformance/run.ts` | **745/745 pass** |
| `git log -- conformance/cases/{control-flow,linear}` | `55bbdbed` (#75, +24 cases) · `32cb0a89` (#76) · `6d360d23` (#77) · `8134fb55` (#78) |
| Read `expect.codes[]` in all 27 control-flow + 13 linear `expected.json` | every item 1-12 code has a pos assertion AND a neg twin |
| Bidirectional sweep: live fire-sites in `compiler/src` vs asserted `codes[]` in `conformance/cases/` | 15 of 17 live §17/§35 codes asserted; **only `E-TILDE-001/002` live-but-unasserted** |
| 11-shape empirical probe of `~` via the impl#1 adapter | **zero** E-TILDE codes on any shape, incl. SPEC §32.5 verbatim invalid examples |

Coverage is in fact BROADER than the 14-item list: follow-on waves pinned `E-CTRL-010`, `E-LIN-004`
and `E-LIN-005`, which the original enumeration missed.

### Items 13-14 — PARKED, re-verified still-blocked

E-TILDE-001/002 are **structurally unreachable**: the type-system dispatches them off AST kinds
`tilde-init` / `tilde-ref` / `lift-stmt`, and **nothing in `compiler/src` produces any of the three**
(the builder emits only `lift-expr`; the `usesTilde` flag the walker reads is never set).

Standing ruling — **bryan S261 ruling-2** (delta-log `[547]`): *"fix-message-now,
scope-wiring-separately."* The message half landed (#78). The wiring is a **parser arc**
(hand-off-261 item 8), not conformance work.

**The S275-specific finding the PA needs:** the two fix rounds that landed AFTER the original
escalation (#76, #78) did **not** wire E-TILDE — #78 touched only the message text. So the parser arc
is still open on current main, and the codes remain the only unasserted live diagnostics in the whole
§17/§35 surface. Re-confirmed empirically, not inferred from the changelog.

### Trap worth keeping visible

`compiler/tests/unit/type-system.test.js:1766+` hand-builds `{kind:"tilde-ref"}` AST literals, so both
codes **appear green over a dead path** — the R26 trap. A future conformance sweep that greps for
"is E-TILDE covered?" will find unit-test hits and may wrongly mark it done. The list file now carries
an explicit do-not-author block to stop that.

### Actions taken

- Rewrote item statuses 1-14 in `spa-lists/ss75-conformance-control-flow-linear-17-35.md` with landed
  SHAs + the concrete case-ids per code, corrected the stale/wrong impl line-refs and trigger
  descriptions (the original list mis-described several — e.g. E-CTRL-011 is `for (… in …)`, not a
  placement error), and added a LIST CLOSED banner + the items-13-14 do-not-author rationale.
- Wrote this progress file.
- No `conformance/cases/` changes. No compiler-source changes. Main untouched, nothing pushed.
