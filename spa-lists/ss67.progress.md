# ss67 progress — conformance: serverDb data-flow RUNTIME (SQL §8 hydrate round-trip)

Branch: `spa/ss67` · Worktree: `../scrml-spa-ss67` · Base: `40b580c5`

- **[COMPLETE]** 4 authorable items landed on `spa/ss67` @ `b6e56a08` (single commit — cohesive `server-db/` category). conformance (impl#1) **390/390** pass (was 386, +4; all 4 `[runtime]`). Pre-commit full suite green (20028 pass / 0 fail, 207s).
  - Authored NEW `conformance/cases/server-db/` category (4 dirs × {case.scrml, expected.json}).
  - RUNTIME cases via the `serverDb` server-eval driver (`runServer` evals the REAL emitted route handlers; `_scrml_sql` stub returns seeded rows).
  - **Every `state` + `domAnchored` value empirically verified through `runServer`** before locking (README OQ4 capture-then-review).
  - HARD CONSTRAINT honored: the SQL engine is a stub (regex `FROM <table>` → whole seeded table; no WHERE/JOIN/write-back). Every case **seeds exactly the rows it asserts and uses NO WHERE clause** → pure data-FLOW, correct against a real engine too (does NOT rely on the stub ignoring WHERE).
  - Result shapes pinned to SPEC §8.3: `.all()`/bare → Row[]; `.get()` → first row.
  - Case 2 is state-only (mirrors `protect/strip-client-visible-runtime`): a nullable `<current> = not` row interpolated into markup would deref `not` at first paint, so it's asserted only in post-load `state`.
- **[5-6 SKIPPED — no gap]** protect-on-DB-rows / SSR-of-DB-rows already covered by the 5 existing green `protect/`+`ssr/` cases; the list marked these explicitly optional ("only if a gap is found"). None found.

## Escalated to PA (parked — out of sPA scope)
- **PARK item: SQL-engine-semantics runtime sub-surface.** WHERE-filter · JOIN/aggregate/ORDER BY ·
  INSERT/UPDATE/DELETE `.run()` + `RETURNING` (§8.5.1) · transaction atomicity + ROLLBACK (§8.5.3) ·
  §8.7 `SqlError` variants · UNIQUE/FK/CHECK enforcement (§39.5). ~6-8 cases, **BLOCKED until a real-DB
  conformance adapter (SQLite-backed `_scrml_sql`) replaces `makeSqlStub`** (impl1-ts.ts ~504-517).
  A compiler/harness DEV task for the PA lane, NOT conformance authoring. Ready-to-file `docs/known-gaps.md`
  entry is in the re-integration message (not written to the PA-owned doc from this branch).

## Parked / dropped
- 1 parked (the SQL-engine-semantics DEV prerequisite, above) — escalated to PA. Nothing dropped.
