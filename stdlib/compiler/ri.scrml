import:host { runRI as _runRI, RIError as _RIError, collectFileFunctions as _collectFileFunctions, generateRouteName as _generateRouteName, buildFunctionIndex as _buildFunctionIndex, buildPageRouteTree as _buildPageRouteTree } from "../../compiler/src/route-inference.ts"

<program>
${
    // ---------------------------------------------------------------------------
    // scrml:compiler/ri — Route Inference
    //
    // Infers server-side routes from function declarations, builds the route map,
    // and detects auth middleware requirements.
    //
    // Usage:
    //   import { runRI } from "scrml:compiler/ri"
    //   const result = runRI({ files, protectAnalysis })
    // ---------------------------------------------------------------------------


    export const runRI = _runRI
    export const RIError = _RIError
    export const collectFileFunctions = _collectFileFunctions
    export const generateRouteName = _generateRouteName
    export const buildFunctionIndex = _buildFunctionIndex
    export const buildPageRouteTree = _buildPageRouteTree
}
</program>
