import:host { runDG as _runDG, DGError as _DGError } from "../../compiler/src/dependency-graph.ts"

<program>
${
    // ---------------------------------------------------------------------------
    // scrml:compiler/dg — Dependency Graph
    //
    // Builds the reactive dependency graph between state variables, derived
    // values, and DOM bindings. Detects circular dependencies and ordering.
    //
    // Usage:
    //   import { runDG } from "scrml:compiler/dg"
    //   const result = runDG({ files, routeMap })
    // ---------------------------------------------------------------------------


    export const runDG = _runDG
    export const DGError = _DGError
}
</program>
