import:host { runCG as _runCG, CGError as _CGError } from "../../compiler/src/codegen/index.ts"

<program>
${
    // ---------------------------------------------------------------------------
    // scrml:compiler/cg — Code Generator
    //
    // Emits JavaScript (client + server), HTML, and CSS from the analyzed AST.
    // Final stage of the compiler pipeline.
    //
    // Usage:
    //   import { runCG } from "scrml:compiler/cg"
    //   const result = runCG({ files, routeMap, depGraph, protectAnalysis, mode })
    // ---------------------------------------------------------------------------


    export const runCG = _runCG
    export const CGError = _CGError
}
</program>
