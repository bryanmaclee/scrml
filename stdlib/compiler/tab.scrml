import:host { buildAST as _buildAST, runTAB as _runTAB, parseLogicBody as _parseLogicBody, TABError as _TABError } from "../../compiler/src/ast-builder.js"

<program>
${
    // ---------------------------------------------------------------------------
    // scrml:compiler/tab — Tree AST Builder
    //
    // Converts block splitter output into a structured FileAST with nodes,
    // imports, exports, type declarations, and component definitions.
    //
    // Usage:
    //   import { buildAST } from "scrml:compiler/tab"
    //   const fileAST = buildAST(bsOutput)
    // ---------------------------------------------------------------------------


    export const buildAST = _buildAST
    export const runTAB = _runTAB
    export const parseLogicBody = _parseLogicBody
    export const TABError = _TABError
}
</program>
