import:host { runBPP as _runBPP, runBPPFile as _runBPPFile } from "../../compiler/src/body-pre-parser.ts"

<program>
${
    // ---------------------------------------------------------------------------
    // scrml:compiler/bpp — Body Pre-Parser
    //
    // Pre-parses logic body statements for pattern matching, destructuring,
    // and other complex syntax before the main analysis passes.
    //
    // Usage:
    //   import { runBPP } from "scrml:compiler/bpp"
    //   const result = runBPP({ files })
    // ---------------------------------------------------------------------------


    export const runBPP = _runBPP
    export const runBPPFile = _runBPPFile
}
</program>
