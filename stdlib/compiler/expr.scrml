import:host { parseExpression as _parseExpression, parseStatements as _parseStatements, walk as _walk, extractIdentifiersFromAST as _extractIdentifiersFromAST, extractReactiveDepsFromAST as _extractReactiveDepsFromAST, astToJs as _astToJs, rewriteReactiveRefsAST as _rewriteReactiveRefsAST, rewriteServerReactiveRefsAST as _rewriteServerReactiveRefsAST } from "../../compiler/src/expression-parser.ts"

<program>
${
    // ---------------------------------------------------------------------------
    // scrml:compiler/expr — Expression Parser
    //
    // Acorn-based structured expression parsing for scrml. Parses JS expressions
    // with @ reactive variable sigil support, walks ESTree ASTs, extracts
    // identifiers and reactive dependencies, and rewrites reactive references.
    //
    // Usage:
    //   import { parseExpression, extractIdentifiersFromAST } from "scrml:compiler/expr"
    //   const { ast, error } = parseExpression("@count + 1")
    // ---------------------------------------------------------------------------


    export const parseExpression = _parseExpression
    export const parseStatements = _parseStatements
    export const walk = _walk
    export const extractIdentifiersFromAST = _extractIdentifiersFromAST
    export const extractReactiveDepsFromAST = _extractReactiveDepsFromAST
    export const astToJs = _astToJs
    export const rewriteReactiveRefsAST = _rewriteReactiveRefsAST
    export const rewriteServerReactiveRefsAST = _rewriteServerReactiveRefsAST
}
</program>
