{
  "_comment": "GENERATED — the browser tier's documented failure NAME SET. Regenerate with `bun scripts/browser-baseline.ts --write`; verify with `--check`. Timings and counts are deliberately excluded (non-deterministic / uninformative). A name LEAVING this list is as much a failure as one joining it: prune it in the same commit that fixes the test.",
  "tier": "compiler/tests/browser",
  "recordedAt": "2026-08-02",
  "count": 48,
  "failures": [
    "Bug 60 — nested compound render-by-tag drives at runtime (happy-dom) > bind:value round-trips — typing the email input writes signupForm.email",
    "Bug 60 — nested compound render-by-tag drives at runtime (happy-dom) > bind:value round-trips — typing the text input writes signupForm.userName",
    "Bug 60 — nested compound render-by-tag drives at runtime (happy-dom) > both nested inputs render in the DOM with the correct type",
    "Bug 60 — nested compound render-by-tag drives at runtime (happy-dom) > reactive write reflects back into the bound input (effect loop)",
    "Bug 60 — nested compound render-by-tag drives at runtime (happy-dom) > validators lowered to native HTML attributes on the rendered inputs",
    "M1 — an <each> list in a swapped region RE-RENDERS > a swapped-in each re-renders after a list change",
    "M1 — an if= mount/unmount controller in a swapped region RE-EVALUATES > a swapped-in if= mounts on true and unmounts on false (not frozen)",
    "M1 — an if= mount/unmount controller in a swapped region RE-EVALUATES > if= re-entry idempotency — nav away+back mounts EXACTLY ONE copy (no double-mount, no leaked scope)",
    "Transition directives (transition-001-basic) > all four if= elements are present in DOM",
    "Transition directives (transition-001-basic) > in:slide (element 1) > gets scrml-enter-slide class when if= becomes true",
    "Transition directives (transition-001-basic) > in:slide (element 1) > hides immediately on exit (no exit transition)",
    "Transition directives (transition-001-basic) > mixed in:fade out:slide (element 3) > does NOT get scrml-enter-slide or scrml-exit-fade",
    "Transition directives (transition-001-basic) > mixed in:fade out:slide (element 3) > gets scrml-enter-fade class on enter",
    "Transition directives (transition-001-basic) > mixed in:fade out:slide (element 3) > gets scrml-exit-slide class on exit",
    "Transition directives (transition-001-basic) > out:fly (element 2) > exit delays display:none until animationend",
    "Transition directives (transition-001-basic) > out:fly (element 2) > gets scrml-exit-fly class when if= becomes false",
    "Transition directives (transition-001-basic) > out:fly (element 2) > shows immediately on enter (no enter transition)",
    "Transition directives (transition-001-basic) > transition:fade (element 0) > display becomes none after animationend fires on exit",
    "Transition directives (transition-001-basic) > transition:fade (element 0) > display is NOT immediately none during exit animation",
    "Transition directives (transition-001-basic) > transition:fade (element 0) > enter class is removed after animationend",
    "Transition directives (transition-001-basic) > transition:fade (element 0) > gets scrml-enter-fade class when if= becomes true again",
    "Transition directives (transition-001-basic) > transition:fade (element 0) > gets scrml-exit-fade class when if= becomes false",
    "engine-gated-each §2 — list populates on arm entry (real module-init order) > idempotent re-entry: Loading -> Browsing -> Loading -> Browsing re-renders correctly",
    "engine-gated-each §2 — list populates on arm entry (real module-init order) > ongoing reactivity: mutating @todos while Browsing is visible re-renders (Mode 1 dep established)",
    "engine-gated-each §2 — list populates on arm entry (real module-init order) > repro-1 (button): Loading shows no <li>; after transition to Browsing the list populates",
    "engine-gated-each §2 — list populates on arm entry (real module-init order) > repro-2 (boot effect): boot opener loads todos + transitions; list populates",
    "extended #1 — a show= display-toggle in a swapped region RE-EVALUATES > a swapped-in show= toggle updates on a cell change (not frozen)",
    "finding #1 — the swapped region stays REACTIVE (not frozen) > a `${@cell}` in a swapped region UPDATES after a cell mutation",
    "finding #2 — the OLD region's reactivity is torn down (no leak) > the swapped-out region's effect STOPS updating after nav",
    "g-each-peritem-markup-value-ternary — item2 (GITI-032 follow-on) > adversarial: in-place active toggle re-renders the markup-value",
    "g-each-peritem-markup-value-ternary — item2 (GITI-032 follow-on) > adversarial: same-key array-replace updates the markup-value label",
    "g-each-peritem-markup-value-ternary — item2 (GITI-032 follow-on) > markup renders for active items, absent for inactive, @. refs resolve",
    "g-emit-lift-markup-text-interp §1 — emit shape (text-child `${}` lowered, not literal) > the `${@cell}` text child lowers to a reactive interpolation (split static + emitExprField)",
    "g-emit-lift-markup-text-interp §2 — post-mount render (real module-init order) > THE bug (item 4+5): the markup text child renders the cell VALUE, not the literal `${@cell}`",
    "g-emit-lift-markup-text-interp §2 — post-mount render (real module-init order) > item 4 reactivity: mutating the cell updates the rendered text",
    "g-emit-lift-markup-text-interp §2 — post-mount render (real module-init order) > item 5 ternary: renders span+value when true, empty when false, re-renders on toggle",
    "i174 (browser) §2 — type-then-clear executed DOM > CANARY: type into the field, then Clear — the visible field clears",
    "i174 (browser) §2 — type-then-clear executed DOM > programmatic @name write updates a dirty field's .value",
    "navigate-wave1c — an ENGINE route loaded cross-chunk renders + transitions > first cross-chunk visit: the initial arm renders after the swap, and a variant transition re-renders",
    "navigate-wave1c — cross-chunk soft navigation loads the chunk + swaps > the swapped route's reactivity works AFTER the cross-chunk swap (@cell + <each> re-render)",
    "navigate-wave1c — finding #5: a MUTATED reactive shell cell survives a cross-chunk nav > the injected route chunk's eager boot + seed-apply does NOT reset a mutated shell cell",
    "§20.8.2 step 3 — rehydration re-wires the swapped region > a non-delegable handler in the swapped-in region fires after rehydration",
    "§51.0.S.2.6 no-arm-for-message — runtime no-op > a message dispatched in a state with NO arms for it is a no-op (.Off declares none)",
    "§51.0.S.2.7 arm-target rule= violation — runtime E-ENGINE-INVALID-TRANSITION > an arm resolving a target not in the from-state rule= throws E-ENGINE-INVALID-TRANSITION",
    "§51.0.S.3 same-state arm — effect runs, onIdle resets, onTransition does NOT fire > .Tick (same-state arm) RESETS the <onIdle> watchdog — §51.0.R handled-message reset",
    "§51.0.S.3 same-state arm — effect runs, onIdle resets, onTransition does NOT fire > .Tick in .Playing resolving .Playing RUNS the effect (@score increments)",
    "§51.0.S.6 worked example — drag flow (happy-dom runtime) > .advance(.Drop(col)) from .Dragging runs the arm body effect (both payload planes) AND transitions to .Idle",
    "§51.0.S.6 worked example — drag flow (happy-dom runtime) > .advance(.Start(id)) transitions Idle → Dragging carrying the id payload"
  ]
}
