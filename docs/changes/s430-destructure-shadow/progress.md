# progress s430-destructure-shadow
- 2026-09-23T21:48:54-06:00 start at /home/bryan-maclee/scrmlMaster/scrml/.claude/worktrees/agent-af4aa90da54f9860a, base 15e60e4b
- 2026-09-23T21:51:54-06:00 fix: dropped !scopeChain.lookup(bind) guard at both destructure bind sites (decl arm + for-of arm); 15 new tests (11 fail pre-fix)
- 2026-09-23T22:03:19-06:00 committed 394c38c1 (hook green). Corpus A/B running: 1148 files (examples samples stdlib compiler/self-host{,-v2} compiler/native-parser docs/website), fixed vs basetree copy w/ 15e60e4b type-system.ts
- corpus A/B (1148 files, COMPILED one at a time): zero exit-code / diagnostic / emitted-artifact change attributable to the fix. The 11 raw diffs were all stdlib/* and an env artifact of the baseline copy (its compiler resolved stdlib from its own copied tree); rechecked each compiler against its own stdlib copy: 11/11 SAME. All other emitted outputs byte-identical.
- full `bun run test`: 32494 pass / 60 fail / 179 skip. The 16 failing files run in isolation give IDENTICAL pass/fail counts with and without the fix (reverse-applied patch) -> pre-existing (9 browser files, self-host/tab.test.js 3 tokenizer-parity; 5 others pass alone = load flakes).
- PRE-EXISTING found: non-rendering `for (let x of xs) { x = ... }` (plain or destructured binder) compiles exit 0 but emits `for (const x of xs) { const x = x + ...` -> runtime ReferenceError (TDZ). s427 fixed only rendering loops. Also type-system for arm ignores `letBinder`: `for (const x of xs) { x = 1 }` gets no E-ASSIGN-004.
- FIX ROUND (coordinator msg): merged origin/main (f554e171, maps-only).
- F1 (a) codegen: new emit-lift.js loopBodyDeclaredNames(); every for-of emitter (plain emitForStmt, §8.10 hoisted, _emitForStmtWithTilde, emitForExprDecl) uses forHeadKeyword + it -> a `let` binder written in a NON-rendering loop gets a `let` head + assignment. (b) parser records constBinder:true (3 live sites + native translate-stmt), typer for-arm binds it isConst -> E-ASSIGN-004. Keywordless unchanged (pinned). (c) shadow tests now EXECUTE emitted fns. New for-loop-binder-mutability-s430.test.js (21 tests, runtime-executed incl. hoisted server handler w/ stubbed sql). within-node allowlist bumped for 6 fixtures: every new constBinder divergence is on a pre-existing MISALIGNED node (0 aligned for-stmt mismatches, verified).
