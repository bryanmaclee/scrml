# progress — each-over-arm-payload-binding-unbound-2026-06-17

- 2026-06-17 startup: worktree pwd=/home/bryan-maclee/scrmlMaster/scrml/.claude/worktrees/agent-acfe15d2699c08221; ff-merged main to 42413515; bun install + pretest OK; tree clean. Next: confirm bug on 2 repros.
- 2026-06-17 confirmed bug: ex16 happy-dom mount THROWS `ReferenceError: rows is not defined`. Emitted `_scrml_each_render_<id>()` is top-level (no params); `const _items = rows;` references arm-payload binding NOT in scope. Identical shape for ex29 (`const _items = items;`). render fn registered + called via `_scrml_remount_each(_mount)` (no-arg invocation) and `_scrml_effect_static`. Binding value lives in reactive cell: `_scrml_reactive_get("phase").data.rows`. Next: read emitEachBodyRenderForFile + buildMatchArms lift-stamp site.
- 2026-06-17 fix part 1 (emit-each + match): added `armPayloadBinding` AST field + `stampArmPayloadEaches` shared helper (emit-each.ts) + arm-payload itemsExpr resolution (resolve from `_scrml_reactive_get(cell).data[field]` gated on `.variant===tag`). Wired match-side stamp in buildMatchArms via resolveOnExpr cell name + per-arm lazy-require stamp. ex16 now MOUNTS OK no throw; emitted `const _items = (_scrml_arm_v_... .variant==="Loaded" && .data) ? .data["rows"] : [];`. Next: engine-side wiring in buildEngineArms.
- 2026-06-17 fix part 2 (engine) + tests + baseline: engine-side stamp wired in buildEngineArms (meta.varName cell). ex29 each render fn resolves from _scrml_reactive_get(itemsPhase).data[items]. Both repros mount clean. observe-one path confirms 16#empty / 16#populated / 29#empty all flip compiles-but-throws -> renders-clean; baseline JSON updated surgically (3 cells red->green + histogram 33->30 / 254->257). Added regression test compiler/tests/browser/each-over-arm-payload-binding-unbound.browser.test.js (10 tests, all pass: §1 emit no-bare-binding + §2 happy-dom mount-no-throw + variant-transition renders rows + empty body). Note: synthetic repro must mirror ex16 explicit-markup form (`<li>${@.name}</li>` + `</each>` + `</>`); the `:`-shorthand `<li : @.name>` body tripped E-MATCH-PARSE-001 arm-closer detection (test-source shape only, not the fix). Next: full suite + flip gap + regen §0.
- 2026-06-17 DONE: full pre-commit suite passed 0-fail (17137 pass / 17228 tests) on the test+baseline commit (no within-node allowlist breakage from emit-each change). Flipped g-each-over-arm-payload-binding-unbound open->resolved in known-gaps.md + regen'd §0 via `bun scripts/state.ts --write` (HIGH count 1->0). All steps complete.
