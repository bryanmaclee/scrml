# Rename E-CODEGEN-INVALID-JS → E-CODEGEN-INVALID-LOGIC (JS→LOGIC error rename)
change-id rename-invalid-js-to-logic-2026-07-04 · agent a3f7b928e8ae2bece · base 60089ca8
User-ratified: the catch-all emit-parse gate is adopter-visible but scrml authors write LOGIC not JS. Rename the ONLY JS code name → E-CODEGEN-INVALID-LOGIC across ACTIVE code (src ~27 / SPEC ~14 / tests ~72 / conformance) + de-JS the message ("could not lower this construct to valid output"). LEAVE append-only history (delta-log/changelog/BRIEFs/resolved-gaps); update SPEC-INDEX + open-gap current-behavior mentions. Secondary: de-JS other adopter-facing "JavaScript" messages (E-MATCH-ARM-MARKUP-IN-VALUE etc.) without renaming those codes. Hard-rename (no alias, pre-V1). Invariant: grep E-CODEGEN-INVALID-JS in src/tests/conformance/SPEC → 0. Full brief = Agent prompt.
