# S430 common dispatch rules (every S430 worktree agent reads this)

## CRITICAL — STARTUP VERIFICATION + PATH DISCIPLINE
1. `pwd` MUST start with `/home/bryan-maclee/scrmlMaster/scrml/.claude/worktrees/agent-`. Else STOP and report.
2. `git fetch origin`; `git merge-base HEAD origin/main` MUST equal `git rev-parse origin/main`.
3. Fetch your brief: `git fetch origin s430/briefs && git checkout FETCH_HEAD -- docs/changes/s430-common/ docs/changes/<your-change-id>/`
4. `bun install`; then `bun run pretest` from the worktree CWD. NOT `bun --cwd … run` (silently no-ops, exit 0).
5. Edit/Write ONLY on worktree-absolute paths. NEVER `cd` into /home/bryan-maclee/scrmlMaster/scrml.
   NEVER `git stash` (the stash stack is shared across worktrees). NEVER bare `pkill -f`/`killall` (kill by captured PID).
   Private scratch ONLY under `<worktree>/.scratch/` — sibling agents are running concurrently.
6. First commit: `WIP(<change-id>): start at $(pwd)`. Commit after every unit (WIP fine). Append
   `docs/changes/<change-id>/progress.md` (timestamped). NEVER `--no-verify`, NEVER touch core.hooksPath.
7. Test with `bun run test` (full) before reporting; the known flake is the `api-decl-codegen` 5 s `node --check` timeout
   under full-suite load (passes alone). Anything else red is yours.

## MAPS — REQUIRED FIRST READ
`.claude/maps/primary.map.md` — stamped 787d4cb4, ~50 commits behind HEAD. Follow its Task-Shape Routing, but treat every
locus it gives as a verify-against-source hypothesis. Report whether the map was load-bearing.

## scrml-writing
If you write ANY scrml: read `../scrml-support/docs/gauntlets/BRIEFING-ANTI-PATTERNS.md` (absolute:
/home/bryan-maclee/scrmlMaster/scrml-support/docs/gauntlets/BRIEFING-ANTI-PATTERNS.md, READ-ONLY) and
`docs/articles/llm-kickstarter-v2-2026-05-04.md` first. No try/catch/throw/async/await/class/null/undefined in scrml.

## Locus discipline
Any file/line this brief names is PA-LOCATED-VERIFY — a hypothesis from a search, not a trace. Report held/refined/wrong.

## Rulings in force (S430, bryan) — do not contradict
P1 `class` rejected (no methods, no virtual fns; free fns over struct values, state by returning a new value) · P2 try/throw
already rejected; the export-decl diagnostic swallow is being closed (separate agent) · P3 `defer` (staged) · P4 `import:host`
built, dynamic `import()` rejected · P5 bootstrap done-gate = hybrid stage-swap + conformance; fixed point · P6 SPEC is
authoritative; divergences become cases · P7 TS fixed only for bootstrap-blocking / adopter-reported / security; else
`status=carried` + an expected-to-fail conformance case.

## Report
branch · FINAL_SHA · files touched · locus hypothesis held/refined/wrong · measured corpus impact (COMPILED, not grepped)
· anything pre-existing found along the way (that list is a queue, not a footnote).
