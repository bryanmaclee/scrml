# BRIEF — 11 corpus files with genuine ?{}-no-db data-loss (surfaced by the E-SQL-004 fix)
The E-SQL-004 R26 sweep surfaced 11 samples/examples with a real `?{}`-without-`db=` data-loss bug the silent `:memory:` fallback was masking — 6 were compiling CLEAN before: `examples/05-multi-step-form.scrml` (signup INSERT silently dropped), `examples/29-engine-vs-flags.scrml`, `samples/gauntlet-r14/htmx-forms.scrml`, `samples/compilation-tests/{server-005-mixed,gauntlet-r10-react-wizard,server-008-form-handler}.scrml`. The other 5 already had errors. SCOPE CALL (bryan): migrate the 6 clean examples (add `db=`) as content fixes, or accept-and-track. Agent did NOT edit example content unilaterally (per the no-unilateral-example-edit rule). None gate the suite.
DONE-PROBE: grep -q 'db=' examples/05-multi-step-form.scrml
