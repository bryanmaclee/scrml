
## 2026-05-29 — Phases 2/3/4 + tests DONE
- Phase 0.5 (NEW pre-req, was blocking): typer suppresses E-ERROR-002 for !-calls inside <errorBoundary> via errorBoundaryDepth counter threaded through visitNode markup descent. Boundary tag is on `n.tag` NOT `n.name` (debug-found). Canonical §19.6 example now COMPILES (was hard-blocked).
- Phase 2: emit-error-boundary.ts (markup-to-HTML re-parse + payload-field substitution + tokenized-markup normalization + variant-renders extraction); emit-html.ts boundary stack + fallback/variant-renders stamp onto logic bindings; emit-event-wiring.ts boundary dispatch (typed !-error -> variant renders/fallback) + C-hybrid host-JS backstop; runtime _scrml_error_boundary_log + _scrml_error_boundary_uncaught (errors chunk, always-on — no chunk-gate / Bug-57 class avoided). Arity-aware payload substitution: single-field -> bare .data, multi-field -> .data.field (mirrors !{} emitGuardedArmBinding envelope).
- Phase 3: E-ERROR-005 static exhaustiveness (errorBoundaryFallbackStack + fnErrorTypes + typeRegistry variant.renders). ALSO fixed a pre-existing parseEnumBody bug: newline-separated `renders` clauses were silently dropped (split on \n orphaned the `renders` continuation) — merged back onto the preceding variant. Without this, E-ERROR-005 mis-fired on variants that DO carry renders.
- Phase 4: PRIMER §6 + kickstarter §6.8 corrected (renders=.Fallback/auto-synth/§19.11 -> §19.6 fallback= + per-variant renders + backstop). intent preserved.
- TESTS: happy-dom 5 tests (typed renders+fallback, backstop+log+sibling-survival, nesting) ALL PASS. conformance 7 tests (emit dispatch, backstop try/catch, arity, E-ERROR-005 fire/no-fire) ALL PASS.
- R26: examples/09-error-handling.scrml (only adopter errorBoundary source) recompiles clean under default-on gate, node --check OK. Its boundary has no markup !-call (all in handleSubmit !{}), so no E-ERROR-005 — correct.
- All emitted JS passes the default-ON parse gate (validateEmit=true at baseline). node --check clean on client.js + runtime.

## 2026-05-29 — RECOVERY NOTE (mid-dispatch HEAD reset)
DETECTED at final-report: my Phase 0 (fdd396a6) + Phase 1 SPEC (af57a877) commits were NOT ancestors of HEAD — mid-dispatch the worktree HEAD reset to db9dba55, dropping both. The SPEC §19.6.8 amendment was LOST from committed history (working tree + HEAD). RECOVERED: af57a877 object survived (not gc'd); cherry-picked cleanly onto HEAD as 18ac01df (SPEC delta applied at the unchanged §19.6.7/§19.7 anchor — no conflict). SPEC §19.6.8 now verified present in HEAD. The Phase-0 progress commit content was superseded by later progress appends; no loss. Lesson: backgrounded commits after a foreground commit-then-reset can silently re-base on the pre-reset HEAD — verify `git merge-base --is-ancestor` of early commits before claiming DONE (this IS the S83 crash-recovery discipline paying off: per-unit commits made the SPEC recoverable).
