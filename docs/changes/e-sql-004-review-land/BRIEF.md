# BRIEF — E-SQL-004 no-db fix: S239 adversarial-review + land (built, held on branch)
The §8.1.1 E-SQL-004 fix is BUILT + gated-green on `fix/s263-e-sql-004` @ 976f98ac: fires E-SQL-004 (was silent `new SQL(":memory:")` → runtime data loss) on a `?{}` with no `db=`, gated `containsSql` + `!isLibraryShapedFile` (emit-server.ts + emit-tool.ts). Premise-verified (E-SQL-004 vs E-SQL-009 = live+disjoint, no drop-ruling). Conservative boundary excludes: the §19.9.6 idempotency shadow-table (E-CPS-NONIDEM-NO-STORAGE), library modules (E-SQL-009), the configured-db-file-absent validation path. R26: 1001 files, 11 real bugs caught, 0 false positives. Migrated 18 test fixtures that relied on the silent fallback. Gated 20598/0. HELD pending the S239 adversarial pass (it modifies codegen — real blast radius) — review-then-land next session.
DONE-PROBE: grep -rq '"E-SQL-004"' compiler/src/codegen/emit-server.ts
