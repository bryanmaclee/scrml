${
    type Color:enum = { Red | Green | Blue }
    fn name(c: Color) -> string {
        return match c {
            .Red :> "r"
            .Green :> "g"
            .Blue :> "b"
        }
    }
    log(name(Color.Red))
}
