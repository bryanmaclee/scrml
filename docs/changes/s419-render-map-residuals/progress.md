- 2026-09-17T04:06:17Z start at C:/Users/pjoli/Documents/GitHub/scrml/.claude/worktrees/agent-aba5185b9d1048b0f; base cc10480f (== origin/main); bun install ok (puppeteer postinstall exit 1, unrelated); pretest produced samples/compilation-tests/dist/
- 2026-09-17T03:55Z pre-fix: copied tier to compiler/tests/e2e-render-map-pre (untracked, deleted at end); full-corpus runCorpus dump started on the copy (no baseline write). C:\tmp\scrml-e2e-render-map held 1495 leaked case dirs before any run.
- 2026-09-17T04:02Z fix 1: render-detectors.js — one "not rendered" predicate isUnrenderedByOwnMarkup (script/style/noscript/template tag, hidden attr, aria-hidden=true, inline display:none / visibility:hidden); isUnrenderedByMarkup (ancestor walk) is the element half; the text half is collectTextNodes(body, isUnrenderedByOwnMarkup) (prunes unrendered subtrees). Probe: happy-dom parses <noscript> children as elements, so <noscript><img src> was reachable by the element half too — now excluded by the same predicate; <template> children live in .content (already invisible).
- 2026-09-17T04:02Z fix 1 consumer decisions: hasRenderedContent text half -> rendered text only (the fix). D3 (bodyText) / D4 + D5 (collectTextNodes, no predicate) -> KEEP all text: they detect a wrong VALUE written by codegen, which is the same defect in a hidden panel. elementCarriesContent(textarea).textContent -> keep (a textarea's text child is its value; hiddenness is checked by the caller).
- 2026-09-17T04:03Z fix 1 tests: +10 must-fire (hidden text x5 kinds, script-only, style-only, noscript-only, all three, noscript img) asserting seeded renders-empty-with-data + unseeded renders-empty, with a has-text precondition; +6 visible-text must-stay-clean guards. detector-validation 37 -> 53 pass, 179 expect().
- 2026-09-17T04:08Z BITE fix 1: render-detectors.js -> pre copy (md5 f30cdb08): 43 pass / 10 fail (all 10 must-fire red; guards green). Restored (md5 9e14d000) -> 53/0. Guard bite A (over-broad mutant: aria-hidden any value, style regex matches any display/visibility, parent-contains-script-or-hidden prunes siblings): 5 of 6 guards red. Guard bite B (text walk prunes everything below <main>): all 6 guards + the existing content case red. Restored md5 9e14d000 -> 53/0.
- 2026-09-17T04:14Z pre-fix full corpus (runCorpus on the -pre copy, no baseline write): 443 cells / 439 apps in 291s; histogram clean 278 / needs-server 8 / throws 21 / fails-compile 117 / empty 17 / smell 2. No new C:\tmp leaks during the run (1495 before and after).
- 2026-09-17T04:21Z pre-fix tier (bun test compiler/tests/e2e-render-map-pre/, byte-identical to cc10480f): 47 pass / 0 fail / 1026 expect(). WARN: 3 new cells (per-route-roles/routes/admin#empty needs-server, 32-external-api#empty smell, 33-endpoint#empty clean), 2 regressions (todomvc#empty, 09-error-handling#empty — known live reds).
- 2026-09-17T04:20Z fix 2a: generate-baseline.js — exported seedLabelsFor(app) (one definition of an app's cells; runCorpus + the suite's slice loop now use it), liveCellKeys(corpus), findOrphanBaselineCells(baselineCells, liveKeys); computeDelta returns orphans and --check prints them (never an exit code). e2e-render-map.test.js §1: WARN-only test names orphans; hard assertions on non-vacuity (live set non-empty and overlaps the baseline) and on an orphan injected into a COPY of the baseline cells (found) and a live key (not found). Baseline untouched.
- 2026-09-17T04:20Z fix 2b: render-harness.js — TMP root resolve("/tmp") (C:\tmp on Windows) replaced by per-case mkdtempSync(join(os.tmpdir(), "scrml-e2e-render-map-")) (no shared root left behind); observeApp owns cleanup in one finally (a mount/detector throw used to skip it) and tags the error with harnessTmpDir; optional hooks.onTmpDir (test-only). Tests: compileApp stages under tmpdir(); observeApp dir gone after success (real D3 mount) and after an injected throw that escapes mountAndObserve's finally.
- 2026-09-17T04:23Z post-fix tier: 67 pass / 0 fail / 1096 expect(). Orphan WARN prints exactly: benchmarks/per-route-roles/routes/loads.scrml#empty. Slice WARN lines identical to pre (same 3 new, same 2 regressions, 0 improvements). 0 scrml-e2e-render-map-* dirs left in %TEMP% after the run.
- 2026-09-17T04:30Z BITE fix 2a: findOrphanBaselineCells mutated to `return []` (generate-baseline.js md5 c1de40d5 -> b67b9fe0): orphan test FAILS on the injected key (Received []). File-copy revert to the cc10480f generate-baseline.js (md5 6bc96051): suite fails to load (SyntaxError: export findOrphanBaselineCells not found). Restored md5 c1de40d5 -> 1 pass.
- 2026-09-17T04:31Z BITE fix 2b: (i) compileApp stages via mkdtempSync(resolve("/tmp", …)) (md5 df9170c4): "stages under the OS temp dir" FAILS, 3 others pass. (ii) pre-fix cleanup shape (no finally; cleanup only before the mounted return): "when the mount throws" FAILS, success test passes. (iii) no cleanup at all: both observeApp tests FAIL. Restored md5 6db9fdde -> 4 pass. (A file-copy revert of render-harness.js cannot load the suite: TMP_PREFIX / the onTmpDir hook do not exist pre-fix, hence mutants.) The (ii)/(iii) bites leaked their dirs by design; removed after the post corpus run.
- 2026-09-17T04:34Z consumer check for the D3–D5 decision: the only two smell cells in the full corpus trip on text in VISIBLE elements, not script/style: examples/32-external-api#empty D4 `/users/${id}` in body>div>div>h2>code; gauntlet-s20-meta/meta-in-component-001#empty D5 `undefined` in body>div>span. So keeping raw text for D3–D5 moves no cell.
- 2026-09-17T04:45Z post-fix full corpus (runCorpus on a copy of the committed tier @ 767dc410, no baseline write): 443 cells in 284s; histogram clean 277 / needs-server 8 / throws 21 / fails-compile 117 / empty 18 / smell 2. Cell diff vs pre: EXACTLY ONE move — samples/compilation-tests/gauntlet-s19-phase2-control-flow/phase2-if-attr-show-compose-110.scrml#empty renders-clean -> renders-empty (baseline: renders-clean). EXPLAINED by fix 1: the app's only content is `<div if=@present show=@visible>hybrid</>` with visible=false; the mounted DOM is `<div data-scrml-bind-show=… style="display: none;">hybrid</div>` (probed), so its only text is hidden. Green -> green; unseeded; no gate outcome changes. All other 442 cells identical (state + smells). No move from fix 2 (staging under %TEMP% did not change any multi-file cell).
- 2026-09-17T04:47Z cleanup: -pre/-post copies deleted; the 3 dirs leaked by the fix-2b bites removed from %TEMP% (0 scrml-e2e-render-map-* left). The 1495 pre-existing C:\tmp\scrml-e2e-render-map case dirs were NOT touched (outside the worktree; report only). Final tier: 67 pass / 0 fail / 1096 expect(); orphan WARN names benchmarks/per-route-roles/routes/loads.scrml#empty. Baseline NOT regenerated.
