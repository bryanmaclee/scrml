# BRIEF — `if=` on `<engine>` / `<match>` / `<each>` (S302, bryan-RULED "make it work")

Archived verbatim at dispatch time per pa-base §5. Ruling: bryan, S302 — "widen the check yes to if on
all 3", confirmed "make it work" when told it was a widening rather than a fix.

Normative target: **SPEC §17.1 amended + NEW §17.1.2** (landed this session, read it first).
