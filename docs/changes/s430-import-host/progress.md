# progress — s430-import-host
- 2026-09-23 start at /home/bryan-maclee/scrmlMaster/scrml/.claude/worktrees/agent-a0d0b326ea9a3a0a9; base 15e60e4b; install+pretest OK
- 2026-09-23 repro confirmed on base (both parsers): `import:host {…}` at file top -> page text in <body>, E-SCOPE-001 on the use site only.
- 2026-09-23 7aac6fca compiler/src/host-import.js — manifest reader (Bun.TOML, nearest scrml.toml up to a .git boundary), post-parse gate (E-IMPORT-003/008/009), host-module scan (Bun.Transpiler.scan, never evaluated).
- 2026-09-24 cd210d50 live front-end: IMPORT_HOST_LIFT_RE lift + parseLogicBody hostTag; api.js MANIFEST stage before BS + gate after TAB; emit-library tag strip; rewriteRelativeImportPaths re-bases .ts/.mts/.mjs + tolerates trailing blanks.
- 2026-09-24 e8d3773a module-resolver: host-module record -> E-IMPORT-006 / E-IMPORT-004; host edges in E-IMPORT-002 DFS.
- 2026-09-24 620a7f2a native parser: parseImport `import:` branch (+ .scrml mirror), lift, hostTag carried in translate-stmt + collect-hoisted.
- 2026-09-24 307708df tests: compiler/tests/integration/import-host.test.js (34, both parsers, e2e runs under bun) + 4 conformance codes-half cases.
- 2026-09-24 corpus A/B (scripts/corpus-emit-differential.ts, base 15e60e4b vs head 307708df): 0 compile-failure delta, 0 diagnostic-code changes, 0 artifact content diffs over 1929 common sources. (1244 "text-only" = the `<OUT>` path normalization missing because the head work dir sat inside the head compiler root — measurement artefact.) 3 new syntax-failing artifacts = the new reject conformance cases' client.js (a failed compile's emitted classic script carrying an `import`).
- 2026-09-24 full `bun run test`: head 32518 pass / 59 fail; base 32480 pass / 59 fail; failure SETS identical (browser/dev-server/self-host-parity, pre-existing in worktree env).
- CHANGELOG (F2, accepted by review): relative imports of `.ts` / `.mts` / `.mjs` modules in emitted library / server / tool JS are now re-based from source to output coordinates like `.js` ones — base emitted them verbatim, a path that did not resolve from the output directory.
- 2026-09-24 review round on ea01e11a (merged origin/main f554e171 first — master-list.md only):
  - F1 HIGH gate bypass: api.js fast path is now `source.includes("import")` (strict superset of every accepted form); the lift gate (live + native) tolerates block/line comments anywhere before `:` and re-joins the text/comment siblings BS / the native trampoline cut at each `//`; emit-library's tag strip anchors on the node and walks back to its `import` keyword across comments. Probe: 512 forms (8 gap kinds in each of 3 slots) x 5 positions x 2 parsers = 5120 compiles, 0 bypasses, 0 page-text leaks, exact per-position codes. Live `import:host {…}\n from` (newline before `from`, a pre-existing live statement-boundary limit shared with plain `import`) now reports E-STMT-EXPECT-FROM instead of vanishing.
  - F3 MED: every rewriteRelativeImportPaths caller passes the emitted file's own dist directory (write phase `pathFor().targetDir`; the emit gate computes the same dir). Evidence: plain `lib/sub/m.scrml` -> `build/dist/sub/m.js` — base emits `../../vendor/util.js` and `bun` fails `Cannot find module`; head emits `../../../vendor/util.js` and runs `{"top":"util","m":"utilmore"}`. Host: `stdlib/compiler/sub/cg2.scrml` runs under bun from dist/sub (tests, both parsers).
  - F4: the rewriter parses the emitted module (acorn) and rewrites each ImportDeclaration's source literal — any count per line, any clause shape, never inside strings/comments; the old line regex is only a fallback for unparseable text.
  - F5: a host module that does not parse -> E-IMPORT-006 "could not be parsed" (no new §34 code — that needs a SPEC amendment).
  - F6/F7: host record is structural — TS type-stripped by Bun.Transpiler.transformSync, then parsed; `export *` detected from the tree; a module with no ESM syntax (CommonJS) skips the name check.
  - F9: manifest lookup and allow-list use realpaths (symlink at stdlib/compiler denied; alias judged by real path).
  - p3-follow isComponent budget for ast-builder.js 29 -> 30 (one more write-side lift stamp).
  - corpus A/B base 15e60e4b vs head 4522454b (work dirs outside both roots): 0 compile-failure delta, 0 code / 0 text diagnostic changes, 0 artifact content diffs of 7482; only syntax deltas are the 3 (4 script-goggle) new reject conformance cases.
- 2026-09-24 re-review of d130c878: merged origin/main (3676d2ae #1042, a9c982dd #1044, 585261d9 #1043) — auto-merged ast-builder.js / api.js / translate-stmt.js; host-import hunks verified intact; import-host tests + gate green post-merge.
  - (1) scanHostModule runs every host module through Bun.Transpiler with the loader matching how Bun loads it (bun 1.3.14 verified): .js -> "jsx" (Bun accepts JSX in .js), .mjs -> "js" (Bun rejects JSX in .mjs, so we report E-IMPORT-006 too), .ts/.mts -> "ts"; decorators parse under all. Tests: decorators + JSX .js host compiles and runs; .mjs JSX rejected; export names still checked.
  - (2) `import:` with a non-host tag and no `{` clause (file-top prose) -> exactly ONE E-IMPORT-009 naming the text found; no E-IMPORT-008 / E-IMPORT-003 / E-STMT-* / E-SCOPE-001. Also closes a silent swallow found while testing: live `import: 3 things ...` (no identifier after `:`) compiled with ZERO diagnostics. Not changed (outside the brief): `import:host is our bridge.` (tag host, no clause) still gets grammar errors + E-IMPORT-008, the native parser with a wider recovery cascade.
