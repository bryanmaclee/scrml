# R27 fix-wave progress — 2026-05-29T15:48:05Z
- Startup verified: pwd=/home/bryan-maclee/scrmlMaster/scrmlTS/.claude/worktrees/agent-ad9c089eec0b7e248, base=feab1207, tree clean, bun install OK.
- [15:48:05Z] C3 DONE: added `int`→integer alias to BUILTIN_TYPES (compiler/src/type-system.ts:629). Repro /tmp/pa-r27-int.scrml now exit-0; emitted client.js byte-identical to integer control. Regression test added (schema-for.test.js §1 "bare int ... lowers to integer"); 54 pass. tableFor/schemaFor/plain-decl all route int→integer consistently.
- Next: C1 (two-bound length validator).
- [15:59:40Z] C1 DONE: two-bound length(>=N, <=M). Root cause BROADER than brief: BOTH formFor synth AND hand-authored Shape-2 mis-handled it (formFor spliced 2nd bound raw into object literal; Shape-2 failed SYM type-check treating 2nd bound as inline-override). Fixes: (a) validator-arg-parser.ts — slot>0 starting with rel-op parses as relational-predicate not inline-override (new startsWithRelationalOp helper); (b) emit-validators.ts — fan out multiple relational args into separate _scrml_validator_fire("length",...) blocks (new emitFireBlock helper); (c) emit-form-for.ts — split argsRaw on top-level commas (new splitTopLevelCommas helper); (d) symbol-table.ts — checkValidatorArity + extractInlineOverride made relational-host-aware (length(>=N,<=M[,"msg"]) legal). All 4 forms verified node-check CLEAN: length(>=2), length(>=2,<=120), length(>=2,"msg"), length(>=2,<=120,"msg"). Tests: validator-arg-parsing §B9.1i/j + form-for.test.js "C1 — two-bound..." (vm.Script syntax check). 327 validator/schema/formFor tests pass.
- Next: C2 (value-return match -> arms).
- [16:08:08Z] C2 DONE: value-return match `->` arms. SPEC §18.2 line 10928 grammar: `match-arm ::= arm-pattern ('=>' | '->') arm-body`; line 10947 normative: "A match arm separator SHALL be => (canonical) or -> (alias). Both forms are accepted." → OPTION (a): `->` lowers IDENTICALLY to `=>`. PRIMER §6.2 is correct, NO doc-fix needed. Root cause: `->` tokenizes as two PUNCT tokens (- >) and the default-logic-mode rejoin inserts a space → `. Idle - > "Idle"`; arm-parsers only accepted =>/:> for dot-prefix forms + only ::Variant for ->. Fixes: emit-control-flow.ts — normalizeMatchArmArrow helper (collapse - > → ->), applied in parseMatchArm + at splitMultiArmString call site; added -> to NEW dot/string/not/else/underscore/presence arm regexes. rewrite.ts — same -> alias added to parseInlineMatchArm NEW forms + armsStr normalized in rewriteMatchExpr. Verified -> emits byte-identical to => (value-return + mixed + payload + else). Tests: bug-1-s95-value-return-match.test.js "C2 (R27)" block (6 tests incl. e2e new Function syntax check). 210 match tests pass.
- Next: C5 (; inside string literal in !{} arm body).
- [16:12:53Z] C5 DONE: ; inside string literal in !{} arm body. AST parse was CORRECT (handler string + handlerExpr both intact); bug was in EMIT — rewriteBlockBody (emit-control-flow.ts:1316) statement splitter scanned for ; / newline at depth===0 tracking only brace/paren/bracket depth, NOT string-literal state. Fix: made the splitter string-literal-aware (strQuote tracker over " ' `, backslash-escape skip). Verified all 3 string types (double/single/template) preserve internal ; AND multi-statement split still works. Tests: error-handler-arm-body-emission.test.js §13 (2 tests: double-quote + single/template-multi). 251 error-handling/match tests pass.
- All 4 bugs fixed. Next: Phase 3 R26 empirical verification.
- [16:15:14Z] PHASE 3 R26 EMPIRICAL VERIFICATION:
  Dev sources (post-fix): dev-1-react exit=0 CLEAN; dev-2-go exit=0 CLEAN; dev-3-elixir exit=1 (environmental E-PA-002 missing expenses.db ONLY — not our bugs) client.js CLEAN; dev-4-svelte exit=0 CLEAN; dev-5-pascal exit=0 CLEAN. ALL 5 outputs FREE of C1-malformed-comparator + C2-match-stub signatures.
  4 minimal repros: pa-r27-{int,len2,match,semi} all exit=0 + node-check CLEAN.
  Canon-form proof: dev-1's reported-broken canon `->` 7-arm value-return match (Item 4) now compiles exit=0 + node-check CLEAN (workaround no longer needed). Dev sources used workarounds at dispatch time because canon forms were broken; minimal repros + canon-form rebuild confirm the canon forms now work.
