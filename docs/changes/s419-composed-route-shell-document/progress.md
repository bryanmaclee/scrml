2026-09-16T21:19:21-06:00 start at /c/Users/pjoli/Documents/GitHub/scrml/.claude/worktrees/agent-a7ce38b91b901ff55 on base cc10480f; bun install: puppeteer postinstall exit 1 (chrome download, not used by gates); pretest OK
2026-09-16T21:29:24-06:00 baseline gates on untouched tree: 23959 pass / 7 fail (foreign-lang lanes 5s timeout, GITI-024 5s timeout, self-host smoke x3, csrf B5, (unnamed)); repro confirmed on HEAD: dist/x.html -> 404 app.css + 404 app.client.js; happy-dom sweep on base: shell/app.html fully live, x.html + deep/y.html fully dead (href '', text/class/if/onclick inert)
2026-09-16T21:31:23-06:00 fix: codegen/index.ts composition resolves shell css/bundle/runtime via toDistRelPath+distRelRef (page dist dir -> real asset dist path). Mechanism for B established by execution: x.html already lists the shell bundle (the only carrier of shell chrome wiring); it 404'd. After fix happy-dom: x.html + deep/y.html href=/admin, text/class:/class=${}/onclick/if= all live. BITE: index.ts base (md5 917e7e73) -> integration 3 fail (1-deep, 2-deep resolve; 1-deep path pin) + browser 4 fail (href '' + missing shell bundle), root pin + own-doc controls stay green; restore (md5 50da28cf) -> 17/17 green
2026-09-16T21:37:04-06:00 gates on fix: 23966 pass / 5 fail (self-host smoke x3, csrf B5, (unnamed)=b4b5 afterAll EBUSY teardown) -- all 5 present in the base run; base's 2 extra fails (foreign-lang lanes, GITI-024) were 5s timeouts that pass on fix. facts --write/--check PASS, state --check PASS. R26: examples/23-trucking-dispatch (115 files) + benchmarks/per-route-roles/routes: base vs fix dist trees byte-identical (diff -r empty; base self-recompile also identical = deterministic)
2026-09-16T21:44:48-06:00 corpus-emit-differential (1928 sources, 7467 artifacts; base = file-flipped index.ts md5 917e7e73, --allow-same-revision since both sides share the git revision): VERDICT NO DIFFERENCES. Related browser tests (cross-chunk, soft-nav, link-boost, multifile-import): identical 10 fails on base and fix (pre-existing); new browser test 9/9. scripts/browser-baseline.ts --check env-broken on Windows (C:/C:/ ENOENT). index.ts restored md5 50da28cf.
