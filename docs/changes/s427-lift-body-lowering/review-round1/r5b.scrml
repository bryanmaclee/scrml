<program>
<items> = [{id: 1, name: "a"}, {id: 2, name: "b"}]
<user> = "U"
<ul>
${
    const name = @user
    lift <h4>${name}</h4>
    for (let item of @items) {
        const label = item.name
        lift <li class="row">${label}</li>
    }
}
</ul>
</program>
