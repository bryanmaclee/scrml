# progress: g-lift-concurrent-transitive-tdz-2026-06-21

2026-06-21T14:03:49Z — startup verified. WORKTREE=/home/bryan-maclee/scrmlMaster/scrml/.claude/worktrees/agent-ad8a0863304529a95. Base merged to 56530d3c (maps/digest/filing FF, no source drift). bun install + pretest OK.
2026-06-21T14:07:07Z — read primary.map (bug-fix routing), scheduling.ts, body-dg-builder.ts, bug-56 test, SPEC §10.5.5 + §19.9.9. Root cause CONFIRMED: scheduleStatements grouping independence check (lines 542-548) only checks direct deps on group MEMBERS; a stmt j depending on an EXCLUDED stmt (not in group) is wrongly batched. Transitive-exclusion closure missing. Designing fix next.
2026-06-21T14:14:04Z — Fix part 1 (transitive closure in scheduling.ts) + part 1b (lambda-body free-read collection in body-dg-builder.ts). R26: repro now emits const profiles BEFORE all uses; n/scored sequential; qtf still batches with fetch; node --check clean; all order assertions pass. Discovered the residual TDZ root: forEachIdentInExprNode deliberately skips lambda bodies (lin scope), so body-DG missed reads captured in .map(pr=>...) arrows — added collectLambdaBodyReads (conservative, param-excluded). Next: Repro B (let-accumulator).
2026-06-21T14:20:47Z — Fix part 2 (Repro B let-accumulator). The reassignment inside the for-loop lowers to a tilde-decl carrying name='acc' (not a bare-expr assign) — collectReassignedNames now also collects tilde-decl/lin-decl names + recurses control-flow bodies. declIsReassignedLater gates batch membership AND seed-leader status. R26: 'let acc = []' now emits sequentially, NOT in const-destructure; node --check clean on both repros; qtf still batches. Next: regression tests.
2026-06-21T14:23:58Z — Added lift-concurrent-transitive-tdz.test.js (4 tests, 14 expects): Facet A transitive-exclusion order-asserting + qtf-still-batches feature-guard; Facet B let-accumulator non-const-destructure + rows/qtf-still-batch. All 4 pass. Bug-56 family still 5/5 green. Next: full pre-commit gate to surface any fixture asserting the OLD batched shape.
2026-06-21T14:45:55Z — DONE. Full gate green: unit+integration+conformance 17526 pass / 68 skip / 0 fail; browser 442/0; within-node 1012/0 (no OVER-BUDGET — codegen-output change, parser-AST unaffected); commands 172/0; lsp 157/0; self-host 142/0; e2e-render-map 7/0. NO existing fixture asserted the old broken transitive-TDZ batch shape (latent miscompile, not test-locked) — zero fixtures needed updating. FINAL_SHA after this commit. git status clean.
