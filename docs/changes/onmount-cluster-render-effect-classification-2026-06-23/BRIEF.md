# On-mount render-codegen cluster — GITI-029 (comment-defeats-recognition) + g-onmount-async (slot-vs-effect)
change-id: `onmount-cluster-render-effect-classification-2026-06-23`

> **Archived per S136.** Dispatched S217 (2026-06-23) to `scrml-js-codegen-engineer`, isolation:worktree, opus, background — one of 3 parallel render-codegen dispatches. Agent `aa0c93ee1e95a2f15`, base `f5f15009`. Full verbatim prompt in the S217 session transcript (this is the faithful archival copy).

## Brief (substance)
Two silent on-mount codegen bugs at one locus; you own on-mount handling (sibling dispatches own each/match). Standard blocks: MAPS-first · STARTUP+PATH-DISCIPLINE (S42/S88/S90/S99/S126) · COMMIT-DISCIPLINE (S83) · R26 (S138) · WITHIN-NODE (S211 — 3 parallel; re-baseline + report).

- **BUG 1 — g-onmount-async:** in the FULL SSR-template-body compile path, a `<program>`-level `on mount { call() }` is mis-emitted as a reactive DISPLAY SLOT (`_scrml_render_value(el, call())` in `_scrml_effect`) instead of a fire-and-forget EFFECT (`call();`) → call's return renders (sync→undefined invisible; async/CPS→`[object Promise]`) + re-runs every update. Root pinned by flogence's real-app Playwright bisect: V0 small-file emits the correct effect; the real SSR-template app emits the wrong slot — the trigger is the SSR-template PATH (not async-ness, not `<match>`). The S214 ss15 markupParentStack fix didn't reach the SSR-template path. Phase-0: construct the SSR-template-path-triggering minimal; fix = on-mount is ALWAYS a fire-and-forget run-once effect, never a slot/re-running-effect, regardless of path.
- **BUG 2 — GITI-029:** a `//` line comment immediately before `on mount {}` makes the whole block emit as literal TEXT (directive not recognized). Blank line / no-comment is fine. Fix = recognize on-mount despite a preceding comment.

## Outcome (RESOLVED — both)
- **BUG 2 (GITI-029) — parser recognition.** Root: `ast-builder.js liftBareDeclarations` — BS extracts the `//` comment as its own `comment` child, flushing the preceding text run, so the on-mount line becomes a standalone text block whose leading `on` token matches no lift gate (BARE_DECL_RE / TOPLEVEL_STATE_DECL_RE / tilde / @-write) → falls through to raw text. (Blank line works because on-mount shares the `<a>=0` block TOPLEVEL_STATE_DECL_RE lifts.) Fix: NEW `TOPLEVEL_ON_LIFECYCLE_RE` lift gate (gated on `isDefaultLogicBody`) wraps the text in synthetic `${...}` for the logic-body parser. +5 tests.
- **BUG 1 (g-onmount-async) — codegen classification.** Phase-0 reproduced two ways: (a) on-mount in a `${}` block that ALSO has a lift-expr (`bodyHasLift` skips the S214 early-return → the per-child binding loop registers the on-mount bare-expr as a render binding), and (b) the flogence root — `<program db=>` body parses to `program>state(db)>logic>bare-expr`, so the enclosing tag is `"state"` → `inDefaultLogicMode` false → the wrong `_scrml_render_value(el, _scrml_boot_188())` + re-running effect. Root: emit-html classifies on-mount POSITIONALLY, but SPEC §6.7.1a/b makes it UNCONDITIONAL — a desugared on-mount/on-dismount is a fire-and-forget effect that NEVER renders in ANY context. Fix: mark the 4 desugar sites with `_onMountEffect`; emit-html skips the render-slot + binding for any such bare-expr, context-independent. Verified against the flogence app (wrong-slot 0, boot_188() runs as mount effect, valid JS). +6 tests.
- **Shared root?** No — DISTINCT loci (parser recognition vs codegen classification); shared the emit-html/ast-builder on-mount region so one dispatch avoided collision.
- **WITHIN-NODE (S211):** re-baselined `rust-dev-debate-dashboard` (FIELD-SHAPE 46→47, MISSING-FIELD 206→212); STRIP_KEYS += `_onMountEffect`/`_onLifecycleLift` (LIVE-only). The only one of the 3 dispatches to touch the allowlist.
- **Sub-decision (Rule 4):** corrected locked `request-tag-and-server-fn-reactive.test.js §6` (was asserting the bug shape — unreachable reactive-display wiring; comment literally said "the on mount block SOMEHOW generates reactive-display wiring") to the §6.7.1a-compliant mount-effect emit. + `p3-follow-no-isComponent` budget 27→28 (the `_onMountEffect` marker).
- Full suite 24985 pass / 0 fail / 213 skip (1060 files). Landed via PA file-delta (this commit). Agent `37421739`.
