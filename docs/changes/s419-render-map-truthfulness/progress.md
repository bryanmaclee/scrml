- 2026-09-17T01:19:12Z start at /c/Users/pjoli/Documents/GitHub/scrml/.claude/worktrees/agent-afcab118c02eae89a; base d1bf550f (== origin/main); bun install ok (puppeteer postinstall exit 1, unrelated to this tier); pretest produced samples/compilation-tests/dist/
- 2026-09-17T01:22Z pre-fix tier run: 12 pass / 0 fail / 912 expect(); delta WARN: 1 improvement (22-multifile throws->renders-empty, the false green), 2 regressions (todomvc#empty, 09-error-handling#empty — known live reds, not mine), 3 new cells.
- 2026-09-17T01:26Z fix 1+2: render-harness.js — findAppDirRoot (string split on "/" over native inputFiles) replaced by exported resolveMultiFileCompileInputs: root = resolve(REPO_ROOT, app.appDir) (declared, not inferred), inputs = path.relative(root, f) re-rooted into tmp, loud throw on missing/outside. entryBase from basename(app.path). locateEntryArtifacts depth + name via path API (split("/") depth was 1 on win32; endsWith matched myapp.html). Enumerator comment documents the two path families.
- 2026-09-17T01:26Z fix 1+2 in-process compile: fullstack 0->2342 html, per-route-roles 0->1435, 22-multifile 0->839, 23-trucking 0->2660; all four html/client md5 distinct.
- 2026-09-17T01:27Z fix 1+2 cell changes (slice): fullstack#empty renders-empty[NO-HTML-EMITTED]->renders-clean; per-route-roles/routes/admin#empty renders-empty->needs-server; 22-multifile#empty renders-empty->compiles-but-throws[D1] (== baseline, same throwMessage); 23-trucking#empty renders-empty->needs-server (== baseline). fullstack/22/23 are (a) corrections. [CORRECTED 02:05Z per review L5: the admin cell is NOT a "correction" — it is a NEW KEY absent from the baseline, caused by PRE-EXISTING baseline key drift (the baseline records `routes/loads.scrml#empty`, an orphan since the S347 deterministic sort made `routes/admin.scrml` the entry). Its new state is not compared against anything.]
- 2026-09-17T01:29Z BITE fix 1+2: new test "each multi-file app mirrors its own inputs..." — harness reverted to pre copy (md5 8de48cf3...) -> 0 pass/1 fail (11 problems incl. fullstack 1/1 inputs not mirrored + no html [gap 2], 22/23/per-route inputs not mirrored + output identical); restored (md5 19b72e76...) -> 1 pass.
- 2026-09-17T01:33Z fix 3: e2e-render-map.test.js — new test pins baseline #populated keys to live seeds (both directions) and names POPULATED_SEEDS keys matching no corpus app. Test-only; no cell can change.
- 2026-09-17T01:33Z BITE fix 3: renamed 3 of 4 POPULATED_SEEDS keys (seed-fixtures.js md5 7c4015cf -> c7640fa8): new test FAILS naming the 3 orphaned baseline cells + 3 orphan seed keys; the pre-existing S417 guard stays PASS (the gap reproduced). Restored via git checkout (md5 7c4015cf) -> 2 pass.
- 2026-09-17T01:40Z fix 4: render-detectors.js — exported CONTENT_BEARING_SELECTOR + hasRenderedContent(body) (non-whitespace text OR a form control / img / picture / svg / canvas / video / audio / iframe / object / embed / [value] / [checked], excluding input[type=hidden]); D6 fires on seeded && !hasRenderedContent(body). Mount shell + structural wrappers do not count. Unseeded renders-empty branch left on text (out of scope, reported). detector-validation: +9 text-free cases (must not fire), +3 genuine-empty cases (must fire). 18 pass / 52 expect().
- 2026-09-17T01:41Z BITE fix 4: render-detectors.js reverted to pre copy (md5 16945dc9) -> 9 pass / 9 fail (all 9 text-free cases red; the 3 genuine-empty + existing D6 cases green on both); restored (md5 985a6851) -> 18 pass / 0 fail.
- 2026-09-17T01:44Z slice re-observed after fix 4: ZERO cell changes vs after fix 1+2 (D6 only runs on the 4 seeded cells, all have text).
- 2026-09-17T01:50Z samples-tier reach check: compiled all 435 single-file corpus apps and compared entry-html choice + entryBase under the old vs new locate logic -> 0 diffs (12 non-direct-html). Fix 1/2's harness edits cannot move any single-file cell; fix 4 only affects seeded cells (all 4 in examples). So the slice diff is the complete cell diff.
- 2026-09-17T01:52Z fix 5: e2e-render-map.test.js header, §2 comment and the regression WARN text no longer claim `generate-baseline.js --check` gates on CI/pre-push; they state what runs the tier (manual `bun test compiler/tests/e2e-render-map/`, whole-tree `bun run test`, or hand-run generator). generate-baseline.js header's "the delta-gate in e2e-render-map.test.js fails" corrected (the suite only warns). Verified: grep of .github/, package.json, scripts/git-hooks for e2e-render-map/generate-baseline = 0 hits; package.json "test" = `bun test compiler/tests/` (sweeps the dir in). No CI files touched.
- 2026-09-17T01:52Z BITE fix 5: text-only; no executable assertion to bite. Verified by grep: "hard gate" / "on CI/pre-push" claims gone from the tier.
- 2026-09-17T02:05Z FIX ROUND (review of tag s419-review-render-map @ 30890e95). Tag not moved; commits on top.
- 2026-09-17T02:05Z M1: hasRenderedContent redesigned — CONTENT_CANDIDATE_SELECTOR (input/textarea/select/progress/meter/img/picture/video/audio/svg/canvas/iframe/object/embed; no bare [value]/[checked], no button/option/output) + elementCarriesContent (text input/textarea non-empty value incl. live .value, placeholder ignored, hidden input never; checkbox/radio checked; select >=1 option; progress/meter value attr; img src/srcset; picture/video/audio src/srcset or source/img child; svg >=1 child; iframe/embed src; object data; canvas on presence — no DOM signal of painting) + isHiddenByMarkup (hidden attr, aria-hidden=true, inline display:none / visibility:hidden, on element or any ancestor up to body). Tests: 10 must-NOT-fire (the 3 content-less ones from round 1 — empty textarea, src-less video/iframe — replaced by content-holding versions and moved to must-fire; + a property-bound input) built in a detached body (a connected iframe/video src made happy-dom attempt a load and log ECONNREFUSED); 21 must-FIRE incl. every reviewer case.
- 2026-09-17T02:05Z L1: final renders-empty branch uses !hasRenderedContent(body) for seeded AND unseeded (one definition of empty; both states green). Text-free tests now assert state renders-clean (seeded and unseeded); must-fire tests assert unseeded renders-empty.
- 2026-09-17T02:05Z BITE M1+L1: render-detectors.js -> 30890e95 copy (md5 985a6851): 6 pass / 28 fail (all 10 text-free on state, all 18 new must-fire). L1 alone (final branch back to bodyText, md5 9ac0813f): 24 pass / 10 fail (the 10 text-free state asserts). Restored md5 f30cdb08 -> 34 pass / 0 fail.
- 2026-09-17T02:07Z L3: compileApp body moved into compileAppInTmp; compileApp wraps it and on a throw rmSync's tmpDir, tags the error with harnessTmpDir, re-throws (success path unchanged: caller owns cleanup). Test: synthetic multi row (fixtures dir, 2nd input in a hidden dir mirrorTree skips) -> throws 'not mirrored' and harnessTmpDir no longer exists.
- 2026-09-17T02:07Z L4: '..' rejected only as a whole segment: rel === '..' || rel.startsWith('..' + sep) || isAbsolute(rel) (cross-drive). Test: in-root '..draft.scrml' accepted; '../escape.scrml' and the root itself rejected; win32-only cross-drive rejected.
- 2026-09-17T02:07Z L2: §1b comment states why this one assertion is deliberately HARD (harness-correctness, not corpus state; same category as §1). 'identical' line only computed for non-empty output.
- 2026-09-17T02:07Z BITE L3: rmSync line removed (harness md5 ba47fc53 -> 6fb3363e): staging-dir test FAILS; L4: condition back to startsWith('..') (md5 2cffd145): '..draft' test FAILS. Restored md5 ba47fc53 -> 2 pass. (The L3 bite leaked one case dir under C:\tmp\scrml-e2e-render-map.) L2 is wording + message shape: no executable bite.
- 2026-09-17T02:07Z L5 (report only, not fixed): baseline orphan benchmarks/per-route-roles/routes/loads.scrml#empty; live entry key is routes/admin.scrml#empty. Also pre-existing: an unseeded body whose only text is <script>/<style>/<noscript> content scores renders-clean (textContent includes it).
- 2026-09-17T02:07Z full-corpus cell diff: ran runCorpus (no baseline write) on a copy of the tier at 30890e95 (compiler/tests/e2e-render-map-pre, untracked, deleted after) -> 443 cells in 274s, histogram clean 277 / needs-server 8 / throws 21 / fails-compile 117 / empty 18 / smell 2. Post run started on the fix-round tier.
- 2026-09-17T02:20Z full-corpus post run (fix-round tier @ 9f3e85be): 443 cells. Cell diff vs the 30890e95 run: EXACTLY ONE move — samples/compilation-tests/gauntlet-s19-phase4-markup/phase4-bind-group-031.scrml#empty renders-empty -> renders-clean (baseline: renders-empty). EXPLAINED: the app is three text-free radio inputs with bind:group=@size initialised to "medium"; the new predicate counts a CHECKED radio as content, so the unseeded L1 branch now resolves clean. This is the deliberate consequence of applying the content predicate to unseeded cells (a selected radio group is not an <empty> fallback). Green->green; no gate outcome changes. It also confirms the predicate needs the binding's checked state (an unchecked radio would not count). All other 442 cells identical, incl. todomvc#empty (compiles-but-throws) and 09-error-handling#empty (fails-compile).
- 2026-09-17T02:20Z comparison copy compiler/tests/e2e-render-map-pre deleted. Final tier: bun test compiler/tests/e2e-render-map/ -> 47 pass / 0 fail / 1026 expect() (was 26 / 948 at 30890e95). Baseline NOT regenerated.
