# primary.map.md
# project: scrml
# updated: 2026-08-27T17:17:26-06:00  commit: 0dd659a1
# generated-at: 0dd659a1 (S380 window, INCREMENTAL_UPDATE by the project-mapper — scoped to the
# codegen surface S380 touched, per the dispatching brief; NOT a full re-walk of every map). Verified
# at write time: `git merge-base --is-ancestor 48f0aaf8 0dd659a1` exits 0 (48f0aaf8 = wrap(s378), the
# prior map set's effective content watermark); `HEAD == origin/main == 0dd659a1`; working tree is
# clean at the repo level EXCEPT one file (see the provenance caveat below).
#
# ═══ SCOPE OF THIS PASS ═══
#
# `git diff --stat 48f0aaf8..0dd659a1 -- compiler/src/` is SEVEN files: `codegen/emit-each.ts`
# (+17/-0, #735 — per-item `<match>` dispatch re-fires on a same-key reconcile field change),
# `codegen/emit-match.ts` (+47/-0, #732 — a derived-cell `<match>` scrutinee wires via an effect, not
# a subscribe that never fires on recompute), `codegen/emit-server.ts` (+12/-0, #728 — the §52.13
# protected-document guard export), `codegen/emit-variant-guard.ts` (+31/-1, #732 + #735's same-value
# short-circuit), `commands/build.js` (+58/-3, #728), `commands/dev.js` (+32/-2, #728, COMMITTED —
# see the provenance caveat), `component-expander.ts` (+122/-23, #726 + #731 + #733). **12 PRs total
# per the wrap commit; the remainder are gap-filing/routing, not source.**
#
# ⚠ **`codegen/emit-html.ts` WAS NAMED AS TOUCHED IN THE DISPATCHING BRIEF AND WAS NOT** —
# `git log 48f0aaf8..0dd659a1 -- compiler/src/codegen/emit-html.ts` is EMPTY, checked twice. Every
# `emit-html.ts` citation already in this map set was left AS-IS (unaffected by this window; spot-
# checked and still correct). See non-compliance findings.
#
# ⚠ **PROVENANCE CAVEAT — `commands/dev.js` CARRIES AN UNCOMMITTED WORKING-TREE EDIT AT WRITE TIME**,
# unrelated to §52.13 (a module-cache-bust fix for #724/#725, +28/-6 on top of committed HEAD). Every
# `dev.js` citation in this pass was verified against **committed HEAD (`0dd659a1`)**, never the dirty
# tree, per this map set's CURRENT-TRUTH-ONLY scope principle. If that edit lands, the three `dev.js`
# anchors this pass added/corrected (`auth.map.md:199/366/1019`) need re-verification.
#
# **WHAT WAS CORRECTED, AND WHY IT COMPOUNDS RATHER THAN BEING ONE FIX.** Both `emit-server.ts` and
# `commands/build.js` gained NEW code (§52.13) landing ABOVE existing dispatch-split / onion-contract
# code that FOUR PRIOR PASSES (S372/S376 among them) had already corrected once for a DIFFERENT
# reason (the #654 wrong-FILE finding, invariant 72). Those citations drifted a SECOND time from a
# change that has nothing to do with the onion at all — see the new invariant 73 below. Separately,
# `codegen/emit-each.ts`'s #735 insertion (`:1516-1533`) sits ABOVE `eachBlockFromMarkupNode`
# (`:3309` -> `:3324`), a citation carried in FOUR places in this file alone (all four corrected) AND
# in structure.map.md's own "Directory Ownership" row for `emit-each.ts` — which turned out to
# carry `:3266`, STALE EVEN PRE-S380 (the S376 pass corrected this file's copy to `:3309` but never
# touched structure.map.md's independent copy; both are now `:3324`, corrected there too).
# `non-compliance.report.md` still names the historical `:3266`/`:3309` pair as a PAST correction
# record — that is a correct historical citation, not a live one, and was left as-is.
#
# **Maps TOUCHED this pass:** primary (this file), structure, auth, domain, error. **Maps NOT
# re-walked or re-verified this pass** (dependencies, schema, test, build, config, infra, migrations,
# non-compliance.report) — their Map Index rows below still name the `fc6df72e` stamp and describe
# content unaffected by this window's diff (verified: none of the seven S380 files appear in their
# citation surface except where corrected above). Re-verify them independently before trusting a
# currency claim beyond what this pass covered.
#
# generated-at: fc6df72e — **THE SAME SHA AS LINE 3, BY CONSTRUCTION, AND THAT IS THE POINT.** The
# working tip at write time was `60803548` on branch `wrap/s376`; `git diff --name-only
# fc6df72e..60803548` returns FOUR DOCS FILES (`docs/changelog.md`, `hand-off.md`,
# `handOffs/delta-log.md`, `master-list.md`) and ZERO source, so the source state actually read IS
# `fc6df72e` — which is `merge-base HEAD origin/main` and IS `origin/main`.
#
# ⚑ **WHY LINE 3 AND LINE 4 NOW CARRY ONE SHA RATHER THAN TWO.** At S372 a refresh bumped line 3 to a
# newer commit while line 4 still named an older `generated-at:` — a **self-contradicting watermark**,
# and the PA correctly unstaged the whole refresh rather than ship it. Two SHAs on two lines is a
# standing invitation to update one and not the other. **When the source diff between the tip and the
# merge-base is EMPTY, there is no second SHA to record — so record one.** The tip is named in prose
# above, where it cannot be mistaken for a watermark. If a future pass generates against a tip whose
# source diff against the merge-base is NON-empty, it must STOP (see the rule below), not stamp two.
#
# ═══ THE MAP-STAMP RULE — READ THIS BEFORE YOU WRITE LINE 3 ═══
#
# **THE WATERMARK ON LINE 3 SHALL BE A COMMIT THAT IS AN ANCESTOR OF `origin/main`.**
#
# One-line reason: **a squash-merge orphans a branch-tip stamp.** `gh pr merge --squash` replays the
# branch onto `main` as a NEW commit with a NEW SHA, so the tip you were sitting on while writing the
# map is never an ancestor of `main` — by construction, not by accident.
#
# The procedure, and it is three commands:
#
#     BASE="$(git merge-base HEAD origin/main)"
#     git diff --name-only "$BASE"..HEAD -- compiler/ scripts/ conformance/ stdlib/ lsp/ .github/ package.json
#     #  EMPTY     => every source claim holds at BASE => stamp BASE on line 3.
#     #  NON-EMPTY => STOP. The map would describe source that is not on main yet.
#     git merge-base --is-ancestor "$BASE" origin/main    # must exit 0
#
# Record the working tip on the `generated-at:` line when it differs from the watermark. **Only ONE
# line may carry `commit: <SHA>`** — `scripts/state.ts:mapsStaleness()` parses line 3 by regex, and a
# second match would be read as the watermark.
#
# ⚠ **RUN THE THREE COMMANDS AT *WRITE* TIME, NOT AT ORIENTATION — AND THIS PASS IS THE SECOND
# CONSECUTIVE ONE TO MEASURE IT, IN A STRONGER FORM THAN THE FIRST.** The S368 pass recorded HEAD
# moving under it by one docs-only commit. **This pass had the WHOLE BRANCH DISAPPEAR.** Generation
# began with `HEAD == 6bb57c66` on `wrap/s368`, and the dispatching brief said in as many words
# *"stay on it, do not switch branches."* Partway through the run the operator squash-merged that
# branch as #676 and checked `main` out, so the checkout was on `main` at `728bdc92` and
# **`wrap/s368` and `6bb57c66` no longer bounded anything.** Re-running the procedure at write time
# returned `BASE = 728bdc92` with an EMPTY source diff (the wrap commit is docs-only:
# `docs/changelog.md`, `docs/pr-reviews.md`, `hand-off.md`, `handOffs/delta-log.md`,
# `master-list.md`), so the stamp is correct and every source claim in this map set holds at it.
# **A pass that had read HEAD once at the start would have stamped a SHA that is not merely a
# non-ancestor but no longer exists on any branch.** The rule as written covers this; running it
# early does not. ⚑ **COROLLARY, NEW THIS PASS: "which branch am I on" is ALSO a write-time
# question, not an orientation-time one.** A generation that recorded its branch at the start and
# committed on that assumption would have committed to a merged branch.
#
# ⚑ **S372 — THIRD CONSECUTIVE PASS TO MEASURE THIS, AND THE COROLLARY ABOVE IS NOW EMPIRICAL, NOT
# THEORETICAL.** This pass ORIENTED on branch `main` at `8b2e4053`. By WRITE time the checkout was on
# **`wrap/s372-bryan` at `b78e444d`** — a branch that did not exist when the pass began. The
# three-command procedure re-run at write time returned `BASE = 8b2e4053` with an EMPTY source diff
# (the branch commit is docs-only) and `--is-ancestor` exit 0, so the stamp is correct. **A pass that
# had recorded its branch once, at the start, would have written `commit: main@8b2e4053` while
# sitting on a branch — and the next pass would have had no way to tell.** Read the branch at write
# time, and record the tip on `generated-at:` when it differs.
#
# ⚑ **AND THE SQUASH-MERGE ITSELF HAPPENED LIVE, MID-PASS, FOR THE SECOND TIME IN FOUR SESSIONS.**
# While this map was being written the operator squash-merged `wrap/s372-bryan`'s first commit as
# **#706 -> `c9a33bfd`**, so `origin/main` advanced under the pass and **`b78e444d` — the SHA an
# earlier draft of this very line carried — is now an orphan.** It is docs-only and changes no source
# claim, and the stamp `8b2e4053` is unaffected precisely BECAUSE it is a merge-base rather than a
# tip. **This is the whole argument for the rule in one observation: a tip you can see is not a tip
# that will exist.** ⚠ **Corollary for a mapper running while a sibling operator lands: stamp the SHA
# you actually VERIFIED against, not the newest one visible at write time.** `origin/main` was
# `8b2e4053` when the source was read and `c9a33bfd` when it was written; stamping the latter would
# have claimed verification that never happened.
#
# ⚠ **ONE CORRECTION TO THIS BLOCK'S OWN CLAIM ABOUT `scripts/state.ts`.** It said a second
# `commit:` match "would be read as the watermark." Read at this watermark: `mapsStaleness()` does
# `const line3 = mapText.split("\n")[2]` (`scripts/state.ts:615`) and matches
# `/commit:\s*([0-9a-f]+)/i` against **that line and no other** (`:616`). So a `commit:` on line 4 or
# below is HARMLESS; the real hazard is a SECOND `commit:` **on line 3 itself**, where the first
# match wins. The one-line-only discipline is kept anyway — it costs nothing and it is what a reader
# will assume.
#
# **WHY THIS BLOCK EXISTS RATHER THAN JUST INVARIANT 48.** Invariant 48 has said "verify the stamp is
# an ancestor" since S326 — and the defect happened THREE MORE TIMES, because 48 as written tells you
# to check the stamp you are READING (`--is-ancestor <old-stamp> HEAD`, which the S331 author ran, and
# which passed) and says nothing about the stamp you are about to WRITE. **The inbound check and the
# outbound check are different checks.** This block is the outbound one.
#
# **THE CONSEQUENCE IS NOT COSMETIC.** `bun scripts/state.ts` refuses to compute a behind-count from a
# non-ancestor watermark (correctly — it will not print a wrong number). So the pre-dispatch
# maps-currency check that `pa-scrml-overlay.md {{maps_fills}}` requires before EVERY dev dispatch
# returned NOTHING, silently, for as long as the stamp was orphaned. **A mandatory step was
# unanswerable and nothing said so.** ⚠ And the weaker half is still true: that check is **WARN-only,
# not gated**. At the start of this pass `bun scripts/state.ts --check` printed
# `maps: 112 commits behind HEAD` and **exited 0**, and it printed `22 commits behind` at the start of
THIS one and exited 0 again. Nothing in the toolchain fails on stale maps.
#
# ═══ ═══
#
# **INCREMENTAL over `8b2e4053` -> `fc6df72e` (S376, 8 commits, PRs #709-#718, TWO operators —
# bryan S375/S376, peter S375/S377).** Ancestry CHECKED FIRST (invariant 48):
# `git merge-base --is-ancestor 8b2e4053 fc6df72e` passes. Outbound check run per the MAP-STAMP RULE
# at WRITE time: source diff against `merge-base HEAD origin/main` is EMPTY, `--is-ancestor` exit 0.
#
# **THE SOURCE WINDOW IS FIVE `compiler/src` FILES AND THE STREAK OF ZERO-NEW-MODULE WINDOWS IS OVER:**
# **`lint-e-state-block-statement-form.js` (NEW, 486L)**, `api.js` (+57, its wiring),
# `component-expander.ts` (+153/-23), `codegen/emit-each.ts` (+43), `codegen/emit-lift.js` (+29/-2).
# **`compiler/SPEC.md` moved by EXACTLY ONE LINE** (37,539 -> 37,540) after two flat windows, and that
# line is a §34 catalog row. `compiler/src/types/` is EMPTY for the FOURTEENTH window;
# `package.json`/`bun.lock`, `.github/`, `scripts/`, `conformance/` and `stdlib/` are all EMPTY.
# +4 test files, zero deleted. **§34 catalog 812 -> 813 — ONE new diagnostic code.**
#
# ═══ WHAT THIS PASS FOUND, AND THE FINDINGS ARE NOT THE NEW MODULE ═══
#
# **(A) A `file:line` CITATION THAT WAS A WRONG *FILE*, NOT A DRIFTED LINE — carried since #654.**
# Both `structure.map.md` and `auth.map.md` said `compiler/src/codegen/emit-server.ts` "splits the
# pipeline remainder into `_scrml_dispatch(req, server)` and wraps it in
# `_scrml_onion_dispatch(req, server)` (`emit-server.ts:521`)". **Neither symbol exists anywhere under
# `compiler/src/codegen/`** — `grep -rn '_scrml_onion_dispatch' compiler/src/codegen/` returns
# NOTHING. Both are emitted by the HOST: `commands/build.js:514` and `:521`. `emit-server.ts:521` is
# §20.5/§52 `@currentUser`-query-gate code, unrelated. **The maps had the right NUMBER against the
# wrong FILE**, which is worse than a stale line — a stale line lands near the truth, a wrong file
# sends a dev agent into 5,000 lines of the wrong module with a plausible-looking anchor. Corrected in
# both maps, split into a codegen row and a host row.
#
# ⚑ **S380 INCREMENTAL: `:514`/`:521` ABOVE ARE THEMSELVES NOW STALE.** §52.13 (auth-protected
# served document, #728) added +58 lines to `commands/build.js` above the dispatch split; the
# symbols are now `commands/build.js:570`/`:577`. Every LIVE citation below is corrected in place;
# this paragraph is the historical record of the #654 wrong-FILE finding and is not itself relied on
# for current line numbers.
#
# **(B) FOUR SEPARATE FIGURES INSIDE THIS MAP SET CONTRADICTED *ANOTHER FIGURE IN THE SAME FILE*.**
# Not stale-vs-HEAD — internally inconsistent, both labelled current:
#   · `error.map.md` — the section headed "Census buckets **at this HEAD**" carried `PINNED 338 ·
#     IMPL-SITES 320 · FALSE-CLAIM 95` (S346 figures) while the catalog section ~35 lines above it
#     printed `343 · 302 · 112` for the same HEAD. **Off by 17 on FALSE-CLAIM.**
#   · `test.map.md` — the "Test Categories" heading said **1,387** total while this same file's header
#     said **1,394**. And its per-category TABLE said unit **901** / browser **94** while the prose
#     four lines above the table said **908** / **95**.
#   · `primary.map.md` (this file) — "Derived-figure authority" published the §34 total as **810**
#     while this file's own header said **812**. That section's text literally reads *"Re-read at this
#     HEAD, not carried."*
#   · `primary.map.md` — the `^| E-` prefix-grep figure published **912** / `W-` **178**. MEASURED at
#     the PRIOR watermark by checking out that SPEC: **915** / **179**. Wrong at its own stamp.
# **⚑ THE RULE THIS PRODUCES, AND IT IS A CHEAP ONE: a figure that appears twice in one map set must
# be DIFFED AGAINST ITSELF before it is carried.** Every one of these four survived multiple passes
# because each pass updated one site. An internal contradiction needs no external oracle to detect —
# it is grep-able inside the artifact — and it is the strongest available signal that a "re-read" was
# a carry. All four corrected, each with the contradiction named in place so the next pass sees why.
#
# **(C) THE NEW MODULE OPENS A PIPELINE SLOT THAT DID NOT EXIST, AND A FUTURE READER WILL MISFILE IT.**
# `lint-e-state-block-statement-form.js` is the **first `E-`-severity pass in the compiler to run
# PRE-AST** — its input is block-splitter output (`bsResults`), at `api.js` Stage 2.5c, before TAB.
# **It scans source text with a regex, and that is NOT an invariant-55 violation**: invariant 55
# forbids a text pass in a stage that ALREADY HAS the AST, and at 2.5c there is no AST to consult.
# The map set says so explicitly in three places because the alternative is a dev agent "fixing" a
# correct design. It also deliberately omits `try`/`catch` where its two siblings have it — a
# swallowed throw in an ERROR gate is fail-OPEN, which is the exact failure the diagnostic closes.
#
# **(D) THE NEW GATE HAS TWO OPEN HOLES AND BOTH WERE REPRODUCED HERE BY COMPILING, NOT READ OFF ITS
# COMMENTS.** A `<div>` one level inside a `<db>` body re-opens the defect at exit 0 with the
# statement in the emitted HTML; and **any unpaired `/`+`*` in ordinary prose — a glob, a path, a
# spaceless division — disarms the gate for the rest of the state block**, also exit 0. `note about
# src/* paths` followed by `on mount { loadDashboard() }` compiles clean and ships. **A glob disarms a
# fatal gate.** Both filed in `docs/known-gaps.md`; both stated in error.map.md, structure.map.md and
# domain.map.md so no reader concludes the locus is closed.
#
# ═══ ═══
#
# ⚑ **THE TWO ROUTING HOLES S372 REPORTED WERE ALREADY FILLED, AND THIS PASS CONFIRMED IT RATHER THAN
# RE-ADDING THEM.** Checked by grep, not assumed: Task-Shape Routing **row 1** covers the `if=`/`show=`
# LOWERING surface and names `emit-event-wiring.ts` (**12 occurrences in this file alone** at this
# watermark, re-counted here, up from 1 before S372); **row 2** covers the LIFT-vs-STRUCTURAL `<each>` split and names
# `eachBlockFromMarkupNode` (present in 4 maps). Both landed in `8731799d` (#707). ⚠ **Row 2's line
# ref DID drift this window and is corrected: `eachBlockFromMarkupNode` is `emit-each.ts:3309`, not
# `:3266`** — #710 inserted 43 lines above it. ⚑ **S380 INCREMENTAL: drifted AGAIN, to `:3324`** —
# #735's per-item `<match>` re-dispatch fix (`emit-each.ts:1516-1533`) inserted 15 more lines above
# it. `emitNestedEachFromMarkup`, its sole caller, is `:3396` (was cited `:3338` even pre-S380 —
# that citation was ALREADY missing the #710 +43; the correct pre-S380 value was `:3381`).
#
# ═══ ═══
#
# Re-walked this pass: **primary, structure, dependencies, domain, test, error, non-compliance.**
# **Currency-VERIFIED but not re-walked (measured ZERO diff in their surface, each carrying its own
# RE-MEASURED evidence in its header): schema, config, build, auth, infra, migrations.** ⚠ **`auth`
# is currency-verified on a zero-diff surface AND carries a corrected row anyway** — finding (A). A
# zero-diff surface is not a correct map; it is only an unchanged one.
#
# Per-window landing narratives stay DELETED (S302 ruling). **History lives in `docs/changelog.md` +
# `handOffs/delta-log.md`.** What earns space here is rules a grep cannot find.

## Project Fingerprint
Language:   TypeScript / JavaScript (mixed) + scrml itself (self-hosting stdlib + self-host compiler experiments)
Framework:  Custom compiler pipeline (no web framework) — Bun-native
Runtime:    Bun >=1.3.13 (no Node support; Bun-specific APIs throughout — `Bun.serve`, `bun:sqlite`, `Bun.$`, `Bun.SQL`, `Bun.hash`)
Type:       CLI compiler + language toolchain (single-file full-stack web-language compiler, with LSP + editor-tooling + MCP surfaces)
Size:       `compiler/src/` **192 files / 245,517 lines** (`docs/FACTS.md` at this watermark, mechanically derived — **+743 lines and +1 FILE**; the three-window "every line landed by editing" streak is OVER: `lint-e-state-block-statement-form.js` is new). `compiler/tests/` **1,398 `*.test.js`** (+4, zero deleted), re-derived by `find` at this watermark and reconciled against FACTS.md: browser 98 · commands 14 · conformance 133 · e2e-render-map 2 · integration 213 · lsp 11 · self-host 4 · unit 909 = **1,384 across the category dirs, +14 at `compiler/tests/*.test.js` ROOT level = 1,398**. ⚠ **A per-directory walk lands on 1,384 and looks authoritative — the 14 root-level files are a category with no directory.** `conformance/cases` **883 `expected.json`**, FLAT for the SEVENTH window. `compiler/SPEC.md` **37,540 lines** (+1: one §34 catalog row).
Version:    v0.7.1 (root package.json — the SOLE manifest; not a workspace monorepo, `files`-allowlisted, publishable). **`package.json` / `bun.lock` are `--name-only` EMPTY this window** — no runtime dep, no dev dep, no version moved. The #665 `typescript@^5.9.2` DEV dependency and the three `types` script entries carry unchanged.
CI:         GitHub Actions — three workflows on `main` (`ci.yml`, `advisory-review.yml`, `cloud-maps.yml`), count unchanged. **`gate` is UNCHANGED at 13 steps this window.** The one new step, `bun scripts/types-gate.ts --check` (#665), is in the NON-BLOCKING `tracking` job and is `continue-on-error` at **both** the job and the STEP level — **not redundant: a failed step HALTS the job even inside a `continue-on-error` job** (verified on run 30742472551, where a later step reported `skipped` and had never run). Promotion into `gate` is an explicit operator call and was NOT taken — it wants a decision on the nine live `never` failures first.

## ⚠️ MAP CURRENCY IS NOW MANUAL — READ THIS BEFORE TRUSTING A STAMP
`cloud-maps` **no longer refreshes `.claude/maps/` on any schedule.** Map regeneration reverted to the
PA at wrap. A stamp is exactly as old as the last wrap and **nothing will move it for you.** This
window's maps sat **8 commits / 10 PRs (#709-#718)** behind at the start of the pass — the prior
window sat 17, before that 21, 22, 24, 6, 23, 19. ⚑ **THE INSTRUMENT IS STILL WORKING, AND IT WAS
RE-RUN AFTER THIS PASS'S WRITE:** `bun scripts/state.ts --check` now prints
`maps: 1 commits behind HEAD (watermark fc6df72e, HEAD 60803548)  [WARN-only — not gated;
project-mapper seam]` — the 1 being this session's docs-only wrap commit, exactly as expected from a
merge-base stamp. ⚠ **It exits 0. Verified again this pass, not assumed:** the command was executed
and its exit status read. Nothing in the toolchain
fails on stale maps. **Nine consecutive passes have now recommended a
deterministic non-AI map-currency check in `cloud-maps` Stage 1; nothing has moved on it** — and this window supplies the
argument that the recommendation was aimed at the wrong failure: the instrument existed
(`state.ts:mapsStaleness()`), and what disabled it was the STAMP, not the schedule.
**THE OFF-MAIN-STAMP HAZARD IS NOW FIXED AT THE SOURCE, NOT JUST WARNED ABOUT — see the MAP-STAMP
RULE block at the top of this file.** It was first recorded here at S326 as a "new hazard" and then
recurred at S331 and again in schema.map.md's carried `fe14c9b2`, because a warning that describes
the failure is not a procedure that prevents it. Three of the five stamps this pass inherited were
PR-BRANCH TIPS, squash-merged onto `main` under different SHAs. Against a non-ancestor stamp,
`git log <stamp>..HEAD` reports a delta that is **both over-stated** (it includes main-line commits
the branch never contained) **and unbounded** (no merge-base guarantee at all) — and
`scripts/state.ts` will refuse to report anything, which is correct and which is also why the
breakage is silent. **The write-side rule is at the top; the read-side check is one command:
`git merge-base --is-ancestor <stamp> origin/main`.** If it fails, the stamp bounds nothing.
Prior hazard, still live: two clones land into the same `main` under COLLIDING session numbers, so
"the last session's maps" is not well-defined — **only the SHA is**, and only if it is an ancestor.
Disambiguate by clone NAME (bryan / peter) as `hand-off.md` does. Treat any map line as a
*verify-against-source hypothesis* if `git log --oneline <stamp>..HEAD` is non-trivial.

## Derived-figure authority
`docs/FACTS.md` is GENERATED (`bun scripts/facts.ts --write`) and CI-gated (`--check` in `gate`). It
is the authority for published counts. **Re-read at `fc6df72e`, by `cat`, not carried:**
`live compiler source` **245,517** lines / **192** files; `test files` **1,398**;
`specification lines` **37,540**; `conformance cases` **883**. ⚠ **THE PRIOR GENERATION OF THIS SECTION WAS STALE WHILE SAYING
"Re-read at this HEAD, not carried" — AND ITS OWN FINGERPRINT SECTION CONTRADICTED IT IN THE SAME
FILE.** It published `244,112` / `1,386` at watermark `b9e97f1b`, where FACTS.md actually read
`244,175` / `1,387` — the figures the Fingerprint block twelve lines above got right. **An
internal contradiction inside one map is the cheapest possible tell that a "re-read" was a carry;
diff the two before you trust either.** stdlib modules (21), CLI verbs (11), LSP capabilities (7), editor
integrations (2), deploy targets (4), gated public samples (12) — all unchanged.

⚠ **"CLI verbs = 11" IS TRUE ONLY BECAUSE OF A HAND-MAINTAINED EXCLUSION LIST, AND NOTHING CHECKS IT.**
`compiler/src/commands/` now holds **14** files. `scripts/facts.ts:83` carries
`NOT_A_VERB = new Set(["module-format-notice", "diagnostic-format", "select-request-onion"])` — the
three shared-rule modules that live in `commands/` but are imported BY the verbs rather than being
verbs. **A new shared-rule module dropped into `commands/` will be counted as a verb until someone
adds it here by hand.** Same hazard class as `SCRML_NON_ELEMENT_TAGS_EXTRA` (a hand-maintained list
sitting beside a derived one, with nothing asserting they agree).

**Independently cross-checked this pass, not merely copied — and the carried number was wrong.** `test files` counts `*.test.js` under `compiler/tests` from the FILESYSTEM: **1,398** here (`find compiler/tests -name '*.test.js' | wc -l`), where this line carried **1,378**. It still EXCLUDES the **15** `*.test.ts` files in the same tree and all of `conformance/`. ⚠ **And a per-CATEGORY-DIRECTORY sum lands on 1,384, not 1,398, because 14 files sit at `compiler/tests/*.test.js` root level with no category dir.** Name the population AND the walk before quoting a test count — three different, all-defensible walks over this tree give 1,384 / 1,398 / 1,413.

⚠ **A PUBLISHED FIGURE THAT DID NOT REPRODUCE (found S341; the rule generalises and carries).** The
old census `1887 source files` cannot be reproduced at any commit: `filesScanned`
(`scripts/s34-census.ts:184`) increments over a FILESYSTEM walk of ten roots, so it counts whatever
gitignored build output happens to be lying in the checkout. **THIS PASS IS THE STRONGEST EVIDENCE
YET AND IT IS WORTH THE THREE NUMBERS:** the census printed **1,858** at S341, **1,864** at S346 (a
fresh worktree, where tracked and on-disk agree exactly), and **1,940** here — in the MAIN checkout,
which carries build output a worktree does not. **Three stamps, three numbers, and only one of the
three gaps is a real repo change.** Rule: a count that walks the filesystem is not a repo fact.
Publish the index-derived number, or say which environment produced it — and never use it as a delta
signal.

> **CORRECTION CARRIED, because the rule it produced is still the rule.** A prior generation of this
> section carried a three-window-old snapshot while the Fingerprint block twelve lines above it said
> something different. **A map that contradicts itself is worse than a map with one stale number**,
> because a reader who checks two places and gets two answers has no way to know which walk each came
> from. Both blocks are derived from the same `docs/FACTS.md` read at the same commit. **If you edit
> one, edit both.**

Second generated authority: `compiler/SPEC-INDEX.md`'s totals block, gated by `--check` in CI `gate`
AND the local pre-push hook. **IT DID MOVE THIS WINDOW — 110 lines, 55 changed rows — AND ALMOST NONE OF IT IS THE GENERATED HALF.** The generated totals block moved by exactly one number (`Total lines: 37,539 -> 37,540`, the one new §34 row). **The other 54 rows are #709's boot-trim ROTATION of amendment narrative out of the AUTHORED per-section prose** — "**S111 amendment (2026-05-20):**", "**D4 +3 subsections:**", "**S114 Ext 1 M1.6:**" and the like, rewritten into current-state descriptions with the session provenance dereffed. **No §-anchor, line range or rule changed**, so every §-anchored claim in this map set still anchors. ⚑ **And that half is UNGATED: `--check` validates the totals and ranges only.** A 110-line edit to the prose a Profile-A boot must full-read passed CI without a single assertion touching it. **Only the totals and ranges
are gated — the AUTHORED per-section prose is ungated and rots** (non-compliance.report.md, carried).

**FACTS.md deliberately does NOT publish a §34 diagnostic-code total.** error.map.md carries the
reconciled count — **813 at this HEAD, +1** (`E-STATE-BLOCK-STATEMENT-FORM`, the state-block locus).
⚑ **CORRECTED S376: this paragraph published `810` while this same file's header published `812`, and
the paragraph's own text reads "Re-read at this HEAD, not carried."** Re-executed here:
`bun scripts/s34-census.ts` returns `813 rows (§34 19352..20236, derived) · 883 conformance cases`. ⚠ **Count a new
code by ABSENCE AT THE BASE, not by a diff grep** (invariant 63): the diff grep also returns
`E-PROGRAM-002` and `E-IMPORT-005`, and both were already in SPEC — do not carry an 806/807/809 from
any doc, brief or memory.

**Carried (retired at S331 and still retired): `bun scripts/s34-census.ts` is not broken anywhere.**
#473 replaced `new URL(import.meta.url).pathname` with `fileURLToPath`. **Run the oracle first, on
any platform**; it answers a §34 lookup in one command. The manual `awk -F'|'` methodology in
error.map.md remains as an INDEPENDENT cross-check, **not as a fallback.**

⚠ **A bare prefix grep is NOT the catalog figure.** At this HEAD `grep -cE '^\| E-' compiler/SPEC.md`
returns **916**, `W-` **179**, `I-` **10**, `H-` **2**. ⚑ **CORRECTED S376: the carried figures were
`912` / `178`, and they were wrong AT THEIR OWN WATERMARK — checking out `8b2e4053`'s SPEC and
re-grepping returns `915` / `179`.** SPEC moved by exactly ONE line this window, so a +4 swing in a
figure that can move by at most +1 is definitionally a stale baseline, not a delta.
The corrected series is `915 -> 916` (+1) against the catalog's `812 -> 813` (+1) — they happen to
agree this window and generally do not — these sweep the §34.x sub-tables AND tables
outside the §34..§35 bounds, and they count ROWS not unique codes. **Bound the extraction at the
headings.** The two series do not track each other and neither can be derived from the other — a
reader who checks only the `^| E-` grep will read a different delta than the catalog moved.

---

## INVARIANTS AND PROHIBITIONS — the rows grep cannot find

> This is the section that earns the map set. Everything here is a rule about what you must NOT do,
> or a property that must keep holding — none of it is discoverable by grepping for a symbol. The long
> form of each lives in the map named in the right column. **Rows 1-63 carried from prior passes**
> (re-verified where this window's diff touched them; row 48's outbound check was RUN this pass and
> passed). **Row 58 stays RETIRED.** **Rows 64-66 carried from S368:** the §40.8 default-logic lift
> surface, `RUNTIME_CHUNK_ORDER` as the client contract, and the `asIs`/`unknown` split.
> **Rows 67-68 are NEW this pass (S371):** which runtime a verification actually executed, and the
> `W-DEAD` reachability walk's non-diagnostic consumers.

| # | Invariant / prohibition | Long form |
|---|---|---|
| 1 | **The module-init / rehydrator boundary is EMISSION ORDER, not a structure.** `emit-event-wiring.ts` pushes `(function(){` + `function _scrml_boot(){` and closes it after every module-init emission. Everything `emit-client.ts` emitted BEFORE its `emitEventWiring(...)` call runs at script-eval; everything that call returns runs inside `_scrml_boot`. **Nothing marks the seam.** | structure.map.md · dependencies.map.md |
| 2 | **`_scrml_nav_rewire` is non-delegable handlers + reactive display binding ONLY.** `<timer>`, `<poll>`, `<request>`, `on mount`, cell inits and engine substrate are NOT in it — they are module-init. A route chunk's timer therefore **starts exactly once, ever** — still true and still unbuilt this window (`g-route-timer-poll-not-stopped-on-soft-nav`, HIGH, open). | domain.map.md · dependencies.map.md |
| 3 | **The emit-time region↔resource association is `_outletResident`, and it is LEXICAL** — it never fires for route content because route content lives in a different FILE from the shell that owns the `<outlet>`. Unchanged this window. | structure.map.md · domain.map.md |
| 4 | **A soft navigation SHALL NOT mount or destroy any SCOPE (§6.7.2).** The `<outlet>` region is a route region keyed on `(route, params)` — a THIRD lifecycle owner (§6.7.2.1), not a scope. | domain.map.md |
| 5 | **`<machine>` does not compile.** `E-DEPRECATED-001`, Error, from `ast-builder.js:16839`. **This window's `#376` fixed the four docs that still taught it as a live deprecated alias** (`docs/PA-SCRML-PRIMER.md`, `compiler/PIPELINE.md`, `compiler/SPEC-INDEX.md`, `docs/external-js.md`) — the prior S313-N1 non-compliance finding is now RESOLVED, re-verified this pass. | domain.map.md · non-compliance.report.md |
| 6 | **A NEW or TOUCHED §34 row SHALL state where it fires, or declare itself spec-ahead (§34.0).** Diff-scoped; never retrofit over the legacy corpus. This window's one new row (`E-FN-EQUALS-BODY`) satisfies it with an emitter-provenance note. | error.map.md |
| 7 | **A failable function's error type SHALL be an ENUM (§19.4.4.1)** — unchanged this window. | domain.map.md |
| 8 | **Gate on the failure NAME SET, not the count and not the exit code.** Unchanged this window; `browser-baseline.ts` stays in `gate`. | build.map.md · test.map.md |
| 9 | **`if=` is fenced at exactly THREE structural elements** — unchanged. | domain.map.md §17.1.2 |
| 10 | **`if=` gates RENDER, never LIFECYCLE (§17.1.2.1)** — unchanged. | domain.map.md §17.1.2 |
| 11 | **A structural `if=` INSIDE an `<each>` row template fails OPEN (§17.1.2.3)** — unchanged. | domain.map.md §17.1.2 |
| 12 | **`E-IF-IN-DISPATCHED-ARM` guards ARM BODIES and must be reverted as a UNIT** (three call sites) — unchanged. | domain.map.md · error.map.md |
| 13 | **ONE `if=` lowering, no second one** — unchanged. | domain.map.md §17.1.2 |
| 14 | **`route-inference.ts`'s `inverseCallerMap` must stay BYTE-IDENTICAL** — **RE-VERIFIED, not assumed: `route-inference.ts` gained +275 lines this window and `inverseCallerMap` was NOT among them.** It moved LINE (`:4466` -> **`:4727`**) because Step 3b was inserted above it; the map's old `:line` was stale, the invariant was not. `indirectInverseCallerMap` is at **`:4968`**. | dependencies.map.md |
| 15 | **The shared call-graph walk must NOT be widened** — unchanged. | dependencies.map.md |
| 16 | **An INDIRECT callee edge is ESCALATION-ONLY** — unchanged. | dependencies.map.md |
| 17 | **`route-inference.ts` holds TWO server-only stdlib module sets and they must not be merged** — **RE-VERIFIED at the file's largest delta in six windows: BOTH SETS ARE BYTE-UNCHANGED.** `SERVER_ONLY_SCRML_MODULES` (`:579`, ASYNC classification, over-inclusion safe) and `ESCALATION_SERVER_ONLY_MODULES` (`:656`, PLACEMENT, neither direction safe). #486 added a second CONSUMER of the escalation set, not a member. | dependencies.map.md · domain.map.md · auth.map.md |
| 18 | **`node.id` is a codegen contract** — unchanged. | domain.map.md |
| 19 | **The dist tree is NOT a mirror of the source tree (§47.9.5)** — the coordinate-space discipline WIDENED this window: `emit-server.ts`'s `distRelativeServerSpecifier` is now a thin wrapper over a generalized `distRelativeLocalSpecifier(sourceSpecifier, importerFilePath, outputBaseDir, targetExt)` (#390), so a §64 tool/library `.scrml` import re-bases to dist space the same way a server import does. **Pick ONE coordinate space and stay in it — still the rule, now enforced at one more call site.** | domain.map.md · dependencies.map.md |
| 20 | **A protected DB column can never reach the client bundle (E-CG-001, §14.8.9)** — unchanged; the SEPARATE `#263`/`#358` export-const client-gate (below) is additive, not a relaxation of this floor. | dependencies.map.md |
| 21 | **`null` and `undefined` do not exist in scrml source in ANY position (§42)** — unchanged. | domain.map.md |
| 22 | **A superuser/table-owner BYPASSES Postgres `FORCE ROW LEVEL SECURITY`** — unchanged. | domain.map.md · migrations.map.md |
| 23 | **A diagnostic code can carry two unrelated meanings from two files. Allocate fresh, never renumber.** — unchanged. | error.map.md |
| 24 | **A §34 row is not evidence of a fire site — and now neither is an emitter string.** — unchanged. | error.map.md |
| 25 | **Emitted ≠ runs.** — unchanged; still four recorded occurrences. | test.map.md |
| 26 | **An auto-GENERATED test artifact must SKIP, never PASS, on an empty run.** — unchanged. | test.map.md |
| 27 | **A silent-drop guard must itself be testable.** — unchanged. | build.map.md |
| **28** | **A `fn`/`function` body admits exactly ONE shape, `{ … }`, and the `=`-expression shorthand SHALL be rejected (§48.2, `E-FN-EQUALS-BODY`, NEW #396).** The form silently miscompiled before the reject: the `-> T`/`: T` return-type consumer swallowed a trailing `= match k {…}`/`= if …` tail, and a match's `{` was misread as the fn body brace — arm results dropped, the fn returned `undefined`, **zero diagnostics**. `rejectFnEqualsBody` (`ast-builder.js:3755`) fires at **FOUR** duplicated decl-body call sites (`:9310`/`:9592`/`:12645`/`:12946`) — sibling of `E-FN-ARROW-BODY`'s five-site topology. **A FIFTH site — the `export` re-parse (`:11625-11654`) — used to SWALLOW this exact error**; it is now the one site that explicitly surfaces `E-FN-EQUALS-BODY` from its sub-parse while suppressing every other sub-error, so an exported form gets the same diagnostic a top-level one does instead of silently re-succeeding. | domain.map.md · error.map.md |
| **29** | **`<page>` has a FIFTH per-route attribute, `keep-alive` (§4.15/§40.8/§20.8.4, ruled S314, #378) — but only its AUTHORING surface is admitted.** `E-PAGE-INVALID-ATTR`'s five-member set now includes it (`attribute-registry.js`, `ast-builder.js` `validatePageAttrs`); **there is still NO runtime cache and NO §52/§38 invalidation wiring** — a doc or map claiming `keep-alive` caches anything at runtime is describing spec-ahead surface, not shipped behavior. `W-ROUTE-REQUEST-DUPLICATES-SERVER-LOAD`'s guidance was corrected in the SAME window it was named: `(b) <page keep-alive>` is a **follow-on to (a)** reading the server-load payload, **never an alternative** — its cache is SQL-server-load-scoped (keyed by the params that reach SQL) and does NOT cover an HTTP `<request>`, so applying (b) alone leaves the duplication the warning names unresolved. | domain.map.md |
| **30** | **`<timer>` and `<poll>` diverge on their FIRST tick, and the divergence is now stated normatively (§6.7.5/§6.7.6, S314).** A `<timer>`'s first execution is one interval AFTER arming (unchanged behavior, now an explicit sentence — the silence was previously filled by a `collect.ts:205` defect that accidentally ran the body at module-init). A `<poll>` SHALL fire its first tick IMMEDIATELY on arming (a poll promises freshness; the §6.7.6 worked example would otherwise render nothing for a full interval) — gated by `running=`, and a `running` false→true RESUME does NOT re-fire the immediate tick (once per arming, not once per resume). | domain.map.md |
| **31** | **A value-form `match`-arm result re-emits from a raw EXPRESSION STRING, and the #87 statement-level await injector never reached it (`g-match-arm-server-call-no-autoawait`, #394).** `scheduling.ts`'s new `parenthesizeAwaitServerCallsInExpr` wraps the CALL node in grouping parens (`fn().ok` → `(await fn()).ok`), not a bare prefix — a bare `await fn().ok` reads `.ok` off the pending Promise first, which is wrong. Await-legality is real-parser-modelled (sync callback bodies / formal-parameter lists are illegal), mirroring `injectServerCallAwaitsViaAst`'s existing discipline. | dependencies.map.md |
| **32** | **A cross-module ASYNC import consumed inside a markup interpolation now gets awaited (`g-crossmodule-async-in-markup-position-not-awaited`, #391) — but the wrap decision is made off the injector's OWN EMITTED OUTPUT, not a re-derived predicate**, after an S239 catch surfaced a page-breaking SyntaxError from an async fn used as a bare combinator callback. | domain.map.md · dependencies.map.md |
| **33** | **A sub-parse's diagnostic span (a `<match>`-arm or `<each>` sub-parse) is now REBASED to file coordinates (`_rebaseSubparseSpans`, ast-builder.js, #389)** — before this, a diagnostic inside a sub-parsed region reported the sub-parse's OWN line/col, not the file's, and the within-node parser-parity gate was silently dead on Windows (enumerator backslash relpaths) as a downstream symptom. | error.map.md · structure.map.md |
| **34** | **`export let` / `export var` are now emitted as runtime VALUE bindings alongside `export const` (`emit-server.ts` `emitModuleValueExportLines`, #388)** — before this fix, a mutable module-level `export let` closed over by an in-process PEER CALLABLE (not a direct `<endpoint>` reference) silently fell through to nothing, producing a boot/route `ReferenceError` with no diagnostic. The companion fix in the same PR widens a `serve=` tool's dead-import scan to union a `main`/setup-only reachability root, so an import used ONLY from `main` survives tree-shaking. | dependencies.map.md |
| **35** | **The §13.2 CPS auto-await choke-point is CONSOLIDATED (PR #405, `649d6fce`, LANDED — corrects the prior pass's "HELD" status).** `injectPromiseAwait` (the old per-statement string-regex pass) is RETIRED. ONE shared AST walker now backs all three auto-await entry points (on-mount, every client fn-body statement, match-arm expressions), and — the part that actually changes program behavior — auto-await now DESCENDS into `given`/match-block/`try` bodies that were previously opaque `isControlFlowBoundary` nodes to the scheduler. Closes the `g-cps-scheduler-opaque-boundary-hides-nested-server-calls` family. | dependencies.map.md · structure.map.md |
| **36** | **A per-row `if=` on a NESTED (non-item-root) element inside `<each>` is a CREATE-TIME append gate, not reactive on a same-key reconcile — the compiler now WARNS (`W-IF-IN-EACH`, §17.1, #416, GH adopter #409) when the condition references the iteration item.** Only the row's SOLE item-root `if=` is reactively swapped today; the reactive fix for the nested case is DEFERRED and routed to bryan. | domain.map.md · error.map.md |
| **37** | **A top-level `@name = expr` REASSIGNMENT of a structurally-declared (`<name>`) cell SHALL NOT re-register the cell's reset init-thunk (§6.8, #417, HIGH) — the runtime's thunk registry is last-write-wins, so a naive re-registration inverted `reset()`.** `collectStructuralDeclNames` (reactive-deps.ts, NEW) distinguishes a genuine reassignment (skip) from an IMPLICIT `@`-declaration (keep — an SSE/channel bind must still re-establish on reset). Residual, out of scope: `g-implicit-cell-double-write-clobbers-reset-init` (MED, NEW — a double-write of an IMPLICITLY-declared cell still clobbers; needs emission-order tracking). | domain.map.md |
| **38** | **§13.2 is POSITION-INVARIANT, and as of #429 the CLIENT enforces it AT THE CALL SITE — `emitCall` gained a fourth await branch for a client->server RPC.** Awaiting at the choke point every position already flows through replaces retrofitting it per position, which is what manufactured one silent gap per position (receiver-tail `loadRows().length`, nested-argument `pick(loadRows())`). **The gate that makes it safe can only ever SUPPRESS an await, never STRAND one** — `ctx.clientAsyncBody` is threaded from the SAME `_fnIsAsync` that writes the `async` keyword, so the structural walk that sees the callee is the one that colours the host. A stranded `await` in a sync host is a WHOLE-BUNDLE SyntaxError, not a local defect; that asymmetry is the whole design. **#429 landed EXPLICITLY NOT CLAIMING ITS BUG CLASS — 142 bare client server-fn call sites remain in cleanly-compiling sources, delta 0.** | dependencies.map.md · domain.map.md |
| **39** | **THERE IS NOW EXACTLY ONE PROVIDER ANSWERING "is this name async in client mode" (#442). Decision sites 3 -> 1. Do not hand-write a fourth disjunct.** `async-combinators.ts`'s `isAsyncCalleeName(name, AsyncNameFacts)` — the rule is MODE-FREE (not-shadowed, then stdlib-Promise-export OR server-boundary-fn OR transitively-async-local-peer; **there is no fourth**), and mode selection lives in exactly one caller, `emit-expr.ts:asyncNameFactsOf`. The drain's bespoke `isAsyncName` closure is DELETED; `combinatorIsAsyncName` collapsed from four disjuncts to one delegation. **`isClientServerFnCall` shares only the provider's server-fn MEMBERSHIP component and that is deliberate** — it asks an IDENTITY question, not an asyncness one, and widening it would capture a stdlib-async callee and route it away from its own branch and its own fail-closed sink. **THE BUG IT SUBTRACTED:** `computeAsyncFnNames` uses `serverFnNames` as a seed TRIGGER (colours the CALLER, never admits the CALLEE), so a client server fn was async to the emitter and sync to the fail-closed drain **in the same compilation** — a MISSING diagnostic, not a wrong emission. Adding a consumer? Hand it `AsyncNameFacts`. | dependencies.map.md · domain.map.md |
| **40** | **A `routeMap` walk SHALL be filtered on the OWNING FILE, because `FunctionRoute` carries no `filePath` and the file lives ONLY in the map KEY (`<filePath>::<start>`).** `runRI` builds ONE routeMap across the whole resolved import graph. An unfiltered walk imported another file's server-fn names into THIS file's client emission: a purely local SYNC `save` gained a spurious `await` and a FALSE `E-ASYNC-STDLIB-IN-SYNC-CALLBACK`, with renaming the local fn as the only user workaround. **`declaredNames` cannot cover this** — a TOP-LEVEL client fn name is never in it. Applied identically in `emit-functions.ts` and `scheduling.ts`, **and the two must agree**, because U1's gate derives from the coloring the filter also feeds. Corpus incidence is zero today and that is NOT reassurance: `examples/23-trucking-dispatch` declares `getCurrentUser` 19x, `refresh` 18x, `getSessionToken` 17x across files. | dependencies.map.md |
| **41** | **`node --check` ACCEPTS a top-level stranded `await` in a bare `.js`; the compiler emits `<script src=…>` with NO `type="module"`, where the same bytes are a FATAL SyntaxError. AND bun's `vm.Script` does not reject it either.** So the emitted-artifact syntax gate is `scripts/corpus-check-goggles.js`, deliberately a separate **NODE** subprocess parsing under BOTH goggles via `vm.Script`/`vm.SourceTextModule` — **`node --check` must not be reintroduced**, and an in-process "simplification" under Bun would be a third hollow gate. Its parent `scripts/corpus-emit-differential.ts` is **the standing pre-land gate for any codegen change** (1878 sources / 7254 artifacts, NOT in CI, run by hand base-vs-head). `diff` exit **2** = NOT A VALID COMPARISON, distinct from 1 = differences found. The class it kills — a truncated enumeration reading exactly like a complete one — had shipped **three times**. | build.map.md · test.map.md · error.map.md |
| **42** | **A runtime reference SHALL NOT be emitted with its BINDING or DEFINITION gated more narrowly than the reference — the DANGLING-REFERENCE class (#357/#435/#440).** Compiles clean, zero diagnostics, `ReferenceError` -> HTTP 500 at request time. Four instances closed this window (`session` in a `?{}` interpolation · `@currentUser` direct read in a plain handler · the same in an SSE `function*` · `<channel auth=>`-only `_scrml_auth_check`). **Every detector in this family is PERMISSIVE BY DESIGN: a false POSITIVE only emits unused session infra; a false NEGATIVE re-opens a 500.** And **the `session` prologue bind SHALL be the `_scrml_session_bind` Proxy, never a raw `const session = _scrml_req._scrml_sess`** — the raw object carries own-properties `sid`/`_rec`, so `session["_rec"]` would disclose the whole record **including the §40.2 `csrfToken`** at HTTP 200. | auth.map.md · domain.map.md · dependencies.map.md |
| **43** | **WHEN A WHOLE-BUFFER TEXT PASS ACTS IN THE WRONG PLACE, CHANGE ITS INPUT — NOT ITS PATTERN (#458).** A pattern patch must be right about every context it will ever meet; a region fence only has to be right about a boundary. `emit-client.ts`'s `post-fn-name-mangle` had taken FIVE pattern patches (Bug D · Bug I · Bug Z · g-spread · PGO P3.A); #458 is the first structural answer and it closed three defects at once. **`joinAroundRuntimeSlot` splices the assembled runtime into its slot AFTER the rewrite, so the runtime is not part of the rewrite INPUT at all** — no lookaround has to recognise it and no pattern can reach into it. Byte-safety is PROVABLE, not asserted: with `rewrite` as the identity the helper is exactly `lines.join("\n")` with the runtime in its slot. **There are now TWO region classes in `code-segments.ts` and they COMPOSE, not merge:** the LEXICAL fence (`rewriteCodeSegments` — code vs string/regex/comment, descending into a template literal's `${…}` because those are CODE) and the STRUCTURAL one (`findObjectShorthandRegions` / `classifyBraceGroup` / `ObjectShorthandRegion`). **THE LIMITS ARE THE DESIGN.** Only `object-literal` is acted on; `binding-pattern` is RECOGNISED and deliberately NOT acted on, because an S239 review showed fencing a pattern while still rewriting the uses it SHADOWS turns a LOUD `TypeError` into a SILENT wrong answer — the exact class the fix removes. `unknown` obliges the caller to change NOTHING. `__proto__` skips the WHOLE region (ECMA-262 B.3.1; a bare shorthand left beside expanded siblings is ENGINE-DEPENDENT — node binds the global prototype, bun throws). **And a fence alone would have been a half-fix:** an object literal is EXPANDED (`{get}` -> `{get: _scrml_get_2}`), not fenced, because fencing would only trade a silent wrong answer for a `ReferenceError`. **Net sites this pass stops rewriting: ZERO.** | domain.map.md · dependencies.map.md · structure.map.md |
| **44** | **A COMPILER-EMITTED SERVER-FN ROUTE HANDLER RETURNS A `Response` ON EVERY PATH, AND THE ORDER AT THAT EXIT IS NORMATIVE IN BOTH DIRECTIONS (#452).** Passthrough **BEFORE** redact: `if (_scrml_result instanceof Response) return _scrml_result;` — without it the envelope does `new Response(JSON.stringify(<a Response>))`, and `JSON.stringify` of a `Response` is `"{}"`, so **an adopter's deliberate 403 comes back 200. MEASURED. Fail-OPEN.** Redact **BEFORE** serialize: §14.8.9/§14.8.10 are FLOORS and the value reaching `JSON.stringify` must already be redacted. **WHY IT HID FOR SO LONG IS THE TRANSFERABLE PART: the exit was SPLIT THREE WAYS and only one third was tested** — the Ext-5 arm returned a `Response`, the protect/tenant arm returned a redacted raw value, and every other shape returned nothing at all, so the adopter's `return` became the handler's. **The ONE arm a test exercised asserted a `Response`; the two that were not asserted a value.** Bun's behaviour is why nobody noticed: the WIRE got a CONSTANT welcome page for EVERY non-`Response` return, while stderr logged **only** for `undefined`/`null` — a bare `"ok"`/`42`/`{…}` was silent in both channels. **Load-bearing side effect: `_scrml_session_cookie_wrap` skips when the return value has no `.headers`, so a bare return also dropped the §20.5 session cookie silently.** ⚠ **The SHALL is in a source comment and a commit subject, NOT in §12.5** — non-compliance.report.md S326-N1. | domain.map.md §12.5 · dependencies.map.md · auth.map.md |
| **45** | **A GAP ENTRY'S SCOPE CAN SHRINK WITHOUT ANYONE EDITING IT — VERIFY AN OPEN ENTRY'S LOCUS AGAINST HEAD BEFORE SCOPING FROM IT.** #452 closed the prototype-chain half of `g-session-get-reserved-key-read-disclosure` as a SIDE-LANDING (`session.get` is now `Object.hasOwn`-guarded, `emit-server.ts:2593`) — executing that entry's own remediation candidate (iii), which its text explicitly called *"separable and should not wait on the ruling"*. **The ledger still states the pre-fix locus (`:2568`) and still asserts "no `hasOwnProperty`", both false at this HEAD.** The generalisation, and it is the one this window earned: **every gate in this repo is built to catch a fix claiming MORE than it did. This one claimed LESS.** An entry can be simultaneously current on one axis (this one carries a fresh, correct REACHABILITY-CHANGED ruling) and stale on another — and a fresh banner reads as "maintained", which is what makes it hard to spot. Sibling rule, same doc: **THIRTEEN known-gaps headings disagree with their `@gap` marker** — twelve say `open` while the marker says `resolved`, and **one runs the OTHER way** (`G-DBAUTH-DOCS-NO-DO-NOT-MARK-USERS-EXAMPLE`: heading `resolved`, marker `open`), which is why a one-way detector would have been a half-fix. `state.ts` parses the MARKER, so the CI gate passes while the line a human greps is wrong. **The count is no longer a hand estimate — #485 shipped `headingMarkerDrift()`, so `bun scripts/state.ts --check` prints it.** **Sibling class, measured this window: THREE entries are `status=open` while their fixes LANDED** (#473, #474, #484) — non-compliance.report.md N3. | non-compliance.report.md S326-N2/N3 · auth.map.md |
| **46** | **A KEYWORD FENCE IN A SCRML-FACING REGEX SHALL BE `(?![A-Za-z0-9_$])`, NOT `\b` — AND IT GOES OUTSIDE THE ALTERNATION (#463, §18.5).** Two independent defects in one line. **(a) Placement:** `/^(const|let\|…\|fail\|on\b)/` fences only the LAST alternative, so every other keyword matched as a bare PREFIX — `formatted` matched `for`, `doc` matched `do`, `letter`/`constant`/`returnValue`/`iface`/`lifted`/`failCount`/`varName` likewise. In `_blockTailIsValueExpr` each was misclassified as a STATEMENT head, the arm's tail was never lifted, and **the arm silently produced `null` with no diagnostic.** It shipped because the corpus tests used `base`/`b`-shaped tail names. **(b) Charset:** JS `\b` is defined against `\w` = `[A-Za-z0-9_]`, which **EXCLUDES `$`** — but scrml identifiers admit `$` (`tokenizer.ts:1343`, `isIdentPart = /[A-Za-z0-9_$]/`). So even the correct-placement `\b` fix still reads `do$thing` / `const$x` / `on$c` as keyword-then-boundary and **silently reproduces the class, including for `on`, the one keyword that always carried a fence.** Matching the fence to the language's OWN identifier charset closes it at the root instead of at twelve positions. Measured: zero corpus identifiers of the form `<keyword>$…` today, but `$`-bearing identifiers (`item$`, `acc_$`, `_$1`) ARE in live use. **Grep `\b` in any classifier that reads scrml source text.** \| domain.map.md §18.5 · structure.map.md |
| **47** | **AN EMIT-TIME DECORATION THAT A LATER RE-MOUNT CANNOT UNDO IS NOT PERMISSIBLE — EVEN WHERE SPEC IS SILENT ON TIMING (#464, the #450 REVERT, operator-ruled).** §17.2 mandates the `show=`-hidden END-STATE and is SILENT on the pre- vs post-hydration axis, so an SSR-time `display:none` was *permissible but unmandated*. It still had to go: **a `<match>` arm body is lowered by the SAME `generateHtml` as file-level markup**, so an emit-time hide becomes part of the string literal `dispatch` assigns to `_mount.innerHTML`; the re-mounted element carries no controller (`wire_<Arm>` does not re-bind a visibility toggle, `_scrml_nav_rewire` is never re-run on a variant swap), so **the baked hide could NEVER be cleared. §17.2 says "toggle"; a toggle needs a toggler.** The governing direction is now **fail-OPEN: a missed hide is a brief flash, a wrong hide is permanently invisible content.** `emit-html.ts` is byte-identical to `71623be3`; `buildInitialBoolMap` does not exist. ⚠ **`ctrl-017`'s rationale flags a DEPENDENCY:** the `_scrml_nav_rewire` variant-render rewire hole is pre-existing and NOT fixed — when it IS closed, the re-mounted element will legitimately carry a controller-set `display:none` again and **`ctrl-017` must be revisited; a failure there after a rewire fix is expected, not a regression.** | domain.map.md §17.2 · test.map.md · dependencies.map.md |
| **48** | **AMENDED (S341 pass) — AND THE AMENDMENT IS THE FINDING. THERE ARE TWO STAMP CHECKS, NOT ONE, AND THE ROW ONLY EVER SPECIFIED THE INBOUND HALF.** *(inbound)* Before trusting a stamp you are READING: `git merge-base --is-ancestor <stamp> origin/main`. *(outbound)* Before WRITING line 3: the watermark SHALL be an ancestor of `origin/main` — see the MAP-STAMP RULE block at the top of this file for the three-command procedure. **This row has existed since S326 and the defect recurred THREE TIMES anyway** (`a3a34d80` S322 · `97576f35` S326 · `616688ea` S331), because a conscientious author CAN run the inbound check, watch it pass, and then stamp a branch tip — which is exactly what the S331 pass did: its header records `git merge-base --is-ancestor 35d4d32e HEAD` passing, and its very next sentence names the correct outbound SHA (`8863d457`) before stamping `616688ea` regardless. **A rule that names the command but not the moment does not bind.** ⚠ **The instrument's behaviour is now understood and it is NOT the broken part:** `scripts/state.ts:mapsStaleness()` reads the watermark off line 3 and REFUSES to print a behind-count when it is not an ancestor (`watermark X is NOT an ancestor of HEAD Y (diverged/rebased) — behind-count unavailable`). That refusal is correct — it will not manufacture a number — but it is also SILENT-BY-DESIGN downstream: the pre-dispatch maps-currency check `pa-scrml-overlay.md {{maps_fills}}` mandates before EVERY dev dispatch returns nothing, and nothing escalates. **A mandatory step was unanswerable for three of the last six map generations.** Measured on arrival this pass: 3 of the 5 distinct live stamps were orphaned branch tips. | this file (MAP-STAMP RULE block) · every map header · non-compliance.report.md |
| **49** | **§18.5 HAS FOUR EMISSION ROUTES AND ONE SHARED LEAF PREDICATE. `planBlockArmLift` IS NOT "THE SINGLE CLASSIFIER EVERY PATH ROUTES THROUGH" — THE PRIOR GENERATION OF THIS MAP SET SAID SO AND IT COST A DISPATCH (S331).** `planBlockArmLift` (`emit-logic.ts:4715`) is the shared SEGMENTER + plan for the two RAW-STRING routes and has **exactly two call sites** (`emit-logic.ts:4738`, `emit-control-flow.ts:2109`). The two STRUCTURED-AST routes never touch it — they call **`_blockTailIsValueExpr` (`emit-logic.ts:4653`) directly** (`emit-logic.ts:4882`, `emit-control-flow.ts:2354`), because an AST arm body already IS a statement list and needs no segmentation. **A grep for `planBlockArmLift` finds two of four. Grep `_blockTailIsValueExpr`.** The four: **A** local-decl structured · **B** local-decl raw-string · **C** IIFE structured · **D** IIFE raw-string (incl. §18.19 multi-scrutinee). **Scope from the LAYER that moved:** a segmentation defect can only reach B and D; a predicate defect reaches all four. **The generalisable rule this row exists to enforce: "single classifier" and "single segmenter" are different claims, and a map that conflates them sends agents to the wrong file.** | domain.map.md §18.5 · dependencies.map.md · structure.map.md |
| **50** | **§12.2 ESCALATION IS DEFINED PER-FUNCTION AND COVERS NO OTHER POSITION — NORMATIVE AT `SPEC.md:7312`, NOT AN INFERENCE.** §12.4 says *"Route inference SHALL be per-function"* and `collectFileFunctions` honours it LITERALLY: it yields `function-decl` nodes only. **A server-only reach in any non-function position is not reached by Trigger 3 at all** — not "escalates differently", *not reached*. Positions and status at `4f034e13` (**the derived row's REACH changed at #500 — see invariant 52**): `function` body → escalates silently, **no diagnostic** (the success path; "my function vanished from the client bundle with zero errors" is expected, not a bug report); `const <name>` **derived-cell RHS, AT ANY DEPTH** → **REFUSED, `E-DERIVED-SERVER-ONLY-REACH` (§6.6.19, #486 + #500)** — a synchronous lazy-pull recompute (§6.6.3) cannot become a round trip, so escalation is not an available answer and refusing is the reversible direction; **mutable-cell initialiser → OPEN, leaks, no diagnostic**; **markup interpolation → OPEN, leaks, no diagnostic**. The derived case reproduced Trigger 3's own founding symptom exactly — measured S331: exit 0, no `.server.js`, `Bun.password.hash` argon2id in the browser bundle. **The §6.6.19 error text warns that deleting the `const` — the shortest edit that silences it — restores the leak.** SPEC's own instruction: a reader adding a new non-function position should follow §6.6.19's REFUSE pattern, not widen Trigger 3. **Never write or read "Trigger 3 covers <position>".** | domain.map.md §12.2/§6.6.19 · auth.map.md · error.map.md |
| **51** | **A PROBE IS CODE, AND `scripts/` IS INSIDE THE REVIEW FLOOR'S CODE-BEARING POPULATION ON PURPOSE (#481).** The S341-window evidence — three probe-integrity fixes in one pass: `state.ts`'s `@gap` bag matched `[^>]*?` and **silently truncated any marker whose `prov=`/`locus=` held a literal `>`**, dropping it from the count (witnessed on a real marker on main); duplicate `@gap` ids **double-counted**; `s34-census.ts` was `ENOENT` on Windows for four windows. **Two rules fall out and both generalise.** (a) **A health metric must measure the population it claims to.** `review-debt.ts` now prints TWO carve-out rates and only the CODE-BEARING one carries an alarm — a wrap/continuity PR has no code path to review BY CONSTRUCTION, so its carve-out is correct classification, not evasion (measured: all-PR 57% while code-bearing was 4%). The population is a directory **WHITELIST** so a new docs dir defaults to not-code and a new source dir must be added consciously. (b) **A threshold must be COUNT-based where the target is ~0%** — a percentage floor scales tolerance with population, and a bite test at S331 put five injected carve-outs at 17% **without firing**. **Prove the bite.** Same session: a naive truncation guard (`length >= limit`) was DELETED rather than tuned because it went red for reasons no change caused — the cry-wolf shape that gets bypassed and then removed — and replaced by "has the scan provably seen a PR below the epoch", plus AUTO-WIDENING rather than a louder warning. | build.map.md · structure.map.md |
| **52** | **A COLLECTOR THAT ENUMERATES AST FIELDS IS A FAIL-OPEN SURFACE, AND "ADD THE MISSING FIELD" IS NOT THE FIX — IT IS THE DEFECT CLASS (#500, §6.6.19).** `collectDerivedCellDecls` (`route-inference.ts:3730`) descended exactly `node.body` and `node.children` while its own doc comment claimed it found derived cells *"at any depth"*. **The claim was false and the gap was a live confidentiality leak:** a `for`-loop stores its `lift` body under an `expr` wrapper, so a derived cell there sits at `…expr.node.children[0].body[0]`, was never visited, was never checked, and the compiler emitted ZERO `.server.js` while shipping `Bun.password.hash(pw, { algorithm: "argon2id" })` and the caller's secret into the browser bundle — **reproduced at S337 by compilation + artifact inspection, at exit 0 with no diagnostic.** SIX positions were measured leaking: `for`-lift body, `while`-lift body, `<each>` row body, `<engine>` state-child body, loop-inside-conditional, and the same shapes inside a `kind="tool"` program. The fix INVERTS the default: every array- and object-valued property is descended, and exclusions are a two-clause DENY-list (`skipDerivedWalkKey`, `:3677` — `span`, plus the codebase's `_`-prefixed side-table convention). **Measured: the deny-list is not load-bearing for the RESULT** — shipped list 68 cells, without `parent`/`loc`/`spans` 68, with no deny-list at all 68 — only for the work done, which is why it is the smallest justified list rather than the safest-looking one. **The safe error direction for a confidentiality check is descending one field TOO MANY** (a loud, reversible spurious refusal) **never one too few** (a silent leak). Termination: an identity `seen` set over every visited object (not just array-reached nodes) + a 512 depth cap, because the walk now descends ESTree expression trees which nest one level per term. **This is the compiler's SECOND instance of the field-listed-walk class** (see `emit-client.ts`'s hand-rolled seed walker drifting against `dependency-graph.ts`'s full sweep). ⚠ `Object.keys` yields INSERTION order, not source order, and Step 3b does not sort by span — do not assume diagnostics come out in source order. | domain.map.md §6.6.19 · auth.map.md · structure.map.md |
| **53** | **A MARKUP TAG WHOSE `${}` BODY IS A DEFERRED LIFECYCLE PAYLOAD SHALL NOT BE DESCENDED BY `collectTopLevelLogicStatements` — AND THE PREDICATE IS "HAS ITS OWN STEP-5 EMITTER", NOT "IS A LIFECYCLE TAG" (#510, §6.7.5/§6.7.6/§6.7.8).** `collect.ts`'s `DEFERRED_LIFECYCLE_BODY_TAGS` is exactly `{timer, poll, timeout}`. Descending one of these collects its body a SECOND time as a top-level statement, so the tick/timeout payload **runs once at module init, before the timer ever fires** — `g-timer-poll-body-runs-once-at-module-init`. **The exclusions from the set are the load-bearing part and both are deliberate:** `<request>` — its descent IS the designed canonical-form fetch, emitted once; `<channel>` — its top-level `${}` is single-run init logic the channel emitter does NOT re-emit, so the descent is its ONLY correct emission. Adding either to the set would DELETE a required emission. **Do not generalise this set by tag category.** Sibling landing in the same PR: `<poll>` now fires an immediate first tick through the same `tick()` path (queuing + error handling), gated by `running=` and **never re-fired on resume** — `_scrml_timer_start(scope, id, ms, fn, immediate)`, with `<timer>` passing no `immediate` argument at all (§6.7.5 says a timer never does). | domain.map.md §6.7 · dependencies.map.md · structure.map.md |
| **54** | **THE `<#request>`-REF ATTRIBUTE-ROUTING CLASS IS NOW CLOSED ON THE TWO PER-ITEM PATHS — AND IT TOOK THREE PRs BECAUSE EACH PATH REACHES CODEGEN AS A DIFFERENT NODE SHAPE (#484 -> #511 -> #512, §6.7.7).** A `<#id>` reference in an attribute must route to the reactive `_scrml_request_<id>` object; mis-routing it to the §36 `_scrml_input_state_registry` yields `undefined.data` — **a runtime TypeError from a bundle that compiled clean.** #484 closed the top-level attrs. #511 closed the per-item `<each>` body attr by stashing `collectRequestIds(fileAST)` on a module-level `_eachRequestIds` (the same synchronous-single-threaded ctx pattern as `_eachBindSupportCtx`, cleared in the same `finally`) and threading it into `emitExprField`. #512 closed the Tier-0 `${for…lift}` attr, which needed a DIFFERENT mechanism: such an attr value arrives as an **escape-hatch node** because `ast-builder.shouldSkipExprParse` skips any `<`-leading expr (its HTML-fragment guard), so there was no structured node to thread anything into — it needs `reparseRequestRefEscapeHatch` FIRST. **Every one of the three fixes is GATED to REGISTERED request ids** (`gateToRegisteredRequests=true`), so a non-request `<#id>` (an input-state ref, or a typo) stays byte-identical to its pre-fix emission. **That gate is why the class could be closed incrementally without a corpus-wide diff**, and it is the transferable move: make the new path opt-in on a positive membership test, not on the absence of one. ⚠ `_eachRequestIds` is deliberately UNSET on the Tier-0 lift path that reaches the template walk without `emitEachBodyRenderForFile` — that path is covered by #512's reparse instead, not by the stash. | domain.map.md §6.7.7 · dependencies.map.md |
| **55** | **A REGEX OVER SOURCE TEXT IN A POST-AST STAGE REQUIRES A ONE-LINE JUSTIFICATION OR THE STRUCTURAL ROUTE — RULED S338, PROBED BY `scripts/source-text-regex-census.ts` (#513).** Five adversarial reviews across two unrelated branches in ONE session found the same substitution every time — **a text-level shortcut standing in for a structural one, where the parsed tree was already in hand.** Five instances, five authors, no coordination: a `wired` classification table validated by COUNTING TEXT OCCURRENCES (and circularly — the delta it measured included the declaration that existed only because the table had already said "wired"); `bareAttrValueIsWired` restating codegen's `name.startsWith("on")` as `/^on[a-z]/`, which then drifted on `on=` / `on-tap=` / `on_tap=`; an `import.meta` fence regexing source text TWO LINES BEFORE the `parseExprToNode` call that would have answered it structurally; `bareBindingReferenceOf` anchored on trimmed RHS source text, so `(@v)` defeated a guard `@v` tripped; a lifecycle variant branch matching `.Published` inside a STRING LITERAL. **NONE was caught by a 22,385-test suite or a 7,375-artifact differential, and that is a property of the class, not a gap in the gates:** a regex over source text is indistinguishable from a correct check until someone writes a string literal. **Scope of the rule:** binds NEW-OR-TOUCHED code only; the tokenizer / block-splitter / pre-AST stages are NOT in scope (there is no tree yet); detection is a RATIO, not an inspection; **it is deliberately NOT a CI gate.** | build.map.md · structure.map.md · test.map.md |
| **56** | **`bunfig.toml [test].timeout` IS NOT A KEY BUN READS — THE PER-TEST BUDGET IS THE DEFAULT 5000 ms EVERYWHERE, AND A TIMED-OUT TEST WEARS AN ASSERTION FAILURE'S `(fail)` MARKER (#537, S346).** The repo carried `timeout = 10000` from the initial split and believed it; the number was NEVER in force (bun 1.3.14 verified; the `root` key in the same file IS honoured, so "the file loads" proves nothing about any one key). DO NOT re-add a timeout key to widen the default silently — a legitimately-slow test declares `{ timeout }` AT THE SITE with a comment; the per-run knob is `bun test --timeout <ms>`. And when triaging any bun test "failure", read for the `^ this test timed out after Nms.` tell FIRST — a synchronous overrun completes its assertions GREEN and then reports `(fail)`. | test.map.md · build.map.md · config.map.md |
| **57** | **`skipPastRanges` (lint-ghost-patterns.js) IS AN EXPORTED FUNCTION WITH ZERO src CALLERS — ON PURPOSE. DO NOT DELETE IT AS DEAD CODE, AND DO NOT "SIMPLIFY" THE BUILDERS BACK ONTO IT (#537).** It is the ORACLE: the contract's reference implementation, equivalence-pinned against the hot path `makeSkipCursor` (forward-only cursor, O(chars+ranges)) by `unit/lint-ghost-patterns-skip-cursor.test.js` over seeded-random range sets. The builders calling the oracle per-CHARACTER was 343 ms of a fresh 36-file compile and, JIT-poisoned by an early empty-range compile, 5.9 s of a 9.7 s compile — the real cause of the flagship browser test's "intermittent" cloud red. Each scan owns its OWN cursor (a shared module-level index would break `findMatchingClose`'s mid-source starts on nested functions). | error.map.md · test.map.md · structure.map.md |
| **58** | **RETIRED — CLOSED THIS WINDOW (#546), AND THE CLOSURE IS VERIFIED AT THE SOURCE, NOT FROM THE PR TITLE.** `compileScrml`'s `inputFiles` order WAS an emission input: route / logic-id / fetch-stub counters minted in traversal order, so the same file SET in two argv/glob orders emitted different artifacts including generated server route URLs. **Fixed at the compile entry — `api.js` now reads `let resolvedInputFiles = inputFiles.map(f => resolve(f)).sort();`** — code-unit order (plain `.sort()`, deliberately NOT `localeCompare`, which is the very non-determinism being removed). `docs/known-gaps.md:8688` carries `status=resolved`. ⚠ **The same PR corrected a claim in the OTHER direction:** `api.js`'s directory-walk sort is defense-in-depth, NOT the determinism guarantee its original comment claimed — `scanDirectory` already ended in a terminal `results.sort()`, so `scrml compile <dir>` was order-stable before. **You may still want to hold input order fixed in a base-vs-head differential, but it is no longer a correctness hazard.** | build.map.md · structure.map.md |
| **59** | **A GATE THAT PARSES A POPULATION MUST REFUSE WHEN IT CANNOT ACCOUNT FOR ALL OF IT — `bracketed !== parsed` IS THE PATTERN (#646, five gates were green while measuring nothing).** Count candidate lines and matched lines SEPARATELY and refuse on a mismatch; reporting PASS on the parseable remainder is worse than no gate. Live implementations: `scripts/delta-lint.ts` (`refuseUnparsedEntries` / `refuseDegenerateScope`) and `scripts/state.ts:706` (`refuseUnparsedDeltaEntries`, called from `runWrite`/`runCheck`/`runDigest`). **Partition the exit codes: 1 = the data is wrong, 2 = the instrument is broken, and 2 is NOT reachable as a PASS.** The concrete failure: the entry shape was three tokens, the writing convention had drifted to four (an emoji marker), and FOUR live entries were invisible to both the gate and the digest. | test.map.md · build.map.md |
| **60** | **A CI STEP NAME IS READ AS A COVERAGE CLAIM — NAME IT AFTER WHAT IT RUNS.** The step named "Within-node parser-parity + canary" ran ONLY `parser-conformance-within-node.test.js`; **the canary was never in that command** (it is gated in the blocking `gate` job by the root `*.test.js` glob). An auditor asking "is the canary covered?" read the name and stopped. Renamed this window to state exactly its command plus where the canary actually IS (`g-ci-does-not-run-root-level-test-files`). **Generalises past CI: any label that names more than its mechanism does is a latent false-coverage claim.** | build.map.md · infra.map.md |
| **61** | **`<program headers="strict">` BINDS THE COMPILER'S OWN EMISSIONS, NOT JUST THE AUTHOR'S — AND §39.2.5's ESCAPE HATCH DOES NOT COVER THEM.** `default-src 'self'` refuses an inline `<style>` and an inline executable `<script>`, so TWO compiler-emitted things were silently dead under strict headers with no author-side fix: the §38 transition keyframes (injected by a `document.createElement("style")` IIFE — now emitted into the stylesheet by `codegen/emit-transition-css.ts`; the `'transitions'` runtime chunk is RETIRED) and the SSR seed (now `<script type="application/json">` + `JSON.parse`, BOTH readers changed). §39.2.5's "override via `handle()`" covers author-loaded EXTERNAL ORIGINS. **Before emitting any new inline asset, ask what `headers="strict"` does to it.** | domain.map.md · auth.map.md |
| **62** | **A §20.8.2 SOFT NAVIGATION NEVER LOADS THE TARGET PAGE'S STYLESHEET — SO ANY PER-PAGE EMITTED ASSET HAS A REACHABILITY HOLE.** Soft nav swaps the target route's markup into the SHELL's live document; `_scrml_nav_sync_head` syncs `<title>` / description / canonical ONLY. And soft nav is the DEFAULT path — `_scrml_link_click_handler` intercepts every same-origin `<a href>`. **The worked fix pattern (transition CSS): scope per-file emission to what the file uses, EXCEPT the `<program>` shell entry, which emits the APP-WIDE UNION** — every composed per-page document already links `<entryBase>.css`, and an own-document page with no shell has no `[data-scrml-outlet]` so its navigations are hard ones served by its own CSS. **Do not "optimise" that union away.** | domain.map.md · structure.map.md |
| **63** | **COUNT A NEW DIAGNOSTIC CODE BY ABSENCE AT THE BASE, NOT BY A DIFF GREP — THE DIFF GREP GAVE TWO FALSE POSITIVES OUT OF THREE THIS WINDOW.** `git diff <base>..<head> -- compiler/SPEC.md | grep '^+' \| grep -oE 'E-[A-Z0-9]+-[0-9]+'` returns any code named on an ADDED LINE, including a new prose mention of a long-standing code. `E-PROGRAM-002` and `E-IMPORT-005` both read as new and neither was. **Use `git show <base>:compiler/SPEC.md \| grep -c '<CODE>'` and require 0**, then cross-check the census total. And the two false positives are not even the same kind: `E-PROGRAM-002` has NO emit site (SPEC `:22811` says "TBD"), `E-IMPORT-005` is fully live (`module-resolver.js:206`). \| error.map.md · non-compliance.report.md |
| **64** | **THE §40.8 DEFAULT-LOGIC AUTO-LIFT COVERS DECLARATIONS ONLY, AND EVERYTHING ELSE FALLS THROUGH TO PAGE TEXT AT EXIT 0 WITH ZERO DIAGNOSTICS — THREE HIGH DEFECTS OF THIS ONE CLASS ARE OPEN ON `main`.** `liftBareDeclarations` (`ast-builder.js:1161`) tests NINE `^`-anchored PREFIX gates against `block.raw`; the main one, `BARE_DECL_RE` (`:651`), is a decl-KEYWORD list (`export`/`server fn`/`type`/`fn`/`function`/`let`/`const`/`import`). **Anything matching no gate reaches the terminal `result.push(block)` and is emitted VERBATIM INTO `<body>`.** PA-REPRODUCED at this watermark by compiling and reading the emitted HTML: `log("M1");` ships · `if (1) { }` ships · a `//` comment FLUSHES the run around it. ⚠ **The masking limb makes this HIGH rather than cosmetic: a statement that was never compiled raises none of its diagnostics** — the original reproducer read an undeclared cell and `E-STATE-UNDECLARED` did not fire. ⚠ **`E-CONTROL-FLOW-IN-MARKUP` LOOKS like it covers this and structurally cannot** — its emit site (`:1857-1860`) is gated `parentType === "markup"`, the COMPLEMENT of the §40.8 locus, while its §34 row asserts the opposite. ⚠ **And `W-PROGRAM-REDUNDANT-LOGIC` actively routes authors INTO the broken mode** — it tells you to remove the `${…}` wrapper, and the wrapped form is the one that works. **The bare-call limb is an OPERATOR RULING (§40.8 is SILENT on a bare call), not a fix.** ⚑ **PARTIALLY CLOSED S376 AT ONE LOCUS ONLY, AND THE SCOPE MATTERS.** #718 added `E-STATE-BLOCK-STATEMENT-FORM` for the `<db>`/`<state>`/`<schema>` limb — and ONLY for the named lifecycle form `on mount {` / `on dismount {`. **The `<program>`/`<page>`/`<channel>` limb this row is actually about is UNCHANGED and all three of its HIGH defects still reproduce on `main`**: a bare call ships, a bare `if (){}` ships, a `//` comment flushes the run around it. **Do not read the new code as closing invariant 64.** It closes a DIFFERENT locus, one the §40.8 auto-lift never reached in the first place. | **Task-Shape Routing rows 1 and 2** · domain.map.md · structure.map.md · error.map.md |
| **65** | **`RUNTIME_CHUNK_ORDER`'s `stdlib-*` MEMBERS ARE THE CLIENT CONTRACT — MEMBERSHIP IS THE PROPERTY THAT DECIDES WHETHER A CLIENT `scrml:` IMPORT WORKS AT ALL, AND THERE SHALL BE ONLY ONE LIST (#669, §41).** A client bundle is a classic script, so the import lowers unconditionally to `_scrml_stdlib.NAME`, which that array defines and nothing else does; absent, the destructure throws AT LOAD and kills the whole page. **Do not add a second hand-maintained list** — `STDLIB_CLIENT_CHUNK_MODULES` and `hasStdlibClientChunk` are DERIVED from the array so the gate and the outcome read the same artifact. **Membership itself is DERIVED from the §12.2 Trigger 3 two-limb criterion**, not curated. ⚠ **Matching is EXACT — a SUBMODULE does not inherit its root's chunk** (`scrml:auth/jwt` lowers to `_scrml_stdlib.auth/jwt`, which JS parses as a DIVISION). ⚠ **`auth` and `crypto` fail the criterion yet carry a chunk (PRE-EXISTING, S95 Bug 18) — a live inconsistency deliberately left alone, not a closed decision.** | dependencies.map.md · auth.map.md |
| **66** | **AN `unknown` THAT CANNOT SAY WHAT DEFEATED IT HAS DECAYED BACK INTO AN `asIs` — `UnknownType.reason` IS REQUIRED AND `tUnknown()` HAS NO ZERO-ARG OVERLOAD (§7.5/§14.7, #665).** `asIs` means a DEVELOPER signed for it; `unknown` means the COMPILER could not tell. Before the split they were one value, so *absence of a diagnostic* and *success* were the same observation. `inferExprType` returns `InferenceResult` so the failure branch cannot be reached by accident, and `InferenceGap.nodeKind` is typed `ExprNode["kind"]` so a gap cannot be constructed without naming a real node kind. ⚠ **The `never`-fallthrough decay-stopper is only real because `scripts/types-gate.ts` landed with it — bun runs `.ts` TRANSPILE-ONLY, and until #665 NOTHING had ever type-checked this compiler; nine sibling exhaustive switches were already failing and nothing had noticed.** **A `never` fallthrough with no checker wired is decoration.** | schema.map.md · build.map.md |
---
| **67** | **A RUNTIME-BEHAVIOUR CLAIM MUST NAME WHICH RUNTIME IT EXECUTED — `SCRML_RUNTIME` IS NOT WHAT SHIPS.** The browser loads the PRUNED per-app `scrml-runtime.<hash>.js` (`assembleRuntime`, `runtime-chunks.ts:541`) over the chunk set `detectRuntimeChunks` (`emit-client.ts:828`) selected. **The conformance runtime half executes the FULL monolith** — `conformance/adapters/impl1-ts.ts:467` and `:924` both concatenate `SCRML_RUNTIME + clientJs` — so **all 883 cases are blind to chunk pruning by construction**, and a case can PASS while its page is dead. Browser tier measured at this watermark: **25 of 94 files import `SCRML_RUNTIME`, 66 read `result.runtimeFilename`, 2 both, 5 neither. Only the 66 can catch a pruning defect.** Live proof: `conformance/cases/each/ternary-markup-giti033` calls `_scrml_each_clear`/`_scrml_resolve_item` (`reconciliation` chunk) against a runtime that pruned them. **Corollary of invariant 59 and of the S265 theme-switch lesson: a probe that resolves to a DIFFERENT artifact than the obligation measures nothing.** | Task-Shape Routing row 3 · test.map.md |
| **68** | **`markupReferencedNames` IS NOT A DIAGNOSTIC-ONLY SET — WIDENING IT MOVES CODE PLACEMENT.** Built at `route-inference.ts:4852` by `walkMarkupContext` (`:4876`) and consumed by THREE gates, not one: the D4 `W-DEAD-FUNCTION` suppression (`:5583`), the **Step-5c indirect server-escalation gate (`:5210`, #284 FIX B)**, and **`clientRootIds` -> `endpointClientSkipIds` (`:6242`)**. **So the argument "over-inclusion only suppresses an advisory warning" is FALSE** and must not be used to justify the next widening — the #688 each-opener widening was fail-safe in BOTH directions and was measured (corpus differential: 0 of 7,388 artifacts changed — **per #688's own in-source note at `route-inference.ts:4963`; NOT re-run this pass**); the next one owes the same measurement against the PLACEMENT consumers. ⚠ The in-source comment at `:4959-4962` cites `:5201`/`:6233` for two of those consumers and both are **9 lines short** at this watermark — grep, do not trust the citation. | Task-Shape Routing row 2 · error.map.md |
| **69** | **A PREDICATE HAS TWO SPELLINGS AND THEY ARE LOWERED BY DIFFERENT CODE — `if=(@x.y)` AND `if=@x.y` ARE NOT ONE PATH.** `computeDisplayToggleCondition` (`emit-event-wiring.ts:473`) branches on `b.condExpr` vs `b.varName`+`b.dotPath`, and WHICH ONE you get is decided UPSTREAM at the `emit-html.ts` binding site by the attribute value's `kind`: a `variable-ref` (bare `@x.y`) records `varName`+`dotPath` (`:3155`), an `expr` (parenthesized / compound) records `condExpr`+`condExprNode` (`:3224`). **The `condExpr` branch threads `synthCellKeys` into `emitExprField` and gets the §55 collapse for free; the `varName` branch consulted NOTHING until #704** — so one predicate, written two ways, lowered to `_scrml_reactive_get("signup.submitted")` and `_scrml_reactive_get("signup").submitted`. The second is `undefined` forever on a §6.3 Variant C namespace, at exit 0, with zero diagnostics: **the flagship `examples/30-validated-form.scrml:136` never rendered its success message in any build ever shipped.** ⚑ **THE GENERAL RULE, and it binds the NEXT lowering as well: when a surface has two spellings, the fix is not to patch the branch that broke — it is to make both branches consult the same artifact, and then to STATE that N is still not 1.** #704 exported `resolveSynthCellPrefix` and the export's own comment (`emit-expr.ts:1029`) says *"DO NOT READ 'shared' AS 'every site that needs it now calls it' … This export reduced N; it did not make N equal 1."* **Five copies survive, in two disagreeing resolution orders** — table in dependencies.map.md. ⚠ **And a gate over a value needs the value's TYPE, which is never recoverable from the syntax of the read**: not from the leaf name, not from the declaration form, not from the presence of a tail, not from key-registration alone. Four review rounds each got that wrong once. | **Task-Shape Routing row 1** · domain.map.md §55 · dependencies.map.md |
| **70** | **A `<each>` HAS TWO PARSE ORIGINS AND ONLY ONE OF THEM PRODUCES AN `each-block`.** `<each>` is in `block-splitter.js`'s `STRUCTURAL_RAW_BODY_ELEMENTS` (`:126`), so a STRUCTURAL each gets its body raw captured and the ast-builder promotes it to `kind:"each-block"` (`ast-builder.js:16863`). **Markup reached through `parseLiftTag` (`ast-builder.js:5458`) is built recursively as GENERIC markup and `<each>` is NEVER promoted** — it arrives at codegen as `{kind:"markup", tag:"each"}`. The bridge is `eachBlockFromMarkupNode` (`codegen/emit-each.ts:3324` (⚑ **CORRECTED S380 from `:3309` — #735 inserted 15 more lines above it; CORRECTED S376 before that, from `:3266`**)); un-bridged, the compiler renders a LITERAL `<each>` DOM element and leaks the raw `@.` sigil. ⚑ **THE CONSEQUENCE THAT MAKES THIS AN INVARIANT RATHER THAN TRIVIA: every AST walker downstream inherits the split, and the ones that switch on node KIND silently skip origin (b).** `detectRuntimeChunks` (`emit-client.ts:828`) is the live instance — `case "each-block"` (`:1650`), `"engine-decl"` (`:1707`), `"match-block"` (`:1750`), **and no `markup-value` case**, so an each inside a `MarkupValueExpr` (`types/ast.ts:2070`) prunes the `reconciliation` chunk and the page throws on first render (invariant 67). **Two of the three fixed instances of this class carry the identical failure sentence in their own comments — that recurrence is the finding.** Before you add a walker over `<each>`, ask which ORIGIN it sees. | **Task-Shape Routing row 2** · structure.map.md · domain.map.md |
| **71** | **A FIGURE THAT APPEARS TWICE IN ONE MAP SET MUST BE DIFFED AGAINST ITSELF BEFORE IT IS CARRIED — FOUR SEPARATE FIGURES WERE INTERNALLY CONTRADICTORY AT S376, EACH IN A SINGLE FILE.** `error.map.md` carried a bucket table headed "at this HEAD" reading `PINNED 338 · IMPL-SITES 320 · FALSE-CLAIM 95` while the catalog section **35 lines above it in the same file** printed `343 · 302 · 112`. `test.map.md`'s section heading said **1,387** while its own header said **1,394**, and its per-category TABLE said unit `901` / browser `94` while the prose four lines above said `908` / `95`. `primary.map.md` published the §34 total as **810** in a paragraph whose own text reads *"Re-read at this HEAD, not carried"* while its header said **812**; and published `grep -cE '^\| E-'` as **912** when checking out that same watermark's SPEC and re-grepping returns **915**. **Every one survived multiple passes because each pass updated ONE site.** ⚑ **The detection needs no external oracle and no compile — it is grep-able inside the artifact**, and it is the strongest available tell that a claimed "re-read" was a carry: a pass that actually re-derived a figure would have written it everywhere it appears. **Corollary for the SHAPE of a map: a bucket/count table SHALL print the SHA it was executed at in its own heading.** "At this HEAD" decays silently and nothing fails when it does. | Map Index · error.map.md · test.map.md |
| **72** | **A `file:line` CITATION CAN BE A WRONG *FILE*, AND THAT FAILS WORSE THAN A DRIFTED LINE — MEASURED S376.** Two maps said `codegen/emit-server.ts` "splits the pipeline remainder into `_scrml_dispatch` and wraps it in `_scrml_onion_dispatch` (`emit-server.ts:521`)". **Neither symbol exists anywhere under `compiler/src/codegen/`**; both are emitted by the HOST at `commands/build.js:570`/`:577` (⚑ S380: was `:514`/`:521` — §52.13 added +58 lines above the split), and `emit-server.ts:521` is §20.5/§52 `@currentUser`-query-gate code. **The maps had the right NUMBER against the wrong FILE** — the number was correct FOR ANOTHER FILE, which is exactly why it survived: it looks precise, it resolves to real code, and it reads as plausible. A drifted line lands near the truth; a wrong file sends a dev agent into thousands of lines of the wrong module with a confident anchor. **Wrong since #654 and carried through every window, never caught because the auth surface kept measuring zero-diff and the map kept being stamp-advanced rather than READ.** ⚑ **Corollary: a stamp-advance on a measured zero-diff proves the SOURCE did not move. It proves nothing about whether the map was ever right.** Verify a citation by grepping the SYMBOL, never by adjusting the line number arithmetically. | auth.map.md · structure.map.md · invariant 48 |
| **73** | **A CORRECT CITATION DRIFTS AGAIN WHEN AN UNRELATED FEATURE LANDS ABOVE IT IN THE SAME FILE — MEASURED S380, TWICE IN ONE WINDOW.** Invariant 72 fixed `emit-server.ts`/`commands/build.js`'s onion-dispatch citations once (S376). S380's §52.13 (an auth-protected served document, #728 — semantically UNRELATED to the onion) inserted code ABOVE those same citations in BOTH files, drifting all six by +12/+56 a second time. Separately, #735's per-item `<match>` re-dispatch fix inserted 15 lines above `eachBlockFromMarkupNode` in `emit-each.ts`, drifting a citation invariant 70 had ALREADY corrected once (from #710). **The lesson invariant 72 does not cover: verifying a citation is grep-accurate at the watermark you last checked is not enough — a feature with NO conceptual relationship to what the citation describes can still invalidate it, because `file:line` couples to every OTHER change in the same file, not just the one you are tracking.** When a task names touched files, diff the WHOLE file against the map's last-verified watermark before trusting any citation into it, not just the citations for the feature the task describes. | primary.map.md · structure.map.md · auth.map.md · invariant 48 · invariant 72 |

## Map Index

**EVERY STAMP BELOW IS `fc6df72e` — the `merge-base HEAD origin/main` at WRITE time, with the source
diff against it EMPTY and `merge-base --is-ancestor fc6df72e origin/main` exit 0.** At this
watermark the merge-base IS `origin/main`; the working tip `60803548` on `wrap/s376` is one
docs-only commit above it. Line 3 is the CURRENCY ANCHOR `scripts/state.ts` parses (and it parses
**line 3 only**, `scripts/state.ts:615`); line 4 carries the SAME SHA deliberately — see the top of
this file. The per-row note carries walk provenance.

⚑ **INCREMENTAL PASS OVER A FIVE-FILE SOURCE WINDOW, ONE OF WHICH IS A NEW MODULE.** Re-walked:
**primary, structure, dependencies, domain, test, error, non-compliance.** **STAMP-ADVANCED ON
MEASURED ZERO-DIFF** (not re-walked, each carrying its own re-measured evidence): **schema, config,
build, auth, infra, migrations.** ⚠ **`auth` is stamp-advanced AND carries a corrected row** — a
zero-diff surface is not a correct map, only an unchanged one. The source span is
`lint-e-state-block-statement-form.js` (NEW) · `api.js` · `component-expander.ts` ·
`codegen/emit-each.ts` · `codegen/emit-lift.js`, plus 4 new test files and docs/handOffs.
**`compiler/SPEC.md` moved by ONE LINE; `compiler/src/types/`, `package.json`, `.github/`,
`scripts/`, `conformance/` and `stdlib/` are ALL `--name-only` EMPTY.**

| Map | Stamp | Ancestor of `origin/main`? | Contents |
|---|---|---|---|
| **primary.map.md** | **`0dd659a1`** | yes (merge-base; == `origin/main` here) | **⚑ S380 INCREMENTAL (this pass): invariant 73 added (a citation drifts a SECOND time from an UNRELATED landing in the same file); FOUR `eachBlockFromMarkupNode` citations + all `build.js`/`emit-server.ts` onion-dispatch citations re-corrected (+15 / +12 / +56, per invariant 73); header rewritten with the S380 scope + provenance-caveat blocks.** this file — the MAP-STAMP RULE (+ the NEW one-SHA-on-two-lines rule that closes the S372 self-contradicting-watermark defect), fingerprint, invariants **1-72 (rows 71-72 NEW; row 64 AMENDED)**, Map Index, Task-Shape Routing (**one new row at the head**), Key Facts. **⚑ S376: this pass's finding is not the new module — it is FOUR figures inside this map set that contradicted ANOTHER FIGURE IN THE SAME FILE, and one citation that was a wrong FILE rather than a drifted line.** Also corrects its OWN carried §34 total (`810` -> **813**, while its own header said 812), its own `^| E-` prefix-grep figure (`912`/`178` -> **916**/**179**, wrong at its own prior stamp), its own FACTS figures (`244,774`/`191`/`1,394` -> **245,517**/**192**/**1,398**), and its own test-population cross-check (`1,378` -> **1,398**). |
| **structure.map.md** | **`0dd659a1`** | yes | **⚑ S380 INCREMENTAL: the wrong-FILE onion-dispatch fix (previous row) drifted a SECOND time — `commands/build.js` `:514`/`:521` -> `:570`/`:577` (+56), `emit-server.ts` `:2934`/`:3093`/`:3230`/`:3247` -> `:2946`/`:3105`/`:3242`/`:3259` (+12), both from §52.13 (#728) landing above them. Not re-walked otherwise this pass.** **RE-WALKED. ONE NEW MODULE — the first in three windows.** `lint-e-state-block-statement-form.js` gets a full row: Stage 2.5c, PRE-AST, first `E-` pass over block-splitter output, why its source-text regex is **not** an invariant-55 violation, why it has no `try`/`catch`, and its TWO open holes (both PA-reproduced by compiling). **FIVE `file:line` citations corrected, four of them stale BEFORE this window's diff** — `api.js:2409`->**2518**, `:1082`->**1134**, `:1208-1210`->**1316**, `:665-677`->**703-716**, and the wrong-FILE onion-dispatch claim. `compiler/src` re-counted by `git ls-files`: **194 tracked** (145 `.ts` + 47 `.js` + 2 other), not the carried 189. |
| **dependencies.map.md** | **`fc6df72e`** | yes | **Manifest ZERO-DIFF** — `package.json`/`bun.lock` `--name-only` EMPTY. **ONE new internal edge, and unlike the last two windows' it is not a deduplication but a NEW PIPELINE STAGE:** `api.js:62` -> `lint-e-state-block-statement-form.js`, consumed at `api.js:1203-1257`. ⚑ **The new node is a LEAF — it imports NOTHING**, by necessity: anything it pulled from a later stage would invert pipeline order. Cost: its `STATE_BLOCK_NAMES` / lifecycle regex are COPIES of `ast-builder.js:1160` / `:756-757`, unenforced — a fourth and fifth instance of the shared-rule duplication this map tracks. New BS-LINT stage row in the module graph. |
| **schema.map.md** | **`fc6df72e`** | yes | **STAMP-ADVANCED ON MEASURED ZERO-DIFF.** `compiler/src/types/` EMPTY for the **FOURTEENTH** consecutive window; `*.d.ts` / `*.proto` / `*.graphql` all EMPTY. Five source files moved including a NEW module and the exported-TYPE surface still did not: `grep -E 'export (interface\|type) '` over the window's `+` lines is EMPTY. The two new exports are `DIAGNOSTIC_CODE` (a string const) and `runEStateBlockStatementForm` — **neither a SHAPE**, and the new module consumes the block-splitter result shape STRUCTURALLY, with no named interface for it. `SPEC.md` +1 line, a §34 row, not a type. |
| **domain.map.md** | **`0dd659a1`** | yes | **⚑ S380 INCREMENTAL: §16.6 (item 3 above) is now PARTIALLY STALE post-#726/#731/#733 — see the new §16.6.1 addendum this pass added (AST-scoped, not textual, param substitution; the `is some` string-prop routing fix; the optional-snippet-prop `null`->`true`-when-filled fix). New §51.3 section added for #732/#735 (`<match>` derived-cell scrutinee + per-item re-dispatch). Not otherwise re-walked this pass.** **FOUR NEW SECTIONS, and the first is a language ruling.** (1) **§4.18.1/§40.8/§34 — a STATE-BLOCK body is markup context and logic there is now REFUSED**: why `on mount {` is logic at `<program>` and TEXT at `<db>`, why the message had to be re-worded because it was FALSE at `<schema>` (measured: HTML occurrences `<db>` 1 · `< state>` 1 · `<schema>` **0**), why `type:"state"` is not a semantic classification (123 such nodes, only 44 named `db`), and why the deprecated `< db>` opener is the one the corpus actually used. (2) **§17.2 `show=` inside `<each>` is REACTIVE while a nested `if=` is create-time FROZEN** — the asymmetry is the rule: `show=` never changes DOM SHAPE. (3) **§16.6 snippet fills route by the DECL's parametric-ness, arity-tolerantly** — and capturing every fill is a CRASH fix, not completeness. (4) **for-lift glued `${…}`**. Both S372 corrections RE-VERIFIED by execution and both still hold. |
| **error.map.md** | **`0dd659a1`** | yes | **⚑ S380 INCREMENTAL: two ALREADY-stale (pre-S380) citations found and corrected while checking this window's `emit-each.ts` diff for shift — `W-EACH-PERITEM-IF-MULTIROOT-DEFERRED` `:1143` -> **`:1286`**, `emit-lift.js`'s two `W-LIFT-TIER0-*` tokens `:1448`/`:1450` -> **`:1484`/`:1486`**. Neither was caused by S380 (both sit before its insertion point); both predate this pass undetected. Not otherwise re-walked this pass.** **§34 catalog 812 -> 813 — ONE new code, `E-STATE-BLOCK-STATEMENT-FORM`**, with a full section: emitter, stage, scope table, the four deliberately-uncovered shapes and the EVIDENCE for each, why a fresh code rather than the reserved `E-STATE-BLOCK-BARE-WRITE-DECL`, the corpus-differential prose-citation hazard, and TWO open holes reproduced by compiling. **Census RE-EXECUTED**: the only moving bucket is IMPL-SITES 302 -> **303** (no conformance case pins it). ⚑ **Corrects a bucket table INSIDE THIS FILE that was three windows stale under a heading reading "at this HEAD" and disagreed with another table 35 lines above it by 17 on FALSE-CLAIM.** Also corrects the `E-EACH-BODY-DECL-UNSUPPORTED` emit site, which this one file cited as BOTH `:1387` and `:1416` — real value **`emit-each.ts:1361`**. |
| **test.map.md** | **`fc6df72e`** | yes | **1,398 `*.test.js` (+4, zero deleted).** ⚑ **THE FINDING IS THE SPLIT: 1 unit, 3 browser — and `compiler/tests/browser/` is NOT in the pre-commit set**, so three of four new regression pins sit outside the commit gate, all three guarding SILENT-WRONG-OUTPUT classes that compile green. New unit suite PA-EXECUTED here: **34 pass / 0 fail / 53 expect() in 416ms**. ⚑ **Corrects THREE of its own numbers that contradicted other numbers in the same file** — heading total `1,387` vs header `1,394`; table unit `901` vs prose `908`; table browser `94` vs prose `95` — and reconciles the whole population: **1,384 category dirs + 14 root-level = 1,398**. |
| **build.map.md** | **`fc6df72e`** | yes | **STAMP-ADVANCED ON MEASURED ZERO-DIFF.** `.github/`, `package.json`, `bun.lock`, `Makefile`, `Dockerfile`, `scripts/` ALL `--name-only` EMPTY. `gate` stays at 13 steps. ⚠ **`scripts/ctx.ts` is NOT on `main` at this watermark and is deliberately NOT mapped** — `git log origin/main -- scripts/ctx.ts` is EMPTY and the path does not exist in the checkout, though five commits touching it exist on unmerged branches. The pre-land codegen gate is still hand-run, not in CI. |
| **infra.map.md** | **`fc6df72e`** | yes | **STAMP-ADVANCED ON MEASURED ZERO-DIFF.** `.github/` `--name-only` EMPTY — no workflow, job, secret, runner or required check moved. No `Dockerfile`, no `docker-compose.*`, no `*.tf`, no `k8s/` path in the repo. |
| **config.map.md** | **`fc6df72e`** | yes | **STAMP-ADVANCED ON MEASURED ZERO-DIFF.** `grep -cE '^[+-].*(process\.env\|Bun\.env)'` over the WHOLE window diff returns **0**; `.env*` / `bunfig.toml` / `tsconfig*` / `package.json` / `bun.lock` EMPTY. The new module reads no environment and takes no flag. |
| **auth.map.md** | **`0dd659a1`** | yes | **⚑ S380 INCREMENTAL: NEW §52.13 section added (an `auth="required"` scope now also gates its served `.html` document, #728) with a 3-row mechanism table (codegen export / build host / dev host). The row-2/2b/3/4 onion-dispatch citations to its LEFT (S376's fix) drifted a SECOND time from §52.13 landing above them — corrected in place (+12/+56/+15/+18/+22 per cell, see the row text). Two further citations corrected: CORS-preflight `:3097-3115`->`:3109-3127`, ratelimit `:2997-3020`->`:3009-3032`.** **STAMP-ADVANCED ON MEASURED ZERO-DIFF (FOURTH consecutive window) — AND CARRYING A CORRECTED ROW ANYWAY.** `emit-server.ts`, `select-request-onion.js`, `protect-analyzer.ts`, `auth-graph.ts`, `compiler/runtime/`, `lsp/` all EMPTY; five source files moved and not one is on the auth surface. ⚑ **But its request-pipeline table named `emit-server.ts:~454-521` as the emitter of `_scrml_dispatch` / `_scrml_onion_dispatch`, and NEITHER SYMBOL EXISTS ANYWHERE UNDER `compiler/src/codegen/`** — both are emitted by the HOST at `commands/build.js:570`/`:577` (⚑ S380: was `:514`/`:521`, +56, §52.13). Wrong since #654, carried through every window since, never caught precisely BECAUSE the surface kept measuring zero-diff. Split into a codegen row and a host row. |
| **migrations.map.md** | **`fc6df72e`** | yes | **STAMP-ADVANCED ON MEASURED ZERO-DIFF — FIFTEENTH consecutive window with no DB/migration surface movement.** `git diff --name-only -- '*migrat*' '*schema*'` returns only the two `.claude/maps/` files, i.e. no source at all. One adjacency noted and it is NOT a migration change: the new diagnostic names `<schema>` in its locus set, so a lifecycle statement in a `<schema>` body is now refused — no DDL, differ or command touched. |
| **non-compliance.report.md** | **`fc6df72e`** | yes | **RE-RUN at this watermark.** Carries this window's map-vs-source drift — five corrected `api.js` citations, the wrong-FILE onion claim in two maps, four figures contradicting a sibling figure in their own file, three stale prefix-grep numbers — plus `compiler/SPEC.md:11686`, re-checked and STILL asserting a behaviour the compiler no longer has. |

⚠ **`.claude/maps/*.generated.md` (`structure`, `dependencies`, `error`, `test`) ARE NOT PART OF THIS
MAP SET AND ARE NOT IN GIT.** `git ls-files .claude/` returns exactly 16 paths — the **13** maps
above, `agents/project-mapper.md`, `settings.json`, `statusline.mjs`. The four `*.generated.md` are
**untracked, per-clone local artifacts**, ignored by `.gitignore:3` (`.claude/`); the 13 curated maps
are in git only because they were force-added.

⚑ **THEY WERE REGENERATED THIS PASS (S376) AND THE TWO-MONTH STALENESS IS CLEARED.** They had been
`@generated` at **2026-06-25** and were wrong against HEAD by ~37 source files and ~340 test files.
Re-run at this watermark with `bun scripts/mapgen.ts --kind {structure,errors,deps,tests} --root
<repo> --write` from `flogence/`:

| generated map | figure at `fc6df72e` |
|---|---|
| `structure.generated.md` | 192 files · 1,478 exported symbols (was 155 · 1,128) |
| `dependencies.generated.md` | 192 files · 672 local import edges (was 155 · 488) |
| `error.generated.md` | **450 E-codes with emit sites** (was 353) |
| `test.generated.md` | 1,384 `.test.js` across 11 dirs (was 1,042) |

⚠ **AND THE TWO E-CODE FIGURES ARE NOT THE SAME POPULATION — do not reconcile them.**
`error.generated.md`'s **450** counts codes with a live EMIT SITE found by a source walk;
`error.map.md`'s **813** counts §34 CATALOG rows. The gap is the census's own FALSE-CLAIM (112) +
DECLARED-AHEAD (18) + STRUCK (34) buckets plus counting-method differences. **Neither is wrong;
they answer different questions.** Same for `test.generated.md`'s **1,384** vs FACTS.md's **1,398**:
mapgen walks category directories and skips `fixtures`/`helpers`, so it misses the 14 root-level
`compiler/tests/*.test.js`. **These four files still commit nothing — they are gitignored — so a
future pass gets no free currency from a `git status` check on them.** non-compliance.report.md C8.


## Task-Shape Routing

| If your task is about… | Read |
|---|---|
| **⚑ LOGIC WRITTEN IN A `<db>` / `<state>` / `<schema>` BODY — an `on mount` that never fired, an initializer that never ran, or a page that renders its own source code. ALSO: any edit to `lint-e-state-block-statement-form.js`, to `api.js` Stage 2.5c, or to ANY pass that consumes block-splitter output before the AST exists.** | **THE LOCUS IS `compiler/src/lint-e-state-block-statement-form.js`, WIRED AT `api.js:1203-1257`, AND IT IS THE FIRST `E-`-SEVERITY PASS IN THE COMPILER THAT RUNS PRE-AST.** ⚑ **READ THIS BEFORE YOU "FIX" ANYTHING IN IT.** (1) **Its regex over source text is NOT an invariant-55 violation.** Invariant 55 forbids a text pass in a stage that ALREADY HAS the AST; Stage 2.5c's input is `bsResults`, the block splitter's `{filePath, blocks}` output, and there is no AST at that stage to consult. A future reader greping for "regex over source" WILL misfile this — the pre-AST position is the whole argument. (2) **Its missing `try`/`catch` is deliberate and the asymmetry with Stages 2.5 / 2.5b is the point.** They swallow a throw and log under `--verbose`; that is correct for a WARNING. Invert the severity and the direction inverts: a swallowed throw in an ERROR gate means a file that SHOULD be refused compiles at exit 0 with nothing saying the gate never ran — fail-OPEN, the exact silent-success mode this closes. The two other error-severity lint stages (`LINT-TRY-CATCH` 3.007, `REJECT-ASYNC-AWAIT` 3.008) also run bare through `stage()`. (3) **Name-guard BOTH block-splitter arms.** `type:"state"` is not semantic — it is what BS calls ANY whitespace-form opener `< Name …>`. Measured over 2,353 corpus sources: **123 such nodes, only 44 named `db`**; the other 79 are engines, typestate transition decls, whitespace-form COMPONENT defs and plain HTML. An unguarded arm claimed all of them and fired at a locus where the diagnostic's premise is FALSE. (4) **THE GATE HAS TWO OPEN HOLES, both PA-REPRODUCED here by compiling.** A `<div>` one level inside a `<db>` body re-opens the defect at exit 0 with the statement in the emitted HTML (`g-state-block-statement-form-misses-a-wrapped-statement`, MED). And **any unpaired `/`+`*` in ordinary prose — a glob, a path, a spaceless division — disarms the gate for the REST of the block**: `note about src/* paths` followed by `on mount { loadDashboard() }` compiles at exit 0, zero diagnostics, statement shipped (`g-state-block-statement-form-disarmed-by-an-unpaired-block-comment-opener`, LOW). **A glob disarms a fatal gate.** (5) **DO NOT cite a sibling `E-`/`W-`/`I-` token in a diagnostic MESSAGE** — `scripts/corpus-emit-differential.ts:431` regexes fired codes out of OUTPUT TEXT, so a cited code is indistinguishable from a fired one; the first cut of this message made the differential report a phantom fire. Cite the SPEC SECTION. **error.map.md `E-STATE-BLOCK-STATEMENT-FORM` section · domain.map.md §4.18.1 section · structure.map.md · invariant 71.** |
| **⚑ AN `if=` OR `show=` THAT NEVER SHOWS, ALWAYS SHOWS, OR KILLS THE PAGE — and ANY edit to how a display/mount/chain toggle predicate is LOWERED. ALSO: anything touching `emit-event-wiring.ts`, §55 synth-cell reads in an attribute, or `resolveSynthCellPrefix`.** | **THE DECISION SITE IS `compiler/src/codegen/emit-event-wiring.ts`, IN THREE NAMED FUNCTIONS — AND THIS ROW EXISTS BECAUSE THE MAP SET HAD NO ROW FOR THE `if=`/`show=` LOWERING SURFACE AT ALL AND NAMED THIS FILE NOWHERE, WHILE FOUR DEFECTS LANDED ON IT THIS WINDOW.** Two dispatches reported the hole INDEPENDENTLY on the same day. **`computeDisplayToggleCondition` (`:473`)** is the one lowering — it takes the toggle binding OR a stamped `ifGuard` sub-object and returns `{conditionCode, subscribeVars}`; **`computeMountToggleCondition` (`:725`)** DELEGATES to it (they were separate paths and had silently diverged — the mount path passed neither `derivedNames` nor `synthCellKeys`, the GH #262/#275 defect class in a second location); **`computeChainBranchCondition` (`:750`)** lowers ONE if-chain branch. Its `condition.name` arm (**`:789`**) consults **nothing** and fabricates an unregistered flat key, so `else-if=@cfg.errors` can NEVER be selected — open, `g-else-if-dotted-cell-ref-emits-unregistered-flat-key` (#692), and ⚠ **the ledger's locus for it (`:647`) is 142 lines stale**; grep `condition.name`. **THE BINDING IS RECORDED UPSTREAM, IN `codegen/emit-html.ts`, AND WHICH SITE YOU HIT DECIDES WHICH FIELDS EXIST:** the §17.1 mount gate at **`:1468`** (`variable-ref` -> `varName`+`dotPath`), **`:1479`** (call-ref) and **`:1481`** (`expr` -> `condExpr`+`condExprNode`); the display-toggle path at **`:3155-3163`** (`variable-ref`, sets `isConditionalDisplay`/`isVisibilityToggle`, `varName`+`dotPath`) and **`:3224-3233`** (`expr`, sets `condExpr`+`condExprNode`+`refs`); and `pushIfGuard` (**`:1386`**) for the inner-interpolation gate. **`if=@var` on a lowercase tag NO LONGER reaches the display branch** — the mount gate returns early — so the only `if=` landing there is on a CAPITAL-initial component tag (`emit-html.ts:3141-3147`, read that comment before you 'fix' the branch). Consumers in emit-event-wiring: `:1734` (mount), `:1955` (display/visibility), `:2202` (`ifGuard`). **THE §55 LIMB IS THE ONE THAT BITES.** `resolveSynthCellPrefix` (`emit-expr.ts:1032`, NEW export #704) collapses `@compound[.field].<synthProp>` to the FLAT dotted key. Before it, `if=@signup.submitted` lowered to `_scrml_reactive_get("signup").submitted` — permanently `undefined` on a §6.3 Variant C namespace — and **the flagship `examples/30-validated-form.scrml:136` "Account created." had never rendered in any build ever shipped, at exit 0**. The per-field 3-level form was worse: it dereferenced `null` inside `_scrml_boot`, so **every `${…}` on the page never wired**. ⚠ **TWO ROWS ARE DELIBERATELY DECLINED AND ARE NOT BUGS TO 'FINISH'** — compound `errors` / `touched` are derived OBJECT MAPS (always truthy), so collapsing them renders a pristine form's error block at boot (MEASURED). `status=ruling-gated`, awaiting an operator ruling over {always-true, never-true, diagnose}. **Full PA-measured collapse/decline matrix: domain.map.md's §55 section. Five copies of the synth-key rule and two disagreeing resolution orders: dependencies.map.md's §55 SYNTH-KEY RULE table.** Invariant 69. |
| **⚑ AN `<each>` THAT BEHAVES DIFFERENTLY DEPENDING ON WHERE IT SITS — a raw `<each>` element in the DOM, a leaked `@.` sigil, `E-CODEGEN-INVALID-LOGIC` from an each body, a pruned runtime chunk, or a dead page from an each inside a ternary/lift/engine/match. ALSO: any edit to `eachBlockFromMarkupNode`, `emitNestedEachFromMarkup`, or `parseLiftTag`.** | **THERE ARE TWO KINDS OF `<each>` IN THIS COMPILER AND THEY TAKE COMPLETELY DIFFERENT PATHS. THE MAP SET HAD NO ROW FOR THE SPLIT, AND IT PRODUCED MULTIPLE DEAD-PAGE DEFECTS.** Reported independently by two dispatches. **(a) STRUCTURAL** — `<each>` is in `block-splitter.js`'s `STRUCTURAL_RAW_BODY_ELEMENTS` (**`:126`**), so its body raw is captured and the ast-builder's each-block dispatch builds a real `kind:"each-block"` node (`ast-builder.js:16863`). **(b) LIFT-PARSED** — markup reached through `parseLiftTag` (`ast-builder.js:5458`) is built RECURSIVELY as GENERIC markup and **`<each>` is never promoted**: it arrives at codegen as `{kind:"markup", tag:"each"}`. Un-handled, that renders a LITERAL `<each>` DOM element and its `${@.}` body reaches the bare-expr text path with no iter-scope rewrite, leaking the raw sigil (`createTextNode(String((@ .) ?? ""))`) -> `E-CODEGEN-INVALID-LOGIC`. **THE BRIDGE IS `eachBlockFromMarkupNode` (`codegen/emit-each.ts:3324` (⚑ **CORRECTED S380 from `:3309`, +15, #735's per-item `<match>` re-dispatch insertion; CORRECTED S376 before that from `:3266`**))**, which converts (b) into the (a) shape from its structured attrs+children — returning **null** when there is no `in=`/`of=` source, which is the caller's signal to keep literal-markup emission. Its one caller is `emitNestedEachFromMarkup` (`:3396`, ⚑ was cited `:3338`, which was already missing the #710 +43 — correct pre-S380 value was `:3381`), reached from `emit-lift.js`'s `tryEmitNestedLiftEach` (`:716`), which `require()`s emit-each at call time to stay init-order safe. ⚑ **`scopeVar` MAY BE NULL AND THAT IS VALID** (GITI-033/S240): an each in a ternary-markup consequent has no enclosing `for`; the pre-fix gate returned null on a null `scopeVar` and the each fell through to literal emission. ⚠ **THE SPLIT ALSO REACHES CHUNK PRUNING, AND THAT LIMB IS STILL OPEN.** `detectRuntimeChunks` (`emit-client.ts:828`) switches on AST **node kinds** and **has no `markup-value` case** — verified again at this watermark, only `each-block` (`:1650`), `engine-decl` (`:1707`) and `match-block` (`:1750`) exist. So an `<each>` inside a `MarkupValueExpr` (`types/ast.ts:2070`) prunes the `reconciliation` chunk and the page throws `ReferenceError` on first render, while its conformance case PASSES (invariant 67). **The class is: an `<each>` reachable only through a container this walker does not descend into — and two of the three fixed instances carry the identical failure sentence in their comments.** structure.map.md's `emit-client.ts` and `emit-each.ts` entries. |
| **⚑ AN `if=` OR `show=` THAT COMPILES CLEAN AND IS SIMPLY NOT THERE — a predicate the compiler ACCEPTED and then DISCARDED, with zero diagnostics** | **§17.1.2.2/§17.1.2.3 ALREADY DOCUMENT THIS CLASS AS FAIL-**OPEN**, WHICH MEANS IT SHIPS THE CONTENT YOU WROTE A PREDICATE TO WITHHOLD. Two distinct mechanisms — scope which one you have BEFORE you start.** **(1) `show=` / `else-if=` on a structural element (`<each>`/`<match>`/`<engine>`) is DISCARDED AT AST-BUILD, not at emit:** `grep -rn showCond compiler/src/` returns **ZERO hits** at this watermark. Only `ifRaw`/`ifCond` is stamped (`ast-builder.js:15892`, `:16891`, `:17932`, `:18082`) and `emit-html.ts`'s `isGateableIfValue` (`:1490`) reads `node.ifCond` and nothing else, so **there is no field for a `show=` gate to read** — the fix starts at CAPTURE. An `else` sibling after a gated structural element fires `E-CTRL-001` (the chain detector recognizes only `kind:"markup"` chain members). `g-structural-element-if-chain-and-show-composition-nominal` (MED, open). **(2) A STRUCTURAL `if=` INSIDE AN `<each>` ROW TEMPLATE is never gated — PA-REPRODUCED at this watermark by compiling:** `<each in=@.tags if=@shown>` nested in an `<each>` row emitted the inner list with **zero references to `shown`** in the row render path, exit 0, zero diagnostics, with `@shown = false`. The row renderers (`emit-each.ts`, `emit-lift.js`) never read `ifCond` at all; only `emit-html.ts`'s `emitGatedStructural` (`:1516`) does, and a row template does not go through it. `g-structural-if-inside-each-row-template-fails-open` (MED, open). ⚠ **AND THE SPEC'S OWN TABLE FOR THIS IS STALE — DO NOT SCOPE FROM IT.** `compiler/SPEC.md:11686` still says a markup `if=` on a NON-ROOT element inside a row template *"emits nothing — fails CLOSED"*. **It does not.** PA-reproduced here: it emits a real create-time append gate (`emit-each.ts:1313`) plus a `W-IF-IN-EACH` warning, so it renders correctly at row-build and then goes STALE. The measured-S302 row was overtaken by #416/GH #409 and never updated. Corrected table + the executed evidence: domain.map.md §17.1.2; filed in `non-compliance.report.md`. |
| **⚑ CODE THAT SHIPPED AS PAGE TEXT — a `<program>`/`<page>`/`<channel>` body statement that never ran, an `on mount` hook that never fired, or a diagnostic you expected and did not get. ALSO: anything touching `liftBareDeclarations`, the §40.8 auto-lift, or `isDefaultLogicBody`.** | **START AT `compiler/src/ast-builder.js:1161` `liftBareDeclarations`. THREE HIGH DEFECTS OF ONE CLASS ARE OPEN ON `main` AND NOTHING ROUTED ANYONE HERE UNTIL THIS PASS — the routing hole is itself part of the finding.** **How the surface works:** nine `^`-anchored PREFIX gates test `block.raw` — `BARE_EXPORT_AT_END_RE` (:1290) · `BARE_DECL_NAME_EQ_AT_END_RE` (:1600) · `USE_FOREIGN_LIFT_RE` (:1656) · **`BARE_DECL_RE` (:1675, defined :651 — decl KEYWORDS ONLY: `export`/`server fn`/`type`/`fn`/`function`/`let`/`const`/`import`)** · `TOPLEVEL_STATE_DECL_RE` (:1709) · `TILDE_TOKEN_RE` (:1748, gated `parentType==="state"`) · `TOPLEVEL_AT_WRITE_RE` (:1783) · `TOPLEVEL_ON_LIFECYCLE_RE` (:1820) · `BARE_CONTROL_FLOW_IN_MARKUP_RE` (:1860). **A run matching NO gate falls to the terminal `result.push(block)` and is emitted VERBATIM INTO `<body>` at exit 0.** **PA-REPRODUCED AT THIS WATERMARK BY COMPILING AND READING THE EMITTED HTML, not from the ledger:** `<program>`+`log("M1");` → `<body>log("M1");<p>ok</p>…` · `<program>`+`if (1) { }` → `<body>if (1) { }<p>ok</p>…` · `<program>`+`// c`+`log("M1");` → same (the comment FLUSHES the contiguous run, **before AND after**) · **control** `<program>`+`<count> = 0` → clean, no page text. ⚠ **THE MASKING LIMB IS WHY THESE ARE HIGH: a statement that was never compiled raises NONE of its diagnostics.** The original reproducer read an undeclared cell and `E-STATE-UNDECLARED` did not fire; delete the comment and the same file exits 1. ⚠ **`E-CONTROL-FLOW-IN-MARKUP` LOOKS LIKE THE COVERING DIAGNOSTIC AND STRUCTURALLY CANNOT FIRE HERE** — emit site `:1857-1860`, gated `parentType === "markup"`, the **COMPLEMENT** of the §40.8 locus — while its own §34 row claims the auto-lift *"fires only at `<program>`/`<page>`/`<channel>` direct-child roots"* and that otherwise such code *"would ship as raw `for(){}` text into the DOM."* **It ships.** ⚠ **`W-PROGRAM-REDUNDANT-LOGIC` ACTIVELY ROUTES AUTHORS INTO THE BROKEN MODE** — it tells you to remove the `${…}` wrapper, and the WRAPPED form is the one that works. **BEFORE YOU FIX ANYTHING HERE, KNOW WHICH LIMB YOU HAVE:** (a) **the `//`-comment flush** is a genuine BUG with a fix branch complete but unlanded (`g-default-logic-comment-flushes-a-run-severing-a-statement-from-its-declaration`, HIGH, open); (b) **the bare CALL is an OPERATOR RULING, not a fix** — §40.8 (`SPEC.md:22813-22814`) enumerates what auto-lifts (DECLARATIONS), carves out writes with a diagnostic, and is **SILENT on a bare call**, so *"searched §40.8, no governing sentence found"* is the finding; the fork is *lift every text run* (prose in a `<program>` body then parses as logic) vs *diagnose non-declaration runs* (must then reject `const bias = 1.2` followed by `log(x)`, which compiles today) — **either needs SPEC text, nobody should guess** (`g-default-logic-bare-call-is-unspecified-and-ships-as-page-text`, HIGH, open); (c) **the bare `if (){}` is covered by NEITHER** and is the limb that contradicts the §34 row. ⚑ **AND THE CLASS IS WIDER THAN §40.8 — a corpus sweep read from source found at least four live members OUTSIDE it:** `on mount { loadDashboard() }` inside a `<db>` state-block body ships as text and **the mount hook never runs** (`samples/htmx-debate-dashboard.scrml:143`, clean compile — and that locus is its own OPEN RULING, because SPEC says state-block bodies are NOT default-logic loci and the sibling `W-STATE-BLOCK-BARE-WRITE-DECL` deliberately chose **Info** there); ~25 bare `@name = expr` writes emitted as text in `gauntlet-r10-bun-admin` / `samples/dashboard`; and `stdlib/http/index.scrml` leaking 8 lines of its own body. ⚠ **LANDING HAZARD:** the comment-flush fix and the bare-call build BOTH touch `liftBareDeclarations` additively in different places — **cherry-pick, never file-delta**, and the bare-call gate is a REJECT gate that must NOT be folded into `matchesAnyLiftGate`. **Invariant 64.** structure.map.md `ast-builder.js` · domain.map.md · error.map.md. |
| **⚑ `W-DEAD-FUNCTION`, "is this function reachable?", or ANY edit to the markup-reference / reachability walk** | **THE LOCUS IS `compiler/src/route-inference.ts`. `compiler/src/codegen/usage-analyzer.ts` IS NOT IT — and the gap ledger said it was, from S369 until S371.** **This row exists because the map set had NO row for this surface at all**: a dev dispatch working it had to re-derive the locus from source, and the wrong `locus=` field survived three sessions because nothing here contradicted it. **The negative claim, verified at this watermark:** `usage-analyzer.ts` contains ONE occurrence of the string `W-DEAD` — a PROSE mention in a comment at `:692` — exports a boolean `FeatureUsage` bitmap and nothing else, and its own header (`:16-17`) reads *"zero new diagnostics, zero AST mutation, zero emission."* **It structurally cannot emit the warning.** **The real machinery:** SOLE emit site `route-inference.ts:5615-5616` (`new RIError(` at `:5615`, the code string at `:5616`, `severity="warning"`), inside the **D4 block at `:5575`** in the `for (const [fnNodeId, record] of analysisMap)` loop at `:5564`. **The gate is a TEN-TERM conjunction (`:5614`)** — `!isHandleHatch && !hasCallers && !isExported && !isExplicitServer && !isMarkupReferenced && !isEndpointReferenced && !isGenerator && !isToolMainEntry && !isReturnedInline && !isLogicReferenced`. **Every existing suppression is a term THERE**; a new one is a term there too, unless the function is genuinely referenced — in which case it belongs in the walker. **The walker:** `markupReferencedNames` is built at `:4852` and populated by `walkMarkupContext` (`:4876`). **NEW #688 (this window, the entire behavioural delta of the window):** a per-kind `each-block` block at `:4966` collects identifiers from `inExprRaw` / `ofExprRaw` / `keyExprRaw` / `ifRaw`, because **an `<each>` does NOT parse to a `markup` node** — it lowers to `kind === "each-block"` carrying **no `attrs` array at all**, so the attribute branch that already covered `<span if=fn()>` never saw it. The each BODY was always covered (`bodyChildren`/`templateChildren` are ordinary markup arrays); only the OPENER was blind. ⚠ **DO NOT WIDEN THIS WALKER ON THE ARGUMENT THAT "over-inclusion only suppresses an advisory warning" — THAT IS FALSE.** `markupReferencedNames` is ALSO read at **`:5210`** (the Step-5c indirect server-escalation gate, #284 FIX B, header at `:4836`) and at **`:6242`** (`clientRootIds` → `endpointClientSkipIds`). **Widening it moves CODE PLACEMENT, not just diagnostics.** (⚠ the in-source comment at `:4959-4962` cites `:5201`/`:6233` for these two — both are **9 lines short** at this watermark; grep, do not trust the citation.) ⚠ **THE WARNING'S OWN PREDICTION IS FALSE, AND IT IS FALSE IN THE SIMPLE CASE.** PA-EXECUTED at this watermark: `<program>${ fn reallyDead() { return 41 } }<p>hello</p></program>` warns *"It will be tree-shaken from the output"* and `dist/page.client.js` **still contains `reallyDead`**. Gap `g-wdead-function-tree-shaken-claim-is-false`. ⚠ **SIBLING STILL OPEN, SAME WALKER, SAME ONE-BLOCK SHAPE:** a `<match for=T on=fn()>` OPENER is not a reachability root — PA-REPRODUCED at this watermark (`fn pick()` used only as the match scrutinee fires `W-DEAD-FUNCTION`). Gap `g-match-block-opener-expr-not-a-wdead-reachability-root`. Ledger also carries the arrow-callback-body and match-ARM-body false-positive shapes (`G-DEAD-FUNCTION-MISSES-ARROW-CALLBACK-BODIES`, `g-ri-dead-function-match-arm-edges`) — **these are LEDGER-SOURCED, not re-executed this pass.** error.map.md · structure.map.md. |
| **⚑ a page that is DEAD IN THE BROWSER while its own conformance case PASSES — or ANY claim of the form "I verified it by executing the runtime"** | **EXECUTING `SCRML_RUNTIME` IS NOT TESTING WHAT SHIPS, AND THE FULL TEMPLATE MASKS EVERY CHUNK-PRUNING DEFECT BY CONSTRUCTION.** What the browser loads is the **PRUNED per-app `scrml-runtime.<hash>.js`**, assembled by `assembleRuntime(chunkNames)` (`codegen/runtime-chunks.ts:541`) out of `RUNTIME_CHUNKS` (`:467`) over the set chosen by `detectRuntimeChunks` (`codegen/emit-client.ts:828`). The monolith in `compiler/src/runtime-template.js:547` is the SOURCE of those chunks, not an artifact any page ever loads. **Measured at this watermark: the conformance runtime half NEVER exercises pruning.** `conformance/adapters/impl1-ts.ts:467` and `:924` both build the executable as `"(function () {\n" + SCRML_RUNTIME + "\n" + clientJs + …`. **All 883 cases' (b) half run against the FULL template.** **The browser tier is SPLIT, and which half a test is in decides what it can catch** — over `compiler/tests/browser/*.js` (94 files) at this watermark: **25 import `SCRML_RUNTIME`** (blind to pruning), **66 read `result.runtimeFilename`** (the emitted pruned artifact — these are the only ones that can catch it), **2 do both**, **5 neither**. **PA-REPRODUCED, and it is a SHIPPED conformance case:** `conformance/cases/each/ternary-markup-giti033` compiles exit 0 and its `page.client.js` calls **`_scrml_each_clear`** (`:31`, `:101`, `:159`) and **`_scrml_resolve_item`** (`:46`, `:54`, `:66`, …) — **both live in the `reconciliation` chunk, which is NOT in the emitted runtime** → `ReferenceError` on the first each render → **dead page, while the case passes its own suite.** ⚑ **PRECISION THE SYMBOL-DIFF ALONE GETS WRONG:** two further symbols are also absent from the pruned runtime — `_scrml_register_rehydrator` (`:213`) and `_scrml_chunk_loading` (`:220`, both `utilities`) — **and both are `typeof`-guarded at their call sites, so they are HARMLESS.** Only the two BARE-CALL `reconciliation` symbols kill the page. Diff the symbols, then read the call site. **ROOT CAUSE, LOCALIZED AT THIS WATERMARK:** `detectRuntimeChunks` walks AST **nodes** and has **no `markup-value` case**. An `<each>` inside a ternary-markup consequent (`${ @show ? <ul><each …></each></ul> : "" }`) is a `MarkupValueExpr` (`types/ast.ts:2070` — `kind: "markup-value"`, holding `node: ASTNode`) **nested inside an EXPRESSION tree**, so the `case "each-block"` at `emit-client.ts:1650` is never reached and `reconciliation` is pruned. ⚑ **THIS IS THE THIRD INSTANCE OF ONE CLASS AND THE OTHER TWO ARE ALREADY FIXED IN THE SAME FILE, each with the identical failure sentence in its own comment:** `case "engine-decl"` (`:1707`) descends into `bodyChildren` for an each in a non-`initial=` engine arm; `case "match-block"` (`:1750`) raw-text-probes `armsRaw` for `<each`. **The class is: an `<each>` reachable only through a container this walker does not descend into.** **THE RULE:** a browser-tier verification claim must name WHICH runtime it executed. "The runtime ran it" is not a result. See also the `scrml:NAME` stdlib-chunk row below — same registry, different limb (that one is a load-time `TypeError`; this one is a first-render `ReferenceError`). test.map.md · structure.map.md · error.map.md. |
| **a client-side `import … from 'scrml:NAME'`, a bundle `TypeError` at page load, or a blank page from a clean compile** | **START AT `compiler/src/codegen/runtime-chunks.ts:RUNTIME_CHUNK_ORDER`, NOT at `compiler/runtime/stdlib/`. IT WAS DOA FOR 17 OF 21 MODULES UNTIL #669.** A client bundle is a CLASSIC SCRIPT and cannot resolve a bare specifier, so the import lowers UNCONDITIONALLY to `const { … } = _scrml_stdlib.NAME;`, and that property is defined by the `stdlib-NAME` entry in that array **and by nothing else**. Four were registered; the other seventeen compiled clean, emitted a shim nothing loaded, and threw at LOAD, killing the whole page, with zero diagnostics. Now **13 registered / 8 refused loudly (`E-STDLIB-CLIENT-CHUNK-MISSING`, `emit-client.ts:3946`) / 0 silent.** **Membership is DERIVED from the §12.2 Trigger 3 two-limb criterion** (`route-inference.ts:ESCALATION_SERVER_ONLY_MODULES`), not curated. ⚠ **The gate scans the FINAL client text AFTER `pruneUnusedClientImports` and that is deliberate** — gating at the emit site rejected 21 correct corpus files (measured), because the prune drops a lowered read no client code references. ⚠ **A SUBMODULE always fires** (`scrml:auth/jwt` lowers to `_scrml_stdlib.auth/jwt`, parsed as a DIVISION). ⚠ **`W-STDLIB-SHIM-MISSING` will NOT help you** — it probes whether a shim FILE exists, and all 21 do. **Invariant 65.** dependencies.map.md · error.map.md. |
| **a `let`/`const` whose type the compiler says it cannot prove, `W-TYPE-031-UNPROVEN`, or anything touching `inferExprType`** | **READ THE `asIs`/`unknown` SPLIT FIRST (schema.map.md, §7.5/§14.7, #665) — the prior map set said it was NOT ON MAIN and that is now FALSE.** `asIs` = a DEVELOPER signed for it (silent by design); `unknown` = the COMPILER could not tell (nobody signed). `UnknownType.reason` is REQUIRED and `tUnknown()` has no zero-arg overload. `inferExprType` returns `InferenceResult`, so the failure branch cannot be reached by accident. **`W-TYPE-031-UNPROVEN` and `E-TYPE-031` are COMPLEMENTS, not alternatives** — 031 is "I proved it does not fit", UNPROVEN is "I could not prove anything". Two resolutions, both one edit: PROVE it (`x: T = …`) or SIGN for it (`x: asIs = …`). ⚑ **Before you add a `never` fallthrough anywhere in this compiler, know that until #665 NOTHING type-checked it** — no `tsconfig.json`, no `typescript` dep, bun runs `.ts` transpile-only, and NINE sibling exhaustive switches were already failing invisibly. **`bun run types:check` is the instrument; it is `tracking`/non-blocking, NOT `gate`.** **Invariant 66.** schema.map.md · build.map.md. |
| **a `${ if … }` value-form that renders NOTHING, renders once and goes stale, or renders an empty text node** | **THREE INDEPENDENT HOLES, ALL FIXED THIS WINDOW, AND THEY ARE ONE CLASS: correct at TOP LEVEL, silently degraded in a NESTED or INDIRECT position.** (a) **inside an `<each>` body (#670)** — the interp was neither a `bare-expr` nor carried `stmt.raw`, so emit-each fell to `inner = ""`; now lowered to a raw ternary via `_eachValueFormIfRaw` so the shared `lowerEachExpr` does the `@.`/`@cell` lowering uniformly. (b) **an empty-string branch `{ "" }` (#672)** — `parseLogicBody`'s blank-token skip tested `tok.text.trim() === ""`, and an empty-string STRING literal has blank `.text` while being a MEANINGFUL expression statement; the fix excludes `tok.kind === "STRING"` in **BOTH** loops (`ast-builder.js:6052` and `:11014`) — **they are patched in LOCKSTEP, change one and change the other.** (c) **a fn-call CONDITION (#673)** — the effect-vs-static decision string-scanned the LOWERED value for `_scrml_reactive_get(...)`, which sees a DIRECT cell read but not one hidden inside a called fn; ANY call now counts as potentially-reactive. ⚑ **The fail-safe DIRECTION generalises: a false positive is a needless effect that never re-fires, a false negative is a stale display — bias toward the effect.** ⚠ **STILL OPEN: a value-form `match` in an each interp is dropped** (`g-each-inline-value-form-match-or-markup-branch-interp-dropped`).  ⚠ **AND THE RESIDUAL IS SILENT, NOT LOUD — CORRECTED #678 (comment-only, this window).** The in-source comment at `codegen/emit-each.ts:1420` used to claim the markup-valued branch "is captured in `_droppedCFStmt` so the skip below WARNS instead of dropping silently." **Both halves were false and are now corrected in place:** `_droppedCFStmt` exists NOWHERE in the tree (grep-verified at this watermark — the comment was its only occurrence), and the skip emits a JS COMMENT into the artifact (`// each: empty logic interpolation skipped`), which is **not a diagnostic**: exit 0, nothing in `result.warnings`. **Do not read that path as loud.** The fix direction is a real `W-EACH-VALUE-FORM-CONTROL-FLOW-DROPPED` + its §34 row. domain.map.md. |
| **a library-mode `fn` `match` arm returning `undefined`, or an object-literal arm** | **PARTLY FIXED (#664, §18) — and the residual is HIGH.** `1 :> { x: 1 }` emitted BARE; JS reads it as a labeled-statement block, the IIFE falls off its end, and the fn silently returns `undefined`. `emitIifeBlockArmBody` (`emit-control-flow.ts:2115`) now emits it in RETURN position via `emitExprField` + `_awaitMatchArmServerCalls` (so a server call inside the object is auto-awaited). ⚠ **The object branch runs BEFORE the empty-`inner` guard so an empty OBJECT `{}` returns `{}` — do NOT reorder them**, that is the exact regression the ordering prevents. ⚠ **OPEN, HIGH: the `else`/wildcard arm was NOT covered** — `g-library-fn-match-else-arm-object-literal-returns-the-bare-identifier`. Same landing added the IIFE-header async scan to `emitMultiScrutineeMatch` (`:2065`), which reads the EMITTED BYTES rather than re-deriving a predicate. |
| **a server-fn call NESTED inside a larger expression (`call().length`, `f(call())`, `call() > 0`) rendering `""` or `undefined`** | **OPEN, HIGH, ROUTED NOT FIXED (S370-peter).** Not awaited at the INNER call site in markup interps and inline event handlers; exit 0, no diagnostic. **The asymmetry was EXECUTED, not inferred: fn bodies and direct cell-assign are correct, so the class is POSITION-SCOPED.** Deliberately routed rather than patched in-lane because it is the §13.2-SHALL-by-RETROFIT axis. Entry: `g-server-call-nested-in-expression-not-awaited-outside-fn-body`. |
| **a request pipeline — `handle()`, `cors=`, `log=`, `ratelimit=`, `headers="strict"`, or "which middleware runs on this request"** | **START AT `compiler/src/commands/select-request-onion.js`. NOT at per-route emit — the entry point MOVED this window (#654).** `handle()` now wraps **TOP-LEVEL DISPATCH** (§40.3.4: "applies to all HTTP requests handled by the compiled server — including statically-served assets"); it used to be applied per-route, so a `handle()` intercepting a custom path 404'd before it ran. `selectRequestOnion(serverModules)` picks the ONE onion and BOTH hosts import it (`commands/build.js:22`, `commands/dev.js:23`) so dev and prod cannot diverge. Two modules declaring a pipeline → **`E-MW-007`** (the only emit site is `select-request-onion.js:72`). **What COUNTS as declaring one:** a `handle()`, or `cors=` / `log=` other than `"off"` / `ratelimit=` / `headers="strict"`. **What does NOT:** `batch-in-list-cap=`, `idempotency-store=`/`-ttl=`, `cors-max-age=`, `channel-reconnect=` — same config bag, no pipeline stage. Emitted shape: `_scrml_dispatch(req, server)` wrapped by `_scrml_onion_dispatch` (`commands/build.js:570`/`:577`, NOT `emit-server.ts` — ⚑ this row carried the #654 wrong-FILE citation, uncorrected through S376 and S380 until this pass; see invariant 72); with no onion the output is byte-identical to pre-onion. auth.map.md §40.3. |
| **an OPTIONS/CORS preflight that comes back stamped by `handle()`, or 401/403 on a preflight** | **FIXED (#654) — the preflight is now pipeline STAGE 1** (`emit-server.ts:3109-3127`, ⚑ S380: was `3097-3115`, +12, §52.13), ahead of logging, rate-limit and `handle()` PRE, and it SHORT-CIRCUITS. It was previously only the `_scrml_cors_options_route` registry entry, so **an author `handle()` ran on every browser preflight — measured.** The domain reason to keep it first: **a preflight carries no credentials**, so an auth-enforcing `handle()` rejects it and the browser's real request is never sent. §40.3.3 pins `[CORS preflight] → [rate limit] → handle() PRE`. |
| **`ratelimit=` 429'ing a first-time visitor on their own first page view** | **FIXED (F1, S355) — `ratelimit=` is a PER-ROUTE concern again** (`emit-server.ts:3009-3032`, ⚑ S380: was `2997-3020`, +12, §52.13, §4.15/§40.2). Applied app-wide it counted the HTML + CSS + runtime + client-bundle sub-requests of ONE page load, so `ratelimit="3/min"` 429'd a visitor on their first view. It now counts only requests a ROUTE serves. **If you are tempted to re-widen it, read §4.15's per-route list first.** |
| **a §38 `transition:` / `in:` / `out:` that does nothing, especially under `headers="strict"`** | **THE KEYFRAMES MOVED OUT OF THE RUNTIME AND INTO THE STYLESHEET (`codegen/emit-transition-css.ts`, NEW).** They were injected by a `document.createElement("style")` IIFE (runtime chunk `'transitions'`, now **RETIRED**), and `<program headers="strict">` pins `default-src 'self'` (§39.2.5) which refuses an inline `<style>` — **so a strict-headers app lost EVERY transition with no author-side fix** (§39.2.5's `handle()` escape covers author-loaded external origins, not compiler-emitted content). Two exports: `collectUsedTransitions(nodes)` (AST walk) and `renderTransitionCss(used)`; wired at `codegen/index.ts:59` / `:1754` / `:2134`. ⚠ **DO NOT "optimise" the app-wide union out of the `<program>` shell entry** — a §20.8.2 SOFT nav swaps markup into the SHELL's document and never loads the target page's stylesheet (`_scrml_nav_sync_head` syncs title/description/canonical only), and soft nav is the DEFAULT path. **Any new per-page emitted asset has this same hole.** |
| **the SSR seed / `window.__scrml_ssr_state` / a CSP error on first paint** | **THE SEED IS NO LONGER AN INLINE EXECUTABLE `<script>`.** It is `<script type="application/json" id="__scrml_ssr_state">` read via `JSON.parse`, for the same §39.2.5 `default-src 'self'` reason as the transitions. **BOTH readers changed — `runtime-template.js:2767-2788` and `:6090-6114`** — so if you are patching one, patch the other. The type check is exact (`type === "application/json"`, lowercased). |
| **a delta-log entry that vanished from the digest, or a `[NNNN]` collision** | **`bun scripts/delta-lint.ts` — NEW, and it is a BLOCKING `ci.yml` step (#652).** The sequence is a shared counter under a single-writer rule that stopped being true at two concurrent operators, and the flogence bridge uses the number as a CHECKPOINT CURSOR — so **two entries sharing a number means the second is silently dropped from the digest.** Exit 1 = a NEW duplicate; **exit 2 = the instrument could not account for the population** and is NOT reachable as a PASS. Nine pre-existing collisions are BASELINED (`handOffs/delta-log-dupes.baseline.json`); the baseline may shrink and must never grow silently. `--fix` renumbers but **REFUSES unless every bracketed line parsed.** Companion: `.gitattributes` `merge=union` — the two land together, neither is sufficient alone. |
| **a gate/probe that reports PASS — before you believe it** | **READ INVARIANT 59. Five gates were reporting green while measuring nothing (#646).** The species: parse a population out of a text file, silently drop the part the regex cannot match, report PASS on the remainder. The fix pattern, now in both `scripts/delta-lint.ts` and `scripts/state.ts`, is an **unconditional `bracketed !== parsed` refusal** — count the candidate lines and the matched lines separately and REFUSE when they differ (`state.ts:706 refuseUnparsedDeltaEntries`, called from `runWrite`/`runCheck`/`runDigest`). What made it necessary: the entry shape was three tokens, the convention had drifted to four (an emoji marker), and four live entries were invisible to the gate AND the digest. **Same species, different surface: a CI step named "within-node parser-parity + canary" never ran the canary.** A step name is read as a coverage claim — name it after what it RUNS. |
| **writing or auditing a conformance case's `expect` block** | **THE VOCABULARY IS NOW A DECLARED TABLE — `EXPECT_SHAPES` at `conformance/run.ts:179`, enforced by `validateExpectContainers` (`:218`, called at `:418`).** Fifteen keys, each with a declared kind. **The load-bearing column is `empty`:** an empty `{}` is REJECTED for every ASSERTION container (`severity`, `codeCounts`, `state`, `firstPaint`) because an empty assertion asserts nothing and reads green; it is ALLOWED only for `serverStub` and `serverDb`, which are INPUTS (a mock table and a seed). `validateExpectContainers` is EXPORTED so it is drivable from a test — *"a validator that can only be reached by running the whole corpus is indistinguishable from one that never fires."* ⚠ **And the standing trap: a grep-match for an E-code inside a case directory is NOT proof the case asserts it** — the hit may be rationale PROSE. Read the `expected.json`. test.map.md. |
| **counting new diagnostic codes across a window** | **DO NOT grep the SPEC DIFF for added `E-*` tokens — it returns false positives and this pass measured two of three.** `E-PROGRAM-002` and `E-IMPORT-005` both showed up as "added" while already being in SPEC at the base; the diff had caught a new PROSE MENTION. **A code is new only when `git show <base>:compiler/SPEC.md \| grep -c '<CODE>'` is 0.** Cross-check against the census total. And the two are not the same kind of thing: `E-PROGRAM-002` has **no emit site at all** (SPEC `:22811` says "TBD"), `E-IMPORT-005` is **fully live** (`module-resolver.js:206`). error.map.md. |
| **a `bun test` failure — ANY failure, before you trust that it is an assertion** | **IT MAY HAVE TIMED OUT. bun marks a timed-out test with the SAME `(fail) <name>` marker an assertion failure produces** — a synchronous overrun completes its assertions GREEN first — and the tell (`^ this test timed out after 5000ms.`) sits on a separate line that `spawnSync` captures swallow. The REAL per-test budget is bun's default **5000 ms** everywhere (`bunfig [test] timeout` is a key bun never read — deleted #537; several old test comments still cite "the bunfig default 10s", which was never in force). Browser tier: `browser-baseline.ts` now prints the reason excerpt beside every new name. Invariant 56. |
| **`scrml dev` serving a stale/partial bundle, or a runtime `X is not defined` right after an edit** | **FIXED, AND THE #539 CAVEAT IS RETIRED — it LANDED this window (`7c4b8345`).** While the last compile is FAILING, EVERY non-infra request serves the real compile error (HTML overlay with hot-reload for `Accept: text/html`, JSON otherwise); last-good disk output is never served while broken (#518, adopter #517). **#539 added the other half: fail-CLOSED on a `compileScrml` THROW** (not just on returned errors), dir-level watches, a 250 ms max-wait debounce and `ctimeMs` in the stat snapshot. State: `noteCompileResult()` / `getCompileFailure()` / `compileThrowDiagnostic` in `commands/dev.js`. *(Prior generations of this row said "IN FLIGHT, NOT ON MAIN" — that is no longer true.)* |
| **emitted output that differs between two machines / two runs / two file orders** | **Two different defects — scope which one you have.** Directory-walk order: FIXED (#528 + the test-side sorted walks #527/#530/#534) — and `scanDirectory` was already terminally sorted, so if you reproduced via `scrml compile <dir>` the walk was never your cause. **Caller-supplied `inputFiles` order: OPEN, HIGH** (`g-compilescrml-input-order-dependent-emission` — 79/115 files differ, route URLs included). Invariant 58. |
| **an open adopter GitHub issue nobody has picked up** | **`bun scripts/issue-debt.ts` (#536)** — every OPEN issue must have a HOME (`docs/known-gaps.md` entry or `handOffs/dpa-queue.md` item) or it prints OWED (0 comments + >2 days = SILENT, the loud shape). Runs at boot via `scripts/boot.ts`; `--check` is for the PA's hand, NEVER CI. |
| **a `<timer>` / `<poll>` / `<timeout>` body that runs ONCE at page load before the timer ever fires** | **FIXED (#510) — and the fix is a THREE-TAG DENY-SET, not a category. Invariant 53.** `collect.ts`'s `collectTopLevelLogicStatements` no longer descends `DEFERRED_LIFECYCLE_BODY_TAGS = {timer, poll, timeout}`; descending them collected the tick/timeout payload a SECOND time as a top-level statement. **`<request>` and `<channel>` are DELIBERATELY EXCLUDED from the set** — for `<request>` the descent IS the designed canonical-form fetch, and for `<channel>` the top-level `${}` is single-run init the channel emitter does not re-emit, so adding either would DELETE a required emission. Same PR: `<poll>` now fires an immediate first tick (§6.7.6) via a fifth `immediate` arg to `_scrml_timer_start`, gated on `running=` and never re-fired on resume; `<timer>` passes no such arg (§6.7.5). domain.map.md. |
| **a `<timeout>` element that fires `E-MARKUP-001` in every position** | **FIXED (#510, one-word fix, S340-peter).** `name-resolver.ts:150`'s `SCRML_NON_ELEMENT_TAGS_EXTRA` lifecycle-keyword list omitted `timeout` (§6.7.8), so an all-lowercase non-HTML tag failed the E-MARKUP-001 gate everywhere. **The generalisable bit: this list is hand-maintained beside a DERIVED one** — the comment on the next block says the structural exclusion is DERIVED, not hand-copied. A new lifecycle tag must be added here consciously; nothing checks the two agree. |
| **an `<each>` list that renders COMPLETELY EMPTY at runtime with a clean compile** | **NOW REJECTED AT COMPILE TIME (#511-adjacent, `E-EACH-BODY-DECL-UNSUPPORTED`, §17.7.3).** The cause was a `let`/`const`/`function` declaration in an `<each>` body interpolation (`${ let nm = @.name }`): it has no `exprNode`/`raw`, so codegen dropped it silently, while a later `${nm}` still lowered to a bare `String(nm)` — a dangling reference that throws inside the per-item render factory and kills the WHOLE list, at exit 0. **The each-body scope (§17.7.3) is the `@.` sigil plus an optional `as` alias — NOT author-declared locals.** The rejection is fail-CLOSED and deliberately does not pre-decide the feature: supporting author locals (replay the binding into the factory closure, as the `for`-lift path does) is a separate §17.7.3 language-surface ruling. |
| **an `<each>` over server data that ships EMPTY in the server HTML / has no SSR first paint** | **NOT A BUG — it is the pre-existing conservative fallback, and it is now LOUD (`I-SSR-EACH-CLIENT-RENDERED`, §52.8, info).** `buildOneRenderer` (`emit-ssr-render.ts`) returns a `{ fallback: string }` descriptor naming WHY instead of a bare `null`, and `emit-server.ts:5162` turns it into the lint. Reasons it names: multi-root or non-markup-root row template, a non-field-read interpolation (call / ternary / method / `@cell` read), a non-literal attribute value, `if=`/`show=`/directive/reactive attributes, or nested `<each>`/`<match>`/component rows. **The lint SURFACES existing behaviour — it does not change what compiles, and it is never fatal.** Widening the subset is the ruling-gated `g-ssr-each-row-template-subset-blocks-all-prerender`. |
| **you are about to write a REGEX over source text in a stage that already has the AST** | **STOP — read invariant 55. There is a RULE (bryan, S338) and a probe (`scripts/source-text-regex-census.ts`).** A regex applied to SOURCE TEXT in a POST-AST stage requires a one-line justification, or the structural route. Binds NEW-OR-TOUCHED code only; pre-AST stages (tokenizer, block-splitter) are out of scope. **Five instances by five uncoordinated authors in one session, none caught by a 22,385-test suite or a 7,375-artifact differential** — the class is invisible to differentials by construction, because a regex over source text is indistinguishable from a correct check until someone writes a string literal. |
| **you are booting a session and want to know what the boot MUST read** | **`bun scripts/boot.ts` (#492/#493/#498) — the executable read-set gate.** It fetches both repos read-only, verifies every Profile-A read-set source exists and is current, delegates the mandatory probes to their authoritative scripts (`review-debt.ts` · `threads.ts`) rather than reimplementing them, and prints the `## ⏭ NEXT-SESSION PICKUP` block FIRST. **Default run is DETECTION, not control** — `--check` is the strict mode and exits 1 on exactly two classes (a MISSING read-set file, an ABSENT PICKUP block); a repo merely BEHIND origin is a warning, because that is timing, not a defect. **Residual it names about itself:** the manifest mirrors the `.pa-base/profile` Profile-A block and each item carries a `needle` proving its mandate, but the REVERSE direction (the contract adding a read the manifest lacks) is not covered. build.map.md. |
| **you are adding or editing a TEXT PASS over emitted JS (a rename, a lowering, a rewrite)** | **STOP and read invariant 43 first.** The rule: **change the pass's INPUT, not its PATTERN.** `code-segments.ts` holds TWO region classes — LEXICAL (`rewriteCodeSegments`) and STRUCTURAL (`findObjectShorthandRegions` / `classifyBraceGroup`) — and they COMPOSE. If you need to exclude a compiler-owned span, splice it in AFTER the pass (`joinAroundRuntimeSlot` is the worked example) rather than teaching the regex to recognise it. **Do NOT add a sixth lookaround to the fn-name mangler.** Still-open shapes are pinned as failing-by-design in `compiler/tests/unit/mangler-region-fencing.test.js` §2f — read that before assuming a shape works. |
| **a server-fn route handler's return value / an HTTP 200 with a wrong body / a missing `Set-Cookie`** | **FIXED (#452), NORMATIVE SINCE #460 (`SPEC.md:7353`), and the ORDER at the exit is normative — invariant 44.** Every handler terminates in a `Response`. `instanceof Response` passthrough sits BEFORE `_egressRedact` (else a 403 comes back 200 — fail-OPEN, measured); `_egressRedact` runs BEFORE `JSON.stringify` (else the §14.8.9/§14.8.10 floors leak). **If a session cookie is missing, check this first** — `_scrml_session_cookie_wrap` skips a return value with no `.headers`, so the pre-fix bare return dropped the sid silently. domain.map.md §12.5. |
| **`session.get(k)` / `session[k]` — what is guarded and what is not** | **PARTIALLY closed this window, and the ledger has NOT caught up.** The accessor is `Object.hasOwn`-guarded (`emit-server.ts:2593`, landed in #452), so the PROTOTYPE-CHAIN read and the `?{}`-bind HTTP 500 are CLOSED. **Still open:** every OWN key of `_rec` reads by an attacker-chosen key, including the §40.2 `csrfToken` — `g-session-get-reserved-key-read-disclosure` (MED, open, ROUTED TO BRYAN). **Read auth.map.md's "Session read-side" block, NOT the gap entry** — the entry's locus and its "no hasOwnProperty" claim are both false at this HEAD (non-compliance.report.md S326-N2). |
| **CI has no checks on your PR and the branch cannot merge** | **First check the boring cause (NEW #532): a branch push alone no longer triggers CI — open the PR and the `pull_request` run fires.** For the dropped-webhook case there is a lever (#454) and it has ONE trap. `gh workflow run CI --ref main`. **`--ref <your-branch>` returns HTTP 422 if the branch was cut before #454 landed** — the dispatch reads the workflow definition from the TARGET ref, so **the lever is prospective, not retroactive.** Rebase onto main first, or dispatch `--ref main`. Do NOT waste a session on force-push / close-reopen / a new commit / a new PR: all four are just another webhook into the same throttled pipe, and all four were measured to produce nothing. build.map.md. |
| **you are CHANGING ANYTHING under `compiler/src/codegen/` and need to know it did not break the corpus** | **`bun scripts/corpus-emit-differential.ts` — the standing PRE-LAND GATE (#428). It is NOT in `ci.yml`, NOT in `bun test`, NOT in a hook. Run it BY HAND, base-vs-head.** `capture` each side, then `diff`. 1878 sources / 7254 artifacts over `examples,samples,conformance,stdlib,benchmarks`. Reports compile-failure delta, artifact-set delta, artifact-CONTENT delta, syntax delta under three goggles, and the bare-server-fn-site count. **`diff` exit 2 means NOT A VALID COMPARISON — do not read it as "no differences".** Full invocation + flags in build.map.md; the why-it-cannot-be-`node --check` argument in invariant 41. |
| **"is this callee async?" / adding an await injector / an async predicate** | **STOP — there is exactly ONE provider (#442). `async-combinators.ts`'s `isAsyncCalleeName(name, AsyncNameFacts)`.** Do not hand-write a fourth disjunct; hand your consumer `AsyncNameFacts`. Mode selection lives in ONE place, `emit-expr.ts:asyncNameFactsOf`. Invariant 39 + dependencies.map.md's "ASYNC-NAME PROVIDER" row. |
| **a client->server call that hands back a pending Promise instead of a value** | **PARTLY FIXED (#429) and the map says so on purpose.** `emitCall`'s fourth await branch (`isClientServerFnCall`, emit-expr.ts:3274) awaits at the CALL SITE, which reaches receiver-tail and nested-argument positions. **It did NOT close the class: 142 bare sites remain in cleanly-compiling sources, delta 0** — `g-auto-await-family-not-closed-150-bare-server-call-sites-in-clean-sources` (HIGH, open; the id bakes the 150 harness figure, the landed count is 142). Unreached shapes: the CPS/failable-fn wrapper, module top-level init, the markup-interpolation lift. Invariant 38 + domain.map.md §13.2. |
| **`reset(@cell)` giving you a Promise instead of a value** | **FIXED (#662, §6.8) — this row said OPEN/HIGH/not-built for the whole prior window and that is now WRONG.** `_scrml_reset` re-invoked the init thunk with **no `await`**, so a cell whose init calls a server fn was correct at mount and became a raw Promise on reset. Both re-invocation paths (`runtime-template.js:1214` default thunk, `:1219` init thunk) now route through **`_scrml_reset_apply(name, r)` (`:1179`)**, which detects a thenable and applies the resolved value through `_scrml_reactive_set`, with `_scrml_error_boundary_log` on rejection. ⚠ **Read `Promise.resolve(r)` FIRST — it is a CORRECTION, not a flourish.** A bare `r.then(...).catch(...)` assumes `.then` returns a promise: true of a real Promise, **NOT** of an arbitrary thenable. `{ then: (res) => res(99) }` returns `undefined` from `.then`, so `.catch` throws a `TypeError` **SYNCHRONOUSLY out of `_scrml_reset` and out of the adopter's event handler**, aborting the rest of it. Adopting the thenable first is also what makes the declaration-path parity claim TRUE rather than merely plausible — `await` on that same value resolves to 99 without throwing. |
| **`session.*` inside a `?{}` SQL interpolation / an HTTP 500 `ReferenceError` from a server handler** | **FIXED (#435 GH #357, #440).** This is the DANGLING-REFERENCE class — see invariant 42. `session` is bound in the handler prologue **as a Proxy** (`_scrml_session_bind`); a raw bind would disclose `_rec`/`csrfToken` at HTTP 200. `@currentUser` and `<channel auth=>` got the same treatment. **Two auth gaps are open and ROUTED TO BRYAN** — read auth.map.md's "Session read-side" block before touching the accessor. |
| **an emitted bundle that is dead on arrival in the browser while the compiler reports green** | **A goggle problem, not a diagnostic problem.** `node --check` accepts a top-level `await` in a bare `.js`; the compiler emits classic `<script src=…>` where it is fatal — **and bun's `vm.Script` does not reject it either.** invariant 41 + build.map.md + error.map.md's "Emitted-artifact SYNTAX" section. Live instance: `g-stdlib-module-resolver-emits-import-meta-into-a-classic-script-bundle`. |
| **"where is per-chunk MODULE-INIT emitted, and what owns the boundary with the soft-nav rehydrator?"** | **`codegen/emit-client.ts` (assembly) + `codegen/emit-reactive-wiring.ts` (the lifecycle bodies) + `codegen/emit-event-wiring.ts` (the boundary).** Unchanged this window. Full table in dependencies.map.md. |
| **a route's `<timer>`/`<poll>` that keeps firing after a soft nav** | **domain.map.md "The route region is a THIRD lifecycle owner"** — still open (`g-route-timer-poll-not-stopped-on-soft-nav`, HIGH). |
| **a `fn`/`function` whose body is `= <expr>` and now rejects at parse time** | **`E-FN-EQUALS-BODY` (§48.2, NEW #396).** `ast-builder.js` `rejectFnEqualsBody` (:3755), four decl-body call sites + the export re-parse. Sibling of `E-FN-ARROW-BODY`. Fix: use a block body (`{ return <expr> }` or the tail-return `{ <expr> }`). See error.map.md / domain.map.md. |
| **`<page keep-alive>` — what it actually does today** | **NOTHING at runtime yet.** The attribute is admitted (parses, validates, five-member `E-PAGE-INVALID-ATTR` set) but there is no cache and no invalidation wiring — Nominal/spec-ahead. See domain.map.md invariant 29. |
| **a server call inside a value-form `match` arm that isn't awaited** | **`scheduling.ts` `parenthesizeAwaitServerCallsInExpr` (NEW #394)** — parenthesize-wraps the call (`(await fn()).field`), not a bare prefix. Consumed by `emit-logic.ts`'s value-form match-arm lowering. See dependencies.map.md invariant 31. |
| **a diagnostic code — ANY code, any prefix** | **error.map.md**, starting at "HOW TO LOOK UP A DIAGNOSTIC CODE". **RUN `bun scripts/s34-census.ts` FIRST, ON ANY PLATFORM** — it answers the lookup in one command; **RE-EXECUTED AT THIS WATERMARK it returns 812 rows**, `§34 19352..20235`, 1958 source files, 883 conformance cases. ⚠ **THIS ROW SAID `810 rows (+1: E-MW-007)` FOR TWO WINDOWS WHILE `error.map.md` AND THE MAP INDEX ROW IN THIS SAME FILE BOTH SAID 812** — a carried S368 figure that survived a pass which had already corrected it elsewhere. Buckets AT THIS WATERMARK, executed not carried: **STRUCK 34 · PINNED 343 · IMPL-SITES 302 · DECLARED-AHEAD 18 · RUNTIME-SURFACED 3 · FALSE-CLAIM 112** (dispositions: BUILD-ARC 71 · HOME-NO-SHALL 27 · NOMINAL-HOME 10 · ORPHAN-INDEX 4). `--full` returns the SAME `PINNED 343` — so test.map.md's *"338 PINNED at this HEAD"* is stale too. **Zero new codes this window**, and `compiler/SPEC.md` is `--name-only` EMPTY. ⚠ **The BUCKETS moved 20x further than the total and it is the INSTRUMENT, not the catalog** — IMPL-SITES 320→300, FALSE-CLAIM 95→112, DECLARED-AHEAD 14→18, because #646 rewrote the classifier and added a disposition table. **Do not diff those figures across the instrument change.** ⚠ **Do NOT quote its `source files` figure as a repo fact** — it walks the filesystem, not the index, and printed 1858 / 1864 / 1940 at three stamps for the same reason. ⚠ **And do not count new codes by grepping the SPEC diff** — see the dedicated routing row above. |
| **the CPS auto-await choke point / `given`/`if`/`match` scheduler boundary** | **LANDED (PR #405, `649d6fce`).** `scheduling.ts`'s `isControlFlowBoundary` no longer fences the fn-body auto-await walker out of `given`/match-block/`try` bodies; `injectPromiseAwait` is retired in favor of the shared `collectAwaitSites`/`applyAwaitSites` walker. See dependencies.map.md's "§13.2 CPS auto-await CHOKE-POINT" row for the mechanism and which gaps it closed. |
| **an `<each>` per-item body inside `<textarea>` / `<option>` / `<title>` — or ANY "can this body take an element child?" question** | **THERE ARE FOUR ANSWERS AND THEY DISAGREE. Read domain.map.md's RESTRICTED CONTENT MODELS table before you touch one.** #466 shared ONE local (`_isRcdataBody`, `emit-each.ts:1169`) between the `:`-shorthand and bare-body branches — **that is two of the four.** Still separate: `eachRcdataValueExpr`'s null-bail (falls through to the mounting recursion, so `<textarea>${x}<b>y</b></textarea>` STILL gets an element child), `emit-html.ts:1835`'s top-level check (**the only site that fires `W-RCDATA-BIND-VALUE-CONTENT-CONFLICT` — the `<each>` path is silent**), and the Tier-0 `lift` path (zero `rcdata` references, never asks). **Scope is RCDATA ONLY, deliberately: `<option>` does NOT lose data, and lowering it to `.textContent = String(expr)` replaced a correct label with `"[object HTMLElement]"`.** `each/shorthand-option-label-preserved` is a counter-gate that will fail you for re-widening. |
| **a `show=` element that flashes visible before hydration, or a `display:none` you expected in the SSR HTML** | **THERE IS NO SSR HIDE. #464 reverted #450 in full** — `emit-html.ts` is byte-identical to `71623be3`, `buildInitialBoolMap` does not exist, and `ctrl-017..ctrl-020` assert its absence. **Do not re-land it without reading invariant 47:** an emit-time hide is baked into the `<match>` arm-render `innerHTML` string, and nothing re-binds a visibility toggle on a variant swap, so it can never be cleared. If you want to fix first paint, fix the `_scrml_nav_rewire` variant-render rewire hole FIRST — that is the actual blocker and it is tracked separately. |
| **a `match` block-arm that silently evaluates to `null` / `undefined` / an empty interpolation** | **READ INVARIANT 49 FIRST — THERE ARE FOUR EMISSION ROUTES AND YOU MUST SCOPE FROM THE LAYER THAT MOVED.** Grep **`_blockTailIsValueExpr`** (`emit-logic.ts:4700` — the ref drifted from `:4653` this window; `planBlockArmLift` is now `:4762`), NOT `planBlockArmLift` — the latter finds only the two RAW-STRING routes. Three landed classes, all fixed at HEAD, all with **no diagnostic** by design: (a) #463 — a tail merely PREFIXED by a keyword (`formatted`/`for`) read as a statement; (b) #469/#470 — a member/index-assignment tail LIFTED on one route and VOIDED on another, **two routes disagreeing about the same source**; (c) **#479 — a tail after a `}`-terminated block statement was swallowed into the `for`-headed segment, so the arm evaluated to `undefined`, which does not exist in scrml (§42.1.1). That defect was SEPARATOR-dependent, not position-dependent**, which is exactly why the corpus never tripped it. domain.map.md §18.5. | ⚠ **THIS ROW NOW CONTRADICTS THE SOURCE AND THE GAP LEDGER, AND THE CONTRADICTION IS UNRESOLVED.** `emit-control-flow.ts:2078` and `docs/known-gaps.md:8508`/`:8515` both call `planBlockArmLift` "the single §18.5 classifier", while this row says it finds only the two RAW-STRING routes. The divergence gap (`g-match-block-iife-tail-classifier-diverges-from-shared-plan`) is marked RESOLVED. **Adjudicate by EXECUTION before trusting either.** non-compliance.report.md N9.
| **a server-only stdlib (`scrml:auth`/`crypto`/`oauth`/…) reaching the browser bundle** | **THE DERIVED-CELL HALF IS NOW CLOSED AT EVERY POSITION (#500) — the other two positions are still open. Invariants 50 + 52.** §12.2 Trigger 3 escalates a **function** and reaches NO other position (`SPEC.md:7312`, normative). A **derived cell** RHS is REFUSED (`E-DERIVED-SERVER-ONLY-REACH`, §6.6.19; Step 3b at `route-inference.ts:4526`, emit at `:4572`) **wherever it sits** — the collector `collectDerivedCellDecls` (`:3730`) is now a STRUCTURAL walk, not a `body`/`children` field list, which is what closed six measured leaking positions (`for`-lift body, `while`-lift body, `<each>` row body, `<engine>` state-child body, loop-inside-conditional, and each shape inside a `kind="tool"` program). **A mutable-cell initialiser and a markup interpolation are STILL OPEN and still leak at exit 0 with zero diagnostics.** Symptom to recognise: no `.server.js` emitted at all, `const { hashPassword } = _scrml_stdlib.auth;` in the client bundle, a real `Bun.password.hash` in the shipped runtime. auth.map.md · domain.map.md. |
| **a `.prepare()` on a `?{}` SQL result / a diagnostic that "exists but never fires"** | **`E-SQL-006` (§44.3) now fires at COMPILE time on every server-fn emit path (#476) — and the FIX is the lesson.** The detector was always correct; the **narrow sink was not drained**. `emit-logic.ts`'s `case "sql"` pushes into `(opts as any).preparedStmtErrors`, and the broad `opts.errors` sink is deliberately NOT wired on those paths. **Before concluding a detector is wrong, check whether its sink is threaded and drained on the path you are testing.** error.map.md. |
| **a `<#request>.data is some` predicate in an attribute** | **THE TWO PER-ITEM PATHS ARE NOW CLOSED (#511 `<each>` body attr, #512 `${for…lift}` attr) — invariant 54.** Fixed across three PRs: top-level `value=` / Boolean attr / `class=` / `class:x=` / `if=` / `show=` (#484), the per-item `<each>` body attr (#511, via a module-level `_eachRequestIds` stash from `collectRequestIds`, cleared in the same `finally` as `_eachBindSupportCtx`), and the Tier-0 `${for…lift}` attr (#512, via `reparseLiftAttrRequestRef` -> `reparseRequestRefEscapeHatch` — a DIFFERENT mechanism, because `shouldSkipExprParse` skips any `<`-leading expr so there is no structured node to thread into). **Every one is GATED to REGISTERED request ids, so a non-request `<#id>` emits byte-identically to pre-fix.** ⚠ **Still open and it FAILS IN THE OPPOSITE DIRECTION: a mixed-text attr template FAILS LOUD** (`emit-bindings.ts:423` → `E-CODEGEN-INVALID-LOGIC`, no bundle written) — verify against `docs/known-gaps.md` before scoping. domain.map.md §6.7.7. |
| **read-side diagnostics (e.g. `E-STATE-UNDECLARED`) not firing inside an `<each>` in a `<match>` arm** | **FIXED (#477, §6.1.1).** The arm body was BLANKED by the ast-builder to avoid the S153 double-emit; `type-system.ts` now re-parses it LOCALLY off a stamped raw body + absolute coordinates. **Three properties make that safe and each is reusable:** the nodes are throwaway and never attached to `fileAST`; a **disjoint id range** is claimed because `buildAST` resets its counter per call and colliding ids clobber the `nodeTypes` memo; the **real `filePath`** is passed (a suffixed one breaks editor jump-to-location). domain.map.md. |
| **you are tracing the compiler's call flow and `compiler/src/codegen/` looks unreachable** | **IT IS NOT DEAD — `api.js:2409` is `const _runCG = selfHostModules?.runCG ?? runCG`.** The Stage-8 call target is a runtime-selected variable, so any static call-graph walk from `compileScrml` terminates there and reports ~35 codegen files as unreachable. **Enter at `codegen/index.ts`'s `runCG` directly.** ELEVEN stages carry the same `selfHostModules` override seam (`api.js:665-677` enumerates them) and every one breaks reachability the same way. structure.map.md Entry Points. |
| **a per-row `if=` on a NESTED element inside `<each>` that silently goes stale** | **`W-IF-IN-EACH` now WARNS at compile (§17.1, #416, GH adopter #409).** The gate is still create-time-only — the WARNING shipped, not the reactive fix (DEFERRED, routed to bryan). See domain.map.md. |
| **`reset(@cell)` restoring the wrong value after a top-level reassignment** | **FIXED (§6.8, #417, HIGH).** `_emitInitThunkSidecar` now skips the reset init-thunk for a reassignment of a structurally-declared cell (`collectStructuralDeclNames`). An implicitly-declared cell written TWICE at top level still has a residual gap — `g-implicit-cell-double-write-clobbers-reset-init` (MED, open). See domain.map.md. |
| **build commands / CI stages / a gate decision** | build.map.md. **`gate` is UNCHANGED at 13 steps this window.** The new `bun scripts/types-gate.ts --check` step is in the NON-BLOCKING `tracking` job, `continue-on-error` at **both** job and STEP level — the step flag is **not** redundant, because a failed step halts the job even in a `continue-on-error` job. **`package.json` gained `types` / `types:check` and the dev dep `typescript@^5.9.2` — the first manifest movement in eleven windows.** |
| **types / interfaces / AST node shapes** | schema.map.md — **RE-WALKED this pass after a streak of currency-only passes, because its header carried a "NOT ON MAIN, NOT MAPPED" exclusion for the `asIs`/`unknown` split and that split LANDED (#665).** `compiler/src/types/ast.ts` is zero-diff for the TWELFTH window and no EXPORTED type was added anywhere in the compiler; the three new shapes (`UnknownReason`, `InferenceGap`, `InferenceResult`) are module-private to `type-system.ts`. |
| **environment variables / config keys / CI secrets** | config.map.md (watermark `8b2e4053`, **currency-VERIFIED not re-walked** — RE-MEASURED at this watermark: `grep -cE '^[+-].*(process\.env\|Bun\.env)'` over the WHOLE window's `compiler/ scripts/ .github/ package.json` diff returns **0**, and `.env*` / `bunfig.toml` / `tsconfig*` / `package.json` / `bun.lock` are all `--name-only` EMPTY). ⚠ **`package.json` DID move (#665) but adds no env var and no config key.** ⚠ **There is STILL no `tsconfig.json` in the repo even though `typescript` is now a dependency — `types-gate.ts` supplies its options programmatically, and adding one would change what the baseline name-set means.** |
| **auth flows / JWT / OAuth / protect-floor / session builtin** | auth.map.md (watermark `8b2e4053`, **currency-VERIFIED not re-walked, THIRD consecutive window** — RE-MEASURED: `emit-server.ts`, `select-request-onion.js`, `protect-analyzer.ts`, `auth-graph.ts`, `compiler/runtime/stdlib/` and `lsp/` are all `--name-only` EMPTY this window (SEVEN source files moved in total, not one of them on the auth surface); the prior pass re-walked it after the request pipeline moved hard, and that content is one window old and carries in full). ⚠ **`scrml:auth`/`crypto`/`oauth` DO appear in this window's diff — inside the #669 client-chunk classification ONLY. No auth code moved.** What changed is what the compiler REFUSES: a client-reachable `scrml:oauth` import is now a hard error. **`auth` and `crypto` are escalation-server-only yet still carry a client chunk (PRE-EXISTING, S95 Bug 18) — a live inconsistency, deliberately unfixed.** |
| **per-session history: what landed when, and why** | **`docs/changelog.md` + `handOffs/delta-log.md` + `hand-off.md`. NOT these maps.** |
| **non-compliant / stale docs** | non-compliance.report.md |

## Key Facts

- **⚑ A `file:line` CITATION IN TWO MAPS NAMED THE WRONG *FILE*, NOT A DRIFTED LINE, AND IT SURVIVED EVERY WINDOW SINCE #654.** `structure.map.md` and `auth.map.md` both said `codegen/emit-server.ts` splits dispatch into `_scrml_dispatch` and wraps it in `_scrml_onion_dispatch` at `emit-server.ts:521`. **`grep -rn '_scrml_onion_dispatch' compiler/src/codegen/` returns NOTHING** — both are emitted by `commands/build.js` at `:570` and `:577` (⚑ S380: was `:514`/`:521`, +56, §52.13), and `emit-server.ts:521` is unrelated §20.5/§52 code. **The number was right for a DIFFERENT file**, which is why it survived: it resolves to real code and reads as precise. **A stamp-advance on a measured zero-diff proves the source did not move; it proves nothing about whether the map was ever right.** Invariant 72.
- **⚑ FOUR FIGURES IN THIS MAP SET CONTRADICTED ANOTHER FIGURE IN THEIR OWN FILE — AND ONE OF THE FOUR SAT UNDER A HEADING READING "at this HEAD" WHILE BEING THREE WINDOWS STALE.** `error.map.md` 338/320/95 vs 343/302/112, 35 lines apart. `test.map.md` 1,387 vs 1,394, and unit 901/browser 94 vs prose 908/95. This file: §34 total `810` vs its own header's `812`, in a paragraph that says "Re-read at this HEAD, not carried"; and `^| E-` published as `912` when the prior watermark's own SPEC returns `915`. **Detecting this needs no oracle and no compile — it is grep-able inside the artifact.** Invariant 71.
- **⚑ THE NEW DIAGNOSTIC'S OWN MESSAGE WAS FALSE AT ONE OF THE THREE LOCI IT FIRES AT, AND IT TOOK COMPILING TO FIND.** `E-STATE-BLOCK-STATEMENT-FORM` asserted the statement "ships into the DOM as literal page text". Measured by compiling and counting occurrences in the emitted HTML: `<db>` **1**, deprecated `< state>` **1**, `<schema>` **0** — a `<schema>` body is consumed as DDL, so the statement is DISCARDED, not rendered. The refusal is still right there (it never runs either way); the stated REASON was not. **A fatal gate that asserts something untrue about one of its own loci is a defect even when its verdict is correct** — the remediation text is what the author acts on.
- **⚑ A GLOB DISARMS A FATAL GATE, AND "SOMEONE WROTE A STRING LITERAL" BADLY UNDERSTATES IT.** `E-STATE-BLOCK-STATEMENT-FORM`'s comment machine deliberately does not track string literals (a tracker over prose would open a "string" at every apostrophe and never close it). Consequence, PA-REPRODUCED here by compiling: **ANY unpaired `/`+`*` in ordinary prose opens a comment region and suppresses the gate for the rest of the state block.** A `<db>` body reading `note about src/* paths` followed by `on mount { loadDashboard() }` compiles at exit 0 with zero diagnostics and ships the statement. No string literal and no code is required — a path is enough. **Size a suppression hole from its real trigger set, not from the example that found it.**
- **⚑ THREE OF THIS WINDOW'S FOUR NEW REGRESSION PINS ARE IN THE TIER THE COMMIT GATE DOES NOT RUN.** `.git/hooks/pre-commit` runs unit + integration + conformance + root `*.test.js`; `compiler/tests/browser/` is not in that set. All three browser pins guard SILENT-WRONG-OUTPUT classes — a literal `show` attribute, an empty render slot, a literal `${…}` in the DOM — and **every one of them compiles green.** A regression in any of the three passes the commit gate and `bun test compiler/tests/unit`, and is caught only by the browser tier.
- **⚑ THE FIRST `E-` PASS TO RUN PRE-AST, AND IT WILL BE MISFILED AS AN INVARIANT-55 VIOLATION.** `lint-e-state-block-statement-form.js` scans source text with a regex at `api.js` Stage 2.5c, whose input is block-splitter output. **Invariant 55 forbids a text pass in a stage that ALREADY HAS the AST — at 2.5c there is no AST to consult**, so text is the only representation available. It also deliberately omits the `try`/`catch` its two siblings have, because a swallowed throw in an ERROR gate is fail-OPEN. **Both of these read as bugs and are the design**; the map set says so in three places for that reason.
- **⚑ `type:"state"` FROM THE BLOCK SPLITTER IS NOT A SEMANTIC CLASSIFICATION, AND ANY PASS OVER BS OUTPUT INHERITS THAT.** It is what BS calls ANY whitespace-form opener `< Name …>`. Measured over 2,353 corpus sources: **123 `type:"state"` nodes, only 44 named `db`** — the rest are engines (×31), typestate transition declarations, whitespace-form COMPONENT definitions and plain HTML. An unguarded arm in the new gate claimed all 123 and fired at a locus where the diagnostic's PREMISE is false; no corpus file happened to trip it, which is luck, not scoping. Same species as invariant 70's `<each>` two-parse-origin split.
- **⚑ THE TWO ROUTING HOLES S372 NAMED WERE ALREADY FILLED, AND THIS PASS CHECKED RATHER THAN RE-ADDING.** Task-Shape Routing rows 1 (`if=`/`show=` lowering, `emit-event-wiring.ts`) and 2 (lift-vs-structural `<each>`, `eachBlockFromMarkupNode`) both landed in `8731799d` (#707). `emit-event-wiring.ts` now appears **12** times in this file alone (re-counted at write time), up from 1 before S372. ⚠ **Row 2's line ref DID drift and is corrected — `eachBlockFromMarkupNode` is `emit-each.ts:3324`, not `:3266`** (⚑ S380: drifted again from the `:3309` this note previously carried, +15, #735). A filled hole is not a permanently correct one.


**Carried from prior windows — each still true and re-checked where this window's diff touched it:**

- **⚑ THE SAME MAP HOLE WAS REPORTED BY TWO INDEPENDENT DISPATCHES ON THE SAME DAY, WHICH MAKES IT MEASURED RATHER THAN SPECULATIVE.** Neither the `if=`/`show=` LOWERING surface nor the LIFT-vs-STRUCTURAL `<each>` split had a Task-Shape Routing row. `emit-event-wiring.ts` appeared in exactly ONE row — module-init ownership, unrelated — while **four defects landed on that file this window**; `eachBlockFromMarkupNode` appeared **nowhere in the map set at all** while the lift-vs-structural distinction produced multiple dead-page defects. **A routing table built by walking what CHANGED grows no row for a surface whose defects are SILENT, and it grows no row for a DISTINCTION either — nothing 'changes' at a fork.** Rows 1-3 of Task-Shape Routing; invariants 69 + 70.
- **⚑ THE FLAGSHIP EXAMPLE'S SUCCESS MESSAGE HAD NEVER RENDERED, IN ANY BUILD EVER SHIPPED, AT EXIT 0 — AND THE CAUSE WAS TWO SPELLINGS OF ONE PREDICATE TAKING TWO CODE PATHS.** `examples/30-validated-form.scrml:136`'s `<p if=@signup.submitted>Account created.</p>` lowered to `_scrml_reactive_get("signup").submitted` — permanently `undefined` on a §6.3 Variant C namespace — while `if=(@signup.submitted)` lowered correctly, because the binding site records DIFFERENT FIELDS for a `variable-ref` than for an `expr` and only the `expr` branch threaded `synthCellKeys`. **PA-VERIFIED FIXED at this watermark by compiling: both spellings now emit the flat dotted key.** Corpus blast radius was exactly 1 artifact of 7,388 — **the smallest possible diff for a defect that had killed the canonical §55 demo since it shipped.** Invariant 69.
- **⚑ TWO OF THIS PASS'S THREE EXECUTED VERIFICATIONS CONTRADICTED A WRITTEN SOURCE, AND ONE OF THEM WAS THE SPEC.** `compiler/SPEC.md:11686` states that a markup `if=` on a NON-ROOT element inside an `<each>` row template *"emits nothing — fails CLOSED (never renders)"*. **Compiled at this watermark it emits `if (_scrml_each_item.ok) _scrml_frag_3.appendChild(_scrml_el_4);` plus a `W-IF-IN-EACH` warning** — a create-time gate that renders and then goes stale. The measured-S302 row was overtaken by #416/GH #409 and never updated. And `docs/known-gaps.md:2561` points `g-else-if-dotted-cell-ref-emits-unregistered-flat-key` at `emit-event-wiring.ts:647`; the arm is at **:789**, 142 lines away, because #704 inserted 192 lines above it. **Both are the same failure mode the maps exist to catch: a derived artifact outliving the code it described.** Reproduce, do not relay.
- **⚑ `origin/main` MOVED TWICE UNDER THIS PASS AND THE CHECKOUT CHANGED BRANCHES ONCE, WHICH IS WHY THE STAMP IS A MERGE-BASE AND NOT A TIP.** Orientation read `main @ 8b2e4053`; write time found `wrap/s372-bryan @ f536d35e` with `origin/main` at `c9a33bfd`, the squash-merge of a commit that had been the working tip minutes earlier. **The stamp is `8b2e4053` — the SHA the source was actually verified against — not the newest SHA visible at write time.** Stamping the newest would have claimed a verification that never happened. Fourth measurement of this hazard in four sessions.
- **⚑ A MAP THAT OMITS A SURFACE IS HOW A WRONG LOCUS SURVIVES THREE SESSIONS — MEASURED, NOT ASSERTED.** `.claude/maps/` carried NO row for `W-DEAD-FUNCTION`, for reachability, or for `usage-analyzer.ts`. The gap ledger's `locus=` field named `compiler/src/codegen/usage-analyzer.ts` from S369 until S371 — a file that holds ONE prose mention of the code in a comment (`:692`), exports a boolean `FeatureUsage` bitmap, and whose own header says *"zero new diagnostics, zero AST mutation, zero emission."* **The real emit site is `route-inference.ts:5615-5616` and nothing contradicted the wrong one**, so the dispatch working that surface re-derived it from source. **Omission is not neutral; it is where a false claim goes unchallenged.** Task-Shape Routing row 2.
- **⚑ THE TEST TIER THAT PROVES A RUNTIME FEATURE MAY BE EXECUTING A RUNTIME NO PAGE EVER LOADS.** The conformance (b) half concatenates the FULL `SCRML_RUNTIME` monolith (`conformance/adapters/impl1-ts.ts:467`, `:924`), while the browser loads the PRUNED `scrml-runtime.<hash>.js`. **All 883 conformance cases are therefore blind to chunk pruning by construction** — and a SHIPPED case proves it: `each/ternary-markup-giti033` passes its suite while calling `_scrml_each_clear`/`_scrml_resolve_item` against a runtime that pruned the `reconciliation` chunk (`ReferenceError`, dead page). Root cause localized: `detectRuntimeChunks` (`emit-client.ts:828`) has no `markup-value` case, so an `<each>` inside a ternary-markup consequent is never visited. **Third instance of a class whose other two instances are fixed in that same file.** Invariant 67.
- **⚑ THE §40.8 DEFAULT-LOGIC SURFACE TURNS CODE INTO PAGE TEXT, AND UNTIL THIS PASS THE MAP SET HAD NO ROW FOR IT.** Three HIGH defects of one class landed on `liftBareDeclarations` (`ast-builder.js:1161`) this session — a `//`-comment flush, a bare call, a bare `if (){}` — and **all three still reproduce on `main`**, verified here by compiling and reading the emitted `<body>`, not by reading the ledger. Nine `^`-anchored PREFIX gates decide what lifts; **`BARE_DECL_RE` is a decl-KEYWORD list, so anything that is not a declaration falls through to `result.push(block)` and is emitted verbatim at exit 0.** The masking limb is what makes them HIGH: **a statement that was never compiled raises none of its diagnostics.** Invariant 64, Task-Shape Routing row 1.
- **A DIAGNOSTIC CAN BE GATED ON THE COMPLEMENT OF THE LOCUS ITS OWN SPEC ROW CLAIMS IT COVERS.** `E-CONTROL-FLOW-IN-MARKUP`'s §34 row says the §40.8 auto-lift *"fires only at `<program>`/`<page>`/`<channel>` direct-child roots"* and that otherwise such code *"would ship as raw `for(){}` text into the DOM."* Its emit site is gated `parentType === "markup"` — **the complement** — so it structurally cannot fire at a default-logic root. **A §34 row is a claim about coverage; check the gate, not the prose.**
- **A GATE CAN WATCH A PROPERTY THAT IS NOT THE ONE DECIDING THE OUTCOME, AND THEN NEVER FIRE WHILE 17 OF 21 CASES ARE DEAD (#669).** `W-STDLIB-SHIM-MISSING` probed `existsSync` of a shim FILE; all 21 exist. The deciding property was membership in `RUNTIME_CHUNK_ORDER`; four were members. **Compile exit 0, `TypeError` at bundle load, dead page, zero diagnostics.** The new gate reads the same array that decides the outcome. Invariant 65.
- **AN EMIT SITE IS NOT THE FINAL WORD WHEN A LATER STAGE CAN DELETE ITS OUTPUT.** Gating `E-STDLIB-CLIENT-CHUNK-MISSING` beside the `lines.push` in `emit-imports` — the co-located, obvious placement — **rejected 21 correct corpus files, measured.** `pruneUnusedClientImports` runs after it. The gate had to scan the FINAL client text. Two subsequent false-positive rounds were both closed by changing the scan's INPUT, not its pattern (invariant 43).
- **UNTIL #665, NOTHING HAD EVER TYPE-CHECKED THIS COMPILER, AND THE CI HEADER SAID OTHERWISE.** No `tsconfig.json`, `typescript` not a dependency, no `tsc` in `package.json` / `scripts/` / `.github/` / either git hook — and bun runs `.ts` **transpile-only**, so every annotation in `compiler/src` was effectively a comment. `ci.yml`'s gate-layering header advertised a *"types (always-on local)"* layer that did not exist. First run: **nine live exhaustive-switch `never` failures**, invisible for as long as `MarkupValueExpr` has been in the `ExprNode` union. **A `never` fallthrough with no checker wired is decoration.** Invariant 66.
- **`asIs` AND `unknown` ARE NOW DIFFERENT VALUES BECAUSE *ABSENCE OF A DIAGNOSTIC* AND *SUCCESS* WERE THE SAME OBSERVATION.** `asIs` = a developer signed for it; `unknown` = the compiler could not tell. `UnknownType.reason` is REQUIRED and `tUnknown()` has no zero-arg overload, deliberately: **an `unknown` that cannot say what defeated it has decayed back into an `asIs`.**
- **THREE INDEPENDENT SILENT-WRONG HOLES IN THE SAME CONSTRUCT, ALL FOUND BY DOG-FOODING AND NONE BY THE SUITE (#670/#672/#673).** The class: **correct at TOP LEVEL, silently degraded in a NESTED or INDIRECT position.** One of them — the empty-string branch — existed because a parser blank-token skip tested `tok.text.trim() === ""` and an empty-string STRING literal has blank `.text`. **The fail-safe direction is the reusable rule: a false positive is a needless effect that never re-fires; a false negative is a stale display.**
- **THE BRANCH YOU ARE ON IS A WRITE-TIME QUESTION, NOT AN ORIENTATION-TIME ONE — MEASURED THIS PASS.** This generation began on `wrap/s368` at `6bb57c66` under an explicit *"stay on it"* instruction; the operator squash-merged it as #676 mid-pass and the checkout moved to `main`. **A pass that recorded its branch once at the start would have stamped a SHA that is on no branch and committed to a merged one.** The MAP-STAMP procedure caught it; nothing else would have.
- Entry point: `compiler/bin/scrml.js` -> `compiler/src/cli.js` dispatches to `commands/*.js` (11 subcommands); the pipeline is `compileScrml()` in `compiler/src/api.js` (block-split -> AST-build -> type-check -> codegen).
- **THE REQUEST-DISPATCH ENTRY POINT MOVED: `handle()` WRAPS TOP-LEVEL DISPATCH AND A SERVER MOUNTS EXACTLY ONE ONION (#654).** Start any request-pipeline task at `compiler/src/commands/select-request-onion.js` — imported by BOTH `commands/build.js:22` and `commands/dev.js:23`, so dev and prod cannot diverge. Two modules declaring a pipeline is `E-MW-007`. **The transferable principle: precedence must be readable off the SOURCE, never off a FILENAME** — composing per-module onions would order them by the filename-sorted module list, so a rename would silently decide which `handle()` won. ⚑ **CORRECTED S376: the dispatch SPLIT is emitted by `commands/build.js:570`/`:577`** (⚑ S380: was `:514`/`:521`, +56, §52.13)**, NOT by `emit-server.ts` — see invariant 72.**
- **TWO FAIL-OPEN PIPELINE DEFECTS CLOSED IN THE SAME WORK, BOTH FOUND BY MEASURING RATHER THAN READING.** The CORS preflight was only a route-registry entry, so an author `handle()` ran on every browser preflight (measured: an OPTIONS request came back stamped by `handle()`) — it is now pipeline stage 1 and short-circuits, because **a preflight carries no credentials to satisfy an auth check with.** And `ratelimit=` was app-wide, so it counted a page's own HTML/CSS/runtime/bundle sub-requests and `ratelimit="3/min"` 429'd a first-time visitor on their first view — it is per-route again (§4.15).
- **`headers="strict"` BINDS WHAT THE COMPILER EMITS, AND THE AUTHOR HAS NO ESCAPE FOR IT (invariant 61).** `default-src 'self'` killed the §38 transition keyframes (an injected inline `<style>`) and the SSR seed (an inline executable `<script>`) — both silently, both with no adopter-side fix, because §39.2.5's `handle()` override covers author-loaded external origins. Both moved: keyframes to the emitted stylesheet (`codegen/emit-transition-css.ts`, NEW; the `'transitions'` runtime chunk RETIRED), seed to `<script type="application/json">` + `JSON.parse`.
- **FIVE GATES WERE REPORTING GREEN WHILE MEASURING NOTHING (#646, invariant 59).** The species: parse a population out of text, silently drop what the regex cannot match, report PASS on the remainder. The house pattern is now an unconditional `bracketed !== parsed` refusal with **exit codes partitioned 1 = the data is wrong / 2 = the instrument is broken**, and 2 unreachable as a PASS. Same window, same species on a different surface: **a CI step named "…+ canary" never ran the canary** (invariant 60).
- **MAPS-CURRENCY IS ADVISORY, NOT GATED — MEASURED AT THIS PASS.** `bun scripts/state.ts --check` printed `maps: 112 commits behind HEAD` and **exited 0.** Nothing in the toolchain fails on stale maps, and the pre-dispatch check `pa-scrml-overlay.md {{maps_fills}}` requires is a WARN. **A stale map will not announce itself; the only thing that moves the stamp is a wrap.**
- **THE MAP-STAMP DEFECT IS FIXED AT THE SOURCE THIS PASS, AND THE DIAGNOSIS IS THE TRANSFERABLE PART.** Invariant 48 has said "verify the stamp is an ancestor" since S326, and the defect recurred THREE more times. It recurred because **the row specified the INBOUND check and the failure is on the OUTBOUND side** — the S331 author ran `--is-ancestor <old-stamp> HEAD`, watched it pass, wrote the correct outbound SHA into the very next sentence of the header, and then stamped the branch tip anyway. **A rule that names the command but not the MOMENT does not bind.** The procedure is now at the top of this file. Measured on arrival: 3 of 5 distinct live stamps were orphaned branch tips, one of them (`fe14c9b2`, schema.map.md) for ~ten sessions.
- **A SILENT INSTRUMENT IS WORSE THAN A RED ONE, AND THIS IS THE WORKED EXAMPLE.** `scripts/state.ts:545` correctly REFUSES to compute a behind-count from a non-ancestor watermark — it will not print a wrong number. But the refusal terminates there: the pre-dispatch maps-currency check `pa-scrml-overlay.md {{maps_fills}}` mandates before EVERY dev dispatch simply returned nothing, and nothing escalated. **A mandatory step was unanswerable for three of the last six map generations and no gate noticed.** Verified fixed at this pass: the probe now prints a real behind-count.
- **A COLLECTOR THAT ENUMERATES AST FIELD NAMES IS FAIL-OPEN (invariant 52).** `collectDerivedCellDecls` descended `body` and `children` while claiming "at any depth"; six positions leaked `Bun.password.hash` into the browser bundle at exit 0. **The fix inverted the default** — descend everything, deny-list the exceptions — and the deny-list was then measured NOT load-bearing for the result (68 cells with it, 68 without it entirely). **Never close a miss in this class by adding the field name.** This is the compiler's second instance of the class.
- **THE §34 CATALOG IS 813 AT S376 (812 -> 813, `E-STATE-BLOCK-STATEMENT-FORM`) AND ITS BUCKETS ARE THE INSTRUMENT, NOT THE CATALOG.** ⚠ **This bullet read `809 -> 810, E-MW-007` for two windows — a carried S368 figure; re-executed at this watermark the census returns 812 rows / PINNED 343 / IMPL-SITES 302 / FALSE-CLAIM 112.** The historical lesson it carries is still the point: IMPL-SITES 320→300, FALSE-CLAIM 95→112, DECLARED-AHEAD 14→18, with STRUCK/PINNED/RUNTIME-SURFACED flat. `scripts/s34-census.ts` gained +143 lines (#646) and now emits a FALSE-CLAIM disposition table it did not have before. **Do not diff those bucket figures across the instrument change, and do not report "112 broken diagnostics".** If you need a trend, re-run the census on BOTH sides of your window.
- **A COUNT THAT WALKS THE FILESYSTEM IS NOT A REPO FACT.** The prior map published `1887 source files` from `s34-census.ts`; it does not reproduce at any commit, because `filesScanned` walks ten directory roots on disk and therefore counts whatever gitignored build output the checkout happens to hold. Tracked truth this window: **1,858 -> 1,864 (+6)**. Same shape, different direction, in FACTS.md: `test files` counts only `*.test.js` (**1,398** at this watermark) and excludes 15 `*.test.ts`, so a `git ls-files '*.test.*'` returning a larger number is not a contradiction. **State which derivation produced a number.**
- **TWO OF THE THREE NEW `scripts/` FILES EXIST BECAUSE AN OBLIGATION WAS RECORDED IN ONE ARTIFACT WHILE EVERY PROBE READ ANOTHER.** `dpa-debt.ts` was written after `dpa-024` sat `BANKED — UNRUN` for six sessions: the boot gate probed review-debt, the thread board, gh issues/PRs and CI — every inbound channel except the deliberation queue. **The rule it discharges: a channel the probe does not read does not exist to the PA.** It is also deliberately BIDIRECTIONAL — it reads the authoritative status TABLE *and* re-parses the per-item `status:` lines and reports disagreement, because the failure it was written for was a STALE TABLE, and a probe that trusts its authoritative surface unconditionally cannot see that surface go wrong.
- **A REGEX OVER SOURCE TEXT IN A POST-AST STAGE NEEDS A JUSTIFICATION OR THE STRUCTURAL ROUTE (invariant 55).** Five instances, five authors, one session, two unrelated branches — **and none was caught by a 22,385-test suite or a 7,375-artifact differential**, because the class is invisible to differentials by construction. Detection is a RATIO (`source-text-regex-census.ts`), deliberately not a CI gate.
- **THREE PRs TO CLOSE ONE ATTRIBUTE-ROUTING CLASS, BECAUSE EACH PATH ARRIVES AS A DIFFERENT NODE SHAPE (invariant 54).** The `<each>` body attr needed a threaded `requestIds`; the `${for…lift}` attr needed a REPARSE first, because `shouldSkipExprParse` skips any `<`-leading expr so no structured node exists to thread into. **Every fix is gated on REGISTERED request ids**, which is what let the class close incrementally with a byte-identical corpus.
- **A DEFERRED-LIFECYCLE BODY IS DEFINED BY "HAS ITS OWN STEP-5 EMITTER", NOT BY TAG CATEGORY (invariant 53).** `{timer, poll, timeout}` must not be descended by `collectTopLevelLogicStatements`; `<request>` and `<channel>` must be, and adding either to the set would delete a required emission.
- **A codegen change has a pre-land gate and it is NOT in CI (#428).** `bun scripts/corpus-emit-differential.ts`, run by hand base-vs-head. Its syntax half must stay a separate NODE subprocess — `node --check` AND bun's `vm.Script` are both blind to a top-level stranded `await`. Invariant 41.
- **A fix can land MORE than its ledger says (invariant 45), and a gap can also be OPEN while its fix landed.** Verify an open gap's locus against HEAD before scoping from it — `git log -1 -S '<the exact code the entry quotes>' -- <file>` is the two-second check.
- **`docs/known-gaps.md` heading-vs-`@gap`-marker drift is carried, not re-counted this pass** (this dispatch is scope-barred from editing that file). `state.ts` parses the MARKER, so the rollup and its CI gate are correct and blind to the disagreement, and **the heading is the line a grep returns.** `bun scripts/state.ts --check` prints the drift count (#485). non-compliance.report.md.
- **There is exactly ONE provider answering "is this name async in client mode" (#442).** Decision sites 3 -> 1. Invariant 39.
- **#429 landed explicitly NOT claiming its bug class**, and that is load-bearing, not modesty: 142 bare client server-fn call sites remain in cleanly-compiling corpus sources. **Do not inherit "the auto-await family is closed" from anywhere.**
- **A SHALL CAN BE NORMATIVE AND CODE-LESS AT THE SAME TIME (#460).** §12.5's route-handler response contract is enforced BY CONSTRUCTION and carries no §34 row. **Assert on the wire, not on an `E-` code.**
- **FOUR PLACES IN THIS COMPILER ANSWER "may this element body take an element child?", AND THEY DISAGREE (#466)** — a standing example of the same shape invariants 49 and 52 record elsewhere. `eachBodyLowering` is NOT a name in this tree.
- **SPEC.md is the sole normative source.** PIPELINE.md documents stage internals. Any doc contradicting SPEC.md is non-compliant per pa.md Rule 4.
- **`.claude/` is gitignored; `.claude/maps/` and `.claude/agents/project-mapper.md` are FORCE-tracked.** Staging a map refresh requires `git add -f`, and the wrap commits it with an explicit pathspec (`git commit -- .claude/maps/`) so it does not sweep other in-flight files.
- **Two clones land into the same `main` under colliding session numbers** (this window carries bryan S343/S345/S346 alongside peter S340/S341/S344). Disambiguate by clone NAME as `hand-off.md` does; **the SHA is the only well-defined coordinate, and only if it is an ancestor of `origin/main`.** These maps have no CI gate of their own and no scheduled refresh — any generation asserting completeness is a hypothesis until re-derived.

- **THE BUDGET YOU BELIEVE IS NOT THE ONE IN FORCE, AND THE INSTRUMENT THAT WOULD HAVE TOLD YOU SWALLOWED THE TELL (#537).** `bunfig [test] timeout=10000` was dead config from the day it was written; the flagship-hos "intermittent assertion failure" was a 5 s timeout; and the gate's name-set key was deliberately blind to everything but the name. Three sessions of misattribution. The fix is three-sided — site-declared budgets, the hot path made fast (`makeSkipCursor`), and the gate now printing the WHY beside the WHICH. Invariants 56 + 57.
- **A PR TITLE CAN CLAIM A CLOSE THE DIFF DOES NOT DELIVER (#528).** "fix(api): sort directory walks — order-dependent output" sorted a walk that was ALREADY terminally sorted; the actual order-dependence (`inputFiles`, api.js:910) is open, HIGH, and PA-reproduced by EXECUTION. Scope determinism claims from the counters that mint ids, not from the walk. Invariant 58.
- **`{ once: true }` ON THE THREE EMITTED DCL BOOT REGISTRATIONS (#526) IS PRODUCTION-IDENTICAL AND EXISTS FOR LONGER-LIVED DOCUMENTS** — a soft-nav'd page or a shared test document re-firing a stale boot against a later chunk's nodes was the S345 repo-wide cloud gate red. Do not "clean it up" as redundant.
- **AN OBLIGATION WHOSE PROBE ONLY READS THE CHANNEL IS UNDISCHARGED — THIRD INSTANCE, THIRD PROBE (#536).** review-debt (PRs), dpa-debt (deliberation queue), now issue-debt (adopter issues): three open issues were NAMED at four consecutive boots and homed by nobody. `issue-debt.ts` asserts the HOME, not the listing. pa-base §10.

## Tags
#scrml #map #primary #index #compiler #bun #invariants #prohibitions #w-dead-function #reachability #route-inference #not-usage-analyzer #runtime-chunk-pruning #scrml-runtime-vs-template #verification-tier #section-40-8 #default-logic-lift #liftbaredeclarations #code-as-page-text #stdlib-client-registry #runtime-chunk-order #asis-unknown-split #types-gate #value-form-if #silent-wrong #map-stamp-rule #module-init #rehydrator-boundary #route-region #machine-retired #e-deprecated-001 #e-fn-equals-body #e-fn-arrow-body #fn-decl-parse-sites #export-reparse-swallow #keep-alive #§4.15 #§20.8.4 #§40.8 #page-fifth-attribute #w-route-request-duplicates-server-load #follow-on-not-alternative #timer-poll-first-tick #§6.7.5 #§6.7.6 #immediate-poll-tick #match-arm-autoawait #parenthesize-await-server-calls #scheduling-ts #crossmodule-async-markup #s239-catch #subparse-span-rebase #within-node-gate-windows-fix #export-let-var-emission #serve-tool-reachability #dist-relative-local-specifier #coordinate-space #§47.9.5 #cps-choke-point #auto-await #puppeteer-skip-download #windows-ci #review-debt-script #s34-census-broken #no-scheduled-map-refresh #changelog-dereferenced #pr-405-landed #cps-choke-point-landed #inject-promise-await-retired #w-if-in-each #each-nested-if-not-reactive #reset-init-thunk-reassignment #collect-structural-decl-names #§6.8 #§17.1 #correction-pass #g-implicit-cell-double-write-clobbers-reset-init #async-name-provider #async-name-facts #is-async-callee-name #is-server-boundary-callee #decision-sites-3-to-1 #one-provider-three-consumers #seed-trigger-not-result-set #u1 #dpa-020 #dpa-023 #client-server-fn-await #is-client-server-fn-call #client-async-body #can-suppress-never-strand #position-invariant #owning-file-filter #routemap-key-carries-the-file #decide-off-emitted-output #match-iife-header #await-absorb #auto-await-family-not-closed #142-bare-sites #option-c-ruled-not-built #reset-init-thunk-promise #corpus-emit-differential #corpus-check-goggles #pre-land-gate #codegen-task-shape #dual-goggle #node-check-blind-to-tla #bun-vm-script-blind #truncated-probe #1878-sources #exit-code-2-invalid-comparison #dangling-ref-class #session-proxy-bind #gh357 #csrf-token-disclosure #ast-reads-current-user-ambient #sse-currentuser-splice #channel-auth-only #permissive-by-design #s34-census-works-on-linux #windows-only-enoent #concurrent-clone-drift #session-numbers-collide #routed-to-bryan #region-fence #two-region-classes #lexical-vs-structural #change-the-input-not-the-pattern #join-around-runtime-slot #classify-brace-group #object-shorthand-expansion #binding-pattern-half-repair #proto-shorthand-b31 #engine-dependent #register-fn-name #identifier-shape-guard #zero-width-alternation #embed-runtime #§12.5 #response-contract #one-exit #instanceof-response-passthrough #redact-before-serialize #fail-open-403-to-200 #bun-welcome-page #stderr-only-for-undefined #session-cookie-wrap #object-hasown #prototype-chain-read-closed #read-side-policy-open #under-claiming #side-landing #verify-locus-against-head #heading-vs-marker #spec-silent-shall #workflow-dispatch #manual-refire #prospective-not-retroactive #422-target-ref #show-false-ssr-REVERTED #ctrl-017-020 #undoable-emit-time-decoration #toggle-needs-a-toggler #fail-open-not-fail-closed #block-arm-value-position #keyword-fence-charset #not-backslash-b #identifier-charset-not-w #rcdata-restricted-parent #four-non-agreeing-answers #is-rcdata-body #eachbodylowering-does-not-exist #option-label-counter-gate #s239-do-not-land #§4.14-byte-identity #§17.7.6 #response-contract-has-a-spec-home #s326-n1-closed #shall-without-a-code #enforced-by-construction #artifact-behaviour-not-js-shape #stamp-must-be-an-ancestor #pr-branch-tip-stamp #merge-base-is-ancestor #selfhostmodules-seam #call-graph-invisible-codegen #run-cg-indirection #test-deleted-with-reverted-code  #near-zero-line-delta #§18.5-four-routes #plan-block-arm-lift-is-not-the-segmenter #leaf-predicate-not-single-classifier #grep-the-predicate-not-the-plan #separator-dependent #closes-block-statement #statement-start-whitelist #per-arm-declarednames #re-dispatch-not-drop-opts #§6.6.19 #e-derived-server-only-reach #refuse-not-escalate #per-function-scope-only #§12.4-literally #two-positions-still-open #shortest-edit-restores-the-leak #kind-tool-carve-out #807-codes #catalog-moved-first-time-in-five #s34-census-fixed-everywhere #fileurltopath #e-sql-006-compile-time #sink-not-detector #dedup-at-drain #request-ref-two-siblings-open #silent-vs-loud #each-arm-reparse #throwaway-id-range #nodetypes-memo-clobber #a-probe-is-code #scripts-is-code-bearing #two-rates-one-signal #count-threshold-not-percentage #prove-the-bite #auto-widen #cry-wolf-guard-deleted #heading-marker-drift-13 #883-conformance #1339-tests #240428-lines #24-commit-window #two-operators #map-stamp-rule #ancestor-of-origin-main #squash-merge-orphans-a-branch-tip #inbound-vs-outbound-check #outbound-stamp-check #generated-at-is-not-the-watermark #state-ts-line-3 #behind-count-unavailable #silent-instrument #mandatory-step-unanswerable #three-of-five-stamps-orphaned #fe14c9b2-orphaned-ten-sessions #honest-older-stamp-still-bounds-nothing #809-codes #catalog-moved-two-windows-running #e-each-body-decl-unsupported #i-ssr-each-client-rendered #§17.7.3 #§52.8 #each-body-scope-is-at-sigil-plus-as #fail-closed-not-silent-drop #dangling-ref-empties-the-whole-list #ssr-fallback-now-loud #fallback-descriptor-not-null #ssr-renderable-subset #§6.6.19-any-position #structural-walk-not-field-listed #collect-derived-cell-decls #skip-derived-walk-key #deny-list-not-load-bearing #depth-cap-512 #identity-seen-set #descend-one-field-too-many #second-instance-of-the-class #six-leaking-positions #object-keys-is-insertion-order #§6.7.5 #§6.7.6 #§6.7.8 #deferred-lifecycle-body-tags #timer-poll-timeout #module-init-double-emit #request-and-channel-excluded-deliberately #poll-immediate-first-tick #never-refired-on-resume #timeout-tag-e-markup-001-false-fire #hand-maintained-beside-a-derived-list #§6.7.7 #request-ref-attr-routing-closed #each-request-ids-stash #reparse-lift-attr-request-ref #escape-hatch-node #should-skip-expr-parse #gated-to-registered-requests #byte-identical-non-request-path #post-ast-source-text-regex #source-text-regex-census #ratio-not-inspection #five-authors-one-substitution #invisible-to-differentials #new-or-touched-only #boot-read-set-gate #detection-not-control #pickup-led-digest #dpa-debt-probe #a-channel-the-probe-does-not-read-does-not-exist #bidirectional-probe #stale-table #filesscanned-is-environment-dependent #tracked-not-on-disk #facts-counts-only-test-js #state-which-derivation #bunfig-timeout-never-in-force #timeout-wears-fail-marker #skip-cursor-oracle #inputfiles-order-open #once-true-dcl-boots #issue-debt-probe #push-scoped-to-main #pr-539-in-flight #810-codes #242954-lines #190-src-files #1378-tests #gate-13-steps #select-request-onion #e-mw-007 #one-onion-rule #handle-top-level-dispatch #precedence-off-source-not-filename #cors-preflight-stage-1 #ratelimit-per-route #emit-transition-css #transitions-chunk-retired #headers-strict-binds-compiler-emissions #ssr-seed-application-json #soft-nav-per-page-asset-hole #app-wide-union #invariant-59-bracketed-vs-parsed #invariant-60-step-name-is-coverage-claim #invariant-61-strict-headers #invariant-62-soft-nav-stylesheet #invariant-63-count-codes-by-absence #invariant-58-retired #delta-lint-gate #expect-shapes #maps-currency-is-warn-only #watermark-moved-mid-run #asis-split-NOT-on-main #types-gate-NOT-on-main #if-show-lowering #emit-event-wiring #compute-display-toggle-condition #synth-cell-collapse #resolve-synth-cell-prefix #section-55 #validated-form-flagship #each-lift-vs-structural #each-block-from-markup-node #parse-lift-tag #fail-open-predicate #accepted-then-discarded #spec-stale-table #stale-locus #self-contradicting-map #merge-base-not-tip #e-state-block-statement-form #state-block-locus #stage-2-5c #pre-ast-lint #not-an-invariant-55-violation #no-try-catch-in-an-error-gate #bs-type-state-is-not-semantic #glob-disarms-a-fatal-gate #wrong-file-not-drifted-line #internally-contradictory-figure #one-sha-on-two-lines #813-codes #192-src-files #1398-tests #browser-tier-outside-the-commit-gate #show-in-each-is-reactive #if-in-each-is-frozen #snippet-arity-tolerant #render-bearing-live-fallback #glued-interp-in-lift-reconcile #invariant-71 #invariant-72

## Links
- [domain.map.md](./domain.map.md)
- [structure.map.md](./structure.map.md)
- [dependencies.map.md](./dependencies.map.md)
- [schema.map.md](./schema.map.md)
- [config.map.md](./config.map.md)
- [build.map.md](./build.map.md)
- [error.map.md](./error.map.md)
- [test.map.md](./test.map.md)
- [auth.map.md](./auth.map.md)
- [infra.map.md](./infra.map.md)
- [migrations.map.md](./migrations.map.md)
- [non-compliance.report.md](./non-compliance.report.md)
- [known-gaps.md](../../docs/known-gaps.md)
- [changelog.md](../../docs/changelog.md)
- [master-list.md](../../master-list.md)
- [pa.md](../../pa.md)
