{"feature":"while","shape":"lift-accumulate-to-tilde","expectedOutcome":"error","expectedCodes":["E-CG-TILDE-UNRESOLVED"],"notes":"§49.6.1 shows this pattern but places the while directly in enclosing ${}. I wrapped with a nested ${} to get ~ to accumulate here since outer is already logic context. Unclear whether outer or inner ${} is canonical. ⛑ S397 UPDATE — outcome flipped clean -> error, and NOT because the shape changed (the .scrml is byte-identical). This file was one of only TWO in the whole 2,383-file corpus whose emitted JS carried the `~ orphaned` codegen fallback; S397 replaced that fail-open fallback with E-CG-TILDE-UNRESOLVED, so the read is now rejected instead of silently bound to null. ⚑ THE ORPHAN WAS A SYMPTOM, NOT THE DISEASE, and the disease is still open: at base the emitted clientJs was `let i = 0; let result = null /* ~ orphaned */;` — the ENTIRE nested `${ while (i < 3) { lift i * 10; i = i + 1 } }` block was absent from the output, and so was the `log(result.length)` that followed it. No loop, no `_tN = []`, no `.push`. So the §49.6 accumulator lowering that PA's review believed covers this role (`let _tN = []` / `.push` per lift / `return _tN`) does NOT cover the nested-${} form; it drops the block. Filed as a finding by the S397 census, deliberately NOT fixed there (out of that arc's fence — it lands the floor only). ⚑ Also note: scripts/gauntlet-s19-verify.mjs extracts codes with /\\b[EWD]-[A-Z]+-\\d+/, which requires a NUMERIC suffix and therefore cannot see ANY named code — not this one, not E-CODEGEN-INVALID-LOGIC, not E-FN-ARROW-BODY. It will match `expectedOutcome` and report a code-level mismatch here until that regex is widened. The expectation records the truth rather than writing UNKNOWN to keep an ungated script quiet."}
