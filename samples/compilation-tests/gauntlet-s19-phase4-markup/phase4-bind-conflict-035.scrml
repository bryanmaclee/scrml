// bind:value + explicit oninput on the same element — composable by design (E-ATTR-012 retired, S249); compiles clean.
<program>
    ${
        <name> = ""
        function log() { let _ = 1 }
    }
    <input type="text" bind:value=@name oninput=log()/>
</program>
